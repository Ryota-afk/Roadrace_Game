// 静的データ（Phase 1で src/main.jsx から分離）。純粋な定数のみ。

// 第13弾Phase3-E: 第12弾までの旧トークンC・FONT_D・FONT_M・ui.jsxのBtn/Eyebrowは
// 全画面のT移行が完了したため削除した（devlog/wave13.md参照）。FONT_Bだけは例外として残す
// ——入力中の文字が読める必要がある入力欄（チーム名等）でchrome.jsxが今も使う、意図的な例外。
export const FONT_B = "'Hiragino Sans','Noto Sans JP','Meiryo',sans-serif";

// ---------------------------------------------------------------------------
// 第13弾Phase1：新デザインのトークン（ユーザー合意済み）
//
// 決定の経緯・却下案は devlog/wave13.md を参照。要点のみ：
//  - 骨格は「計器盤」。キーと値の表組みで、値は右揃え。
//  - パネルの外周に枠線は引かない。面（surface / surfaceUp）の濃淡だけで区切る。
//    rule は「表の行区切り」専用で、パネルの輪郭には使わない。
//  - 角丸は 0。丸めない。
//  - 画面に出る文字はすべて FONT_DOT（数字も見出しも本文も）。
// ---------------------------------------------------------------------------

// 埋め込みドットフォント。@font-face の宣言は src/styles/fonts.css。
// 実測でこのフォントは完全な等幅（和文＝1em・数字＝ちょうど0.5em）であり、
// 数字が必ず桁で揃う。計器盤の表組みはこの性質に依存している。
export const FONT_DOT = "'CheckpointPeriod',monospace";

export const T = {
  color: {
    // 第61弾(devlog/wave61.md・2026-08): 明度を底上げした（bg15→27/255）。旧値は
    // bg15/surface24/surfaceUp31/rule46の4段が全域のわずか12.2%に密集し、隣接コントラストが
    // 1.077:1（肉眼でほぼ同一）だった。かつtextが対bg17.23:1で上限に張り付いていたため、
    // 階層を作るには文字を暗くする以外の逃げ場が無かった。背景を上げることで面の刻みを
    // 1.15〜1.32:1へ広げ、textのギラつきも緩めた（ユーザー承認済みの「案2」）。
    bg: "#1A1A20",         // 画面の背景
    surface: "#26262E",    // パネル。枠線は使わず、この面の差だけで背景と区切る
    surfaceUp: "#33333D",  // パネルの中に置く小要素（入れ子の面）
    rule: "#45454F",       // 罫線。表の行区切りのみに使う
    text: "#F2F0EA",       // 本文
    sub: "#A09E96",        // ラベル・単位・補足。階層を作るための弱い文字色
    // 第13弾Phase3-D-5: 実測（シーズン17画面430万点）で無彩色93%・黄3%・他1%未満と判明し、
    // かつtext/subが黄と同じ色相（45〜48°）だったため「色相が1つしかない」状態と診断された。
    // 「読むもの」と「押すもの」で役を分け、同じ色を両方の役割に兼任させない（CLAUDE.md §9）。
    accent: "#F2C94C",    // データ強調専用：数値・見出し・状態語（成長フェーズ・調子等）
    action: "#A76ADC",    // 操作専用：主ボタン（1画面1つ想定）・選択中のメニュー・リンク
    good: "#7FB069",      // 勝ち・回復。結果の良し悪しにのみ使う
    bad: "#C0554D",       // 負け・消耗。結果の良し悪しにのみ使う
    // 第61弾(devlog/wave61.md): bgを底上げした結果、bgを「明るい面の上に乗る文字色」に
    // 流用していた既存コード（主ボタン等）のコントラストが崩れる（bad比4.27:1→3.83:1で
    // WCAG AA割れを実測）ため分離した。旧bgの値で固定し、明るい面（accent/action/good/bad等）
    // の上に文字を乗せるときは常にこちらを使う。ページ背景には使わない（CLAUDE.md §9：
    // 同じ色を両方の役割に兼任させない）。
    ink: "#0E0E10",
    // 第61弾: 背景を上げたことで初めて「bgより暗い段」が使えるようになった。役割を1つ与える
    // （CLAUDE.md §9：役割のない色はただの装飾に戻る）＝ゲージ・バーの溝（空側）専用。
    // surfaceUpは新パレットでは明るすぎて溝が沈まないため、溝にはこちらを使う。
    sunken: "#101014",
  },

  // 第32弾（第2次UI改革）: 「5段のみ・12px床」ルールは失効した。第13弾時点の
  // 「9〜11pxは漢字が潰れて読めない」という判定は実装側の内部判定に過ぎず、読めるか
  // どうかの最終判断はユーザーが行う（2026-08明示）。実サイズ見本（高精細/低DPI両条件）
  // をユーザーに提示した上で、上位は第13弾スケールを維持しつつ下限を押し下げた10段に
  // 改訂した。詳細な経緯・却下案はdevlog/wave32.md参照。
  size: {
    hero: 38,    // 1画面に1つの主役数字（マイライフホームの総合力など）
    display: 28, // 数値・大（所持金、順位など画面の主役になる数字）
    title: 20,   // 画面名・ヒーロー領域の選手名・一覧のOVR数字
    head: 16,    // 行の主役（一覧の選手名・レース名）
    body: 13,    // 本文・ボタン・一覧の行
    label: 11,   // 値付きラベル・能力数値
    caption: 10, // 注釈・単位・ラベル・チップ/タグ
    micro: 8,    // 高密度画面の補足漢字かな
    nano: 6,     // 漢字かなの下限（最密画面専用。使用開始時は必ず実機確認）
    digit: 5,    // 数字専用の下限（最密の表。使用開始時は必ず実機確認）
  },

  // 余白は4pxの倍数のみ。
  space: { xs: 4, sm: 8, md: 12, lg: 16, xl: 24 },

  radius: 0,
};

// 虹（大勝負／バッジ最上位段階）は特定の意味色1つに割り当てられない「特別枠」の演出色なので、
// T.color（accent/action/good/bad）とは別枠の固定パレットとして持つ。第9弾（DecisionCard判断
// カードのレア度）と第45弾（バッジ最上位段階「虹」のマーク）が共有する（devlog/wave44.md）。
export const RAINBOW_STOPS = ["#e88bb0", "#F2C94C", "#7FB069", "#4f8fe8", "#A76ADC"];

