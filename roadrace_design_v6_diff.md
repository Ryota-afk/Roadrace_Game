# 設計差分メモ v5→v6（集団ベースレースシミュレーション・Unity実装用）

## 1. 出走指示の刷新：チーム一括戦術 → 選手ごとの役割指定

- 廃止：`TACTICS`（逃げ／集団待機／山岳アタック）というチーム単位の1択
- 新設：`ROLES` = 選手ごとに **牽引役（domestique）／逃げ要員（breakaway）** を指定（エースは既存の指名ロジックを維持し、役割とは独立。エース自身にも役割を指定でき、エースを逃げ要員にすれば「アタック役のエース」も表現可能）
- 出走人数（squad）でクラスタモデルの適用範囲を切り替え（`groupModeFor`）：
  - squad=1（個人TT）：`solo` — 集団要素なし。既存の個人区間タイム計算を維持（`calcIndividualSegTime`/`computeIndividualFrom`）
  - squad=2（ヒルクライム）：`pelotonOnly` — 逃げ要員の選択UIは出さないが、メイン集団内の千切れ（後述）は適用
  - squad≥3：`full` — 逃げ集団・メイン集団のフルクラスタモデル

## 2. 集団クラスタシミュレーション（コア）

v4以来の「区間タイム配列＋任意区間からの再計算」というアーキテクチャを維持したまま、選手個人ではなく**クラスタ（逃げ／メイン／千切れ）単位**で区間タイムの土台を計算する方式に拡張した。

### 2.1 クラスタの状態

各区間境界ごとに以下をスナップショット（`sim.groupSnap[j]`）として保持し、任意区間からの再計算（📻介入）を可能にしている：

```
{ clusterOf: {riderId: "break"|"pelo"|"dropped"}, breakCum, peloCum, caught, hasBreak }
```

- `clusterOf`：各選手の現在の所属クラスタ
- `breakCum`/`peloCum`：逃げ集団／メイン集団それぞれの「クラスタとしての」累積経過時間（実時間ではなく、キャッチ判定専用の仮想累積値）
- `caught`：逃げ集団がメイン集団に吸収されたかどうか
- `hasBreak`：そもそも逃げ集団が存在するか（誰も逃げを選ばなければ従来のメイン集団のみのレースになる）

### 2.2 非最終区間のタイム計算

- メイン集団能力 = 集団内選手の実効能力（`abilityFor`、cookedペナルティ込み）の平均
- 逃げ集団能力 = 逃げメンバーの実効能力平均 ＋ **ローテーションボーナス**`min(8, (人数-1)*2.5)` ＋ 逃げ屋特性(+3/人) ＋ 初速ジャンプ（区間0のみ+10）
- 📻「追走強化」で+6、「静観」で-6（メイン集団能力に加算。ギャップ勝負のみに影響）
- 区間タイム = `seg.base*(1-能力/260)` を土台にクラスタで共有し、各選手の**個人疲労（fat）＋乱数ジッター**だけを個別加算 → 集団は自然にひとかたまりの実時間を持つ（現実のバンチと同じ挙動）
- 逃げメンバーは疲労蓄積が1.25倍（ローテーションで脚を使う描写）

### 2.3 キャッチ（吸収）判定

`peloCum`/`breakCum`という2つの仮想累積値を毎区間更新し、`peloCum >= breakCum`になった時点で吸収成立。以降その区間からは逃げメンバーのクラスタを`"pelo"`に書き換える。実際の描画位置は各選手の本物の`cum`（実経過秒）から算出しているため、キャッチの瞬間は自然に「差が詰まって追いつく」映像になる（追加の座標補正ロジック不要）。

### 2.4 山岳区間での千切れ（グルペット分離）

`hill`/`climb`/`mtn`区間で、メイン集団内の最強登坂力(`lead`)との差が13以上の選手は`"dropped"`へ移行。以後その選手は**個人計算**（`calcIndividualSegTime`）に切り替わる。個人の真の能力がクラスタ平均より低いために自然に遅れていく＝追加の「遅延量」変数を持たせる必要がない。

### 2.5 牽引役の脚（フューエル）

`role === "domestique"`の選手はメイン集団に留まる限り毎区間フューエル（初期100）を消費（通常30、📻「追走強化」時は自チームのみ1.6倍、「静観」時は0.4倍）。0になると`cooked=true`となり、以後の登坂能力判定・千切れ判定・集団平均計算のすべてで実効能力-6のペナルティを受ける（＝脚を使い切った牽引役は次の登りで千切れやすくなるという、追加ロジックなしの自然な副作用）。

### 2.6 最終区間（ゴール前）

- 千切れ組は個人計算のまま
- それ以外は選手ごとに`abilityFor(final)`による**個別スプリント計算**（逃げ生存なら逃げメンバー同士、吸収済みならメイン集団の残存メンバー同士で事実上の決着）
- エースは同一クラスタ内・非千切れの残存チームメイトの平均牽引力からリードアウトボーナスを得る（旧`assistBase`の考え方を、クラスタ状態に応じて動的に算出する形に変更）

## 3. 📻無線の意味変更

チーム一括戦術がなくなったため、観戦中の3ボタンを以下に変更：

| ボタン | 効果 |
|---|---|
| 追走強化 | メイン集団能力+6・自チーム牽引役のフューエル消費1.6倍 |
| 静観 | メイン集団能力-6・自チーム牽引役のフューエル消費0.4倍 |
| エース早期発射（1回限り） | エースを即座に`"break"`クラスタへ移動。逃げ集団が存在しなければ新規に発生させ、`breakCum`を現在の`peloCum`にリセットして「今から仕掛ける」を表現 |

いずれも`regroup(sim, rtNow, chaseMode, aceEarly)`が、自チームエースの現在区間＋1から`computeGroupFrom`を呼び直すことで実現。**未走行区間だけ再計算**という設計方針（v4メモ）をクラスタ方式にそのまま継承している。

## 4. 3D演出：クラスタ単位のブロック表示

- 旧v5の「チーム内トレイン連結（エース基準の2パス座標補正）」を廃止
- 新方式：毎フレーム、各ライダーの`clusterAtSeg[現在区間]`でグルーピングし、クラスタ内の最先頭選手を**アンカー**として、他メンバーをアンカーの`trainOff = idx*1.6`後方に配置（チームごとの左右レーンは視認性のため維持）
- 千切れ・吸収・エース発射は、クラスタ切替に伴う実位置（`frac`）の差が自然に映像へ反映される（追加の補間ロジック不要）。実況コメントは、静的な区間突入イベントと`sim._events`（キャッチ／千切れ／エース発射の動的イベント）をマージして表示

## 5. Unity実装メモ

- `RaceSimulation`グラフに`ClusterState`構造体（区間ごとの配列）を追加し、`ComputeGroupFromSegment(fromIdx, chaseMode, aceEarly)`サブグラフとして実装すれば、ブラウザ版の`computeGroupFrom`をほぼそのまま移植可能
- クラスタ描画はEntity/GameObjectの`currentCluster`タグを見て、アンカー追従のTransform補間を行うだけでよく、既存の隊形描画パイプライン（v5で実装済み）を流用できる
- フューエル・cookedペナルティはRiderコンポーネントに`fuel: float`, `cooked: bool`を追加するだけで済み、既存の疲労（fatigue）システムと独立して管理できる

## 6. スポンサー指定レース

- 契約時（`sponsor`選択画面）に`pickMandateMonths(mandates, seed)`で年間1〜2ヶ月（5月〜2月、3月・4月は対象外）を抽選し、`sponsor.mandateMonths`に保持
- 該当月の当該クラス向け開催レースのうち最初の1件に`sponsorMandate: true`を付与（`genMonthRaces`）
- 出場して完走：ポイント+30%ボーナス、`sponsor.mandatesMet`加算
- その月を指定レース以外の消化・または休養で終えた場合：`sponsor.mandatesMissed`加算、年度末査定時に**追加で-15万円/回**（ノルマ達成可否とは独立に加算される違約金）

## 7. QoL改善

1. キャンプ券（`camp`）を選手・練習タブの先頭からも使用可能に（ショップタブと同じ`useCamp()`を共有）
2. 編成画面（lineup）の選手カードで、連闘数を**1連闘から**常時表示するよう変更（従来は2連闘以上でのみ警告表示）
3. 「📅 年間レースプログラムを見る」画面を新設（ホームタブから遷移）。`genMonthRaces`を確定ポイント抜きで12ヶ月分プレビュー呼び出しし、開催予定（種目・グレード・スポンサー指定月）を一覧表示
4. 4月（`g.month === 0`）に限り、選手カードから「解雇」ボタンでロースターを離脱させる`releaseRider()`を追加（ロースター1名を下回る解雇は不可）

## v7への申し送り

- 逃げ集団自体の内部分裂（複数の逃げグループが形成される多層クラスタ）は今回スコープ外。必要になれば`clusterOf`の値域を`"break1"/"break2"/...`に拡張するだけで同じアーキテクチャ上に乗る
- 昇格解禁ラダー（ステージレース・グランツール・FA移籍市場・スタッフ雇用）は本メモの対象外（v2設計書のv7予定を参照）
