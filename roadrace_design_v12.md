# ロードレース v12 設計書

前提：v11リリース後のプレイフィードバック（2026-07-07セッション）を受けての設計。実装はまだ行っていない。既に確定・実装済みの3件のバグ修正（後述§0）は本設計書の対象外（コードには反映済み）。最終直線演出の方向性（高完成度2Dシネマティック）・事前プランの深さ（レース全体で1つの作戦）・追加スコープ（AIチーム戦略バリエーション／天候・横風エシュロン）はユーザーとの一問一答で確定済み。俯瞰マップの2次元化については、実際のロードレース（ツール・ド・フランス等）の隊列力学を調査し、その知見を設計に反映した。

参考にした実際の隊列力学（要点）：
- 巡航・低強度時：集団は横に広がった団子状（コンパクトなブロブ）になり、道幅いっぱいに広がる
- 高強度・登坂・向かい風時：集団は縦に伸びて単騎列になる
- 横風時のエシュロン：斜めの隊列になり、ローテーションは「風上側を前進→先頭で風下側へ抜けて後退」という閉じたループ（コンベアベルト）状に回る
（出典：[Cyclist誌](https://www.cyclist.co.uk/news/how-does-an-echelon-work)、[Canadian Cycling Magazine](https://cyclingmagazine.ca/sections/training-guide/technique/riding-in-an-echelon/)、[Siroko](https://www.siroko.com/blog/c/echelons-dealing-with-crosswinds/)、[teammc.org](https://teammc.org/blog/what-is-a-cycling-peloton/)）

---

## §0. v11から即修正済みの3件（本設計書の対象外・記録として明記）

実装・検証・pushまで完了済み。詳細はコミット履歴参照。

1. **セーブ/ロード後のID衝突バグ**：`initGame()`のダミー生成でRIDが低い値のまま残り、ロード後のスカウト/FA生成が既存選手とID衝突→Reactのkey衝突で古い選手の能力値表示が残る。`resyncRid()`で修正。
2. **GCステージ2のstale closure**：`startStage2()`がレンダークロージャの古い`g`からroster2/simを計算していた。`setG`のfunctional updater内で毎回最新状態から計算するよう変更し、連打防止ロックを追加。
3. **相手チーム人数が自チーム選択人数に連動**：`buildSim`内でAIチームがレース規定の`squadMin`〜`squadMax`の範囲で独立に人数を決めるよう変更。

---

## 1. 無線指示の廃止→事前プラン化、ローテーション演出の実時間デカップリング

### 1.1 方針（確定：レース全体で1つの作戦、既存3択を出走前に選ぶ形式）

現状`RaceView`内の📻ボタン（`CHASE_MODES` = 追走強化/静観/エース早期発射、`roadrace_v11.html:136-140`）と、それに伴う`regroupTicks(sim, rtNow, chaseMode, aceEarly)`（`:828`〜）による中断・再計算の仕組み一式を、**レース観戦中の操作から撤廃**する。

- 編成画面（エース指名・役割指定の直後、「観戦しながらスタート」ボタンの手前）に新規セクション「作戦」を追加し、`CHASE_MODES`の3択（＋エース早期発射のON/OFF）を**出走前に1つだけ**選択する。
- `startRace(watch)`内で、選択された作戦をそのまま`directive`として`buildSim`に渡し、レース全体を一発計算する（現状の`buildSim(race, squad, aceId, roles, equip, itemBoost, classIdx, undefined, dayTag)`呼び出しに`directive`を追加する形）。
- `RaceView`からは📻ボタン群・`changeChase`・`regroupTicks`呼び出し・`lockedRef`による無線ロック表示を全て削除する。カメラ操作（先頭集団／選手フィーチャー切替・速度・スキップ）のみ残る「観戦専用画面」になる。
- **実装上の効果**：`regroupTicks`が担っていた「現在tickから未来だけ再計算」というスナップショット機構が不要になり、`RaceView`の状態管理が大幅に簡素化される（`chaseRef`/`chaseUi`/`aceEarlyUsed`/`locked`/`lockedRef`が丸ごと不要に）。

### 1.2 ローテーション演出の実時間デカップリング（確定）

現状のスワップ演出（`SWAP_DURATION_MS=550`, `SWAP_STAGGER_MS=140`、`roadrace_v11.html:1178-1179`）は、シミュレーション内部の実際の交代tick（`ROTATION_PERIOD_TICKS=20`仮想秒ごと）を検知して起動している。レース結果が既に確定した事前計算物である以上、**この検知をやめ、実時間ベースの固定サイクルで「見た目上のローテーション」を回す**。

- 新設：`VISUAL_ROTATION_CYCLE_MS`（例：2400ms）ごとに、そのグループの「現在の先頭（見た目上のpull）」を機械的に次の選手へ送る、独立した見た目専用のタイマー。
- 対象となる選手の順序（誰が何番目に牽引するか）は、実際の`slotHist`（現在tickでの真の待ち順）を**サイクル開始時にスナップショットとして採用**し、そのサイクル内は固定して周回させる。これにより「本物のデータに基づいた順序」と「心地よい実時間ペース」を両立する。
- §2で述べる2次元ループ表現とセットで実装する（後述）。

### 1.3 道路幅の拡張（確定）

現状、俯瞰マップの道路は`strokeWidth="9"`（`roadrace_v11.html:1421`付近）に対し、選手の横方向の広がりは最大で約27px相当（チームレーン±10 + slotオフセット最大9.6 + 千切れ演出7）に達しうる（TOP_H=120のviewBox内）。**道路の描画幅を選手の想定横幅より広く（目安：strokeWidth 40〜48程度）拡張**し、選手が常に道路の内側に収まって見えるようにする。§2の2次元化で選手の横方向の広がり方自体も見直すため、最終的な道路幅は2次元化後の実測値に合わせて微調整する。

---

## 2. 俯瞰マップの2次元化（隊列力学に基づく設計）

### 2.1 現状の課題（コード確認済み）

現在の横位置（Y軸）は`laneOffset = r.lane + r.dispSlot * SLOT_LANE_SPACING + dropOffset`（`:1418`）という**チームごとの離散レーン＋待ち順の小さな線形オフセット**のみで構成されており、前後位置（X軸）も`rotFrac`による単純なオフセットのみ（`:1424`）。結果として、集団は「複数の平行な直線」にしか見えず、実際の集団のような塊（ブロブ）・伸縮・エシュロンが表現できていない。

### 2.2 方針（確定：グループの状態に応じて形が変わる楕円軌道モデル）

各グループ（`gid`単位）に対して、以下の3パラメータを持つ「隊列シェイプ」を毎tick計算し、個々の選手はそのシェイプ上を周回する円周パラメータ`u ∈ [0,1)`で位置決めする。

```
groupShape(members, segType, windActive) = {
  elongation: 0(団子状) 〜 1(縦一列),
  tiltAngle:  0(正対) 〜 ±60°程度(横風エシュロン時の斜め傾き),
  width:      集団の道路占有幅（人数に応じて拡大）
}
```

- **elongation の決定**：`mode`の分布（pull中の選手がいるか）・区間タイプ（climb/mtnで高、flat巡航で低）・グループ内の速度分散から算出。ペースが上がるほど1（縦一列）に近づき、集団がまとまって流している時ほど0（団子）に近づく。
- **tiltAngle**：§4の横風フラグが立っている区間でのみ非ゼロになり、風向きに応じて隊列全体を斜めに傾ける。
- **周回運動**：選手ごとの円周パラメータ`u`を§1.2の「見た目上のローテーションサイクル」に同期させ、`u`が増えるほど「風上側（またはelongation時は片側）を前進→先頭で反対側へ抜けて後退」という**閉じたループ**を描く。具体的な座標変換：
  ```
  L = baseLength * (1 + elongation * K_L)   // 縦方向の半径
  W = baseWidth  * (1 - elongation * K_W)   // 横方向の半径（伸びるほど細くなる）
  θ = u * 2π + tiltAngle
  (dx, dy) = (L*cos(θ), W*sin(θ))  をtiltAngle分だけ回転
  ```
- 大人数グループ（目安8〜10名超）は、全員を厳密なループに乗せると不自然になりやすいため、先頭付近の少数（実際にpull/draft rotationに参加する`eligible`メンバー）のみループ運動させ、残りは緩やかなジッター付きの静的クラスタとして描画する（実装時にプレイテストで閾値を調整）。

### 2.3 実装コスト・リスク

- 中〜大。`laneOffset`/`rotFrac`を丸ごと置き換える設計変更のため、既存の千切れ演出（`dropOffset`）・アタック誇張（`attackBonus`）との統合が必要（アタック中の選手はループから一時的に離脱し前方へ直進する特殊状態として扱う）。
- 実データ（`finishTime`等）には影響しない見た目専用チャンネルのままなので、既存のバランス調整・GC集計ロジックへの影響はゼロ。

---

## 3. 最終直線の高完成度2Dシネマティック演出（確定：2D方向で進める）

### 3.1 方針

最終区間突入（既存の`lockedRef`相当の判定）ではなく、**ゴール前の最後の直線区間に入った瞬間**に、以下の専用シーケンスへ切り替える。

1. 画面を一瞬暗転（フェードアウト、0.4秒程度）
2. 暗転後、最終直線専用のカメラ・レイアウトへ切り替え（横長ではなく、ゴールに向かって手前から奥に伸びる構図に変更。俯瞰マップとは別の専用SVGコンポーネントとして新規実装）
3. **結果は既に確定済み**（`finishTime`・着順とも事前計算済み）であるため、実際のtickデータから逐次描画するのではなく、**確定済みの着差・着順から逆算して滑らかなスプリント演出を振り付ける**：先頭集団の各選手について、ゴールタイム差を「最後の数秒での加減速カーブ」にマッピングし、実際にはあり得た程度の駆け引き（誰かが早めに仕掛けて失速する、外から差すなど）を疑似乱数で選び、毎回微妙に異なる見え方にする（実際の着順・着差は変えない、あくまで魅せ方のみ）。
4. ゴール通過後、通常の結果画面へ遷移。

### 3.2 実装コスト・リスク

- 中。新規コンポーネント（`FinalSprintCinematic`）として切り出せば既存の`RaceView`ロジックへの影響は小さい。振り付けロジック（着差→加減速カーブのマッピング）は新規実装だが、実データを参照するだけで改変はしないため安全。
- 3D化は今回見送り（技術構成がReact+SVGのみでthree.js等のCDN追加・描画エンジン構築が必要になり投資が大きいため）。今回の2D高完成度版で十分な訴求力が出るかをプレイテストし、なお物足りなければ次フェーズで3D化を再検討する。

---

## 4. 天候・横風エシュロン（確定：追加スコープに含める）

### 4.1 方針

- コース生成時（`generateCourse`）に、flat区間の一部へ`windActive: true`のフラグをランダムに付与する（頻度は要調整、例：flat区間の20〜30%程度）。
- シミュレーション側：`windActive`な区間では`GROUP_GAP_DIST`（現行0.22）を実効的に縮小し、集団が分裂しやすくする。また、ローテーションに参加しない選手（風除けの外に出された想定）はエネルギー消費がやや増える効果を追加する。**これはcosmetic-onlyではなく実際のシミュレーション結果に影響する変更**のため、既存レースのバランス（千切れ発生頻度等）への影響をプレイテストで確認する必要がある。
- 見た目：§2のtiltAngleパラメータと連動し、風区間突入時に隊列が斜めに傾く演出を出す。HUDに「🌬 横風区間：集団分裂に注意」等の予告テキストを表示。

### 4.2 実装コスト・リスク

- 中。コース生成・`simulateTicks`のグループ判定・視覚表現の3箇所にまたがる。cosmetic-onlyではなくバランスに実影響するため、他の項目より慎重な調整が必要。

---

## 5. AIチーム戦略バリエーション（確定：追加スコープに含める）

### 5.1 現状

`simulateTicks(course, riders, fromTick, directive, noGroup)`の`directive.chaseMode`はレース全体・全グループに対して一律に適用される（`roadrace_v11.html:773-774`）。§1の変更後も、プレイヤーの事前選択した作戦が全チームに一様に効いてしまう状態は変わらない。

### 5.2 方針

- AIチームごとに、`buildSim`内でチーム生成時に隠しパラメータ`aiStyle`（`aggressive`/`balanced`/`conservative`）をランダムに割り当てる。
- `simulateTicks`のグループ処理ループ内で、`rotSpan`計算時にそのグループの**多数派チームの`aiStyle`**（または該当グループを支配する1チームの値）を参照し、プレイヤーの`directive.chaseMode`と同様の効果（`push`相当／`hold`相当）を適用する。プレイヤーの作戦は自チームが関与するグループにのみ効かせ、AIチーム主体のグループはそのチーム自身の`aiStyle`に従う形に整理する。
- レース結果画面のコメント等で「〇〇チームは終始ハイペースを維持」のような一言を出すと、バリエーションが体感しやすくなる（任意）。

### 5.3 実装コスト・リスク

- 中。§1のdirective廃止（プレイヤー操作としては廃止だが、シミュレーション内部の`directive`パラメータ自体はチームごとに拡張する形で存続させる）と設計を合わせる必要がある。

---

## 6. 実装順（推奨）

1. **§1.1 事前プラン化**（無線UI撤廃・作戦の事前選択・`RaceView`簡素化）— 最も基盤となる変更。他の項目より先に着手し、以降の作業対象を減らす。
2. **§1.2 ローテーション演出の実時間デカップリング＋§2 俯瞰マップ2次元化**— セットで設計・実装（同じ「隊列シェイプ」データ構造を共有するため）。
3. **§1.3 道路幅拡張** — §2の実装後、実測しながら調整。
4. **§5 AIチーム戦略バリエーション** — §1.1のdirective整理と合わせて着手すると効率的。
5. **§4 天候・横風エシュロン** — §2の2次元モデル（tiltAngle）が前提となるため、§2の後に着手。バランス調整に時間を要する見込み。
6. **§3 最終直線シネマティック** — 独立コンポーネントとして最後に着手可能。

---

## 7. 次フェーズ以降の検討候補（今回スコープ外）

- 最終直線の本格3D化（three.js等の導入）
- 複数セーブスロット
- ハイライトリプレイ
- 選手の通算成績・殿堂入り
- グランツール・海外遠征（v7設計書§6.2の積み残し）
