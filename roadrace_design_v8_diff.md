# 設計差分メモ v7→v8（プレイテストFB反映）

v7実装後のFB6件を反映。実装は`roadrace_v8.jsx`/`roadrace_v8.html`（Downloads）。

## 1. 3D観戦の全面刷新（ワープ・点滅・結果との乖離）

**原因**：(a) ティック解像度が5秒に1回と粗く、(b) 描画時に非牽引選手を「牽引者の位置へテレポート」させるアンカー追従方式を採っていた。ティック物理演算上、ドラフト中の選手はすでに牽引者とほぼ同じ距離を進むため、このテレポート処理は不要などころか、牽引交代のたびに描画位置が不連続にジャンプする原因になっていた。

**対処**：
- `TICK_SEC`を5→1に変更（解像度5倍）。`BASE_TICK_DIST`・`GROUP_GAP_DIST`は実時間あたりの速度／グループ判定基準を維持するよう比例縮小、`ROTATION_PERIOD_TICKS`・`ATTACK_TICKS`・`MAX_TICKS`は実時間の秒数を維持するよう比例拡大
- アンカー追従テレポートを完全廃止。各選手は自身の本当の位置(`r.frac`)をそのまま描画し、チームごとの左レーンオフセットのみ視認性のため付与
- 観戦開始前に「コース読み込み中…」の演出を追加。ローディング解除は独立タイマーではなく、シミュレーションの初回HUD更新に連動させ、タイマー由来の固着リスクを排除
- 📻無線の「スキップ」ボタンは、内部の再生ループ（`setInterval`）を介さず、`tick()`を直接同期呼び出しするよう変更。ブラウザがタブを長時間バックグラウンドにして完全凍結させた場合でも、スキップだけは確実に効くようにした（実機で「タブが最初から非表示」という最悪ケースでも即座に結果画面へ遷移することを確認済み）

## 2. GCステージレースの2日目バグ

**原因**：`generateCourse`が`raceMeta.id + raceMeta.name`のみをシードにしており、2日目も同じraceMetaを使い回すため、コースが1日目と完全に同一だった。さらにティックシミュレーション自体に乱数要素が一切なく、同一コース・同一選手なら結果もほぼ決定論的に再現されていた。

**対処**：
- `generateCourse(raceMeta, dayTag)`にdayTagを追加し、1日目は`"day1"`、2日目は`"day2"`をシードに混ぜてコースを別物にする
- ティックの速度計算に±3%の微小なゆらぎを追加し、同一条件でも毎回結果が変わるようにした

## 3. ゲームバランス（キャンプ券連発・資金余剰）

- ソフトキャップを強化：発動閾値90→88、減衰係数/6→/4（同じ練習を続けても100超えへの到達が大幅に遅くなる）
- キャンプ券に1ヶ月のクールダウンを追加（連続使用不可）
- 選手数に応じた**月次維持費**（1名あたり3万円/月）を新設。ロースターを無闇に増やすことに実質的なコストが生じる

## 4・5. クラスごとの育成速度差・昇格の恩恵

- スカウト候補数をクラス連動に：B1=5名／A=7名／PRO=9名
- **逸材（プロディジー）スカウト**を新設：年ごとに一定確率（B1=28%／A=38%／PRO=50%、おおよそ3〜4年に一度の体感）で、性格＝天才・成長力＝S・能力+12ボーナスの候補が1名混ざる。「🌟逸材」の金色バッジで明示表示
- ロースター上限をクラス連動に：B1=12／A=14／PRO=16

## 5. 選択肢付きランダムイベント（栄冠ナイン風）

- 月替わり（4月・年度末を除く）に35%の確率で、12種類のイベントから1つが発生
- 各イベントは2択で、資金・全体疲労・全体コンディション・キャンプ券付与・故障回復・ポイントなどへの軽い効果を伴う
- イベント解決後は結果テキストを表示し、「続ける」で通常のホーム画面に戻る

## 実機確認状況

- コンソールエラーなしで、スカウト方針確認→スポンサー契約→ショップ（逸材候補5名を確認）→レース編成→3D観戦→スキップ（タブ非表示の最悪ケースでも即座に機能）→リザルト→月送り→ランダムイベント発生・選択→結果反映、の一連を確認済み
- GCステージレース（2日目のコース差異・ティックゆらぎ）はコードレビューで検証済みだが、3月到達に多くのプレイ月数を要するため実機での2日間通しプレイは未確認
