# DEVLOG — ロードレース・プロチーム運営（開発引き継ぎ）

新しいチャットはこのファイルを最初に読めば、巨大な本体ファイルを全読みしなくても文脈を安く復元できます。
**使い方**：新チャット冒頭で「`DEVLOG.md` を読んで続きから」と指示 → 必要な箇所だけ本体を部分読みする。

**サイズ規律**：運用ルールの正本は**CLAUDE.md §4**（閾値60,000B検討／90,000B必須、Read上限
25,000トークン、換算2.5〜2.6バイト/トークン）。ここでは適用先だけ示す：
- 圧縮対象は**各弾の詳細プローズ**（`devlog/waveNN.md`へ移すか、「件名＋要点1行＋
  `git show <hash>`」の索引へ落とす）。
- **生きた参照情報は残す**：§3.5機能一覧・§6方針・「次のアクション」。
  アーキテクチャ（旧§3）は**`ARCHITECTURE.md`へ分離済み**——本ファイル中の「§3」はそちらを指す。
- 過去の圧縮実績・前後バイト数は§27。

---

## 0. これは何か

単一HTMLファイルで動く React + SVG のロードレース・ゲーム。2モード構成：
- **シーズンモード**：6名ロースターのチーム運営（経営）
- **マイライフモード**：一人の選手のキャリア育成（人生）

自転車ロードレースのチーム戦術（エース／アシスト、逃げ、集団、山岳など）をSVGで可視化しつつ、
育成・血統・世界ランキングなどのメタ要素を積み上げてきた。

> **作業ブランチはその時のタスク指定に従う**（セッションごとに変わる）。指定されたブランチが
> 現行の`src/`構成（§1参照）を含んでいるか怪しい場合は作業前に確認すること
> （過去に旧いブランチへ誤って積みそうになった事例あり）。

> ⚠️**デプロイの規律（2026-08・26弾分が未反映だった事故を受けて明文化）**
> リポジトリ直下の`index.html`は**手で書くファイルではなくビルド成果物**
> （`vite-plugin-singlefile`の単一HTML）で、**GitHub Pagesはこれをそのまま配信する**。
> ⚠️**検証で`npx vite build`を使わないこと**——これは`dist/`へ書くだけで直下の
> `index.html`を更新しないため、ビルドが通っても配信物は古いままになる。
> **必ず`npm run build`**（＝`vite build && cp dist/index.html index.html`）を使い、
> 成果物の`index.html`も一緒にコミットする。
> ⚠️**さらに配信ブランチは`master`**。作業ブランチへpushしただけでは反映されない。
> 実機で確認してもらう前に「`master`へマージ済みか」「`index.html`を再生成済みか」の
> 2点を必ず確認すること。実際に第41弾〜第67弾の26弾分がこの2点の抜けで未反映だった。

---

## 1. ファイル構成と開発フロー（重要 / v12モジュール移行後）
**2026-07 に単一HTMLからモジュール分割＋Viteビルドへ移行した（経緯は §8）。真実の源は `src/` に移り、`roadrace_v12.html`/`.jsx` は廃止。**

- **`src/`** … 唯一の編集対象・真実の源
  - `src/main.jsx` … 本体（`App()`の状態・ハンドラ＋画面ディスパッチの薄い配線）
  - `src/index.html` … Viteの入口HTMLテンプレート（薄い雛形）
  - `src/data/` … 静的データ定数のみ（ロジック禁止・JSX/domain import禁止）。`abilities/breeding/course/items/progression/theme/economy/events/directives/gear/teams` 等
  - `src/view/` … 文字列生成の純関数（JSX無し）。`flavor.js`(選手フレーバーテキスト)、`news.js`(ライバル動向・世界ニュース)
  - `src/domain/` … 純粋なドメインロジック（生成器・計算関数、JSX無し）。`season/*`・`mylife/*`・`shared/*`
  - `src/sim/` `src/breeding/` `src/world/` `src/state/` `src/components/` … 分割済み
  - `src/controllers/` … `main.jsx` App() のハンドラのうち `(state, ...args) => newState` の形のものを純関数として抽出。`season/*`・`mylife/*`
  - `src/logic/support.js` … 表示ヘルパー＋残存ロジック（画面イベント効果適用・監督評価・配合表示・実績判定等）
  - `src/screens/season/` `src/screens/mylife/` … 用途クラスタ単位で分割済み。各`hub/`配下はさらに小分類ディレクトリに分割（§9末尾・Step13第7弾参照）
- **`index.html`（リポジトリ直下）** … `npm run build` が生成する**自己完結の単一HTML成果物**（デプロイ用）。React/JSXはビルド時に変換・バンドル済みで**CDNもBabelも不要**。手で編集しない
- **`.nojekyll`（リポジトリ直下・空ファイル・消さないこと）** … GitHub PagesのJekyll処理を止める。
  ⚠️ **これが無いとPagesが全ての`.md`をHTML化しようとし、`devlog/`内のJSXコード（`style={{…}}`）を
  Liquidのテンプレート変数と誤読してデプロイが丸ごと失敗する**（実際に第34弾で発生し、
  9回連続で失敗して公開ページが第32弾で止まっていた）。このリポジトリはJekyllサイトではなく
  ビルド済み`index.html`を1枚配信するだけなので、Jekyllを走らせる必要がそもそも無い。
- `package.json` / `vite.config.js` / `package-lock.json` … ビルド定義
- `dist/`, `node_modules/` … gitignore（追跡しない）
- `archive/` … 過去バージョンのアーカイブ（`roadrace_v5〜v11.*`＝分割前の単一ファイル版、`archive/design/`＝旧設計メモ）。src/から未参照・触らない

### 編集→ビルド→コミット→push の手順
1. `src/`を編集（対象モジュールだけ読めばよい＝トークン激減）
2. `npm run build`（`vite build && cp dist/index.html index.html`。初回のみ`npm install`）
3. コミット（日本語メッセージ。モデル識別子は書かない）→ 指定の作業ブランチへpush
   （defaultへ直push禁止）
- **`index.html` は生成物**。src を直したら必ず `npm run build` してから両方コミット（＝新しい二重管理を作らない）。

---

## 2. 検証（Playwright）— ビルド成果物をそのままテスト
**移行でCDN依存が消えたため、vendor版React差し替えハックは不要**。`npm run build` が出す
`index.html` は自己完結（React/JSXバンドル済み・CDN/Babel不参照）なので、クラウド環境でも本物をそのまま開ける。

`npm run build` → `npx http-server -p 8844 -s -c-1 .`（run_in_backgroundで起動・curlで200待ち）
または`npx vite --port 5190`の開発サーバ → Playwright（scratchpadに置く。
`executablePath: /opt/pw-browsers/chromium`）。
テストのコツ：作戦ピッカーは**メイン画面**（「このレースに出場する」ボタンの上）にあるので、
作戦を選んでから出場する。controlled input は `page.fill` を使う（`inp.value=` はReactに効かない）。

---

## 3. アーキテクチャ → **`ARCHITECTURE.md` へ分離**（2026-08・第38弾）

現行仕様の正本は **`ARCHITECTURE.md`**（レースsim／マイライフ状態／配合・血統／生きた世界／
UIコンポーネント／スプライト描画／画面の外枠／ボタンとアフォーダンス／機械検証ゲート／
localStorageキー一覧）。**本ファイル内の「§3」「§3「〜」参照」はすべてそちらを指す。**
コードの計算式・定数・「触ると何が壊れるか」を知りたいときは、まずそちらを直接Readすること。

## 3.5 実装済み機能インベントリ（全体像・再実装しないための一覧）
※ v11〜現行で積み上げ済み。細部は本体を参照。**ここにある物は「もうある」前提で扱うこと。**

### レースsim・可視化
- レース全体を先計算→再生する方式／2D集団フォーメーション／ローテーション・パスラインの縦可視化／カメラの選手追従
- 天候（晴/雨/猛暑）＋横風エシュロン、特能「悪天候巧者」
- 最終スプリントのドット絵演出（`PixelBike`、§16）／フィニッシュのズーム＆スローモ／周回レース
- コースレコード、実況の拡充、コースを道の曲線に沿わせる、逃げ要員が実際に逃げる、最終区間スプリント戦術
- 選手識別は引き出し線＋名前タグ方式に統一（自分=水色／エース=黄／自チーム=青／ライバル=赤／殿堂選手=別トーン赤、§20）

### シーズンモード（チーム経営）
- 6名ロースター運営、B1/A/PRO の3クラス昇格
- スタッフ雇用／FA移籍市場／選手トレード／移籍志願／退団選手をライバルチームが拾う／被引き抜き・引き抜き（双方向）
- グランツール（年3つ・海外遠征）＋副次分類、ステージ間の役割再配置
- チーム化学反応、キャプテン制、実績システム＋報酬、チーム順位表、拠点(BaseView)の部屋グレードアップ（§14）
- ユースアカデミー枠、血統ユース、施設アップグレード可視化、トロフィールーム
- ライバルチーム動向ニュース、会場相性/ホームアドバンテージ、クリア後の王朝継続(NG+)
- カイロソフト式の拠点画面（間取り・カメラ・什器・人物ドット絵・吹き出し、§10〜13）

### マイライフモード（選手育成）
- 月次ループ（出走／練習／完全休養／私生活イベント［受動発火・§18］）、経歴選択（高卒/大卒/実業団卒）
- 監督指示＋監督評価、年俸改定、契約交渉、移籍オファー、移籍入札戦
- ショップ（パーツ/消耗品/恒久ギア/車/住居/成長力・成長タイプ買い切り）、能力別コーチ（Lv制・月給・§50〜68）
- 家計と赤字ペナルティ（生活費／所持金はマイナスになる／3段階で悪化し最後は強制売却、§50〜68）
- ライバル（1人目・2人目）、節目の大会（世界選手権/五輪）、代表での役割
- 人生の岐路（結婚／大怪我＋3択リハビリ／第一子／恩師との別れ）、オフシーズンの過ごし方
- 個人スポンサー＋人気度＋スポンサーの仕事、自伝出版、引退後エピローグ（監督/完全引退）
- 恩師（師弟）＝新人時代の指導、教え子（プロテジェ）、メンター役
- フォーム、ピーキング、フォーム予報、天候予報、伸びしろヒント（成長力マスク中は専用しきい値、§25）
- 成長キャップの実績連動（§17）、出走経験による成長、対戦相手(AI)の年々底上げ
- 世界ランキング＋アンビション4路線（勝利/大舞台/献身/世界）、キャリアの生き様（称号）
- 配合（血統）：ニック／インブリード／累代+値／金の特殊能力クロス／配合限定特能／生き様の血／相性表／系譜ツリー
- 爆発力＆配合評価／危険度／系統確立＋因子／特殊配合／生きた世界（永続ペロトン＋殿堂の血流入）／モニュメント／アシスト作戦
- 血脈レシピ（第15弾）：世代をまたぐ順序付き隠しレシピ5件。成立で伝説特能＋成長曲線緩和＋
  専用称号。配合画面に段階的ヒント（最有力1件のみ強調・§46）
- ライバルの加齢・引退・後継登場、月次world ticker（他クラス優勝者の一言）、年度末世界ニュース
  7優先度への拡充（第16弾・§47）
- イベント：能力/新ステ変動・賭け（ダイジョーブ博士系）・覚醒・悪イベントの5分類、46件以上（継続拡充中、§18）
- イベント・岐路・オフシーズン・弟子指導での能力変化は必ず差分表示（`abilityDeltaSummary`/`protegeEffectSummary`、§21）
- 「今月の行動」の推奨表示（疲労・レース有無・大舞台から1つ判定、§24）

### 共通・メタ
- 難易度ティア（成長キャップ＋AI強度）、クリアポイント＋パーク屋（メタ進行）
- 選手ごとの戦績・ニックネーム・フレーバー、殿堂（プレイ跨ぎ）、累積タイトル台帳
- 特殊能力システム（銅→金への進化、特殊能力図鑑）、副ステータス（加速/体格/メンタル/突破力/安定感/運）
- 生涯プレステージスコア画面、ヘルプタブ（両モード）
- UIテキストの平易化・重複表示の統合（§22/§23/§26、CLAUDE.md §7に恒常タスクとして明文化）

---

## 4. 実装履歴（コミット索引）

v33系〜現行まで、すべてPlaywright（＋純ロジックはNode単体）検証・push済み。
**「何が実装済みか」は§3.5のインベントリを見るのが早い。** ここは「いつ・どのコミットか」を
辿るための入口として、キーワード群だけ残す。`git log --oneline --grep="<キーワード>"`→
`git show <hash>`で当時の実装箇所・判断根拠・検証結果まで復元できる。

- **レース演出の系譜**：`v39`（判断カード導入＝観戦→"プレイ"へ）／`v39.1`（脚質・特性・役割・
  地形と連動）／`v39.2`〜`v39.11`（ゴールスプリント演出を放送カメラ風→横視点→斜め→
  アイソメ→ドット絵化と繰り返し作り直した経緯）／`v39.12`〜`v39.20`（俯瞰マップ強化・
  展開戦術をsim本体へ・沿道の追従・難易度連動）
- **シーズンの駆け引き**：`v40`（中期目標）／`v41`（引き抜き合戦）／「相手チームを永続ロースター化」
- **世界の統合（#9）**：`A-1`統合ダイナスティ・ハブ／`A-2`レジェンド招聘／`A-3`共有ワールド／
  `A-4`選手→監督の転身ブリッジ／`B案`配合・系統の深掘り(B-1〜B-4)
- **拡充系**：特殊能力（5種→第2弾7種）／パーソナリティ3種＋異名／CPショップ／クリアポイント／
  チームTT／逆メンター（弟子育成）／引退セレモニー＆殿堂／メディアナラティブ・因縁の育つライバル
- **バランス系**：配合点検・危険度実効化・難易度つまみ・エース早期発射・地形別フィニッシュ決着／
  アシストの観戦⇔リザルト同期ズレ根治

---

## 5. 今後の候補（ゲーム内容）

**未着手の候補は「次のアクション」へ一本化済み**（このセクションは意図的に空）。残すのは
現状維持を選んだ設計判断だけ：
- **山岳×クライマーのアタック勝率(81%)／oni難易度の判断効果(0.66)**：設計意図として現状維持中
  （下げる場合の変更点は把握済み、`committedBreak`/`MOVE_EFF_BY_DIFF`）。

---

## 6. 守るべき方針メモ
- 配合・世界系のボーナスは **伸びしろ／称号／金の特殊能力** に限定（初期能力インフレ厳禁）。
- 新機能は必ず Playwright で end-to-end 検証してからコミット（数値ロジック＋UI表示＋0エラー）。
- ヘルプ（`mylife_help`画面）にも新機能の説明を追記して整合を保つ。
- **編集は `src/` のみ**。`src/`を直したら必ず`npm run build`してから両方コミット。`index.html`は生成物なので手編集しない。
- **機能設計の仕様書に書いたUIも、CLAUDE.md §8の引き算を先に通す（第37弾で判明）**。
  §8は「デザインは足し算ではなく引き算」を掲げているが、**機能の設計書の中でUIを規定すると
  その検討をすり抜ける**。第36弾がまさにそれで、仕様どおり実装した結果が同じ情報を3箇所に
  出すパネルになった。機能設計でUIに触れるときは、その場で「消せないか」を1度通すこと。
- ⚠️**`wc -m`で日本語の文字数を数えない**（第65弾で発覚）。この環境は`LANG`/`LC_ALL`が
  未設定のため`wc -m`はバイトを数え、日本語は**約3倍に膨らむ**（ヘルプ本文を「16,769文字」と
  誤記録していたが正しくは5,676文字）。`node -e '[...str].length'`を使う。
  （`wc -c`＝バイト前提のDEVLOGサイズ閾値管理には影響しない。）
- ⚠️**`T.color.bg`を文字色に使わない**（第61弾）。「背景」と「明るい面の上の文字色」の
  兼任がWCAG AA割れの原因だった。明るい面に乗る文字には`ink`を使う。
- ⚠️**「アニメーションが動かない」報告はコードより先に`prefers-reduced-motion`を疑う**
  （第68弾）。OS側の「視差効果を減らす」がONだと全遷移が`.01s`になり、完全に静止して見える
  （仕様どおりの安全策であってバグではない）。
- ⚠️**画面遷移の静的解析は変数経由の遷移を取りこぼす**（第64弾で誤検出3件）。
  `screen: "X"`だけを見ると`linkBtn(…,"X")`・三項演算子・変数経由を見落とす。
  件数を報告する前に1件ずつ裏取りすること。
- **GIFの中身を確認する手順**：base64でHTMLに埋め込み→Playwrightで開いて一定間隔で
  コマ撮り。⚠️`file://`のサブリソースはブロックされるため必ずbase64で埋め込む（第66弾）。
- ⚠️**弾を締めるとき、その弾で先送りにしたものをDEVLOG本体の「次のアクション」へ上げる**
  （2026-08・ユーザー指摘で判明）。`devlog/waveNN.md`に「別弾」「別途」「未実装」と書いた
  だけでは**誰も拾わない**——実際に全62ファイルを洗い直して9件の積み残しが埋もれていた
  （TODO #27）。`devlog/`は「読みたい時だけ開く詳細」なので、次に効く情報は本体へ出すこと。

---

## 7〜12. 2026-07 の構造移行とカイロソフト式への刷新（索引）

**設計原則は CLAUDE.md §5 が正**（依存の向き・`data/`と`view/`の制約・データとロジックの線引き）。

- **§7〜8 モジュール分割＋Viteビルド移行**（完了）：単一HTML（約9,300行）から
  Vite + `vite-plugin-singlefile`で「自己完結の単一`index.html`」を出力する方式へ。
  `main.jsx`は9,624行→2,395行→（Step11で）48行の合成ルートのみに。撤廃済み：`.jsx`手作業ミラー／
  実行時Babel／実行時CDN依存。`git log --oneline --grep="Phase"`
- **§9 モジュール細分化（Step1〜13）**：`domain/`・`controllers/`・`hooks/`の抽出を段階的に実施。
  過程で複数の実バグ（グランツール3日目進行不能・連打二重加算・OBコーチ雇用即クラッシュ・
  `rankSim`二重呼び出し）を発見・修正。Step13第7弾でメニューを17小分類へ細分化し
  `hub/{riders,facility,market,race,records}/`へ分割。`git log --oneline --grep="Step"`
- **§10〜12 カイロソフト式動線への移行／敷地画面のカットアウト化／ドット絵化**：常設マップ＋
  右下メニューへの刷新、BaseViewの部屋割り・什器・カメラの作り込み、人物・自転車をベクターから
  ドット絵グリッドへ全面差し替え。`git log --oneline --grep="Wave"`
  - **CLAUDE.md §6の根拠となった弾**（向き系バグの真因が参考画像側にあった件。手順は§6が正）。

---

## 13〜23. 2026-08 Wave群（索引）

件名・要点1行・`git show <hash>`で詳細を復元できる。§17〜18の技術的な核心（成長キャップ式・
新ステータス）は§3へ転記済み。

- **§13 拠点の吹き出し**：持ち場ごとのセリフを性格・状態から決定論的に選ぶ`riderChatter.js`。`git show a4d57fe`
- **§14 部屋のグレードアップ**：4持ち場を資金で0〜3段階に強化（見た目のみ）。`git show 427b4f7`
- **§15 トレーニング室の「ぼっ立ち」修正**：新規ドット絵無しで解決。`git show`で復元可
- **§16 最終スプリントのドット絵化**：`IsoRider`→`PixelBike`。`<defs>+<use>`方式で22名分を描画
- **§17 マイライフ難易度調整 Phase 1**：成長曲線の逓減カーブ是正・成長キャップの実績連動化（式は§3が正本）
- **§18 同 Phase 2**：私生活イベントを受動発火へ、新ステ「運」追加、イベントスキーマを整理
- **§19 最終スプリントの姿勢問題**：姿勢判定がPRO帯基準の絶対値で下位クラスでは発火しなかった
- **§20 選手識別マーカーの統一**：★・水色の丸を廃し引き出し線＋名前タグ（`RiderNameTag`）へ。`git show bee11a2`
- **§21 イベントによる能力変化を必ず明示**：無言だった3箇所を`abilityDeltaSummary`等で構造的に解消。`git show 7414ae9`
- **§22 UIテキスト 第1波**／**§23 第2波**：開発語彙・保証文・重複解説を一掃

---

## 24〜31. 2026-08 改善群（索引）

この範囲にあった**今も有効な技術的事実**——`mlNextAction()`の優先順位、マスク値の扱いと
`mlTalentRank`/`potentialHint`の非公開時しきい値、`breakthroughMul()`の感度式、能力表示UIの構成、
`Btn`のrole分類、`makeWrap`/`makeMetaWrap`の使い分け——は**すべて§3へ転記済み**。
通常はそちらを見れば足りる。

- **§24 導線・ユーザビリティ**（→#15）：「今月の行動」ブロック新設、密度109→96要素へ
- **§25 素質ランク圧縮＋成長力リーク修正**：デビュー特典が成長力の等級を生で漏らすバグを是正
- **§26 能力表示の重複統合＋用語統一**：棒グラフとレーダーの二重表示を整理、「コース適性」へ統一。`git show 5e19c01`
- **§27 DEVLOGスリム化＋サイズ基準の明文化**：閾値をCLAUDE.md §4へ明文化（本ファイルは第9回まで実施）
- **§28 能力上限の収束問題**（→#16）：突破力の感度が緩すぎて最終到達値の差が+2.2点しか出ていなかった
- **§29 スタート画面の整理＋導入説明**（→#10）：モード非依存の画面にシーズン情報が漏れていた
- **§30 CP関連の導線・文言整理＋ジャーゴン一掃**：CP交換所をモード選択から直接開けるように
- **§31 UIテキスト 第3波**：8並列エージェント監査で40ファイル・約80箇所を修正

---

## 32〜37. 2026-08 バグ修正・観戦品質・描画コスト群（索引）

件名・要点1行・`git show <hash>`で詳細を復元できる。**今も有効な技術的事実（残脚ゲート・
演出専用乱数・スプライト描画方式）は§3へ転記済み**、永続ロースターの定員は§3「生きた世界」に転記済み。

- **§32 最終スプリント演出のバグ4件**：順位表が全員TOP表示・ゴールラインの形状ほか
- **§33 永続ロースターの摩耗補充**：引き抜きで摩耗した`ageWorldRosters()`の定員を補充（定員は§3）
- **§34 キャリアグラフのラベル重なり＋観戦カメラの緩和＋序盤2年の引き抜き抑制**
- **§35 早駆けが常時最適解だった問題**：残脚が空でも早駆けが最上位勝率になっていた（残脚ゲートは§3）
- **§36 第5弾：レース観戦の不整合一掃**：ゴール後の順位変動・スプリント解除の早期化ほか
- **§37 第6弾：ドット絵スプライトの描画コスト**：真因は出走人数ではなくジャージ色数だった

---

## 38〜49. 第7弾〜第18弾（索引）

技術的な核心はいずれも**§3「アーキテクチャ」へ転記済み**（成長キャップ式・残脚ゲート・
チームドラフト・TTペース配分ゆらぎ・血脈レシピ等）。ここは件名と復元先のみ。

- **§38 第7弾（マイライフの成長設計）**：加齢衰退のバグ修正／練習コスト／年次インフレ
  `yearBonus`廃止→実在ロースターへ一本化。`git show 012cbaa`（実装）`704fa38`（設計）
- **§39 第8弾（レース作戦・判断カードのバランス）**：`ML_TACTICS`5種の生死確認、位置取りを
  実力ベースへ再設計、`LEGS_EMPTY`/`LEGS_FULL`再較正、判断カード6種を9区分で較正。
  `git show d10e37e` `d9acd62`
  **教訓**：①実測値は測った時期を併記し土台が動いたら測り直す②ゲートは発火の有無を先に確認して
  から較正する③効かない係数を動かす前に支配的な効果を切り分ける。
- **§40 第9弾（判断カードの演出）**：レア度は「強さ」ではなく**振れ幅**（虹＝大勝負／金＝手堅い）。
  `domain/shared/moveEdge.js`・`components/DecisionCard.jsx`新設。**全文は`devlog/wave09.md`**
- **§41 第10弾（味方との協力の実感）**：ドラフト消耗軽減が集団人数だけで決まり「前が味方か敵かで
  計算が1ビットも変わらない」＝協力が存在しなかった問題をチームドラフト新設で解消。あわせてAI選手の
  能力が毎レース再ロールされていた実装漏れを`idYearSeed`で統一。**全文は`devlog/wave10.md`**
- **§42 第11弾（リーグの可視化＋能力開示）**：順位表の実体化・二重世界の統合
  （`mlWorldStarsForYear`退役）・査定値の4段階開示・人口300名化。**全文は`devlog/wave11.md`**
- **§43 第12弾（アイテム・機材と経済圏）**：選手年俸制（`domain/season/salary.js`）・パーツ強化Lv
  （`player.partLv`）・CP新カテゴリ。旧セーブは初回移行支援金。**全文は`devlog/wave12.md`**
- **§44 第13弾（デザインの作り直し＋`support.js`分割）**：絵文字653個ほぼ全廃・全画面新トークン化。
  **恒久ルール＝可読性R1〜R5**（R1数値をlabelへ連結しない／R2可変幅3値以上は固定幅右揃え列／
  R3状態は対象物自体の色で／R4主役16px・脇役12px／R5アクセントは少数の値だけ）。配色は
  CLAUDE.md §8/§9が正。**全文は`devlog/wave13.md`**
- **§45 第14弾（simの精度＋`RaceView.jsx`分割）**：luck項を`Math.random()`へ・マイライフ個人TTを
  単独走化＋TTペース配分ゆらぎ新設・5ファイルへ分割。**全文は`devlog/wave14.md`**
- **§46 第15弾（血脈レシピ＋`state.js`分割）**：順序を含む隠しレシピ`ML_BLOOD_RECIPES`5件＋伝説特能
  5種＋段階的ヒントUI。判定は`mlCreateChar`に閉じシーズン血統ユースへ無影響。**`devlog/wave15.md`**
- **§47 第16弾（生きた世界＋`sim/race.js`分割）**：ライバルのライフサイクル（`ageRival`）・年度末
  ニュース7行化・月次worldTicker・`seasonWorldNews()`実データ化。**`devlog/wave16.md`**
- **§48 第17弾（機材トレードオフ）**：PARTS19種を「特化↔無難」へ全面リワーク＋天候・地形特化ギア
  3種＋AIの`aiSetupFor()`。**較正の教訓：単独走は能力差測定に不適**（集団力学が働かない）。
  **`devlog/wave17.md`**
- **§49 第18弾（僚友と絆・スピリット接続）**：新サブステ「スピリット」＋レーダー7角形化・`ml.bonds`
  （共闘で育つ絆→結束chemMul→年度末baseline上乗せ）。実測：絆0→100でpodium率+2.7pt。
  **`devlog/wave18.md`**

---

## 50〜68. 第19弾〜第37弾（索引）

いずれも**完了**。各弾の全文は`devlog/waveNN.md`（NN＝弾番号）。現行の仕様・定数・部品は
`ARCHITECTURE.md`が正本、恒久ルールはCLAUDE.md §7〜§9が正本。
コミット本文は`git log --oneline --grep=第NN弾`／`git show <hash>`で復元できる。

- **50〜57（第19〜26弾）＝拠点のドット絵化と再設計**：ドット絵化／L字コース・T字間取り・
  条件解禁部屋／芝のはみ出し修正＋屋外の行き先／実データ連動の見た目成長／重なり順・導線の
  総点検＋散歩先・セリフ拡充（**屋外ルート×建物footprintの交差チェックを恒久化**）／
  室内プロップ11種の再抽出／F分類（芝・コース・プラザ・床）／ズームボタンの補間。
  描画方式は`ARCHITECTURE.md`「ドット絵スプライトの描画方式」、導線の担保は
  `tools/verify_baseview.mjs`（第20弾で新設・実装中に3件の配置バグを検出）。
- **58〜60（第27〜29弾）＝マイライフの中身**：イベント46→76件／新ステータスの配合遺伝／
  脚質ごとの能力別成長上限差（`ML_TYPE_CAP_OFFSET`）。
- **61〜62（第30〜31弾）＝レーダーと成長キャップ**：レーダーの外周を最大上限で全軸統一＋
  各能力の上限を点線多角形で重ねる（`tools/verify_radar.mjs`を新設。⚠️**レーダーに触る変更は
  必ずこのゲートを通す**）／AIの`cap`が全能力一律だった問題を`newRider`の`opts.capOffset`で解消。
- **63〜66（第32〜35弾）＝第2次UI改革**：診断「文字が多すぎる」の実体は**12px補足文字の平坦な
  敷き詰め**（フォント指定の68%がcaption）で、処方は全体縮小ではなく**「12pxの量を削り、
  残りに差をつけ直す」**——**この診断の型は再利用できる**。絵文字653個は機能記号`★``☰`を除き
  全撤去。第35弾の診断は「1つのサイズ段が5つの仕事を兼任している」で、原因は共通部品2つ
  （`kit.jsx`の`PrimaryBtn`が型に反する`body`13px／`rider.jsx`・`archive.jsx`が`Item`を使わず
  ローカル再実装＝CLAUDE.md §5の二重管理）。確立した級数の型は`ARCHITECTURE.md`
  「ボタンとアフォーダンス」。副産物：モックの脚質説明文が**実データに無い創作文**だったと
  判明（＝CLAUDE.md §8「モックは実データ・実文言で作る」の由来）。未達は「次のアクション」12番。
- **67〜68（第36〜37弾）＝マイライフ経済とそのUI圧縮**：v27の生活費が「資金のダブつきを抑える」
  狙いを宣言しながら`month.js`の`Math.max(0,…)`で不足分を黙って帳消しにしていた問題を修正し、
  専門コーチをLv制＋月給制、赤字を3段階のペナルティへ。仕様と定数は`ARCHITECTURE.md`
  「マイライフ状態」。続く第37弾でコーチ節を372px相当→**138px**（かつ**雇用人数によらず高さ一定**）
  へ圧縮。生きたルール2つ（**同じ情報を2箇所に出さない**／**機能設計のUIも§8の引き算を通す**）
  は`ARCHITECTURE.md`・§6へ転記済み。


## 69〜81. 第38弾〜第51弾（成長カーブとバッジシステム）— 索引

いずれも**完了**。各弾の全文・実測表は`devlog/waveNN.md`。**現行の仕様・定数・実測で確定した
結論は`ARCHITECTURE.md`が正本**（成長フェーズ／バッジの3分類／枠と段階／`FINISH_BADGE_K`／
退行が成立しない理由／集団の速度構造／測定の必須手順）。ここは経緯の索引のみ。

- **69（第38弾）成長カーブ**：カンストの真因は成長上限ではなく`growthPhase`の30歳の崖だった
  （旧メモ「3〜4年目にカンスト」は測定誤り）。係数と`GROWTH`のpeak終端を再較正。
- **70〜71（第39〜40弾）バッジ Phase 1〜3**：「レース経験でpt→解放・強化」は新機能ではなく
  **既に存在していた**（`ABILITIES`48種・sim適用点47箇所）。NBA 2Kの3世代を調査し、
  ③属性ゲート制は第38弾で外した頭打ちを前提にするため不採用、②使用量成長を歯止めの方針に。
  条件を`{gate,cur,need,unit}`へ構造化し（74,000パターンで挙動一致を確認）、月15%抽選を廃止して
  プレイヤーが選んで習得する形へ。使用量の計装（`segMix`/`badgeExposure`）もここで入れた。
- **72（第41弾）レース選択＋目標宣言**：Phase 4に着手したら前提が崩れていた——マイライフは
  月1レースが暦から決定的に生成され**プレイヤーは地形を選べず**、露出率が選択を反映するのは
  36種中3種のみだった。`mlGenRaceCandidates`で通常月3候補を出す形にし、目標宣言（しおり）を対で追加。
- **73（第42弾）退行モデル＝撤回**：出荷直後の計測で撤回（⚠️**原理的に成立しない。再検討しない**）。
  設計時の誤りは`base`/`target`を**キャリア平均で測り分散を測らなかった**こと（第36・38弾と同じ
  「測らずに置いた定数」の型）。さらに出荷後の検証でも比較対象を誤り**一度誤った報告をしている**。
- **74（第43弾）出走計画**：退行撤回の根本原因（地形を選べない）を`ml.raceFocus`で解消。
- **75〜78（第44〜48弾）枠・4段階化・取得経路・伝達バグ**：枠をクラス連動で増やし、
  段階を銅/銀/金/虹の4段へ。9種に「そのバッジらしい遊び方」の取得経路を新設。
  第48弾は第45弾のバグ修正（**銀・虹がエントラントへ伝わっていなかった**）。
- **79（第49弾）計測のみ**：**AIへの段階配布は不要**で決着（第45弾からの積み残しを終了）。
  同一シードで段階だけ差し替えた対応比較（n=192/クラス）で**着順の平均改善は全区分で誤差内**。
  効果は実在するが**2,500秒のレースで0.13%**＝個別レースの振れ幅(±58秒)の1/10以下。
  較正すると**シミュレータ自体は能力に素直**（全能力+7で勝率+6.8pt・+40で77%）＝鈍いのではなく
  **効果が特定区間にしか乗らず実効的な能力差が小さい**。代わりに「**段階の効果量そのものが
  小さすぎる**」という課題が浮上した（4弾を費やした4段階システムが機構は正しいのに体感できない）。
- **80（第50弾）決着への合流**：`finishAbility`が`segmentAbility`のバッジ分を見ておらず
  **僅差ゴール＝約半分のレースでバッジが無かったことになっていた**のを塞いだ。
  合流自体は成功（勝率22.4%→27.1%）だが**段階差は小さいまま**（銅→虹で+1.0pt＝誤差内）。
- **81（第51弾）「選べる回数」への転換**：数値で個性を出す道が塞がっている（上記79・80）ため、
  強さではなく**判断の機会**をバッジの報酬にした。基本スロットを`mid`+`finale`の2枚に削減し
  （バッジ無しは3〜4回→2回。ユーザー合意のうえ意図的）、得意地形の専用カードを新設して
  所持する地形バッジの**最高段階**で回数を決める（銅銀金＝1回／虹＝2回。⚠️シーズンでは出さない）。
  専用カード限定の一手`tempo`「ふるいにかける」＝自分は速くならず**同じ集団の他選手の
  `keepThresh`を厳しくして集団を削る**。⚠️**`tempo`の3定数は未確定のまま**（実測の過程で判断
  カード全体に効く既存バグ2つを発見し「壊れた前提の上で測った数字」と判明したため）。


## 82〜85. 第52〜55弾（逃げを成立させる4回の挑戦）— **打ち切り済み（§88〜89で決着）**

⚠️**再開条件は「次のアクション」の「やらないと決めたこと」が正本。ここは索引としてのみ読む。**
成果として残ったのは第52弾Phase 1（`resumeSim`の順序バグ修正）のみ。第53・54・55弾は
3回とも失敗し`src/sim/ticks.js`は第52弾完了時点（`c76c0b6`）へbyte一致まで戻してある。

| 弾 | 何を触ったか | 失敗の仕方 |
|---|---|---|
| 52 | `attack`等の定数 | 定数では動かない（構造の問題と判明） |
| 53 | 集団ごとのテンポ | 先頭集団が最も遅くなり後続が追いつき融合 |
| 54 | レース全体のテンポ | `hold`が3〜6着悪化・グループ数−24% |
| 55 | 協調による速度(`coopMul`) | `hold`が5着悪化（レースが短くなり選抜が減る） |

⚠️**確定した結論：速度を触るアプローチは尽きた。軸そのものが行き止まりで再提案しない。**
`hold`の維持と逃げの成立が同じレバー（選抜が累積する時間）から出ているため、
速度を動かすと必ずどちらかが壊れる。代わりに残る軸（`brk`＝エネルギー経済）は
`ARCHITECTURE.md`のレースsim節が正本。全文は`devlog/wave52.md`〜`wave55.md`。

## 86〜114. 第56〜85弾（2026-08セッション）— 索引

各弾の全文・実測値・却下案は`devlog/wave56.md`〜`wave80.md`（第75〜80弾はすべて
`devlog/wave75.md`にまとまっている）。ここは**1〜3行の索引と、今も有効な事実だけ**。

- **§86 第56弾（レース終盤が計算されていない）— 完了**
  ⚠️**牽引者が居ない集団は全員の前進が0**になる不具合を修正（全テンプレの13〜43%で発生）。
  未完走率が全テンプレ0%に。⚠️`MAX_TICKS`7000は**予算ではなく安全上限**（全員ゴールで早期break）。
- **§87 第57弾（勝負所のカードから「不発」を廃止）— 完了**
  ⚠️**各moveには効果の下限があり「効果0」は存在しない**ため`不発`表示を廃止。
  ⚠️この弾以降、**実機スクリーンショットでの確認**を検証項目に含める運用にした。
- **§88〜89 第58〜59弾（プレイヤーの逃げを成立させる試み）— 打ち切り**
  実装した機構（`bridgedFrom`/`bridgeCap`/`rejoinLeft`）は撤退基準を満たすためrevertせず残置。
  ⚠️**理由と再開条件は下の「やらないと決めたこと」が正本。**
- **§90〜§92 第60〜62弾** — 内部指標の生値露出など9件の修正／配色トークンの底上げ
  （⚠️原因は「太さの軸が無い」ではなく**基準の明度が床に張り付いて逃げ場が無いこと**だった）／
  ⚠️`TAB_HIDDEN_SCREENS`の入れ忘れで**目標バッジを永久に設定できなくなる**不具合の修正。
- **§93〜§95 第63〜65弾（入口とヘルプ）— 完了**
  入口30秒の導線整備／`遊び方を見る`を独立行へ／ヘルプ22節を17節3グループへ再編。
  ⚠️**シーズン側は未着手で`遊び方`/`ヘルプ`の表記の食い違いが残る**（既知）。
- **§96〜§98 第66〜68弾（画面遷移アニメーション）— 完了**
  マイライフ35画面を6分類へ／シーズンは⚠️**メニューの18セクションが一切アニメしていなかった**
  （`g.screen`が`main`のまま`menuState.section`で中身を差し替える方式のため）。
  ⚠️iPadの「動かない」報告の原因は**iPadOSの「視差効果を減らす」**（仕様どおり・バグではない）。
  ⚠️**WebKitはこの環境で実測できない**ため、実機側で走る診断Artifactを作って確定させた。
- **§99 第69弾（「戻る」の文言統一）— 完了** マイライフ側10箇所を`← 戻る`へ。
  `モード選択に戻る`/`タイトルに戻る`は意味が異なるため対象外とした。
- **§100 第70弾（クリアポイント経済の再設計）— 完了**
  ⚠️**生きた設計原則**：CPを**強さ**（`CP_BOOST_DIFF_MUL`で難易度が高いほど絞る）と
  **選択肢**（出走計画・恒常上限拡張・情報の解禁＝難易度不問）に分離した。
  ⚠️出走計画はシーズンに宣言UIを作らず**ロースターで最も多い脚質を自動で宣言地形とみなす**。
- **§101 第71弾（第2次UI改革・引き算での再着手）— 完了**
  ⚠️**配色変更では級数分布は動かない**（当然）。引き算で3種類の重複を除去。
  ⚠️**キャラ作成8.3%・パーツ16.1%・共通ヘッダは目標未到達**——恒常タスク#9で継続点検。
- **§102 第72弾（初期のまま触っていない要素の棚卸し＋脚質の穴埋め）— 完了**
  RUL専用コース「平坦ロード」新設・`buildLastRaceMeta`をハードコード添字から`favors`検索へ。
  ⚠️**`segMixOf`の距離indexずれと`terrainOfMix`のフォールバック追加は同時に入れないと
  山岳クラシックが`null`へ退行する**（この2件はセットで扱うこと）。
- **§103 第73弾（早期カンスト再発疑い）— 測定のみ・修正不要でクローズ**
  ⚠️**教訓：古い観察はその後の設計変更で無効になっていることがある。** 埋もれた宿題を
  掘り起こしたら、まず現状のコードで再現するかを測ること。
- **§104 第74弾（出走計画の実測・TODO#27-b/27-d・ナイトクリテ撤去）— 完了**
  `m_plan2`を「2本目」から**「2地形目」**へ作り替え／グラベルを丘陵優勢・丘決着へ／
  ナイトクリテ撤去でflatの占有率50%→33%。⚠️**教訓：「選択肢が増える」商品は、
  プレイヤーがその選択肢を行使できる回数とセットで測らないと価値が分からない。**
- **§105〜§109 第75〜80弾（脚質バランスの破綻・TODO#28）— 5/6まで到達**
  ⚠️**残った平坦ロードのtt決着問題は次の§110〜111で完結する。** 詳細は`devlog/wave75.md`。
- **§110〜§111 第81〜82弾（「TTの居場所を定める」・TODO#26/27-e/28完結）— 6/6達成**
  #28・#26・#27-eが同じ問題（TTが自分の居場所を持たず他人の居場所を奪う）の別の顔と判明。
  ⚠️**`finishAbility`に`flat`決着の分岐が無くelse→sprintに落ちていたのが根本原因**——
  平坦ロードは`tt`決着でも`flat`決着でもRULが勝てない構造だった。`flat`決着を新設
  （`fl*0.6+sp*0.4`）し平坦ロードの決着を`tt`→`flat`へ変更した結果、**想定脚質が最上位の
  コース：5/6→6/6（8回平均で確認・全コース目標達成）**。あわせて世界選手権を
  「サーキットレース(SPR) or 個人TT(TT)」の2択へ（月を消費せずSPR/TTへgrade4の
  大舞台を開放・候補1本の月6/12→5/12）、グランツール3戦を組み直し（`tt`比率0%→8.9%・
  死蔵コース4→2種）。⚠️**自分の測定ミスを1件訂正**：「年度末が山岳ロード固定」は
  5年目1年分だけを見た誤報告で、実際は年ごとにシャッフルされ既に実装済みだった
  （実際の偏りは世界選手権が全年PUN固定だったこと）。詳細は`devlog/wave75.md`。
- **§112 第83弾（実機での通しプレイ）— 文言バグ2件を発見・修正**
  ⚠️**第75〜82弾の8弾を一度も実機で遊ばずに進めていた**ため、DEVLOG §87（第57弾）の
  教訓に従いPlaywrightで実際に操作した。⚠️**①チームTTの結果画面で「null位から後退」**
  ——第60弾が同じ不具合を直した際、通常レースの結果画面だけ修正しチームTT側
  （同一ファイル内のもう1箇所）を取りこぼしていた。⚠️**②48人中27位で「完勝。今日は
  完全にあなたの一日だった」**——`rivalDrama`が`myRank`を引数で受け取りながら使わず、
  勝敗とタイム差だけで文言を決めていた。sim側の破綻は観測されず（出走計画・TT関連の
  変更・モニュメントいずれも正常、エラーゼロ）。⚠️**教訓：同じ表示が複数箇所にある場合
  片方だけ直すと取りこぼす／引数で受け取りながら使っていない値はバグの温床。**
  ⚠️**1キャリア通しは未達**（自動プレイがイベント選択肢で詰まりタイムアウト）——
  確認できたのは1年目4月〜9月のみ。詳細は`devlog/wave83.md`。
- **§113 第84弾（チームTTのレース後処理を追加）— 人気度・絆・成績台帳の欠落を修正**
  第83弾の横断調査で見つけた「チームTTだけ人気度・絆・成績台帳が一切更新されない」
  欠落を、案①（チーム順位ベースで付与・careerWins系は据え置き）で実装。あわせて
  副次的な配線漏れ2件も修正：⚠️**絆がチームTTでは`sim.ranked`不在により更新自体が
  落ちていた**（`sim.entrants`へフォールバック）、⚠️**マイライフのチームTTは
  `computeTeamTT(sim, 1)`のハードコードで結束(chemMul)が常に無効化されていた**
  （シーズン側は元から正しく渡していた）。後者を直した効果を実測すると絆0→満で
  平均チーム順位6.84→2.87(n=200、9チーム中)——設計時の見積りより大きいが、
  シーズン側で既に稼働中の計算式(`CHEMISTRY_TIERS`)と同じ範囲を配線し直しただけ
  と確認し、新規のバランス調整はしていない。詳細は`devlog/wave84.md`。
- **§114 第85弾（説明文とコードの突き合わせ監査）— 全画面の文言9件を修正**
  「⚠️実装を変えたのに説明文を直していない」箇所を探し、説明文を持つ全画面から
  **数値・月・割合を含む＝機械的に検証できる主張165件**を抽出して値の出どころと照合した。
  食い違い7件：⚠️**オリンピックの開催月が3月と書かれていたが実際は7月**（`month === 3`という
  0始まりの内部インデックスをそのまま月名にしていた＝§7の数値版）／⚠️**成長の上限の説明が
  第7弾B-3で廃止した旧設計のまま**（「練習を続ければ天井が上がる」と読め、逆の育成方針を
  教えていた）／世界選手権の2択化（第82弾）が未記載／出走計画・チームTTがヘルプに皆無／
  ⚠️**丘陵の能力配合55/45（第80弾で60/40に変えていた）**／成長ピーク年齢が5タイプ全部で
  終端ずれ／キャプテンの練習ボーナスの境界が2歳（実際は3歳）。実装中にさらに2件——
  ⚠️**同じ主張が別画面にもあり片方だけ直していた**（第83弾の類型を、その教訓を書いた本人が
  同じ弾で踏んだ。救ったのは修正後のgrep確認）と、⚠️**誤りの発生源はコード側のコメント**
  （`month.js:40`が実装と食い違い、UI2箇所がそれを写していた）。⚠️**一致していた30件超も記録**
  （ヘルプ全体が腐っているのではなく、触った機能の周辺だけが取り残されている）。
  ⚠️**教訓：simの係数を変える弾は、その係数を説明している文言とコメントも同じ弾で直す。**
  詳細は`devlog/wave85.md`。
- **CLAUDE.md §2に例外を追加（2026-08・ユーザー明示）**：⚠️**軽微な変更はOpusのまま
  直してよい。** 1〜2ファイル・数行の文言/定数/明らかな誤りの修正で、確定した文面や値が
  既にあり設計判断を伴わないもの限定。⚠️設計判断・新しい分岐・sim/バランスに触れるものは
  行数が少なくても対象外。背景は「分析の途中で見つけた1行の修正でモデル切り替えの往復を
  するほうが高くつき、ユーザー不在だと作業が止まる」（第84・85弾で実際に複数回発生）。


## 次のアクション（ユーザーメモ・未着手のみ）

ユーザーから明示的に指示された、今後着手すべき項目のメモ。優先度・着手順は未確定。
**完了項目はここから削除する**（詳細は各弾の§番号・`devlog/waveNN.md`に残る。番号の欠番は
削除済みの完了項目の跡）。⚠️**§番号は統合された索引の範囲で読むこと**（例：§79は「§69〜81」）。

###  やらないと決めたこと（再提案しないこと）

一度検討して**却下・完了**した項目。新しいチャットが蒸し返さないための一覧。

- ⚠️**逃げの「速度」を触る案は入れない**——4回失敗し軸そのものが行き止まりと確定（§82〜85）。
  詳細と代わりに残る軸（`brk`＝エネルギー経済）は`ARCHITECTURE.md`のレースsim節が正本。
- ⚠️⚠️**`attack−hold`の数値を追う仕事はもう再開しない**（2026-08・第58〜59弾の打ち切り時に
  ユーザーと合意・§88〜89）。**同じ1つの問題に第52弾から数えて7回挑戦し、効いたのは1回だけ**
  （Phase2の+12.87→+5.78）。パターンは毎回同じで「機構を1つ特定→狭く直す→効かない→
  別の機構を特定」——simは相互作用する機構が多いのでこの輪は無限に回せてしまう。
  ⚠️**そもそも目標（+1〜+3）がプレイ体験と結びついているか検証されていない。** 実測では
  仕掛けが決まった時は−3.39着goodで成功率42〜47%あり、「平均では損だが決まれば勝てる」は
  諸刃の剣として正しい姿の可能性が高い。**再開する場合は、数値ではなく「実際にプレイして
  felt problemとして観測されたか」を先に示すこと。**
- **バッジの退行（使わないと段階が落ちる）は入れない**——恒久確定（§69〜81・`devlog/wave43.md`）。
  第43弾後に測り直すとN=8で7/7分離し技術的には成立したが、①「事前にビルドを考えて育てる」
  目的と逆向き②本家2Kも2K25で廃止③プレイヤーが弱くなる方向、の3点で見送った。
- **AIへのバッジ段階配布はやらない**（§69〜81・第49弾の実測）。プレイヤーが虹まで上げても
  勝率が測定上動かず、釣り合いを取るべき差が無い。
- **バッジ所持上限の撤廃はやらない**（§69〜81）。上限こそが個性の源で、外すと目的を壊す。
- **スキルポイント制（旧14番）は廃止**。練習→能力の経路を書き換える大手術は不要になった。
- ⚠️**「12px未満は漢字が潰れる」という旧メモは失効**（下限は`micro:8`/`nano:6`/`digit:5`。
  `data/theme.js`）。この失効した制約を根拠に設計しないこと。

### 育成の方向転換（2026-08・ユーザー明示・以降の設計はこれに従う）

**「能力値で個性を出す」のをやめ、「バッジで個性を出す」へ舵を切る。** 能力値が横並びでも
持っているバッジが違えば別の選手になる、という構図に寄せる。「極めると全員同じ万能型」問題を、
能力値側の上限調整ではなくバッジ側で解く。

### 未着手のTODO

27. **`devlog/`から回収した積み残し**（2026-08・ユーザー指摘で全62ファイルを機械的に洗い出し）。
   ⚠️**各弾の`devlog`に「別弾」「別途」「未実装」と書いたまま、DEVLOG本体へ上げ忘れていたもの。**
   以後、弾を締めるときは「この弾で先送りにしたものはここへ上げたか」を必ず確認すること。
   - **27-c. `segs`の死んだ`base`列の除去**（第72弾）。`[type, base, dist]`の`base`は
     simから一切参照されない（`sim/course.js`が保持するだけ）。13テンプレート×全区間の
     書き換えになるため見送り中。
   - **27-f. `.ml-enter-*`→`.app-enter-*`の一括改名**（第67弾）。マイライフ専用の名前だが
     実体は両モード共通。動いているものに触るため見送り中（cosmetic）。
   - **27-g. `タイトルに戻る`の置き場所**（第64弾）。⚠️**無害な`タイトルに戻る`が
     `最初からやり直す`（データが消える）の真下にある。** 押し間違いの観点で望ましくない。
   - ⚠️**27-h. 出走表・観戦画面への「実順位」バッジ**（第11弾Phase2から持ち越し）。
     ただし当時の案は絵文字（🌍）前提で、⚠️**現在の§8（絵文字は原則使わない）と衝突する**。
     着手するなら見せ方から設計し直すこと。
   - **27-i. 透明度差・同色相による階層表現**（第13弾・ユーザーの留保付き提案）。
     正式なトークンにはせず、次のデザイン検討時の材料として記録されていたもの。
   - **27-j. 同じものを2つの言葉で呼んでいる箇所の統一**（第85弾で発見・§7）。
     実例：キャプテン制度を`season/hub/help.jsx`は「**キャプテン**」、
     `hub/riders/team.jsx`は「**主将**」と呼ぶ。もう1件、`season/race.jsx`の総合結果だけ
     「**ポイント**」で他2箇所は「**チームポイント**」（第83弾の横断調査で発見）。
     ⚠️いずれも数値の誤りではないため第83・85弾では触っていない。着手時は
     §8の手順（どちらの語に寄せるかを候補提示→合意）を踏むこと。

26. **初期のまま触っていない要素・残る候補**（第72弾の棚卸しの続き・`devlog/wave72.md`）。
   脚質の穴埋め・グランツールの作り直しは完了（第81〜82弾）。残る候補（優先度未確定）：
   - **会場に個性を与える**：国内12・海外8の会場が名前だけ（`VENUE_REGION`の本拠地
     ボーナス以外は無意味）。
   - **配合データの新旧重複を調査**：`data/breeding.js`（100%未変更）と第15弾で
     追加された`breeding/recipes.js`が併存。重複の有無は未調査。


11. **スプライト（スプリント／ダンシング）モデルの描き直し**（⚠️凍結中）。
   「スプリントしている感がない」との指摘。試した3方式（AI新規発注／行挿抜での姿勢変更／
   レイヤー分離の手続き的描画）は**全て失敗＝再挑戦しないこと**。
   **再開条件**：dancing/sprint 6コマ分の新しい参考画像を用意し、CLAUDE.md §6の手順で抽出する。

### 恒常タスク（終わりが無い）

9. **UIテキスト・ビジュアルの作り込み**（CLAUDE.md §7・§8）。新画面・新機能を作るたびに
   §7・§8の基準で点検する。経緯は文言面＝§13〜23・§24〜31、土台面＝§38〜49・§50〜68。
8. **マイライフのイベント本文の拡充**（Phase 1〜3は完了）。「数百種類」は長期目標
   （現在76件・§50〜68）。既存5分類のタグ付け方針を踏襲し`data/events.js`の
   `ML_EVENTS`へ追記していく。
