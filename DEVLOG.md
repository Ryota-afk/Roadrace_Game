# DEVLOG — ロードレース・プロチーム運営（開発引き継ぎ）

新しいチャットはこのファイルを最初に読めば、巨大な本体ファイルを全読みしなくても文脈を安く復元できます。
**使い方**：新チャット冒頭で「`DEVLOG.md` を読んで続きから」と指示 → 必要な箇所だけ本体を部分読みする。

**サイズ規律（2026-08・実測で確定した具体的な基準）**：このファイルへのRead呼び出しは実測で
**25,000トークンが上限**（超えると先頭から切り捨てられ末尾の「次のアクション」が読めなくなる）。
このファイルの実測換算はおよそ**2.5〜2.6バイト/トークン**（1781行・177,155バイトが69,148トークン
相当だった実測に基づく）。よって：
- `wc -l -c DEVLOG.md` を大きな節目ごとに確認する。
- **65,000バイトを超えたら整理を検討**、**90,000バイトを超えたら次の区切りで必ずスリム化する**
  （90,000バイト≒35,000トークンで安全域を明確に超える）。
- 整理方法は毎回同じ：**完了済みWaveの詳細プローズを「件名＋要点1行＋`git show <hash>`での
  復元手順」の索引へ圧縮**する。生きた参照情報（§3アーキテクチャ・§3.5機能一覧・§6方針・
  「次のアクション」）はプローズのまま残す。情報はgit履歴に残るため実質的な損失はない。
- 直近の実績：2026-08にこの基準で177KB→約55KBまで圧縮（本ファイル末尾の圧縮記録を参照）。

---

## 0. これは何か

単一HTMLファイルで動く React + SVG のロードレース・ゲーム。2モード構成：
- **シーズンモード**：6名ロースターのチーム運営（経営）
- **マイライフモード**：一人の選手のキャリア育成（人生）

自転車ロードレースのチーム戦術（エース／アシスト、逃げ、集団、山岳など）をSVGで可視化しつつ、
育成・血統・世界ランキングなどのメタ要素を積み上げてきた。

> **作業ブランチはその時のタスク指定に従う**（セッションごとに変わる）。過去に旧いブランチ名が
> 誤って指定され、モジュール分割以前の孤立した土台に積みそうになった事例があるため、
> 指定されたブランチが現行の`src/`構成（本ファイル§1参照）を含んでいるか怪しい場合は
> 作業前に確認すること。

---

## 1. ファイル構成と開発フロー（重要 / v12モジュール移行後）
**2026-07 に単一HTMLからモジュール分割＋Viteビルドへ移行した（経緯は §8）。真実の源は `src/` に移り、`roadrace_v12.html`/`.jsx` は廃止。**

- **`src/`** … 唯一の編集対象・真実の源
  - `src/main.jsx` … 本体（`App()`の状態・ハンドラ＋画面ディスパッチの薄い配線）
  - `src/index.html` … Viteの入口HTMLテンプレート（薄い雛形）
  - `src/data/` … 静的データ定数のみ（ロジック禁止・JSX/domain import禁止）。`abilities/breeding/course/items/progression/theme/economy/events/directives/gear/teams` 等
  - `src/view/` … 文字列生成の純関数（JSX無し）。`flavor.js`(選手フレーバーテキスト)、`news.js`(ライバル動向・世界ニュース)
  - `src/domain/` … 純粋なドメインロジック（生成器・計算関数、JSX無し）。`season/*`・`mylife/*`・`shared/*`
  - `src/sim/` `src/breeding/` `src/world/` `src/state/` `src/components/` … 分割済み
  - `src/controllers/` … `main.jsx` App() のハンドラのうち `(state, ...args) => newState` の形のものを純関数として抽出。`season/*`・`mylife/*`
  - `src/logic/support.js` … 表示ヘルパー＋残存ロジック（画面イベント効果適用・監督評価・配合表示・実績判定等）
  - `src/screens/season/` `src/screens/mylife/` … 用途クラスタ単位で分割済み。各`hub/`配下はさらに小分類ディレクトリに分割（§9末尾・Step13第7弾参照）
- **`index.html`（リポジトリ直下）** … `npm run build` が生成する**自己完結の単一HTML成果物**（デプロイ用）。React/JSXはビルド時に変換・バンドル済みで**CDNもBabelも不要**。手で編集しない
- `package.json` / `vite.config.js` / `package-lock.json` … ビルド定義
- `dist/`, `node_modules/` … gitignore（追跡しない）
- `archive/` … 過去バージョンのアーカイブ（`roadrace_v5〜v11.*`＝分割前の単一ファイル版、`archive/design/`＝旧設計メモ）。src/から未参照・触らない

### 編集→ビルド→コミット→push の手順
```bash
# 1) src/ を編集（対象モジュールだけ読めばよい＝トークン激減）

# 2) ビルド（index.html を再生成）。Node/npm 必須。初回のみ npm install
npm install          # 初回だけ
npm run build        # vite build && cp dist/index.html index.html

# 3) コミット（日本語メッセージ）。末尾に必ず以下のトレーラを付ける
#    Co-Authored-By: Claude ...
#    Claude-Session: https://claude.ai/code/session_...
git add -A
git commit -F - <<'EOF'
（日本語の要約タイトル）

（本文）
EOF

# 4) push（指定された作業ブランチへ。default へ直push禁止）
git push -u origin <その時の作業ブランチ名>
```
- コミットメッセージは日本語。モデル識別子はリポジトリに書かない。
- **`index.html` は生成物**。src を直したら必ず `npm run build` してから両方コミット（＝新しい二重管理を作らない）。

---

## 2. 検証（Playwright）— ビルド成果物をそのままテスト
**移行でCDN依存が消えたため、vendor版React差し替えハックは不要**。`npm run build` が出す
`index.html` は自己完結（React/JSXバンドル済み・CDN/Babel不参照）なので、クラウド環境でも本物をそのまま開ける。

```bash
# 1) ビルド
npm run build

# 2) サーバ（リポジトリ直下を配信、または npx vite --port 5190 で開発サーバ）
npx http-server -p 8844 -s -c-1 .   # ← run_in_background で起動。curlで200を待つ

# 3) Playwright（scratchpad に置く）
#    executablePath: /opt/pw-browsers/chromium （args不要な場合が多い）
```
テストのコツ：作戦ピッカーは**メイン画面**（「このレースに出場する」ボタンの上）にあるので、
作戦を選んでから出場する。controlled input は `page.fill` を使う（`inp.value=` はReactに効かない）。

---

## 3. アーキテクチャ（関数名で参照・随時更新する生きた参照）
### レースsim（両モード共通）
- `simulateTicks(course, riders, fromTick, directive, noGroup)` … **レース全体を先に計算**（finishTimeまで）。
  RaceView は `posHist/energyHist/modeHist/...` の**再生専用**。
- `buildMyLifeSim(raceMeta, player, myTeamName, classIdx, difficultyId, dayTag, directiveKey, rival, year, rival2, teammates, tactic)`
- `canPull(en, segType)`（誰が牽引できるか）, `assignAIRoles`, `effAbilities`, `overall`, `newRider`, `mulberry`(seeded RNG), `pickRiderName`
- 役割 role：`ace/lead/sub/mountain/flat/breakaway`。draftモードは省エネ、pullは全消費。

### マイライフ状態
- `initMyLife()` / `mlCreateChar(type, background, master, partner, cpMeta)` / `mlGenRace(year, month, classIdx)`
- 月次：`mlAdvanceMonth`, `mlApplyMonthEffect(player, mode, ctx)`
- セーブ：`ML_SAVE_FIELDS`（配列）、`saveMyLife/loadMyLife`。**playerはまるごと保存**されるので player.* は保存フィールド追加不要。
- **成長キャップ（2026-08 Phase 1で再設計・§17参照）**：
  `mlGrowthCap(year, player, ml) = min(140, 90 + timeComponent(年数、最大+20) + achievementBonus×難易度係数(易1.3/普1.0/難0.75/鬼0.5) + player.talentCap)`。
  `growthPow`（S/A/B/C）は**到達速度のみに影響**、最終天井には影響しない（意図的な設計・§28参照）。
  シーズン側は`DIFFICULTIES.growthCap`（88/94/102/112、難易度で固定・選手ごとの差なし）。
  `bumpGrowthPow`, `GROWTHPOW_ORDER=["C","B","A","S"]`, `POW`。マスク：`mlGrowthPowRevealed(ml)`（`ml.year>=3`まで`🔒???`）。
- 能力成長の逓減カーブ：`growthFactor(v, cap, breakthrough)` / `softFactor(v, cap, breakthrough)`（`logic/support.js`）。
  上限を超えてどこまで伸ばせるかは`breakthrough`（突破力、生成時固定値・タイプ別base44〜58）の
  `breakthroughMul(breakthrough) = max(0.05, 1.0 + 0.08×(breakthrough-50))`で決まる（2026-08・§28で
  傾きを強化＝「極めると全員同じになる」問題の是正。season/mylife共通関数のため両モードに自動適用）。
- 監督指示：`MANAGER_DIRECTIVES`（keyed, check-fn付き）, `mlGenDirective`。**復元時はキーで引き直す**（JSONで関数が消えるため）。
- 作戦：`ML_TACTICS`（balanced/wait/early/aggressive/assist）。

### 配合・血統
- `mlBreedBonus(parentA, parentB)` が全部返す：`nick/inbreed/plusValue/plusPer/abBonus/extraAbilities/subBonus/goldInherit/exclusive/archNotes/bakuhatsu/matingGrade/growthSteps/talentCap/danger/dangerLabel/healthMit/special`
  - **設計方針（厳守）**：爆発力・系統・特殊配合のボーナスは**初期能力に足さない**。伸びしろ（growthPow段数＋talentCap）＋称号＋金の特殊能力に還元する（序盤インフレ回避）。
  - `BREED_NICKS`（脚質相性）, `ARCH_BREED`（生き様の血）, `ML_SPECIAL_MATINGS`+`mlSpecialMating`（特殊配合）, `mlGradeColor`
- 系統レジストリ（プレイ跨ぎ／localStorage `roadrace_v12_bloodlines`）：`loadBloodlines/saveBloodlines`, `mlRegisterBloodline`(引退時), `mlBloodlineTier`（未確立→確立→名門→大系統）, `mlBloodlineFactor`, `mlBloodlineBonus`

### 生きた世界
- `mlWorldStarsForYear(seed, targetYear, legendPool)` … **キャリア固有シードから決定論的**に24人の永続スターを生成し年1からtargetYearまで再シミュ（加齢→成長/衰え→引退→世代交代）。状態は`ml.worldSeed`のみ保存。
- `mlWorldNews(seed, year, legendPool)`, `mlWorldBoard(ml)`, `computeWorldRank`
- **殿堂の血が流入**：`legendPool = loadMlLegends()`を渡すと上位スターが殿堂選手の姓・脚質を継ぐ`bloodOf`付きになる。
- **殿堂の頭数と対戦相手への出現は無関係**（次のアクション調査済み）：`legendPool`は無制限に蓄積するが、1レースあたりの殿堂選手代打は`nLeg=0/1/2`（55%×35%分布）で固定上限。

### アンビション／キャリア／モニュメント
- `ML_AMBITION_PATHS`（victory/bigstage/devotion/world）, `mlCurrentAmbition`, `mlFirstUnmetRung`, `mlAmbitionCleared`, `applyAmbitionReward`
- `mlCareerArchetype(s)` … 引退時の生き様（称号）。`careerWins/careerPodiums/careerBigWins/careerTitles/careerClassics`を参照。
- `ML_MONUMENTS` … ワンデー古典3種（石畳/丘陵/山岳）。`race.monument`フラグ、`careerClassics`を加算。

### 殿堂・その他
- 殿堂：localStorage `roadrace_v12_mylife_legends`。`ML_LEGENDS_KEY`, `mlLegendSnapshot(s)`, `mlRecordLegend(s)`, `protegeInherit`
- 特殊能力：`ABILITIES`（`iron`鉄人 / `tough`頑丈 / `glass`ガラスの体[bad] / `big`大舞台 / `finisher` 等）, `GOLD_CONDITIONS`, `hasAbility/hasGoldAbility`
- 新ステータス（2026-08・Phase1/2）：`breakthrough`(突破力)/`stability`(安定感)/`luck`(運)。生成時固定値、`core/core.js`の`genSubStats()`。
  `RiderRadarChart`（`components/RadarChart.jsx`）で6角形表示。イベント経由のみ例外的に増減可（`*Delta`系effects）。
- シーズンモード：別state（`g`/`initGame`）。`TEMPLATES`(コース), `signBredYouth`（シーズンの血統ユース）。

### UIコンポーネント（能力表示、2026-08 §26で再編）
- `DisciplineGrid`（`components/panels.jsx`）… コース適性（S〜G評価の5項目バー）。マイライフ・シーズン共通の見出しは「コース適性」に統一済み。
- `AbilitySoshitsuRadarPair`（`components/RadarChart.jsx`）… 能力5項目レーダー＋素質6項目レーダーの横並び。マイライフ本編・シーズン選手一覧の両カードで**常時表示**（旧`AbilityGrid`棒グラフ・`SubStatLine`テキストは重複のため廃止済み）。
- 他カード（デビュー画面・FA市場・トレード・引き抜き市場・レース当日選択）は`AbilityGrid`（棒グラフ）のみ、密なリスト用途のため据え置き。

### Btnのrole（2026-08 §24で導入・生きたUIルール）
`components/ui.jsx`の`Btn`に`role`（`primary`/`month`/`menu`/`danger`）。primary=黄塗り（今の画面の
おすすめ、1つだけ）／month=黄枠線（月が進む行動）／menu=グレー枠線（月は進まない）／danger=赤枠線
（取り返しがつかない操作のみ。引退・データ消去等）。**新しいボタンを追加する際はこの4分類に従う**。

### localStorage キー一覧
- `roadrace_v12_mylife_save` … マイライフ本体
- `roadrace_v12_mylife_legends` … 殿堂（プレイ跨ぎ）
- `roadrace_v12_bloodlines` … 系統レジストリ（プレイ跨ぎ）
- `roadrace_v12_save` … シーズン本体
- クリアポイント等は `loadMeta()`（`totalEarnedCP` でアンロック判定）

---

## 3.5 実装済み機能インベントリ（全体像・再実装しないための一覧）
※ v11〜現行で積み上げ済み。細部は本体を参照。**ここにある物は「もうある」前提で扱うこと。**

### レースsim・可視化
- レース全体を先計算→再生する方式／2D集団フォーメーション／ローテーション・パスラインの縦可視化／カメラの選手追従
- 天候（晴/雨/猛暑）＋横風エシュロン、特能「悪天候巧者」
- 最終スプリントのドット絵演出（`PixelBike`、§16）／フィニッシュのズーム＆スローモ／周回レース
- コースレコード、実況の拡充、コースを道の曲線に沿わせる、逃げ要員が実際に逃げる、最終区間スプリント戦術
- 選手識別は引き出し線＋名前タグ方式に統一（自分=水色／エース=黄／自チーム=青／ライバル=赤／殿堂選手=別トーン赤、§20）

### シーズンモード（チーム経営）
- 6名ロースター運営、B1/A/PRO の3クラス昇格
- スタッフ雇用／FA移籍市場／選手トレード／移籍志願／退団選手をライバルチームが拾う／被引き抜き・引き抜き（双方向）
- グランツール（年3つ・海外遠征）＋副次分類、ステージ間の役割再配置
- チーム化学反応、キャプテン制、実績システム＋報酬、チーム順位表、拠点(BaseView)の部屋グレードアップ（§14）
- ユースアカデミー枠、血統ユース、施設アップグレード可視化、トロフィールーム
- ライバルチーム動向ニュース、会場相性/ホームアドバンテージ、クリア後の王朝継続(NG+)
- カイロソフト式の拠点画面（間取り・カメラ・什器・人物ドット絵・吹き出し、§10〜13）

### マイライフモード（選手育成）
- 月次ループ（出走／練習／完全休養／私生活イベント［受動発火・§18］）、経歴選択（高卒/大卒/実業団卒）
- 監督指示＋監督評価、年俸改定、契約交渉、移籍オファー、移籍入札戦
- ショップ（パーツ/消耗品/恒久ギア/車/住居/成長力・成長タイプ買い切り）、能力別コーチ
- ライバル（1人目・2人目）、節目の大会（世界選手権/五輪）、代表での役割
- 人生の岐路（結婚／大怪我＋3択リハビリ／第一子／恩師との別れ）、オフシーズンの過ごし方
- 個人スポンサー＋人気度＋スポンサーの仕事、自伝出版、引退後エピローグ（監督/完全引退）
- 恩師（師弟）＝新人時代の指導、教え子（プロテジェ）、メンター役
- フォーム、ピーキング、フォーム予報、天候予報、伸びしろヒント（成長力マスク中は専用しきい値、§25）
- 成長キャップの実績連動（§17）、出走経験による成長、対戦相手(AI)の年々底上げ
- 世界ランキング＋アンビション4路線（勝利/大舞台/献身/世界）、キャリアの生き様（称号）
- 配合（血統）：ニック／インブリード／累代+値／金の特殊能力クロス／配合限定特能／生き様の血／相性表／系譜ツリー
- 爆発力＆配合評価／危険度／系統確立＋因子／特殊配合／生きた世界（永続ペロトン＋殿堂の血流入）／モニュメント／アシスト作戦
- イベント：能力/新ステ変動・賭け（ダイジョーブ博士系）・覚醒・悪イベントの5分類、46件以上（継続拡充中、§18）
- イベント・岐路・オフシーズン・弟子指導での能力変化は必ず差分表示（`abilityDeltaSummary`/`protegeEffectSummary`、§21）
- 「今月の行動」の推奨表示（疲労・レース有無・大舞台から1つ判定、§24）

### 共通・メタ
- 難易度ティア（成長キャップ＋AI強度）、クリアポイント＋パーク屋（メタ進行）
- 選手ごとの戦績・ニックネーム・フレーバー、殿堂（プレイ跨ぎ）、累積タイトル台帳
- 特殊能力システム（銅→金への進化、特殊能力図鑑）、副ステータス（加速/体格/メンタル/突破力/安定感/運）
- 生涯プレステージスコア画面、ヘルプタブ（両モード）
- UIテキストの平易化・重複表示の統合（§22/§23/§26、CLAUDE.md §7に恒常タスクとして明文化）

---

## 4. 実装履歴（コミット索引）
v33系〜現行まで、すべてPlaywright（＋純ロジックはNode単体）検証・push済み。件名で
`git log --oneline --grep="<キーワード>"` すれば該当コミットが見つかり、`git show <hash>` で
当時の詳細（実装箇所・検証結果込み）を復元できる。以下は件名のみの索引（新しい順、直近の
セッションは §17〜§26 に詳細プローズあり）：

v41 移籍市場の駆け引き（引き抜き合戦） ／ v40 シーズンの中期目標 ／ v39.20 sim本体に展開戦術
（トレイン/発射/前待ち/ピールオフ） ／ v39.19 展開のリアリティ＋ゴール導入カメラ ／ v39.18
沿道を道のカーブに追従＋難易度連動 ／ v39.17 俯瞰マップのスピード感 ／ v39.16 ゴール演出の
走行距離4倍 ／ v39.15 ゴール演出のスピード感強化 ／ v39.14 残像対策＋育成カーブ再設計（カンスト
対策） ／ v39.13 残像対策＋最終スプリント判断＋縮尺バー ／ v39.12 俯瞰マップ大幅強化 ／ v39.11
ロードバイクのドット絵化 ／ v39.10 ゴール演出のドット絵化・密集緩和 ／ v39.9 ゴール演出の重なり
解消 ／ v39.8 ゴール演出をカイロソフト風アイソメに ／ v39.7 ゴールを斜め視点 ／ v39.6 ゴール
スプリントを横視点に ／ v39.5 ゴール演出を放送カメラ風に ／ v39.4 ゴールスプリント演出を横
フロー化 ／ v39.3 スロー＋ズームの山場と実況拡充 ／ v39.2 最終直線シネマティックの集団対応 ／
v39.1 判断カードを脚質・特性・役割・地形と連動 ／ v39 レース中の判断カード導入（観戦→"プレイ"へ）
／ #9 A-4 選手→監督の転身ブリッジ ／ #9 A-2/A-3 レジェンド招聘＋共有ワールド ／ #9 A-1 統合
ダイナスティ・ハブ ／ #9 B案 配合・系統の深掘り(B-1〜B-4) ／ フィードバック9項目バッチ ／
シーズン:相手チームを永続ロースター化 ／ 両モード:チーム数拡張 ／ マイライフ:ワールド選手の
世代交代 ／ マイライフ:永続ワールドロースター ／ 拡充:パーソナリティ3種＋異名 ／ 拡充:特殊能力
7種追加(第2弾) ／ 拡充:特殊能力5種追加 ／ メタ:CPショップ ／ メタ:クリアポイント拡充 ／
マイライフ:観戦マップに選手名ラベル＋成績台帳 ／ マイライフ:チームTT ／ D演出:引退セレモニー＆
殿堂の集大成 ／ B-2 逆メンター:弟子を育てる ／ シーズン深掘り:スタッフ/育成/ケミストリー/
ライバル/タイトル争いの個性化 ／ UI:選手詳細のキャリア軌跡・実況の充実・セーブの安心感 ／
D物語:メディアナラティブ・因縁が育つライバル ／ バランス:配合点検・危険度実効化・難易度つまみ・
エース早期発射・地形別フィニッシュ決着 ／ 修正:アシストの観戦⇔リザルト同期ズレ根治 ／
改善:ライバル会話ドラマ双方向化 ／ 両モード:性格ベースのイベント ／ マイライフ:完全休養・私生活
充実・新聞号外・性格ベースのライバル会話ドラマ・リセマラ強化・経歴固有メリット・弟子育成深化・
強制引退→契約更改選択制。

---

## 5. 今後の候補（ゲーム内容・未着手のみ）

第1候補（シーズンの駆け引き①②③）は全て完了済み（§4参照）。以下、着手順は未確定：

- **生きた世界の深掘り**：`src/world/world.js`はv33.10で停滞。ライバルの成長/全盛期/引退・
  世界ランキング変動のニュース化・世代交代の物語化を深める余地あり。
- **アイテム・設備の刷新**：`src/data/items.js`は少数のまま。機材選択にトレードオフ（軽量
  ホイール＝登り有利/平坦不利、雨用・石畳用など）を入れる案（未整理）。
- **特殊配合の深化**（2026-07 ユーザーメモ・未整理）：ドラクエモンスターズ的な「適切な組み合わせで
  超強力な1体が生まれる」着想。対象を選手そのものにするか能力/特能/因子にするかは未整理。
  着手前にCLAUDE.md §1の設計合意が必要。
- **リーグ全体の生きてる感**（2026-07 ユーザーメモ）：強い選手・強いチーム・世界ランキング
  推移の把握しづらさの指摘。上記「生きた世界の深掘り」と合わせて検討する。
- **山岳×クライマーのアタック勝率(81%)／oni難易度の判断効果(0.66)**：設計意図として現状維持中
  （下げる場合の変更点は把握済み、`committedBreak`/`MOVE_EFF_BY_DIFF`）。

---

## 6. 守るべき方針メモ
- 配合・世界系のボーナスは **伸びしろ／称号／金の特殊能力** に限定（初期能力インフレ厳禁）。
- 新機能は必ず Playwright で end-to-end 検証してからコミット（数値ロジック＋UI表示＋0エラー）。
- ヘルプ（`mylife_help`画面）にも新機能の説明を追記して整合を保つ。
- **編集は `src/` のみ**。`src/`を直したら必ず`npm run build`してから両方コミット。`index.html`は生成物なので手編集しない。

---

## 7. アーキテクチャの岐路（決着済み・2026-07）
単一HTML（旧・約9,300行）のままか、モジュール分割＋ビルド移行するかの検討。
**→ モジュール分割＋Vite移行を採用・完了**（詳細は§8）。以降の作業は`src/`上で行う。

---

## 8. モジュール分割＋Viteビルド移行（採用・完了 2026-07）
Vite + `vite-plugin-singlefile`で「自己完結の単一`index.html`」を出力する方式。Phase 0〜4まで
全て完了（HEAD=`d0c15ba`「Phase 4-2：App分解完了」）。

**到達点**：`main.jsx`は9,624行→2,395行（当初の約1/4）。ソースは`src/`の18モジュール
（data/core/sim/breeding/world/state/logic/support.js/components/screens 等）。デプロイ物は
リポジトリ直下`index.html`（自己完結の単一HTML、npm run buildの生成物）。撤廃済み：`.jsx`
手作業ミラー／実行時Babel／実行時CDN依存。

**フェーズ索引**（詳細は`git log --oneline --grep="Phase"`→`git show <hash>`）：
Phase 0＝ビルド土台の敷設（挙動不変でsrc/へ移動）。Phase 1＝静的データ52定数を`data/`へ。
Phase 2＝純ロジックを層構造で分離（core/sim/breeding/world/state）。Phase 3＝`RaceView`を
`components/`へ。Phase 4＝App(モノリス)の分解、`ctx`経由で`screens/`へ画面ディスパッチ分離。
各フェーズとも`npm run build`→Playwright実機確認（実エラー0）を経てpush。

---

## 9. モジュール細分化・整理（2026-07・Step1〜12完了）

**設計原則（新規追加は必ずこれに従う・生きたルール）**：
1. 依存は下向き一方通行：`data → core/sim → domain/state/view → controllers → screens → app`
2. `data/`・`view/`は JSX を import しない（＝常にNodeで単体テスト可能に保つ）
3. データ（`export const`）とロジック（外部関数呼び出しを含む`apply`/`effects`）を混同しない。
   `check:`/`match:`のような**自己完結の小さな述語関数**（引数のみ参照）はデータとして扱ってよいが、
   `bumpRosterAbAll(s, 8)`のように**外部の状態変更関数を呼ぶクロージャ**は論理（domain）であり
   `data/`へ移送しない（`CP_MILESTONES`・`ML_CROSSROADS`・`ML_OFFSEASON_CHOICES`・
   `SEASON_ACHIEVEMENTS`は`support.js`に意図して残置）。

**実施索引**（`git log --oneline --grep="Step"`→`git show <hash>`で復元）：
Step1＝死コメント削除。Step2＝archive整理。Step3＝純データを`data/`へ移送（互換シム方式）。
Step4＝文字列生成の純関数を`view/`へ。Step5＝`domain/`新設（生成器・計算関数）。Step6＝
`state.js⇄breeding.js`循環import解消。Step7（第2〜12弾）＝`controllers/`抽出をリスクの低い
reducerから危険地帯（`startRace`系のuseRef連打防止・stale closure）まで段階的に解消し、
過程で複数の実バグ（グランツール3日目進行不能・連打二重加算・OBコーチ雇用即クラッシュ・
`rankSim`二重呼び出し）を発見・修正。第10弾で`ctx`89メンバーを`hooks/useAppShell`等へ
フック化、第11弾で`main.jsx`を48行（合成ルートのみ）まで縮小。Step8＝`screens/season.jsx`・
`screens/mylife.jsx`を用途クラスタ単位で分割。

**続き（2026-07・カイロソフト動線移行後）**：Step13第7弾でメニュー大分類を17小分類へ細分化、
`hub/riders(facility/market/race/records)/`の5ディレクトリへさらに分割（`git show 49d9241`）。
新機能は必ず「data / domain / controller / screen」の4箇所に配り、既存の巨大ファイルへ
"にじむ"追記を禁止する方針を継続中。

---

## 10〜12. カイロソフト式動線への移行／敷地画面のカットアウト化／スプライト作り直し（2026-07・完了）

**背景**：カイロソフト系シムのような常設マップ＋右下メニューへの刷新依頼から始まり、
敷地画面（BaseView）の部屋割り・什器・カメラの作り込み、最終的に人物・自転車をベクターから
本物のドット絵グリッドへ全面差し替えるまでの一連の流れ。すべてPlaywright/Node検証・push済み。
件名で`git log --oneline --grep="Wave <A-G>"`すれば該当コミットが見つかる。

**主な到達点（索引）**：
- **Step13第1〜4弾**：`hub.jsx`分解→メニューシェル新設→BaseView新設→カットオーバー（旧5タブ撤去）。`git show 1bd77c8` `1b6330b` `5df6498` `ca529ac`
- **Wave D/D2**：BaseViewの視覚磨き込み（可視面バグ修正・建物ディテール）→カイロソフト準拠の
  投影再設計（footprint回転バグ等6件の根因修正、SVG占有率26.8%→82.0%）。`git show 664400b` `541ce76`
- **Wave E-1〜E-2 redo**：カメラ(ズーム/パン)導入→3D箱建物を廃しカットアウト部屋へ→単一
  クラブハウス＋4持ち場(training/mechanic/medical/scout)へ作り直し。`git show 3d35b09` `8828894` `bffec16`
- **Wave F-1〜F-3**：敷地境界可視化＋屋外装飾購入枠／部屋割りを玄関→廊下→部屋の現実的な
  間取りへ／什器のディテール強化／選手の活動状態機械(`riderActivityAt`)新設／自転車3姿勢化。
  `git show 10eaaf2` `bc7da0d` `bb80230` `a3682ef` `943cebd` `33c6618`
- **Wave G-1（改）**：8方向参考画像との比較診断でベクター路線を破棄し、**参考ドット絵から
  実際のドットを機械的に抽出する**パイプラインへ転換。人物16×36・自転車を3姿勢×2方向で
  抽出し`PixelPerson`/`PixelBike`へ統合。統合後、反転条件・ペダリングアニメ・コース離脱
  バグ等を10件超修正。`git show 9053e44` `83ff5f7` `e0d7f80`
  - **真因判明（CLAUDE.md §6の根拠）**：「dancing_NEの選手の向きがおかしい」バグが長期化した
    真因は、コードの反転ロジックではなく**参考画像そのもの**が「通常」と「ダンシング」で
    NEの左右handednessが食い違っていたこと。以後、向き系のバグ報告はまずコードでなく
    参考画像同士を直接見比べる、というルールをCLAUDE.md §6に明文化。`git show 06f1735` `9e59b69`

**DEVLOG圧縮の実績**：2026-07に142KB→約108KBへ、2026-08に177KB→約55KBへ（本ファイル末尾参照）。

---

## 13〜23. 2026-08 Wave群（索引）

以下は2026-08に完了した一連の改善。件名・要点1行・`git show <hash>`（当時の実装箇所・
判断根拠・検証結果を復元可能）。§17〜18の技術的な核心（成長キャップ式・新ステータス）は
§3「アーキテクチャ」に生きた参照として転記済み。

- **§13 Wave H-1・拠点の吹き出し**：持ち場ごとのセリフを性格・状態（故障>疲労>調子）で
  決定論的に選ぶ`riderChatter.js`新設。`git show a4d57fe`
- **§14 Wave H-2・部屋のグレードアップ**：4持ち場を資金で0〜3段階に強化（見た目のみ・
  実績連動）。壁の塗り替えは視認性の理由で見送り、ラグ・照明・金枠バッジに集約。`git show 427b4f7`
- **§15 Wave H-3・トレーニング室の「ぼっ立ち」修正**：新規ドット絵無しで解決——
  ローラー台のある部屋では選手を`PixelBike`（自転車ごと）で描画する方式に変更。`git show 6a3ab36`
- **§16 Wave H-4・最終スプリントのドット絵化**：`IsoRider`→`PixelBike`。`<defs>+<use>`方式で
  22名分約2万ノードになるはずが実描画233ノードに抑制。エースは★マーカー、識別はリアルタイム
  順位表に一本化。`git show 794cd20`
- **§17 マイライフ難易度調整 Phase 1**：成長曲線の逓減カーブ是正（超晩成が無代償で上位互換に
  なっていた問題）・成長キャップの実績連動化（§3参照）・新ステ「突破力」「安定感」・成長力の
  3年目までのマスク化・レーダーチャート導入。`git show d8c3dfa`（選手カードのレーダー2枚化は
  `git show dce6608` `e4a3c5f`）
- **§18 マイライフ難易度調整 Phase 2**：私生活イベントを能動発火から受動発火へ、新ステ「運」
  追加、イベントスキーマを5分類（能力/新ステ変動・賭け・覚醒・悪イベント）へ拡張し46件へ
  拡充。`git show 31d8a52`
- **§19 最終スプリント演出の姿勢問題**：姿勢判定がPRO帯基準の絶対値だったため下位クラスで
  ほぼ発火しない上、dancingの`<symbol>`自体が存在せず選手が消える、という2つの真因を特定・
  修正。判定を集団内の相対順位へ変更（`domain/shared/sprintPosture.js`新設）。尺を1.4倍に延長、
  スロー時のペダリング速度を距離連動に修正。`git show 35c34c7` `8b2746c` `4c08434`
- **§20 選手識別マーカーの統一**：★・水色の丸（顔と重なり視認性が悪かった）を廃し、引き出し線
  ＋名前タグ方式（`RiderNameTag`）へ統一。最終スプリント演出と俯瞰マップ（上・側面）で共通化。
  `git show bee11a2`
- **§21 イベントによる能力変化を必ず明示**：弟子指導・オフシーズン・人生の岐路の3箇所で
  能力変化が無言だった漏れを発見。before/afterの差分を機械的に文字列化する
  `abilityDeltaSummary`/`protegeEffectSummary`を新設し構造的解消。`git show 7414ae9`
- **§22 UIテキストの作り込み 第1波**：レーダー2枚を共有部品化・「（生涯不変）」等の不要注記
  削除・「外周」等の開発語彙一掃・CP表記を「クリアポイント」に統一・バージョン番号入り見出し
  撤去。`git show 6e117a0`
- **§23 UIテキストの作り込み 第2波**：全53画面ファイルを機械棚卸しし、開発語彙・保証文・
  重複した仕組み解説を一掃（「金特」→「金の特殊能力」全廃、疲労バーの注釈をFatigueBarの
  色分けへ一本化、等）。完了報告後にユーザーの再確認で複数行にまたがるJSXテキストの
  見落とし3件が発覚し追加修正（棚卸しスクリプトが単行正規表現しか拾えなかった教訓を記録）。
  第3波候補3件（ヘルプ本文／`OVR`略語／スポンサー行）はユーザー判断で現状維持に決着。
  `git show 5fcbc75` `06c767d`

---

## 24. 導線・ユーザビリティの作り込み → 次のアクション#15（2026-08・完了）

**位置づけ**：次のアクション#15の設計合意（「上部に『今月の行動』を新設」「理由付きで
出す」の2点をユーザー承認）を受けての実装。

**`domain/mylife/nextAction.js`（新設）**：疲労・レースの有無・大舞台かどうかから
「今月は何をすべきか」を1つだけ判定する純関数`mlNextAction()`。優先順位は
疲労85以上→休養（大舞台でも最優先）／大舞台レース＆疲労70未満→出走／通常レース＆
疲労75未満→出走／それ以外→練習、の4段。Node単体テスト19/19件パス。

**`components/ui.jsx`の`Btn`に`role`プロップを追加**：意味ベースの色分け（§3参照）。

**`screens/mylife/hub.jsx`の`mylife_main`を全面再構成**：選手カード直下に「🎬 今月の行動」
ブロックを新設（黄塗りの強調ボタン＋理由1行）。判断材料となる「今月のレース」「今月の
練習メニュー」を行動ブロック直後へ移動。監督指示／先月の成長／新聞／弟子／状況はその後段へ。
📂メニュー8ボタンを`role="menu"`へ統一。⚙その他・キャリア管理を閉じた折りたたみに変更し、
`role="danger"`は引退・データ消去のみに限定。

**検証**：`npm run build`成功。Playwrightで新規デビュー→本編表示まで進め、今月の行動ブロック・
メニューの均一化・その他折りたたみの分離を確認。コンソール／ページエラー0件。
密度（末端テキスト要素数・ページ高さ）before/after：109個/1830px → 96個/1644px。

**副産物：`金特`のdata/controllers/domain残存**：§23完了後の恒例チェックで、第2波が
「全廃」したはずの`金特`がスカウト・引き抜き画面に再出現していると判明。原因は第2波の
棚卸し対象が`screens/`と`components/`のみで、`data/`・`controllers/`・`domain/`・`state/`の
ユーザー可視文字列（能力説明・ショップ項目・キャリアログ）が範囲外だったこと。6ファイル
10箇所を修正し`wave2_verify.mjs`で残存0件を再確認。

`git show 60f7bf4`

---

## 25. 素質ランク・伸びしろヒントの非公開時ランク圧縮＋才能の片鱗の成長力リーク修正（2026-08・完了）

**位置づけ**：ユーザー指摘「マイライフの素質ランクの判定、成長力を隠したせいで変になって
ないか。才能の片鱗で成長力がわかっちゃったりする」への対応。

**問題1：才能の片鱗が成長力の等級を生でリーク**：デビュー特典「🌱 才能の片鱗」のノート文が
`成長力C→B`のように変化前後の等級を直書きしていた。同じ画面の他表示は🔒???でマスクして
いるのに、この一言だけ生の値が読める情報漏洩バグだった。`domain/mylife/createChar.js`の
ノート文から等級表記を削除。Playwrightで60回リロールし実際に5回発生させ、等級表記が
一切出ないことを確認。

**問題2：非公開時、素質ランクが実質常にCへ潰れる**：`mlTalentRank`は成長力の寄与
（`powScore*2`、最大+6）が他の全要素の合計より大きい式で、非公開時に`powScore`をゼロ化
すると公開時と同じしきい値では上位ランクに届かなくなる。実測（3経歴×5脚質・8万体）：

```
公開時（成長力込み）：SS 0.0% / S  0.4% / A 11.0% / B 44.8% / C 45.2%
非公開時（旧しきい値のまま）：SS 0.0% / S 0.0% / A  0.0% / B  3.9% / C 96.1%
```

デビュー画面は常に非公開なので、この96%Cが実際にプレイヤーが見る分布そのものだった。
`potentialHint`（伸びしろ表示）も同型（非公開中「伸びしろ大」が理論上到達不能）。

**修正**：両関数とも`revealPow=false`専用のしきい値を新設（`revealPow=true`側は無改修）。
`mlTalentRank`：`{B:0.5, A:2.1, S:3.8, SS:4.8}`。`potentialHint`：`{中:2, 大:3}`。

**検証**：Node単体テストでリーク再発なし（growthPowをS/Cに変えてもスコア完全一致、500件）・
較正後分布が単一ランクへの collapse から脱していること（`mlTalentRank`非公開：B59%/A15%/
C25%/S0.7%）を確認。`npm run build`成功。Playwrightで25回リロールしA/B/SS複数ランクの
出現・成長力は引き続き🔒???表示のままであることを確認。

`git show 3fb53f2`

---

## 26. 能力表示の重複統合＋用語統一（コース適性/種目別適性）（2026-08・完了）

**位置づけ**：ユーザー指摘「能力表示をレーダーだけにしてもいい気がする」「同じ要素を2回
説明しているもの・違う表現で説明しているものがあれば統合・統一したい」。着手前にExplore
エージェントで全画面の能力表示コンポーネントを棚卸しし、CLAUDE.md §1に従い適用範囲を
ユーザーに確認してから実装。

**確認できた重複**：マイライフ本編の自分の選手カードとシーズンの選手一覧カードの2箇所で、
`AbilityGrid`（棒グラフ）と`AbilitySoshitsuRadarPair`（レーダー）が同一データを二重表示。
素質6項目も`SubStatLine`（テキスト）とレーダーで同様に二重。この2箇所以外（デビュー画面・
FA市場・トレード・引き抜き市場・レース当日カード）は重複が無いため対象外（ユーザー確認済み）。

**用語ゆらぎ**：`DisciplineGrid`は同一コンポーネント・同一データなのに、マイライフでは
「コース適性」、シーズンでは「種目別適性」と表記が割れていた。「コース適性」に統一。

**実装**：両画面とも`AbilityGrid`・`SubStatLine`の呼び出しを削除。`DisciplineGrid`を上、
`AbilitySoshitsuRadarPair`を下に常時表示で配置（マイライフは元の折りたたみを廃止、シーズンは
「▼レーダーで見る」トグルとstateごと削除）。詳細は§3「UIコンポーネント」参照。

**検証**：`npm run build`成功。Playwrightで両モードとも旧文言の残存なし・コンソール／
ページエラー0件を確認。

**宿題（次回・次のアクション#16）**：能力上限の収束問題はバランス変更の大仕事のため今回は
スコープ外。

`git show 5e19c01`

---

## 27. DEVLOG.mdの大規模スリム化＋サイズ基準の明文化（2026-08・完了）

**位置づけ**：ユーザーから「スリム化して。なるべく一回のReadで読めるようにしたい。基準を
明確化できたらいいな」との依頼。CLAUDE.md §4は「25,000トークン相当＝十数万バイト以上」という
目安を既に持っていたが、具体的な閾値・実測根拠は無かった。

**実測で確定した基準**：本ファイル（当時1781行・177,155バイト）をRead呼び出ししたところ、
**実際に25,000トークンで切り捨てられ382行分（全体の21%）しか読めなかった**（ツールの
切り捨てメッセージが「69,148トークン、上限25,000」と明示）。これにより
①1回のRead呼び出しの実際の上限が25,000トークンであること、②本ファイルの実測換算が
約2.56バイト/トークンであること、の2点が確定した。この実測値から「65,000バイトで整理検討・
90,000バイトで必須」という具体的な閾値を導出し、本ファイル冒頭に明文化した。

**圧縮内容**：§13〜§23（2026-08のWave群、旧prose形式で計約57KB）を、§10〜12で既に確立して
いた「件名＋要点1行＋`git show <hash>`」の索引形式へ圧縮。§5（2026-07時点の古い企画メモ、
一部は既に完了・一部は現在も有効な未着手候補）を、有効な候補だけの短い箇条書きへ再構成。
§7〜9（モジュール分割の経緯）も同様に、今も有効な「設計原則」はプローズのまま残し、
「どう至ったか」の物語部分を索引へ圧縮。§3（アーキテクチャ）には、圧縮対象になった
セクションの中にあった**今も有効な技術的事実**（成長キャップの現行式、新ステータスの生成
ロジック、能力表示UIの現在の構成、Btnのrole分類）を転記し、生きた参照として存続させた。
§0/§1のブランチ名がハードコードされていた古い注意書き（旧ブランチ`imtukw`を指していた）は
ブランチが毎セッション変わる実態に合わせて汎用の注意書きへ書き換えた。

**結果**：177,155バイト・1781行・約69,148トークン → **51,871バイト・606行・約20,300トークン**
（71%削減）。25,000トークンの上限に対し約20%の安全マージンを残せた。情報はすべてgit履歴
（コミットメッセージ・diff）に残っているため実質的な損失はない。

---

## 28. 能力上限の収束問題を是正 → 次のアクション#16（2026-08・完了）

**位置づけ**：次のアクション#16の設計合意を受けての実装。ユーザーとの設計対話で4点を
先に確定：①growthPow（成長力）は引き続き到達速度のみに影響させ、cap自体には加点しない
（マスク化との整合を優先）／②差別化は`breakthrough`（突破力）側で行う／③シーズンモード
にも同じ考え方を適用する／④最終到達点の差は目安+15〜20点／⑤既存セーブへも遡及適用する。

**実測で確定した現状の問題**：`growthFactor`/`softFactor`の突破力感度式`(0.5 + breakthrough/100)`
は、実際に生成される突破力のレンジ（p10≈41〜p90≈63）に対して感度が低すぎ、キャリア終盤の
最終到達値の差をシミュレーションしたところ**わずか+2.2点**だった（ユーザーの「差があると
しても2、3ぐらい」という指摘と一致）。

**設計**：`mlGrowthCap`/`DIFFICULTIES.growthCap`という共有の天井そのものは変えない（合意①）。
代わりに、上限を超えてどこまで伸ばせるかを左右する突破力の感度式の**傾きだけを強化**する。
`breakthrough=50`（既定・旧セーブ互換）で係数1.0という性質はそのまま保ち、そこからの傾きを
0.08に較正（200ティックのシミュレーションでp10/p90相当の最終値差が+17.4点になる値を探索）。
`growthFactor`（マイライフ・シーズン共通の5能力成長）と`softFactor`（加速力/メンタルの
副ステ成長）の両方が同じ感度式を共有していたため、共通ヘルパー`breakthroughMul(breakthrough)`
に集約して両方に適用。season/mylife共通の関数のため**シーズンモードにも選手ごとの差別化が
自動的に及ぶ**（合意③）。突破力はプレイヤー・NPCとも生成時に既に確定済みの固定値のため、
既存セーブの選手にも次の成長ティックから新しい感度式が適用される（合意⑤・特別な移行処理は
不要）。

**実装**：`logic/support.js`に`BREAKTHROUGH_SENSITIVITY = 0.08`・
`breakthroughMul(breakthrough=50) = max(0.05, 1.0 + 0.08×(breakthrough-50))`を新設。
`softFactor`・`growthFactor`内の`(0.5 + breakthrough/100)`をこのヘルパー呼び出しへ置換。

**検証**：Node単体テスト20/20件パス（`breakthrough=50`で旧式・旧`growthFactor`/`softFactor`の
定数展開と完全一致する回帰確認／下限クランプ／p10-p90相当の最終値差が15〜22点の目標域に
収まること／`addAb`・`growSub`で突破力が高い選手ほどよく伸びることを実際の関数呼び出しで確認）。
`npm run build`成功。Playwrightでマイライフを90ターン連続実行し、`NaN`/`undefined`表示漏れ0件・
コンソール／ページエラー0件を確認。

`git show 398ab39`

---

## 次のアクション（ユーザーメモ・未着手）

以下はユーザーから明示的に指示された、今後着手すべき項目のメモ。優先度・着手順は未確定。

**★最優先：UIテキスト・ビジュアルの作り込み（2026-08指摘・CLAUDE.md §7に明文化済みの恒常タスク）**
9. **ゲーム内の無駄なテキストを削り、「ゲームとして良いUI」に作り込む。**
   第1波（§22）・第2波（§23）・レーダー/棒グラフ重複統合（§26）を実施済み。第3波候補
   （ヘルプ本文／`OVR`略語／スポンサー行の情報量）は「削らなくてよい」とユーザー判断済み
   （現状維持で決着）。**新規UIを作るたびに以下3点を自問すること**（CLAUDE.md §7参照）：
   (a) その情報は本当に要るか、(b) 開発上の語彙をそのままユーザーに見せていないか、
   (c) 小さい文字の長い説明で誤魔化していないか。画面全体の導線・情報設計は別観点として
   #15（完了・§24参照）で対応した。

10. **チュートリアル的な導入説明の追加＋スタート画面の整理**（未着手）。
   - ゲームを開いた最初の画面と、モード選択後の最初の画面である程度の説明をする。
   - スタート画面（起動一発目）に、まだ選んでいないシーズンモードの自チーム情報が
     見えているのは不要。`MODE SELECT — v14`は既に撤去済み（§22）だが、その下の
     説明文は内容に誤りを含むうえ視認性も低いため、文言修正＋視認性向上が必要。

11. **スプライト（スプリント／ダンシング）モデルの描き直し**（2026-08・凍結中）。
   - 「スプリントにスプリントしている感がない」「ダンシングも分かりづらい」との指摘。
   - **試した3方式は全て失敗と判明・凍結済み**：(a) AIへの新規描画発注（デザイン理解の
     限界で形が崩れる）、(b) 行の挿入・削除で姿勢変更（選手と自転車が同じ行で絡み合い
     原理的に不可能）、(c) レイヤー分離＋手続き的な腕脚描画（線が棒/塊になり見るに耐えない）。
   - **結論**：現行の絵は手描き参考画像からの抽出でのみ実績がある。再開時はdancing/sprint
     6コマ分の新しい参考画像をユーザーに用意してもらい、CLAUDE.md §6の手順
     （確定済み別コマと同じ参考画像上でハンドル位置を目視突き合わせる）に従って抽出する。
   - 検査ツール`tools/bike_sprites.py`のみ現存（発注仕様書は削除済み）。

12. ✅ 最終スプリント演出のマーカー位置（完了・§20参照）。
13. **最終スプリントで、ゴールしてしばらく経ってから通常姿勢(normal)に戻ってほしい**
   （2026-08指摘・未着手）。現状は`domain/shared/sprintPosture.js`の`sprintPosture(rem, isDancer)`が
   `rem <= 0.05`でnormalへ切り替えており、ゴール直前でスプリント姿勢が解除されるように
   見えることがある、との指摘。しきい値`rem`の基準や演出内のカメラ・vtとの同期タイミングを
   見直す必要がある。
14. **シーズンモードで「引き抜き」「対談申し込み」系のイベント頻度が高くしんどい
   （特に序盤）**（2026-08指摘・未着手）。「最初の2年くらいは無しでいい」という要望。
   実装調査済み：`controllers/season/month.js`の被引き抜き（`makePoachOffer`）が年数を問わず
   毎月16%の確率で発生（`month !== 0 && Math.random() < 0.16`）。「対談申し込み」に該当
   しそうなのは同ファイルの移籍志願（`requester`、月25%）と推測されるが要ユーザー確認。
   対応案：`s.year <= 2`の間はこれらの発生確率を0にする、または大幅に下げる。
15. ✅ **導線（ナビゲーション）とユーザビリティ全般の作り込み**（完了・§24参照）。
16. ✅ **能力上限の収束問題（極めると全員同じステータスになる）**（完了・§28参照）。
   growthPowは到達速度のみに据え置き、`breakthrough`（突破力）の感度式を強化して
   最終到達点に恒久的な個体差（目安+15〜20点）をつけた。season/mylife共通の関数のため
   シーズンモードにも自動的に及ぶ。

**UI/メニュー構造**
1. ✅ メニュー画面で「ホームに戻る」を大分類の即時選択項目に格上げ（完了・§9末尾参照）。
2. ✅ 大分類の中の小分類を17小分類へ細分化（完了・§9末尾「Step13第7弾」参照）。

**拠点(BaseView)の部屋の見た目強化**
5. ✅ 部屋のグレードアップ（完了・§14参照）。
6. ✅ トレーニング室の「ぼっ立ち」修正（完了・§15参照）。**メカニック室は今回スコープ外**
   （作業台にかがむ等の新ポーズが必要で`PixelPerson`のスプライト追加を伴うため、下記4番の
   ドット絵化ウェーブと一緒に検討する）。

**ドット絵化の拡張**
3. ✅ レース最終区間のスプリント演出にドット絵を適用（完了・§16参照）。
4. **拠点(BaseView)のオブジェクト類（什器・プロップ・建物など）もドット絵化する**（未着手）。
   ユーザー提示の分類（A〜Fの計41項目）：
   - A. 人物共通モデル(Person.jsx)：①立ち/歩行 ②座り
   - B. 自転車に乗った選手(IsoRider.jsx、3姿勢)：③通常 ④ダンシング ⑤スプリント
   - C. 各部屋の主要什器・常時表示：⑥ローラー台 ⑦作業台 ⑧診察ベッド ⑨スカウトデスク ⑩空き部屋什器
   - D. 各部屋の二次什器（18種、施設Lvで段階解禁）
   - E. 屋外プロップ（10種）
   - F. 建物・地面・コース

**配合・育成の深掘り**
7. **配合面の深掘り**（未着手）。血統表の可視化（家系図）、特殊配合の実装（ドラクエ
   モンスターズ的な組み合わせ配合）。§5「特殊配合の深化」と統合して検討する。
8. ✅ マイライフの難易度調整。Phase 1・Phase 2完了（§17・§18参照）。
   **保留（判断③）**：脚質ごとの能力別成長上限差は今回見送り。育成上限の実績連動が定着
   してから改めて検討。
   **保留（判断⑤）**：資金の使い道の拡充・野心ルートの上位段追加は今回スコープ外。
   **保留（判断⑰）**：新ステータス（突破力・安定感、将来的にスピリット・運も）を配合の
   遺伝要素にする案は、上記7番「配合面の深掘り」のフェーズで接続する。
   **Phase 3（次回以降・メモのみ）**：マイライフに自チームの僚友システムを新設し、新
   ステータス「スピリット」（チームメイトの育ちやすさ・チームケミストリー）を接続。
   **✅ イベント本文の継続拡充（恒常タスク）**：「数百種類」は長期目標。既存5分類
   （`bad`/`weight`/`outcomes`/`abKeyDelta`/`breakthroughDelta`/`stabilityDelta`/
   `luckDelta`/`growthPowBump`/`talentCapDelta`）のタグ付け方針をそのまま踏襲し、
   `data/events.js`のML_EVENTSへ追記していく。
