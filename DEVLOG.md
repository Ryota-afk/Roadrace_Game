# DEVLOG — ロードレース・プロチーム運営（開発引き継ぎ）

新しいチャットはこのファイルを最初に読めば、巨大な本体ファイルを全読みしなくても文脈を安く復元できます。
**使い方**：新チャット冒頭で「`DEVLOG.md` を読んで続きから」と指示 → 必要な箇所だけ本体を部分読みする。

**サイズ規律（2026-08・実測で確定した具体的な基準）**：このファイルへのRead呼び出しは実測で
**25,000トークンが上限**（超えると先頭から切り捨てられ末尾の「次のアクション」が読めなくなる）。
このファイルの実測換算はおよそ**2.5〜2.6バイト/トークン**（1781行・177,155バイトが69,148トークン
相当だった実測に基づく）。よって：
- `wc -l -c DEVLOG.md` を大きな節目ごとに確認する。
- **65,000バイトを超えたら整理を検討**、**90,000バイトを超えたら次の区切りで必ずスリム化する**
  （90,000バイト≒35,000トークンで安全域を明確に超える）。
- 整理方法は毎回同じ：**完了済みWaveの詳細プローズを「件名＋要点1行＋`git show <hash>`での
  復元手順」の索引へ圧縮**する。生きた参照情報（§3アーキテクチャ・§3.5機能一覧・§6方針・
  「次のアクション」）はプローズのまま残す。情報はgit履歴に残るため実質的な損失はない。
- 直近の実績：2026-08にこの基準で177KB→約55KBまで圧縮（本ファイル末尾の圧縮記録を参照）。

---

## 0. これは何か

単一HTMLファイルで動く React + SVG のロードレース・ゲーム。2モード構成：
- **シーズンモード**：6名ロースターのチーム運営（経営）
- **マイライフモード**：一人の選手のキャリア育成（人生）

自転車ロードレースのチーム戦術（エース／アシスト、逃げ、集団、山岳など）をSVGで可視化しつつ、
育成・血統・世界ランキングなどのメタ要素を積み上げてきた。

> **作業ブランチはその時のタスク指定に従う**（セッションごとに変わる）。過去に旧いブランチ名が
> 誤って指定され、モジュール分割以前の孤立した土台に積みそうになった事例があるため、
> 指定されたブランチが現行の`src/`構成（本ファイル§1参照）を含んでいるか怪しい場合は
> 作業前に確認すること。

---

## 1. ファイル構成と開発フロー（重要 / v12モジュール移行後）
**2026-07 に単一HTMLからモジュール分割＋Viteビルドへ移行した（経緯は §8）。真実の源は `src/` に移り、`roadrace_v12.html`/`.jsx` は廃止。**

- **`src/`** … 唯一の編集対象・真実の源
  - `src/main.jsx` … 本体（`App()`の状態・ハンドラ＋画面ディスパッチの薄い配線）
  - `src/index.html` … Viteの入口HTMLテンプレート（薄い雛形）
  - `src/data/` … 静的データ定数のみ（ロジック禁止・JSX/domain import禁止）。`abilities/breeding/course/items/progression/theme/economy/events/directives/gear/teams` 等
  - `src/view/` … 文字列生成の純関数（JSX無し）。`flavor.js`(選手フレーバーテキスト)、`news.js`(ライバル動向・世界ニュース)
  - `src/domain/` … 純粋なドメインロジック（生成器・計算関数、JSX無し）。`season/*`・`mylife/*`・`shared/*`
  - `src/sim/` `src/breeding/` `src/world/` `src/state/` `src/components/` … 分割済み
  - `src/controllers/` … `main.jsx` App() のハンドラのうち `(state, ...args) => newState` の形のものを純関数として抽出。`season/*`・`mylife/*`
  - `src/logic/support.js` … 表示ヘルパー＋残存ロジック（画面イベント効果適用・監督評価・配合表示・実績判定等）
  - `src/screens/season/` `src/screens/mylife/` … 用途クラスタ単位で分割済み。各`hub/`配下はさらに小分類ディレクトリに分割（§9末尾・Step13第7弾参照）
- **`index.html`（リポジトリ直下）** … `npm run build` が生成する**自己完結の単一HTML成果物**（デプロイ用）。React/JSXはビルド時に変換・バンドル済みで**CDNもBabelも不要**。手で編集しない
- `package.json` / `vite.config.js` / `package-lock.json` … ビルド定義
- `dist/`, `node_modules/` … gitignore（追跡しない）
- `archive/` … 過去バージョンのアーカイブ（`roadrace_v5〜v11.*`＝分割前の単一ファイル版、`archive/design/`＝旧設計メモ）。src/から未参照・触らない

### 編集→ビルド→コミット→push の手順
```bash
# 1) src/ を編集（対象モジュールだけ読めばよい＝トークン激減）

# 2) ビルド（index.html を再生成）。Node/npm 必須。初回のみ npm install
npm install          # 初回だけ
npm run build        # vite build && cp dist/index.html index.html

# 3) コミット（日本語メッセージ）。末尾に必ず以下のトレーラを付ける
#    Co-Authored-By: Claude ...
#    Claude-Session: https://claude.ai/code/session_...
git add -A
git commit -F - <<'EOF'
（日本語の要約タイトル）

（本文）
EOF

# 4) push（指定された作業ブランチへ。default へ直push禁止）
git push -u origin <その時の作業ブランチ名>
```
- コミットメッセージは日本語。モデル識別子はリポジトリに書かない。
- **`index.html` は生成物**。src を直したら必ず `npm run build` してから両方コミット（＝新しい二重管理を作らない）。

---

## 2. 検証（Playwright）— ビルド成果物をそのままテスト
**移行でCDN依存が消えたため、vendor版React差し替えハックは不要**。`npm run build` が出す
`index.html` は自己完結（React/JSXバンドル済み・CDN/Babel不参照）なので、クラウド環境でも本物をそのまま開ける。

```bash
# 1) ビルド
npm run build

# 2) サーバ（リポジトリ直下を配信、または npx vite --port 5190 で開発サーバ）
npx http-server -p 8844 -s -c-1 .   # ← run_in_background で起動。curlで200を待つ

# 3) Playwright（scratchpad に置く）
#    executablePath: /opt/pw-browsers/chromium （args不要な場合が多い）
```
テストのコツ：作戦ピッカーは**メイン画面**（「このレースに出場する」ボタンの上）にあるので、
作戦を選んでから出場する。controlled input は `page.fill` を使う（`inp.value=` はReactに効かない）。

---

## 3. アーキテクチャ（関数名で参照・随時更新する生きた参照）
### レースsim（両モード共通）
- `simulateTicks(course, riders, fromTick, directive, noGroup)` … **レース全体を先に計算**（finishTimeまで）。
  RaceView は `posHist/energyHist/modeHist/...` の**再生専用**。
- `buildMyLifeSim(raceMeta, player, myTeamName, classIdx, difficultyId, dayTag, directiveKey, rival, year, rival2, teammates, tactic)`
- `canPull(en, segType)`（誰が牽引できるか）, `assignAIRoles`, `effAbilities`, `overall`, `newRider`, `mulberry`(seeded RNG), `pickRiderName`
- 役割 role：`ace/lead/sub/mountain/flat/breakaway`。draftモードは省エネ、pullは全消費。
- **残脚ゲート（`legsLeft01()`・§35で新設）**：判断カードの一手（`send`/`attack`/`conserve`等）の
  威力は残脚（-90〜40の実測レンジ）に比例する。`send`の持続(4〜18tick)・追い込み量(0.02〜0.07)、
  `attack`の持続(10〜30tick)が残脚で決まる。`conserve`の持続は500tick（脚を溜める価値を持たせるため）。
  素朴に0〜60で切ると終盤は全員が下限に張り付くため、範囲は実測分布に合わせてある。
  判断カードには「脚の残り」ゲージ（バー＋一語）で可視化。**RACE_MOVESは両モード共通定義**。
- **演出専用の乱数（`domain/shared/viewHash.js`・§36-Cで新設）**：`sim/race.js`の`riderHash01`は
  着順計算に使うため変更不可（`(id*2654435761+salt*40503)%100000`は`2654435761 mod 100000=35761`で
  線形写像に退化し、演出に使うと集団が規則的に動いて見える）。集団描画の揺らぎ等**演出専用の箇所は
  必ず`viewHash01`/`viewWander`（ビット混合する32bitハッシュ）を使うこと**。

### マイライフ状態
- `initMyLife()` / `mlCreateChar(type, background, master, partner, cpMeta)` / `mlGenRace(year, month, classIdx)`
- 月次：`mlAdvanceMonth`, `mlApplyMonthEffect(player, mode, ctx)`
- セーブ：`ML_SAVE_FIELDS`（配列）、`saveMyLife/loadMyLife`。**playerはまるごと保存**されるので player.* は保存フィールド追加不要。
- **成長キャップ（2026-08 Phase 1で再設計・§17参照）**：
  `mlGrowthCap(year, player, ml) = min(140, 90 + timeComponent(年数、最大+20) + achievementBonus×難易度係数(易1.3/普1.0/難0.75/鬼0.5) + player.talentCap)`。
  `growthPow`（S/A/B/C）は**到達速度のみに影響**、最終天井には影響しない（意図的な設計・§28参照）。
  シーズン側は`DIFFICULTIES.growthCap`（88/94/102/112、難易度で固定・選手ごとの差なし）。
  `bumpGrowthPow`, `GROWTHPOW_ORDER=["C","B","A","S"]`, `POW`。マスク：`mlGrowthPowRevealed(ml)`（`ml.year>=3`まで`🔒???`）。
- 能力成長の逓減カーブ：`growthFactor(v, cap, breakthrough)` / `softFactor(v, cap, breakthrough)`（`logic/support.js`）。
  上限を超えてどこまで伸ばせるかは`breakthrough`（突破力、生成時固定値・タイプ別base44〜58）の
  `breakthroughMul(breakthrough) = max(0.05, 1.0 + 0.08×(breakthrough-50))`で決まる（2026-08・§28で
  傾きを強化＝「極めると全員同じになる」問題の是正。season/mylife共通関数のため両モードに自動適用）。
- 監督指示：`MANAGER_DIRECTIVES`（keyed, check-fn付き）, `mlGenDirective`。**復元時はキーで引き直す**（JSONで関数が消えるため）。
- 作戦：`ML_TACTICS`（balanced/wait/early/aggressive/assist）。
- **今月の行動レコメンド（§24）**：`domain/mylife/nextAction.js`の`mlNextAction()`が「今月何をすべきか」を
  1つだけ返す純関数。優先順位は4段：疲労85以上→休養（大舞台でも最優先）／大舞台レース＆疲労70未満→出走／
  通常レース＆疲労75未満→出走／それ以外→練習。
- **マスクされた値の扱い（§25で確立した原則・厳守）**：`growthPow`は3年目まで`🔒???`で伏せる仕様のため、
  **他の文言から等級を逆算できる形で出してはいけない**（デビュー特典「才能の片鱗」のノート文が
  `成長力C→B`と生の等級を書いており情報漏洩バグになっていた）。
  またマスク中は素質ランク・伸びしろヒントが単一ランクへ潰れるため、`mlTalentRank`・`potentialHint`は
  **`revealPow=false`専用のしきい値**を持つ（`mlTalentRank`＝`{B:0.5, A:2.1, S:3.8, SS:4.8}`／
  `potentialHint`＝`{中:2, 大:3}`。`revealPow=true`側は別のしきい値のまま）。
  伏せる値を増やす時は必ず「その値抜きで分布が潰れないか」を実測すること。

### 配合・血統
- `mlBreedBonus(parentA, parentB)` が全部返す：`nick/inbreed/plusValue/plusPer/abBonus/extraAbilities/subBonus/goldInherit/exclusive/archNotes/bakuhatsu/matingGrade/growthSteps/talentCap/danger/dangerLabel/healthMit/special`
  - **設計方針（厳守）**：爆発力・系統・特殊配合のボーナスは**初期能力に足さない**。伸びしろ（growthPow段数＋talentCap）＋称号＋金の特殊能力に還元する（序盤インフレ回避）。
  - `BREED_NICKS`（脚質相性）, `ARCH_BREED`（生き様の血）, `ML_SPECIAL_MATINGS`+`mlSpecialMating`（特殊配合）, `mlGradeColor`
- 系統レジストリ（プレイ跨ぎ／localStorage `roadrace_v12_bloodlines`）：`loadBloodlines/saveBloodlines`, `mlRegisterBloodline`(引退時), `mlBloodlineTier`（未確立→確立→名門→大系統）, `mlBloodlineFactor`, `mlBloodlineBonus`

### 生きた世界
- `mlWorldStarsForYear(seed, targetYear, legendPool)` … **キャリア固有シードから決定論的**に24人の永続スターを生成し年1からtargetYearまで再シミュ（加齢→成長/衰え→引退→世代交代）。状態は`ml.worldSeed`のみ保存。
- `mlWorldNews(seed, year, legendPool)`, `mlWorldBoard(ml)`, `computeWorldRank`
- **殿堂の血が流入**：`legendPool = loadMlLegends()`を渡すと上位スターが殿堂選手の姓・脚質を継ぐ`bloodOf`付きになる。
- **殿堂の頭数と対戦相手への出現は無関係**（次のアクション調査済み）：`legendPool`は無制限に蓄積するが、1レースあたりの殿堂選手代打は`nLeg=0/1/2`（55%×35%分布）で固定上限。
- **永続ロースター（§33）**：`WORLD_ROSTER_SIZE = 8`（`data/teams.js`）。
  `genWorldRosters(rng, count, teams)`で生成→`ageWorldRosters()`が年次で加齢/引退/新人補充＋定員補充、
  `topUpWorldRosters()`が定員割れを末尾に追記（既存メンバーの順序・identityには触れない）。
  **⚠️ 絶対にやってはいけないこと**：`genWorldRosters()`の`count`を直接変えること。
  同じseedでもcountを変えると生成列がずれ、**全チームの顔ぶれが総入れ替わりになる**
  （実測で確認済み。だから定員拡張は初期生成6名のまま別rngストリームでtopUpする形にしてある）。
  出走人数は「ロースター実在数＋alumni」までclampし、埋まらない枠を使い捨て選手で埋めない。

### アンビション／キャリア／モニュメント
- `ML_AMBITION_PATHS`（victory/bigstage/devotion/world）, `mlCurrentAmbition`, `mlFirstUnmetRung`, `mlAmbitionCleared`, `applyAmbitionReward`
- `mlCareerArchetype(s)` … 引退時の生き様（称号）。`careerWins/careerPodiums/careerBigWins/careerTitles/careerClassics`を参照。
- `ML_MONUMENTS` … ワンデー古典3種（石畳/丘陵/山岳）。`race.monument`フラグ、`careerClassics`を加算。

### 殿堂・その他
- 殿堂：localStorage `roadrace_v12_mylife_legends`。`ML_LEGENDS_KEY`, `mlLegendSnapshot(s)`, `mlRecordLegend(s)`, `protegeInherit`
- 特殊能力：`ABILITIES`（`iron`鉄人 / `tough`頑丈 / `glass`ガラスの体[bad] / `big`大舞台 / `finisher` 等）, `GOLD_CONDITIONS`, `hasAbility/hasGoldAbility`
- 新ステータス（2026-08・Phase1/2）：`breakthrough`(突破力)/`stability`(安定感)/`luck`(運)。生成時固定値、`core/core.js`の`genSubStats()`。
  `RiderRadarChart`（`components/RadarChart.jsx`）で6角形表示。イベント経由のみ例外的に増減可（`*Delta`系effects）。
- シーズンモード：別state（`g`/`initGame`）。`TEMPLATES`(コース), `signBredYouth`（シーズンの血統ユース）。

### UIコンポーネント（能力表示、2026-08 §26で再編）
- `DisciplineGrid`（`components/panels.jsx`）… コース適性（S〜G評価の5項目バー）。マイライフ・シーズン共通の見出しは「コース適性」に統一済み。
- `AbilitySoshitsuRadarPair`（`components/RadarChart.jsx`）… 能力5項目レーダー＋素質6項目レーダーの横並び。マイライフ本編・シーズン選手一覧の両カードで**常時表示**（旧`AbilityGrid`棒グラフ・`SubStatLine`テキストは重複のため廃止済み）。
- 他カード（デビュー画面・FA市場・トレード・引き抜き市場・レース当日選択）は`AbilityGrid`（棒グラフ）のみ、密なリスト用途のため据え置き。

### ドット絵スプライトの描画方式（§37で確立・生きたルール）
`PixelBike`/`PixelPerson`/`PixelBikeUse`は**canvasで1回ラスタライズし`<image>`1ノードで描く**
（`components/sprites/rasterize.js`の`spriteImageUrl()`。モジュールレベルのMapでキャッシュ）。
**`<rect>`を1ピクセルずつ積む旧方式・`<symbol>`+`<use>`方式のどちらも使わない**（実測で`<rect>`直描きは
出走22名×12色でSVG67,961ノード・2.3fps、`<image>`化で295ノード・60fpsへ改善済み。詳細は`git show bdd1eff`）。
**新しいドット絵キャラクターを追加する時も必ず`spriteImageUrl()`経由にすること。**

### 画面の外枠（wrap）の使い分け（2026-08 §29/§30で確立・生きたUIルール）
`components/chrome.jsx`に2種類ある。**新しい画面を追加する時はどちらに属するかを必ず判断すること。**
- `makeWrap`（season用）… `SeasonHeader`（クラス・チーム名・予算・ポイント）付き。
  **実在するチームが確定した後の画面だけ**に使う（`scoutpolicy_initial`以降）。
- `makeMetaWrap`… ヘッダー無し。モード選択・生涯評価・系譜ツリー・因子図鑑・CP交換所と、
  シーズンの`intro`・`newgame_setup`（＝まだチームが存在しない段階）に使う。
  過去2回、ここを取り違えて「起動直後の画面に架空のチーム情報が出る」バグを出している。

### Btnのrole（2026-08 §24で導入・生きたUIルール）
`components/ui.jsx`の`Btn`に`role`（`primary`/`month`/`menu`/`danger`）。primary=黄塗り（今の画面の
おすすめ、1つだけ）／month=黄枠線（月が進む行動）／menu=グレー枠線（月は進まない）／danger=赤枠線
（取り返しがつかない操作のみ。引退・データ消去等）。**新しいボタンを追加する際はこの4分類に従う**。

### localStorage キー一覧
- `roadrace_v12_mylife_save` … マイライフ本体
- `roadrace_v12_mylife_legends` … 殿堂（プレイ跨ぎ）
- `roadrace_v12_bloodlines` … 系統レジストリ（プレイ跨ぎ）
- `roadrace_v12_save` … シーズン本体
- クリアポイント等は `loadMeta()`（`totalEarnedCP` でアンロック判定）

---

## 3.5 実装済み機能インベントリ（全体像・再実装しないための一覧）
※ v11〜現行で積み上げ済み。細部は本体を参照。**ここにある物は「もうある」前提で扱うこと。**

### レースsim・可視化
- レース全体を先計算→再生する方式／2D集団フォーメーション／ローテーション・パスラインの縦可視化／カメラの選手追従
- 天候（晴/雨/猛暑）＋横風エシュロン、特能「悪天候巧者」
- 最終スプリントのドット絵演出（`PixelBike`、§16）／フィニッシュのズーム＆スローモ／周回レース
- コースレコード、実況の拡充、コースを道の曲線に沿わせる、逃げ要員が実際に逃げる、最終区間スプリント戦術
- 選手識別は引き出し線＋名前タグ方式に統一（自分=水色／エース=黄／自チーム=青／ライバル=赤／殿堂選手=別トーン赤、§20）

### シーズンモード（チーム経営）
- 6名ロースター運営、B1/A/PRO の3クラス昇格
- スタッフ雇用／FA移籍市場／選手トレード／移籍志願／退団選手をライバルチームが拾う／被引き抜き・引き抜き（双方向）
- グランツール（年3つ・海外遠征）＋副次分類、ステージ間の役割再配置
- チーム化学反応、キャプテン制、実績システム＋報酬、チーム順位表、拠点(BaseView)の部屋グレードアップ（§14）
- ユースアカデミー枠、血統ユース、施設アップグレード可視化、トロフィールーム
- ライバルチーム動向ニュース、会場相性/ホームアドバンテージ、クリア後の王朝継続(NG+)
- カイロソフト式の拠点画面（間取り・カメラ・什器・人物ドット絵・吹き出し、§10〜13）

### マイライフモード（選手育成）
- 月次ループ（出走／練習／完全休養／私生活イベント［受動発火・§18］）、経歴選択（高卒/大卒/実業団卒）
- 監督指示＋監督評価、年俸改定、契約交渉、移籍オファー、移籍入札戦
- ショップ（パーツ/消耗品/恒久ギア/車/住居/成長力・成長タイプ買い切り）、能力別コーチ
- ライバル（1人目・2人目）、節目の大会（世界選手権/五輪）、代表での役割
- 人生の岐路（結婚／大怪我＋3択リハビリ／第一子／恩師との別れ）、オフシーズンの過ごし方
- 個人スポンサー＋人気度＋スポンサーの仕事、自伝出版、引退後エピローグ（監督/完全引退）
- 恩師（師弟）＝新人時代の指導、教え子（プロテジェ）、メンター役
- フォーム、ピーキング、フォーム予報、天候予報、伸びしろヒント（成長力マスク中は専用しきい値、§25）
- 成長キャップの実績連動（§17）、出走経験による成長、対戦相手(AI)の年々底上げ
- 世界ランキング＋アンビション4路線（勝利/大舞台/献身/世界）、キャリアの生き様（称号）
- 配合（血統）：ニック／インブリード／累代+値／金の特殊能力クロス／配合限定特能／生き様の血／相性表／系譜ツリー
- 爆発力＆配合評価／危険度／系統確立＋因子／特殊配合／生きた世界（永続ペロトン＋殿堂の血流入）／モニュメント／アシスト作戦
- イベント：能力/新ステ変動・賭け（ダイジョーブ博士系）・覚醒・悪イベントの5分類、46件以上（継続拡充中、§18）
- イベント・岐路・オフシーズン・弟子指導での能力変化は必ず差分表示（`abilityDeltaSummary`/`protegeEffectSummary`、§21）
- 「今月の行動」の推奨表示（疲労・レース有無・大舞台から1つ判定、§24）

### 共通・メタ
- 難易度ティア（成長キャップ＋AI強度）、クリアポイント＋パーク屋（メタ進行）
- 選手ごとの戦績・ニックネーム・フレーバー、殿堂（プレイ跨ぎ）、累積タイトル台帳
- 特殊能力システム（銅→金への進化、特殊能力図鑑）、副ステータス（加速/体格/メンタル/突破力/安定感/運）
- 生涯プレステージスコア画面、ヘルプタブ（両モード）
- UIテキストの平易化・重複表示の統合（§22/§23/§26、CLAUDE.md §7に恒常タスクとして明文化）

---

## 4. 実装履歴（コミット索引）

v33系〜現行まで、すべてPlaywright（＋純ロジックはNode単体）検証・push済み。
**「何が実装済みか」は§3.5のインベントリを見るのが早い。** ここは「いつ・どのコミットか」を
辿るための入口として、キーワード群だけ残す。`git log --oneline --grep="<キーワード>"`→
`git show <hash>`で当時の実装箇所・判断根拠・検証結果まで復元できる。

- **レース演出の系譜**：`v39`（判断カード導入＝観戦→"プレイ"へ）／`v39.1`（脚質・特性・役割・
  地形と連動）／`v39.2`〜`v39.11`（ゴールスプリント演出を放送カメラ風→横視点→斜め→
  アイソメ→ドット絵化と繰り返し作り直した経緯）／`v39.12`〜`v39.20`（俯瞰マップ強化・
  展開戦術をsim本体へ・沿道の追従・難易度連動）
- **シーズンの駆け引き**：`v40`（中期目標）／`v41`（引き抜き合戦）／「相手チームを永続ロースター化」
- **世界の統合（#9）**：`A-1`統合ダイナスティ・ハブ／`A-2`レジェンド招聘／`A-3`共有ワールド／
  `A-4`選手→監督の転身ブリッジ／`B案`配合・系統の深掘り(B-1〜B-4)
- **拡充系**：特殊能力（5種→第2弾7種）／パーソナリティ3種＋異名／CPショップ／クリアポイント／
  チームTT／逆メンター（弟子育成）／引退セレモニー＆殿堂／メディアナラティブ・因縁の育つライバル
- **バランス系**：配合点検・危険度実効化・難易度つまみ・エース早期発射・地形別フィニッシュ決着／
  アシストの観戦⇔リザルト同期ズレ根治

---

## 5. 今後の候補（ゲーム内容・未着手のみ）

第1候補（シーズンの駆け引き①②③）は全て完了済み（§4参照）。以下、着手順は未確定：

- **生きた世界の深掘り**：`src/world/world.js`はv33.10で停滞。ライバルの成長/全盛期/引退・
  世界ランキング変動のニュース化・世代交代の物語化を深める余地あり。
- **アイテム・設備の刷新**：`src/data/items.js`は少数のまま。機材選択にトレードオフ（軽量
  ホイール＝登り有利/平坦不利、雨用・石畳用など）を入れる案（未整理）。
- 特殊配合の深化／リーグ全体の生きてる感は、**「次のアクション」の配合7番・25番に統合済み**
  （こちらでは二重管理しない）。
- **山岳×クライマーのアタック勝率(81%)／oni難易度の判断効果(0.66)**：設計意図として現状維持中
  （下げる場合の変更点は把握済み、`committedBreak`/`MOVE_EFF_BY_DIFF`）。

---

## 6. 守るべき方針メモ
- 配合・世界系のボーナスは **伸びしろ／称号／金の特殊能力** に限定（初期能力インフレ厳禁）。
- 新機能は必ず Playwright で end-to-end 検証してからコミット（数値ロジック＋UI表示＋0エラー）。
- ヘルプ（`mylife_help`画面）にも新機能の説明を追記して整合を保つ。
- **編集は `src/` のみ**。`src/`を直したら必ず`npm run build`してから両方コミット。`index.html`は生成物なので手編集しない。

---

## 7〜12. 2026-07 の構造移行とカイロソフト式への刷新（索引）

**設計原則は CLAUDE.md §5 が正（ここでは繰り返さない）**：依存は下向き一方通行
`data → core/sim → domain/state/view → controllers → screens → app`／`data/`・`view/`はJSXを
importしない／自己完結の述語関数はデータ扱いだが外部の状態変更関数を呼ぶクロージャはdomainに残す。

- **§7〜8 モジュール分割＋Viteビルド移行**（完了）：単一HTML（約9,300行）から
  Vite + `vite-plugin-singlefile`で「自己完結の単一`index.html`」を出力する方式へ。
  `main.jsx`は9,624行→2,395行→（Step11で）48行の合成ルートのみに。撤廃済み：`.jsx`手作業ミラー／
  実行時Babel／実行時CDN依存。`git log --oneline --grep="Phase"`
- **§9 モジュール細分化（Step1〜13）**：`domain/`・`controllers/`・`hooks/`の抽出を段階的に実施。
  過程で複数の実バグ（グランツール3日目進行不能・連打二重加算・OBコーチ雇用即クラッシュ・
  `rankSim`二重呼び出し）を発見・修正。Step13第7弾でメニューを17小分類へ細分化し
  `hub/{riders,facility,market,race,records}/`へ分割。`git log --oneline --grep="Step"`
- **§10〜12 カイロソフト式動線への移行／敷地画面のカットアウト化／ドット絵化**：常設マップ＋
  右下メニューへの刷新、BaseViewの部屋割り・什器・カメラの作り込み、人物・自転車をベクターから
  ドット絵グリッドへ全面差し替え。`git log --oneline --grep="Wave"`
  - **真因判明（CLAUDE.md §6の根拠）**：「dancing_NEの向きがおかしい」バグが長期化した真因は、
    コードの反転ロジックではなく**参考画像そのもの**が「通常」と「ダンシング」でNEの左右
    handednessが食い違っていたこと。以後、向き系のバグはまず参考画像同士を見比べる。

---

## 13〜23. 2026-08 Wave群（索引）

件名・要点1行・`git show <hash>`で詳細を復元できる。§17〜18の技術的な核心（成長キャップ式・
新ステータス）は§3へ転記済み。

- **§13 拠点の吹き出し**：持ち場ごとのセリフを性格・状態から決定論的に選ぶ`riderChatter.js`。`git show a4d57fe`
- **§14 部屋のグレードアップ**：4持ち場を資金で0〜3段階に強化（見た目のみ）。`git show 427b4f7`
- **§15 トレーニング室の「ぼっ立ち」修正**：新規ドット絵無しで、ローラー台のある部屋では
  選手を`PixelBike`（自転車ごと）で描画する方式へ。`git show 6a3ab36`
- **§16 最終スプリントのドット絵化**：`IsoRider`→`PixelBike`。`<defs>+<use>`方式で22名分
  約2万ノードになるはずが実描画233ノードに抑制。`git show 794cd20`
- **§17 マイライフ難易度調整 Phase 1**：成長曲線の逓減カーブ是正・成長キャップの実績連動化・
  新ステ「突破力」「安定感」・成長力の3年目までのマスク化・レーダーチャート導入。`git show d8c3dfa`
- **§18 同 Phase 2**：私生活イベントを受動発火へ、新ステ「運」追加、イベントスキーマを
  5分類へ拡張し46件へ拡充。`git show 31d8a52`
- **§19 最終スプリントの姿勢問題**：姿勢判定がPRO帯基準の絶対値で下位クラスでは発火せず、
  dancingの`<symbol>`自体が存在せず選手が消えていた2つの真因を修正。判定を集団内の
  相対順位へ（`domain/shared/sprintPosture.js`新設）。`git show 35c34c7` `8b2746c` `4c08434`
- **§20 選手識別マーカーの統一**：★・水色の丸を廃し引き出し線＋名前タグ（`RiderNameTag`）へ。`git show bee11a2`
- **§21 イベントによる能力変化を必ず明示**：無言だった3箇所を`abilityDeltaSummary`等で構造的に解消。`git show 7414ae9`
- **§22 UIテキスト 第1波**／**§23 第2波**：開発語彙・保証文・重複解説を一掃。第2波では
  複数行にまたがるJSXテキストの見落とし3件が後から発覚（棚卸しスクリプトが単行正規表現しか
  拾えなかった教訓）。`git show 6e117a0` `5fcbc75` `06c767d`

---

## 24〜31. 2026-08 改善群（索引）

この範囲にあった**今も有効な技術的事実**——`mlNextAction()`の優先順位、マスク値の扱いと
`mlTalentRank`/`potentialHint`の非公開時しきい値、`breakthroughMul()`の感度式、能力表示UIの構成、
`Btn`のrole分類、`makeWrap`/`makeMetaWrap`の使い分け——は**すべて§3へ転記済み**。
通常はそちらを見れば足りる。

- **§24 導線・ユーザビリティ**（→#15）：本編先頭に「🎬 今月の行動」ブロック新設、メニューの
  role統一、`danger`を引退・データ消去のみに限定。密度109要素/1830px→96要素/1644px。
  副産物：第2波が全廃したはずの`金特`が`data/`・`controllers/`（棚卸し範囲外）に残存。`git show 60f7bf4`
- **§25 素質ランク圧縮＋成長力リーク修正**：デビュー特典が成長力の等級を生で漏らすバグと、
  成長力を伏せた結果ランクがCへ潰れる問題（実測：非公開時C 96.1%）を是正。`git show 3fb53f2`
- **§26 能力表示の重複統合＋用語統一**：棒グラフとレーダーの二重表示を整理、「コース適性」へ統一。`git show 5e19c01`
- **§27 DEVLOGスリム化＋サイズ基準の明文化**：Readの実上限25,000トークン・約2.56バイト/トークンを
  実測で確定し、閾値をCLAUDE.md §4へ明文化。177,155→51,871バイト。
  **2026-08 第2回**：97,603→約63,000バイト。§7〜§12と§24〜§31を索引化し、生きた技術的事実は
  §3へ転記。§5と「次のアクション」の二重管理2項目（特殊配合・リーグ全体の生きてる感）も統合。
  **2026-08 第3回**：85,754→60,577バイト（29%減）。§32〜37（バグ修正・観戦品質・描画コスト群）を
  索引化し、残脚ゲート(`legsLeft01`)・演出専用乱数(`viewHash01`)・ドット絵の`<image>`化方式を
  §3へ転記してから圧縮。§38（第7弾）は最新作業のため索引化せず、「設計」と「実装結果」の
  重複記述を1本化する形で圧縮（356行→約80行）。
- **§28 能力上限の収束問題**（→#16）：突破力の感度が緩すぎて最終到達値の差が+2.2点しか
  出ていなかったのを`breakthroughMul()`へ集約し傾き0.08へ較正（p10/p90差+17.4点）。`git show 398ab39`
- **§29 スタート画面の整理＋導入説明**（→#10）：モード非依存の5画面にシーズンの自チーム情報が
  漏れていたバグを`makeMetaWrap`新設で解消。`git show 3f3b843`
- **§30 CP関連の導線・文言整理＋ジャーゴン一掃**：CP交換所をモード選択から直接開けるように、
  CPリセットを交換所へ移設、内部id漏れ・英語見出し13箇所・半角カタカナを一掃。`git show 7328a9f`
- **§31 UIテキスト 第3波**：8並列エージェント監査で40ファイル・約80箇所を修正
  （計算ロジックには一切触れず）。`git show 623160a` `35d75e8`

---

## 32〜37. 2026-08 バグ修正・観戦品質・描画コスト群（索引）

件名・要点1行・`git show <hash>`で詳細を復元できる。**今も有効な技術的事実（残脚ゲート・
演出専用乱数・スプライト描画方式）は§3へ転記済み**、永続ロースターの定員は§3「生きた世界」に転記済み。

- **§32 最終スプリント演出のバグ4件**：順位表が全員TOPと表示される・ゴールラインが菱形で
  線に見えない・ゴール後のカメラがガクつく・ゴール後すぐ通常姿勢に戻る、の4件を同じ波で修正。
  `git show 579e520`
- **§33 永続ロースターの摩耗補充**：引き抜きでロースターが摩耗すると`ageWorldRosters()`が
  定員を回復せず、恒久的に使い捨て選手で埋まり続けるバグを修正。定員8名化＋`topUpWorldRosters()`
  新設（詳細は§3「生きた世界」参照）。`git show 409d616`
- **§34 キャリアグラフのラベル重なり＋観戦カメラの緩和＋序盤2年の引き抜き抑制**：
  グラフの軸目盛りと代表点ラベルが同値だと重なるバグを修正、`smoothRaceCamera()`を純関数化し
  ズーム速度を調整、序盤1〜2年は引き抜き/移籍志願が発生しないよう抑制。`git show e23e61d`
- **§35 早駆けが常時最適解だった問題**：最終スプリントでは残脚が空でも早駆けが最上位勝率になる
  トレードオフ崩壊を発見。`legsLeft01()`による残脚ゲートを新設し状況依存の駆け引きに是正
  （詳細・計算式は§3「残脚ゲート」参照）。`git show af2d46e`
- **§36 第5弾：レース観戦の不整合一掃**：ゴール後の順位変動・スプリント解除の早期化・
  個人TT観戦の静止（`interpFrac`のデータ切れを外挿で解消、個人TT観戦自体は`soloTT`フラグで廃止）・
  俯瞰マップが先頭集団を捉えない・演出の動きが規則的（`viewHash01`新設、§3参照）の5件を修正。
  `git show fa589d7`（個人TTの原因訂正：`git show 60f3666`）
- **§37 第6弾：ドット絵スプライトの描画コスト**：真因は出走人数ではなくジャージ色数だった。
  `<rect>`1ピクセルずつ描画→canvasラスタライズ＋`<image>`化で67,961ノード/2.3fps→295ノード/60fps
  （25倍改善）。現行の描画方式は§3「ドット絵スプライトの描画方式」参照。`git show bdd1eff`

---

## 38. 第7弾（マイライフの成長設計）— 調査・実装（A/B/C完了・Dは保留）

**状態**：A・B・C実装済み・検証済み・commit/push済み。対象は#35＋ユーザー追加指摘。
変更ファイル：`controllers/mylife/month.js`／`logic/support.js`／`domain/mylife/nextAction.js`／
`screens/mylife/hub.jsx`／`state/state.js`（`git log --grep=第7弾`で参照可）。

**測定方法**：`mlApplyMonthEffect`（純粋なreducer）と`buildMyLifeSim`をNodeから直接呼び、
実コードのまま月次アクション・レースsimを回して実測した（体感ではなく数値で確認する方針）。

### 経緯：当初の指摘は4つの問題に分解された

当初は「毎年インフレする上限を前提にした設計になっていない」という指摘だったが、調査の結果
以下の4つに分解された（すべて実測で確認済み）。

1. **練習にコストが無い（根本原因）**：マイライフで`fatigue`を読む箇所は`sim/race.js`の
   `fatPen`（疲労100でも-15%）と`mlNextAction()`のUI表示だけで、成長にも`form`にも一切効かない。
   シーズンにある`fatigue>90`の故障判定もマイライフには無い。さらに**通常練習は疲労を-15する**
   （練習が休養を兼ねる）ため、休む理由が存在しなかった。
2. **合宿ローテが成長上限を素通りする**：専門トレ3種の対象能力が5能力を完全被覆しており、
   `gainMul 1.7`のまま回し続けられる。実測：レースを一度も走らず合宿だけ回した場合、
   成長力S・装備全部だと**年7で全5能力が101/102＝カンスト**。
   > **却下した案**：当初「練習配分(主軸0.9/他0.14)が偏りすぎ」と読み0.55/0.23への緩和を検討したが、
   > 合宿ローテで5能力すべてが埋まる以上これは対症療法だった。**配分は変更していない。**
3. **加齢衰退が「練習した月だけ」適用されるバグ**：`dec`適用がマイライフでは`train`/専門トレの
   分岐の中にしか無かった。実測（大学卒/B・年20時点）：毎月練習し続けると全能力が下限20まで
   崩壊し、30歳で練習をやめると一切衰えない、という逆転現象。シーズン側は`dec`をmode非依存で
   正しく適用しており、マイライフだけの例外＝バグと判断した。
4. **年次インフレが世代交代モデルと二重実装**：`buildMyLifeSim`の`yearBonus`（経過年数だけで
   AIの地力を最大+24底上げ）は「新世代の台頭」を謳うが、同じ役割は既に`ageWorldRosters()`
   （加齢・ピーク・衰え・33〜38歳での引退・ルーキー補充）が本物として実装済みだった。
   `yearBonus`は①プレイヤーから見えない②自チームの僚友も同率で強化する③`aiCap`に吸収される
   ④17年で頭打ち、という欠陥のある二重実装で、難易度は年5〜10がピークで年15以降易化する
   非単調な曲線になっていた（丘陵G3・能力105固定の勝率：年10=13%→年15=63%）。

### 決定事項（ユーザー合意済み）

衰え方＝**緩やかに衰える**（練習有無に関わらず毎月少しずつ・目安年-3）／育成＝**配分ではなく
「練習のコスト」を作る**／後半＝**年次インフレの概念自体を捨て実在する世界の選手に一本化する**／
既存セーブ＝**気にせず適用**（新旧分岐フラグは作らない）。

### 実装内容と検証結果

**A（衰退の是正）** — `dec`適用をtrain/専門トレの分岐から出し、if/elseチェーンの外＝mode非依存で
毎月1回に統一（シーズンと同じ形）。縮小係数`ML_AGE_DECLINE_MUL=0.2`を追加（シーズンの最大1.2/月は
急峻すぎるため、最大dec=0.24/月＝年-2.9に縮小。`growthPhase()`自体はシーズンと共有のため不変）。
`mlNextAction()`に`declining`引数を追加し、衰え期は練習より休養を優先するよう分岐した。
検証：毎月練習し続けた場合と30歳で練習をやめた場合の20年後の平均能力差は**1.7**（目標「10以内」
達成）。年推移も22歳→30歳で50→61まで滑らかに上昇し、以降38歳にかけて61→45へ緩やかに下降する
カーブになり、旧実装の「全能力が下限まで崩壊する」break的挙動は解消された。

**B（練習にコストを作る）** — `fatMul=max(0.4, 1-max(0,疲労-50)*0.012)`を追加し、train・専門トレの
`gain`/`base`双方に適用（疲労50以下は無影響、100で最大60%減）。通常練習の疲労効果を
「-15（回復）」から「+10×(ガラスの体なら1.3倍)（消費）」へ反転。`mlGrowthCap`の時間成分を
「年数×2・最大+20」から「年数×1・最大+8」へ縮小し、`mlAchievementBonus`の重みを引き上げ
（rungs×3→4、majors×4→5、winsBonus上限10→16）て上限を実績側へ寄せた。配分（0.9/0.14）は不変。
検証：合宿ローテのみで最低能力が上限に達する割合は年7で**85%**（旧99%から改善）。休養を挟まず
合宿だけ回すより、4ヶ月に1回休養を挟む方が7年後の平均能力で上回った（83.9→85.0）。
`mlGrowthCap`は実績（勝利30・タイトル3）の有無で年1時点から+42の差が付くようになった。

**C（年次インフレの廃止）** — `buildMyLifeSim`の`power`式から`yearBonus`項を削除。`aiCap`
（易92/普96/難102/鬼112）は「能力100超のプレイヤーにAIが頭打ちで無双できる」問題への対処で
`yearBonus`とは無関係のため変更していない。調査の結果、AIチームは**既に**`sharedWorldRosters()`
経由で実在の永続ロースターから出走しており、匿名生成は`worldRosters`が無い防御的フォールバック
のみだった＝実質的な変更は`yearBonus`の削除だけで済んだ。検証：`buildMyLifeSim`を8つのworldSeedで
平均し年1〜24のAIエントラント能力を測定したところ、平均82〜86・最大92〜97・上位10%が90〜95の
範囲で**単調な増減も極端な崩れも無い**（旧実装は年5〜10がピークで年15以降易化する非単調カーブ
だった）。本番コード経路（`buildMyLifeSim`→`simulateTicks`→`rankSim`）での勝率測定でも
能力85/95/105間で自然な序列が保たれ、退行は確認されなかった（フィールド規模が小さくノイズは
残るため、体感の最終調整は実プレイのフィードバックに委ねる）。

**共通検証**：`npm run build`成功。Playwrightで新規デビュー→複数レース→年度またぎを自動操作し
6年目まで到達、NaN・`undefined`リーク・コンソールエラーはいずれも0件。

**D（成長上限140の頭）＝保留**：B-3で時間成分を縮小し実績成分を強化した結果、無実績プレイヤーが
140に張り付く年数は後ろに伸びた（無実績なら年11時点で上限98、旧実装は110）。Cの結果AIの地力が
年次で単調に伸び続ける仕組みではなくなったため「相手はどこまで伸びるか」という当初の判断材料が
無くなった。140を動かす明確な根拠が今は無く、実プレイで終盤の頭打ち感が指摘されたら着手する。

---

## 39. 第8弾（レース作戦・判断カードのバランス）— Phase 2(A案)完了・Phase 3以降は継続

**状態**：Phase 0〜2完了（実装・検証・commit/push済み）。Phase 3（判断カード行列の実測）はこれから。

**発端**：「レース作戦って生きてる？判断カードも含めたバランスを調整したい。マイライフでは相手に対する
ステータス優位がある状態／ない状態など様々な状態が考えられるので、それぞれシミュレーションして
いいバランスに収めたい」（2026-08・ユーザー）。

### 実測1：作戦（ML_TACTICS）は生きている

年8・PRO・丘陵G3・ノーマル、フィールドは平均83.4／上位10%=92.3／最強94.3。各セル60レース。
**勝率**（カッコ内は平均順位）：

| 能力 | balanced | wait | early(逃げ) | aggressive | assist |
|---|---|---|---|---|---|
| 85 | 0%(12.8) | 0%(12.6) | **7%**(12.7) | 0%(14.1) | 0%(18.5) |
| 95 | 7%(7.0) | 3%(6.2) | **30%**(15.1) | 13%(5.8) | 0%(14.9) |
| 110 | 40%(3.8) | 35%(4.3) | 33%(20.6) | **43%**(4.6) | 2%(12.9) |

**設計として良い形になっている**：`early`は勝率が高いのに平均順位は最悪（85→12.7／95→15.1／
110→20.6）＝**格下の一発逆転の手段で、格上が使うと損をする**。`aggressive`は格上の手（110で最高43%）、
`balanced`/`wait`は安全策。**この4作戦の関係は調整不要と判断した。**

### 見つかった問題3つ

1. **`assist`（アシストに徹する）が全区分で無意味**。自分の順位を捨てるのに**チーム最上位の順位すら
   悪化する**（能力85：balanced 5.6→assist 6.8／能力110：1.8→3.2）。献身の道（アンビション）の
   中核メカニクスが数字上まったく報われていない。→ **第10弾(#31 味方との協力の実感)へ統合**。
2. **能力が105を超えると勝率が頭打ち**。balancedで105=14%／110=13%／115=19%／120=14%、平均順位も
   6位台から動かない。最強AI(94.3)より25点上でも勝てない＝**育成の到達点が結果に変換されていない**。
   → **今回のスコープに含める（ユーザー判断）。**
3. **互角帯で作戦の差が能力差より大きい**。能力95で`early`は30%・`balanced`は7%＝4倍差。一方
   `balanced`のまま95→110に上げても7%→40%。「作戦を1つ変える」が「能力を15点上げる」と同等以上。

### 訂正の記録

調査途中で「75%が逃げ切り決着」と報告したが**誤り**。`winner.role === "breakaway"`（逃げ役に
指名されたか）を数えており、実際に独走したかを見ていなかった。**実測し直すと独走勝ち（勝者が2位に
5秒以上）は5%**で、決着は勝者から5秒以内に平均6.6人が入る**先頭集団のスプリント**。
教訓：ロール名のフラグを「結果」の指標として使わない。着差で判定すること。

### 決定事項（ユーザー合意済み）

- **強弱の区分は「フィールド上位10%の地力」との差で定義する**（絶対値ではなく相対。年ごとに
  フィールドが動くため）：**格上＝−5以下／互角＝±5以内／格下＝+10以上**。
  年8・PRO換算では能力87以下／88〜97／102以上。素の勝率（balanced）はそれぞれ0%／5〜15%／35〜45%。
  調整の狙いは、格上＝良い判断で表彰台が見える、互角＝判断が勝敗を分ける、格下＝判断ミスで取りこぼす。
- **上記2（能力の頭打ち）を今回のスコープに含める。** カードより先に土台を直す（土台が動くと
  カード側の較正がやり直しになるため）。
- 実装順序を改訂（下記「実装順序」参照）：**バランス調整を第8弾に繰り上げ、#33（判断カードの演出）を
  第9弾へ**。理由は両者が`RaceView.jsx`の同じ判断カードUIを対象にしており、#33の設計要点
  「`legsLeft01()`から"強い判断"を算出して演出強度に接続する」がバランス調整で変わるため。
  **中身を決めてから見た目を作る。**

### Phase 2：能力頭打ちの原因調査 → A案の実装（完了）

**原因**：`sim/race.js`の`canPull()`は`if (en.isAce) return false`でエースが絶対に牽引せず、
ドラフト中の選手は`dist = groupDist`（牽引者の進んだ距離）でしか動かない。実測でプレイヤー
（エース）のtick別モード内訳を取ったところ、balanced作戦では**能力に関わらずレースの97〜100%が
draft（集団と完全同速）**で、能力が速度に変換されるのは千切れ判定・最終区間・ゴール決着の数tickに
限られていた。これが「能力105超で勝率が頭打ち」の正体（②③は同じ問題の裏表だった）。

**A案（ユーザー選定）で実装**：`sim/race.js`。
1. `en.slot`（集団内の前後位置）の意味を「牽引ローテ待ち順」から「脚力・残脚・判断カードで
   決まる前後位置」へ作り直した。draft中の選手を`ownCapable/groupDist`（地形適性）＋
   判断カードの意思（`conserveLeft`＝後方へ譲る／`holdOn`＝位置を死守）で毎tickランク付けし、
   trainZone/frontZoneで既に前方固定された選手はそのまま最前列を占有させる。
2. 千切れ判定`keepThresh`に位置依存の項`positionTight = posShare * POSITION_TIGHT_SPAN`を追加
   （`posShare`＝0が先頭寄り〜1が最後方）。前方ほど千切れにくく、後方ほど千切れやすい
   （実際の集団分裂が前から割れずに後方から千切れていく現象を再現）。
3. UIの「次に牽引」表示が旧slotの意味（ローテ待ち順）に依存していたため、`en.nextPuller`
   （新規フィールド・`nextPullerHist`で記録）へ分離し、`RaceView.jsx`の3箇所を差し替えた
   （`nextPullerAt()`を新設）。`en.slot`は前後位置のレンダリング用途（`biasX`）・
   `backRatio`（エネルギー回復）にそのまま使い続ける。

**較正**：`POSITION_TIGHT_SPAN`は約1300tick/レースにわたり毎tick効くため複利的に増幅される。
0.05→効きすぎ（能力90でも勝率18%）→0.015に較正。年8・PRO・丘陵G3・ノーマル、6seed×35試行
（計210試行/セル）での最終結果：

| 能力 | 旧実装の勝率 | 新実装(0.015)の勝率 |
|---|---|---|
| 85 | 0% | 0% |
| 90 | 0% | 2% |
| 95 | 1% | 10% |
| 105 | 14% | 30% |
| 110 | 13% | 25% |
| 115 | 19% | 48% |
| 120 | 14% | 46% |

105超の頭打ちは解消（105→120で概ね単調増加）。85〜95（格上〜互角境界）も決定事項の目標区分
（格上0%／互角5〜15%）にほぼ合致。100〜110間に多少のノイズが残るが、210試行でも傾向は
安定しており誤差の範囲と判断した。

**検証**：`npm run build`成功。Playwrightで新規デビュー→複数年（4年目まで到達）自動プレイし、
エラー0件・NaN/undefinedリーク0件を確認（devサーバ稼働中にソースを編集し続けたため一度
「createRoot二重呼び出し」警告が出たが、devサーバを再起動してクリーンな状態で再現しないことを
確認した＝HMR特有のテスト時アーティファクトで、本番ビルドには存在しない）。
`sim/race.js`のRACE_MOVES/simulateTicksは両モード共通のため、この変更はシーズンモードの
レースにも及ぶ（意図どおり）。

### この先の段取り

Phase 3（判断カード行列の実測：3区分×発火点3枚×各カード。1レースが3〜4連続の意思決定なので
手順の組み合わせも見る）→ Phase 4（設計提示・合意）→ Phase 5（実装・検証・DEVLOG）。
Phase 1（計測ハーネス）はPhase 2の実測で`buildMyLifeSim`を直接呼ぶ形が機能したため、
Phase 3もこれを土台に拡張する（判断カードは`resumeSim`経由の発火が必要なため追加実装が要る）。

---

## 次のアクション（ユーザーメモ・未着手）

以下はユーザーから明示的に指示された、今後着手すべき項目のメモ。優先度・着手順は未確定。

**★最優先：UIテキスト・ビジュアルの作り込み（2026-08指摘・CLAUDE.md §7に明文化済みの恒常タスク）**
9. **ゲーム内の無駄なテキストを削り、「ゲームとして良いUI」に作り込む。**
   第1波（§22）・第2波（§23）・重複統合（§26）・ジャーゴン一掃（§30）・8並列監査による
   横断80件超の修正（§31）を実施済み。ヘルプ本文／`OVR`略語／スポンサー行は「削らなくてよい」と
   ユーザー判断済み。**終わりのない恒常タスク。判断基準はCLAUDE.md §7が正**（新規UIのたびに
   (a)その情報は要るか (b)開発語彙を見せていないか (c)小さい文字の長文で誤魔化していないか を自問）。

10. ✅ **チュートリアル的な導入説明の追加＋スタート画面の整理**（完了・§29参照）。

11. **スプライト（スプリント／ダンシング）モデルの描き直し**（2026-08・凍結中）。
   「スプリントしている感がない」との指摘。**試した3方式は全て失敗＝再挑戦しないこと**：
   (a) AIへの新規描画発注（形が崩れる）、(b) 行の挿入・削除で姿勢変更（選手と自転車が同じ行で
   絡み合い原理的に不可能）、(c) レイヤー分離＋手続き的な腕脚描画（見るに耐えない）。
   **再開条件**：dancing/sprint 6コマ分の新しい参考画像をユーザーに用意してもらい、
   CLAUDE.md §6の手順で抽出する。検査ツール`tools/bike_sprites.py`のみ現存。

12. ✅ 最終スプリント演出のマーカー位置（完了・§20参照）。
13. ✅ 最終スプリントで、ゴールしてしばらく経ってから通常姿勢(normal)に戻る（完了・§36-A参照）。
14. ✅ シーズンモードの「引き抜き」「移籍志願」系イベントを開幕1〜2年目は発生させない
   （完了・§34参照）。
15. ✅ **導線（ナビゲーション）とユーザビリティ全般の作り込み**（完了・§24参照）。
16. ✅ **能力上限の収束問題（極めると全員同じステータスになる）**（完了・§28参照）。
17. ✅ クリアポイント関連の導線・文言整理＋横断ジャーゴン一掃（完了・§30参照）。
18. **クリアポイント交換所の内容拡充**（2026-08指摘・未着手）。「内容を拡充したい」との
   要望のみで具体的な方向性は未確認（既存3カテゴリ〈シーズン/マイライフ/特別〉の
   ラインナップ増強か、新カテゴリの追加か等）。バランス判断を伴うため、着手前に
   CLAUDE.md §1に従いユーザーへ方向性を確認し設計図を提示すること。

**★2026-08 追加分（レース演出のバグ3件・情報開示3件・バランス1件）**

19. ✅ 最終スプリント画面で順位表が全員TOPになる（完了・§32参照）。
20. ✅ ゴールラインが菱形で線に見えない（完了・§32参照）。
21. ✅ ゴール時のカメラがガクガクする（完了・§32参照）。

22. ✅ レース観戦画面の俯瞰カメラのズームをもっとゆっくりにする（完了・§34参照）。

22b. **拠点画面（BaseView）のズームボタンも未補間のまま残置**（2026-08発見・未着手）。
   `hooks/useIsoCamera.js`の`zoomBy: (mul) => applyZoom((k || 1) * mul, ...)`は
   ボタン1タップで倍率を一段階いきなり適用しており補間が無い。22番の誤調査で見つかった
   別問題。22番とは別スコープなので、着手する場合は改めて設計を提示してから。

23. ✅ 敵チームの永続ロースター定員拡張＋摩耗補充＋出走人数の下限引き上げ（完了・§33参照）。

24. **敵チーム・味方チームの選手の能力を見られるようにしたい**（2026-08指摘・未着手）。
   マイライフ・シーズン両モード。現状、相手選手の能力は出走表の「下馬評」印と
   OVR推定でしか窺えない。23番（ロースターの永続化）は完了済み（§33参照）で前提が整った。
   23番の結論が出てから設計するのが安全。どこまで開示するか（全能力を常時公開か、
   スカウトLv等で段階的にか）はゲームバランスに直結するので、CLAUDE.md §1に従い
   着手前に設計図を出してユーザー合意を取ること。

25. **全チーム成績や名鑑を充実させ、リーグ全体の流れを把握できるようにしたい**
   （2026-08指摘・未着手／2026-08に主旨を拡張）。24番と同じく「世界の解像度を上げる」系の
   要望で、23番の永続ロースター（完了・§33参照）が土台になる。
   **拡張された主旨**：「現時点だとリーグ全体の動きや強い選手・チームなど、ゲーム全体の流れを
   ユーザーが把握しきれないのが面白さを損ねている」との指摘。単なる名鑑の充実ではなく、
   **「今シーズンのリーグで何が起きているか」が読み取れる状態にすること**が目的。
   具体的な観点：今の最強チーム／最強選手は誰か、順位争いはどうなっているか、
   誰が伸びていて誰が落ちているか、自分はその中のどこに位置するか。
   既存の`rivalStats`・殿堂名鑑・世界ランキング画面との役割分担を整理してから着手する。
   情報量が多くなる領域なのでCLAUDE.md §7（figureで伝える／長文で説明しない）が特に重要。

26. ✅ マイライフのキャリアグラフに数字を追加（完了・§34参照）。

27. ✅ **【バランス】早駆けが常時最適解だった問題**（完了・§35参照）。残脚ゲートの実装と
   残脚の可視化で「脚が残っていれば決まる／空なら不発」の駆け引きに戻した。
   **残課題**：「食らいついて粘る」(hangOn)は依然ほぼholdと同値。千切れ寸前の局面でしか
   効かない設計なので、その局面を作る／効果を上げるかは別途検討。→ **38番（第8弾）で扱う。**

**★2026-08 追加分（第2次・レース観戦の質6件）**

※このとき挙がった9件のうち、「特殊配合をDQM的にしたい」は下記「配合・育成の深掘り」7番へ、
「リーグ全体の流れが把握できない」は上記25番へ統合した（重複のため新規採番せず）。
「ゴールラインを過ぎてからスプリント解除」は13番の再オープンとして扱う。

28. ✅ ゴール後に順位が変動しないようにする（完了・§36-A参照）。

29. ✅ ドット絵スプライトの描画が重い（完了・§37参照）。当初「人数が多いと重い」として
   登録したが実測で前提が誤りと判明、真因はジャージ色数と`pixelSprite()`の1ピクセル=1`<rect>`
   だった。canvasラスタライズ＋`<image>`化で実機60fpsを達成（改修前は色数12で2.3fps）。

30. ✅ 俯瞰マップで、メイン集団から脱落した選手をカメラの枠に入れない（完了・§36-B参照）。

31. **レース中に味方と協力できている実感がない**（2026-08指摘・未着手）。俯瞰カメラでも
   ゴールスプリントでも、僚友と協力している感触がまったく無いとの指摘。
   「俯瞰カメラのシミュレーションを変えてもいい」＝**見た目の演出だけでなく、
   隊列・牽引・風除けのシミュレーション自体に手を入れてよい**という許可が出ている。
   既存の素材：`role`（lead/sub/mountain等）、`isLeadingOut`／`leadoutSurging`（リードアウト）、
   `slingshotAceIds`（射出されたエースの伸び）、`teamShelter`/`teamChase`/`assistLaunch`
   （監督指示）は既に実装済みだが、**画面上でそれと分かる形になっていない**のが本質。
   大きめの改修になるためCLAUDE.md §1に従い設計図を出してから着手する。
   マイライフの僚友システム（下記8番のPhase 3）と接続する余地あり。
   **2026-08・§39で数値的な正体が判明**：見た目だけの問題ではなく、`assist`作戦が全区分で
   実際に無意味だった（自分の順位を捨ててもチーム最上位の順位が改善せず、むしろ悪化する。
   能力85で5.6→6.8／能力110で1.8→3.2）。**assist作戦の是正をこの項目に統合し、第10弾で扱う。**

32. ✅ 俯瞰カメラで楕円軌道状の規則的な動きが見えてしまう（完了・§36-C参照）。

33. **レース中の判断にカードゲームのような演出が欲しい**（2026-08指摘・未着手）。
   「強い判断はキラカードみたいなイメージ」。現在の判断カードは通常のボタンの縦並びで、
   選択の重みが視覚的に伝わっていない。§35で「脚の残り」ゲージを追加した場所と同じ
   `components/RaceView.jsx`の判断カードUIが対象。
   **設計の要点**：何をもって「強い判断」とするか（§35で入れた`legsLeft01()`＝残脚から
   その一手が決まりやすいかを算出できるので、これを演出の強度に接続できる）。
   自己完結した見た目の改修で、他の項目と依存が無い。

**★2026-08 追加分（第3次）**

34. ✅ 個人TTで俯瞰カメラも選手も動かない（倍速が効かない）（完了・§36-B-2参照）。

35. **【実装済み・2026-08】マイライフの成長設計**（→§38参照）。当初は「毎年インフレする上限を
   前提にした設計になっていない」という指摘だったが、実測の結果**4つの問題**に分解された：
   ①**練習にコストが無い**（疲労はレース当日にしか効かず、通常練習は疲労を減らす＝根本原因）
   ②専門トレ3種が5能力を完全被覆し、走らず合宿を回すだけで全能力がカンストする
   ③**加齢衰退が「練習した月だけ」適用されるバグ**（シーズン側は正しい形なのでマイライフだけの例外）
   ④年次インフレが`ageWorldRosters`の世代交代モデルと二重実装で、`aiCap`に吸収され自チームの
   僚友も強化してしまう。A（衰退の是正）・B（練習にコストを作る）・C（年次インフレ廃止・
   世界ロースターへ一本化）を実装・検証済み。D（成長上限140の頭）は明確な根拠が無いため保留。

36. **【要バランス検証】simの決着ジッターにも同じ規則性が残っている**（2026-08・§36の調査で発見）。
   `sim/race.js`の最終区間の着差計算にある`riderHash01(en.id, tick+4409)`は、tickごとにsaltが
   +1されるため**毎tick +0.40503の規則的な鋸波**になっており、乱数として機能していない。
   §36-Cでは「着順が変わる」ためsim側は意図的に据え置いた（ユーザー合意）。直す場合は
   着順分布の前後比較を実測してから（#27・#35と同じ手順）。

37. **マイライフの個人TTが集団走行としてシミュレートされている**（2026-08・§36の調査で発見）。
   `buildMyLifeSim`は`groupMode`を`"solo"`にする分岐を持たず、個人TT（出走1名の競技）でも
   `"full"`のまま10名でドラフティングしながら走っている（シーズン側は`groupModeFor(squadN)`で
   正しく`"solo"`になる）。§36で観戦を廃止するため見た目には露出しなくなるが、
   **結果（着順・タイム）は集団走行前提のまま**なので、TTらしい決着になっていない。
   直すと着順が変わるためバランス確認込みで別途扱う。

38. **【バランス】レース作戦・判断カードのバランス調整**（2026-08指摘・**第8弾として着手中**・§39参照）。
   「レース作戦って生きてる？判断カードも含めたバランスを調整したい。マイライフでは相手に対する
   ステータス優位がある状態／ない状態など様々な状態が考えられるので、それぞれシミュレーションして
   いいバランスに収めたい」との要望。実測の結果、**作戦4種（balanced/wait/early/aggressive）の
   関係は既に良好**（`early`＝格下の一発逆転、`aggressive`＝格上の手）で調整不要と判断した。
   今回扱うのは①**能力が105を超えると勝率が頭打ち**になる問題（土台なのでカードより先に直す）
   ②判断カード各手の強弱（27番の残課題`hangOn`＝実質`hold`と同値を含む）。
   強弱の区分は**フィールド上位10%の地力との差**で定義（格上−5以下／互角±5以内／格下+10以上）。
   `assist`作戦の是正は31番（第10弾）へ送った。

**実装順序（2026-08時点・上から着手する）**

- ✅ **第5弾：レース観戦の不整合・違和感の一掃**（完了・§36参照）— 28番＋13番＋34番＋30番＋32番。
- ✅ **第6弾：ドット絵スプライトの描画コスト**（完了・§37参照）— 29番。実機60fpsを達成。
- ✅ **第7弾：35番（マイライフの成長設計）**（完了・§38参照）— A(衰退の是正)・B(練習にコストを作る)・
  C(年次インフレ廃止・世界ロースターへ一本化)を実装・検証済み。D(成長上限140の頭)は根拠不足のため保留、
  実プレイで終盤の頭打ち感が指摘されたら着手する。
- **第8弾（今ここ・§39）：38番（レース作戦・判断カードのバランス調整）** — 2026-08に新規追加。
  作戦4種の関係は良好と実測で確認済み。**能力の頭打ち（105超で勝率が伸びない）を含む**。
- **第9弾：33番（判断カードの演出）** — 第8弾から降格。両者が同じ判断カードUIを対象にしており、
  #33の「`legsLeft01()`から強い判断を算出して演出強度に接続する」がバランス調整で変わるため、
  **中身（バランス）を確定させてから見た目（演出）を作る**。
- **第10弾：31番（味方との協力の実感）＋assist作戦の是正** — シミュレーション本体に手を入れる大物。
  §39でassistが全区分で無意味（自分の順位を捨ててもチーム順位が悪化する）と判明し、
  「協力の実感が無い」の数値的な正体が取れたため統合する。要設計。
- **第11弾：25番（リーグ全体の流れの可視化）→ 24番（能力開示）** — 「世界の解像度」系。
  2件は表裏なのでまとめて設計する。
- **以降：** 7番（特殊配合）、18番（CP交換所の拡充）、4番（拠点のドット絵化）、
  11番（スプライト描き直し・凍結中）、36番（simジッターの規則性・要バランス検証）、
  37番（マイライフ個人TTの集団シミュレーション・要バランス検証）。

**UI/メニュー構造**
1. ✅ メニュー画面で「ホームに戻る」を大分類の即時選択項目に格上げ（完了・§9末尾参照）。
2. ✅ 大分類の中の小分類を17小分類へ細分化（完了・§9末尾「Step13第7弾」参照）。

**拠点(BaseView)の部屋の見た目強化**
5. ✅ 部屋のグレードアップ（完了・§14参照）。
6. ✅ トレーニング室の「ぼっ立ち」修正（完了・§15参照）。**メカニック室は今回スコープ外**
   （作業台にかがむ等の新ポーズが必要で`PixelPerson`のスプライト追加を伴うため、下記4番の
   ドット絵化ウェーブと一緒に検討する）。

**ドット絵化の拡張**
3. ✅ レース最終区間のスプリント演出にドット絵を適用（完了・§16参照）。
4. **拠点(BaseView)のオブジェクト類（什器・プロップ・建物など）もドット絵化する**（未着手）。
   ユーザー提示の分類（A〜Fの計41項目）：
   - A. 人物共通モデル(Person.jsx)：①立ち/歩行 ②座り
   - B. 自転車に乗った選手(IsoRider.jsx、3姿勢)：③通常 ④ダンシング ⑤スプリント
   - C. 各部屋の主要什器・常時表示：⑥ローラー台 ⑦作業台 ⑧診察ベッド ⑨スカウトデスク ⑩空き部屋什器
   - D. 各部屋の二次什器（18種、施設Lvで段階解禁）
   - E. 屋外プロップ（10種）
   - F. 建物・地面・コース

**配合・育成の深掘り**
7. **配合面の深掘り**（未着手）。血統表の可視化（家系図）、特殊配合の実装（ドラクエ
   モンスターズ的な組み合わせ配合）。§5「特殊配合の深化」と統合して検討する。
   **2026-08に方向性が具体化**：「例えばドラクエモンスターズで**適切な4体を適切な順番で**
   配合すると、新しく**超強力な一体**が生まれるのが面白い。これを活かしたい」との指摘。
   ＝単なる能力の足し算ではなく、**特定の組み合わせ＋順序が隠しレシピになっていて、
   到達すると質的に別物の1体が生まれる**という設計。着手時の論点：レシピを何個用意するか、
   発見の導線（完全に隠すのか、ヒントを出すのか）、生まれる個体の強さの上限と
   既存バランス（突破力・成長上限）との整合。CLAUDE.md §1に従い設計図を出してから実装する。
8. ✅ マイライフの難易度調整。Phase 1・Phase 2完了（§17・§18参照）。
   **保留（判断③）**：脚質ごとの能力別成長上限差は今回見送り。育成上限の実績連動が定着
   してから改めて検討。
   **保留（判断⑤）**：資金の使い道の拡充・野心ルートの上位段追加は今回スコープ外。
   **保留（判断⑰）**：新ステータス（突破力・安定感、将来的にスピリット・運も）を配合の
   遺伝要素にする案は、上記7番「配合面の深掘り」のフェーズで接続する。
   **Phase 3（次回以降・メモのみ）**：マイライフに自チームの僚友システムを新設し、新
   ステータス「スピリット」（チームメイトの育ちやすさ・チームケミストリー）を接続。
   **✅ イベント本文の継続拡充（恒常タスク）**：「数百種類」は長期目標。既存5分類
   （`bad`/`weight`/`outcomes`/`abKeyDelta`/`breakthroughDelta`/`stabilityDelta`/
   `luckDelta`/`growthPowBump`/`talentCapDelta`）のタグ付け方針をそのまま踏襲し、
   `data/events.js`のML_EVENTSへ追記していく。
