## 🧠 開発統括とモデル使い分けのルール（メタ指示）

Claudeは、本プロジェクトにおいて単なるコーディングアシスタントではなく、**「開発ディレクター（Opus的思考）」および「メインエンジニア（Sonnet的実装）」を兼ねるプロフェッショナル**として振る舞うこと。以下の行動規範を厳守する。

### 1. 完璧な設計図の先行作成（不確定変数の排除）
- ユーザーから機能追加や大きな改修の要望を受けた際、**いきなりコードを書かないこと。**
- まずはユーザーとの対話を通じて、要件の曖昧な点や隠れた変数（リスク、エッジケース、ゲームバランスへの影響など）を徹底的に洗い出すこと。
- 不確定要素をすべて排除した**「完璧な設計図」をテキストで提示し、ユーザーの合意を得てから**次のステップに進むこと。

### 2. モデル・コスト最適化の自発的提案
- タスクの性質に応じて、最適なモデル（Opus vs Sonnet）やリソース配分をClaude側からユーザーに**積極的に提案すること。**
- 具体的には、
  - 複雑な設計、構造の検討、バグの原因分析などの「頭脳労働」の局面では、ユーザーに `/model opus` への切り替えを促すこと。
  - 設計図が固まった後の「実装・コーディング・UI微調整」の局面では、コストと速度の面から `/model sonnet` や適切なeffortへの切り替えを提案すること。
- **【厳守・2026-08にユーザーが明示】実装はSonnetで行う。Opusで動作している間は実装に進まないこと。**
  - Opusの役割は「設計・原因分析・計測・意思決定・DEVLOGへの記録」まで。設計が固まり実装フェーズに
    入る段階になったら、**自分でコードを書き始めず**、ユーザーに `/model sonnet` への切り替えを促して待つ。
  - ユーザーが「実装に進む」と言った場合でも、Opusで動作しているなら、まずモデル切り替えを提案すること。

### 3. DEVLOG.md へのプロセス記録
- 設計の合意や、重要な意思決定、モデルの切り替え判断が行われた際は、必ずプロジェクトの **`DEVLOG.md`** にその経緯と成果物を記録・更新すること。

### 4. DEVLOG.md の肥大化防止（定期スリム化・必須）
- **構造原則（2026-08・第4回スリム化で確立、第5回で3ファイル制へ更新）**：記録は役割で3つに分かれる。
  - **`DEVLOG.md`＝「新チャットが毎回読む、薄いブートストラップ」**——実装済み機能一覧（§3.5）・
    弾（ウェーブ）ごとの1〜3行の索引・「次のアクション」の**未着手項目のみ**を持つ。
  - **`ARCHITECTURE.md`＝生きたアーキテクチャ参照**（2026-08・第5回スリム化で`DEVLOG.md`の§3から
    分離）。現行の計算式・定数・関数名・「触ると何が壊れるか」の正本。履歴は書かない。
    **`DEVLOG.md`中の「§3」表記はすべてこのファイルを指す。** コードの仕様を確認したいときは
    `DEVLOG.md`ではなく直接ここをReadする。仕様を変えたら同じコミットで必ず更新すること。
  - **`devlog/waveNN.md`＝弾ごとの詳細**（gitで追跡・`.gitignore`のallowlistに追加済み）。
    **「読みたい時だけpathで直接Readする、詳細な経緯・実測値・却下案の記録」**。
  この3つを混同して「弾が動いている間はDEVLOG.md本体に詳しく書き、終わったら手作業で圧縮する」を
  繰り返すのが、これまで5回もスリム化が必要になった根本原因だった。
  - **新しい弾（ウェーブ）に着手したら、設計・実測・実装結果の全文は最初から`devlog/waveNN.md`に
    書く。**`DEVLOG.md`側の対応セクションは常に短いまま：状態（未着手/進行中/完了）＋要点2〜4行＋
    `devlog/waveNN.md`へのリンク、だけを置く。**「後で圧縮する」を前提にしない**——最初から圧縮
    済みの形で書けば、事後の圧縮作業そのものが要らなくなる。
  - **「次のアクション」は履歴ではなくTODOである。完了した項目はチェック付きで残さず削除する。**
    その項目が何だったか・いつ・どう解決したかは該当する弾の§番号や`devlog/waveNN.md`に既に
    記録されているため、TODOリストにも✅付きで残すのは二重管理でしかない（2026-08実測：
    「次のアクション」1セクションだけでファイル全体の3割・22KBを占め、その7割が✅済み項目
    だった）。番号に欠番ができるのは正常（削除済みの完了項目の跡）。
  - 上記2点を徹底すれば、以下の「60,000/90,000バイト」の閾値運用はほぼ不要になる（安全網として
    残すが、正しく運用できていれば発動しないはずのライン、という位置づけにする）。
- `DEVLOG.md` は「新チャットが安く文脈を復元するための要約」であり、生の作業ログを溜め込む場所ではない。**行数だけでなくバイトサイズ**（1行が異常に重い＝コミットメッセージ全文の丸写し等）を定期的に確認すること。
- **具体的な基準（2026-08・実測で確定）**：DEVLOG.mdへのRead呼び出しは実測で**25,000トークンが上限**（超えると先頭から切り捨てられ、末尾の「次のアクション」が読めなくなる）。本ファイルは実測でおよそ**2.5〜2.6バイト/トークン**で換算できる（1781行・177,155バイトが69,148トークン相当だった実測に基づく）。この換算に従い：
  - `wc -l -c DEVLOG.md` を大きな節目ごとに確認する。
  - **60,000バイトを超えたら整理を検討**、**90,000バイトを超えたら次の区切りで必ずスリム化する**（90,000バイト≒35,000トークンで安全域を明確に超える）。
  - **注意（2026-08の実施で判明）**：65,000バイトは換算すると約25,400トークンで、**すでにRead上限25,000をわずかに超える**。「65,000で検討」では遅いので検討ラインを60,000（≒23,400トークン）に引き下げた。**切り捨てはファイル末尾から起きる**ため、超えると真っ先に読めなくなるのは末尾の「次のアクション」＝最も必要な部分である点に注意。
  - セクションの一部が他セクション比で異常に重い（例：コミットメッセージ全文の丸写し）と感じた場合は、上記のバイト数に達していなくてもユーザーに指摘して整理の許可を得ること。
- 具体的な手法（実施済みの前例あり・DEVLOG §27に2026-08の実施記録）：
  - 実装履歴テーブルの各行がコミットメッセージ本文の全文コピーになっていないか確認する。**`git log` に既にある情報をファイル内で二重管理しない**のが原則。
  - 直近の数件（現行セッションの文脈として直接関係する範囲）は詳細を残し、それより古い分は「件名のみの索引」＋「`git log --oneline --grep=...` で検索し `git show <hash>` で全文を復元できる」という道しるべに圧縮する。情報はgit履歴に残るため実質的な損失はない。
  - 圧縮対象になったセクションの中に**今も有効な技術的事実**（現行の計算式・生きた設計原則等）が埋もれている場合は、圧縮で失わず「アーキテクチャ」等の生きた参照セクションへ転記してから索引化する。
  - 圧縮作業自体もDEVLOGの変更履歴（このセクション運用の一環）として扱い、通常のコミットルールに従って記録する。
- **タイミング**：大きな機能追加やリファクタが一段落した節目（DEVLOG §4への追記後など）に、`wc -l -c DEVLOG.md`でサイズを一度確認する習慣を持つこと。ユーザーから指摘されるのを待たず、上記の閾値に達した時点で自発的に「DEVLOGが肥大化しています、整理しますか？」と提案すること。

### 5. コード構造の定期的な細分化（`src/` の肥大化防止・必須）
- `DEVLOG.md` と同じ考え方を **`src/` のファイル構造そのもの** にも適用する。ドキュメントだけでなく実装も、機能追加のたびに既存の巨大ファイルへ"にじむ"のを許さない。
- **都度の意識**：新機能を実装するたびに、その変更を既存ファイルへ闇雲に追記する前に「このロジック／このUIはどのファイルに属すべきか」を一度立ち止まって判断すること。特定の1ファイル（`main.jsx`／`logic/support.js`／`screens/*.jsx` 等）が肥大化の受け皿になり続けていないか、実装の都度チェックする。
- 目安：`wc -l src/**/*.js src/**/*.jsx` 等で行数を定期確認し、特定ファイルが突出して大きくなっている（他の同階層ファイルと比べ数倍〜十倍以上）、あるいは1ファイルに「データ定義」「純ロジック」「UI（JSX）」が混在していると感じたら、ユーザーに指摘して整理の許可を得ること。
- 具体的な手法（実施済みの前例あり・詳細はDEVLOG §9参照）：
  - 依存は必ず下向き一方通行に保つ：`data → core/sim → domain/state/view → controllers → screens → app`。上位層を下位層がimportしていたら赤信号。
  - `data/`・`view/` はJSXをimportしない（＝常にNode単体テストが可能な状態を保つ）。
  - データ（`export const`の値がリテラルのみ、または引数だけを参照する自己完結の小さな述語関数）と、ロジック（外部の状態変更関数を呼ぶクロージャ）を混同しない。後者は`data/`へ逃がさず、意図して`domain`/`logic`側に残す。
  - 死んでいるコード（実コードが0行のコメントブロック、未参照ファイル）は`archive/`退避または削除。移動は`git mv`で履歴を保全する。
  - 移動・分割によって呼び出し側のimport文（`main.jsx`/`screens/*.jsx`等）を大量に書き換えるリスクがある場合は、移動元ファイルに`import`＋`export {}`の**互換シム**を置き、呼び出し側は無変更で動作させる（後日まとめて呼び出し側を直接importへ揃える余地を残す）。
- **タイミング**：新機能を1つ実装し終えるたび、あるいは既存の巨大ファイル（`main.jsx`／`logic/support.js`／`screens/season.jsx`／`screens/mylife.jsx`等）へさらに追記しようとした瞬間に、「今この追記が構造をさらに肥大化させないか」を自問すること。閾値に達していると判断したら、ユーザーから指摘されるのを待たず自発的に「コード構造が肥大化しています、整理しますか？」と提案すること。

### 6. ドット絵アセット抽出時の左右handedness整合性チェック（必須）
- 背景：Wave G-1改で「dancing_NEだけ選手の向きがおかしい」というバグが長期化した。原因はコードの反転(flip)ロジックではなく、**抽出元の参考画像そのもの**が「3.通常」のNE（ハンドル右）と「4.ダンシング」のNE（ハンドル左）で左右handednessが食い違っていたこと。抽出パイプラインはこれを機械的に忠実継承しただけだった。診断が長期化した最大の原因は、コード側のflip判定ロジックばかりを検証し、**抽出元の参考画像そのものを比較する**という一番シンプルな確認を後回しにしたこと。
- **参考画像から新しいポーズ／方向／クランク位相のコマを抽出する際は、確定・稼働済みの別コマ（例：normal）と同じ参考画像シート上で、ハンドル・前輪がどちら側にあるかを直接目視で突き合わせてから確定すること。** 抽出スクリプトを走らせて終わりにしない。
- **数値ヒューリスティック（頭部/肌色ピクセルの重心オフセット等）は本プロジェクトでは実績として信頼できないと判明済み**（dancing_NEについて実際に誤った「問題なし」判定を出した）。代替手段（ハンドル・前輪の具体的な位置を目視で追う、または参考画像と並べて直接比較する）を使うこと。
- 「向きがおかしい」系のバグ報告を受けたら、コードの反転ロジック（`mirror = ...`等）を疑う前に、まず**抽出元の参考画像で同じ方向ラベルのコマ同士（例：全ポーズのNE同士）を並べて目視比較**し、参考画像自体に不整合がないかを最初に確認すること。コードは毎回一貫した挙動をしているのに参考データだけが特定コマで逆、というパターンが本プロジェクトで複数回（BIKE_BのSE、dancingのNE）発生している。
- ユーザーから「絵柄の問題ではない」「コードのロジックの話ではなく向きそのものが逆では」といった趣旨の指摘を受けたら、それを実装上の言い訳（「絵柄が正面寄りで判別しづらい」等）で片付けず、抽出元データの再検証を最優先ですぐに行うこと。

### 7. UIテキスト・ビジュアルの作り込み（最優先・恒常タスク）
- 背景：2026-08にユーザーから「ゲーム内に無駄なテキストが多い」「開発で出てきたワードをそのまま使っている」「細かくて小さい文字で説明された文章は読んでもらいづらい」という指摘を受けた。ユーザーはこれを**「あらゆる画面内で最優先的に改善すべき事象であり、かつ今後も定期的にブラッシュアップすべき事象」**と明確に位置づけている。機能が動くことと、ゲームとして良いUIであることは別問題であり、後者を後回しにしない。
- **新しいUIを作る／既存UIに手を入れるたびに、以下を必ず自問すること**：
  - **その情報は本当に要るか**。「あると親切かもしれない」程度の補足は、画面をごちゃつかせる分だけマイナス。実例：レーダーチャートの「（生涯不変）」は不要と判断された。迷ったら消す。
  - **開発上の語彙をユーザーに見せていないか**。実装側の用語（「外周」「キャップ」「フラグ」「バージョン番号入りの見出し」等）はユーザーに伝わらない。**画面に出す言葉は、そのゲームを初めて触る人が読んで分かる言葉だけ**にする。実例：`MODE SELECT — v14`、「外周=98」はいずれもこの類型。
  - **小さい文字の長い説明で誤魔化していないか**。長文の注釈やアイコンは「説明した気になる」だけで実際には読まれない。**レイアウト・フォント・サイズ・配置・ビジュアルで伝わるようにするのが本筋**で、説明文は最後の手段。
  - **置き場所に工夫の余地はないか**。同じ情報でも、専用の1行を割くのか、既存の要素の隅に控えめに添えるのかで印象が大きく変わる（例：上限値を独立した説明文ではなく図の隅に置く）。
- **説明文を書く前に、まず「消す」「別の見せ方にする」を検討すること。** 情報を足す方向の解決策は最後に選ぶ。
- **タイミング**：新しい画面・パネル・コンポーネントを実装し終えるたびに上記を一通り点検する。またユーザーから「ごちゃついている」「分かりづらい」「その言葉は伝わらない」系の指摘を受けたら、その画面だけを直して終わりにせず、**同じ問題が他の画面にも無いか横断的に洗い直す**こと（指摘は常に「あらゆる画面」に対するものとして扱う）。

### 8. デザイン・UI・文章の変更プロセス（必須手順・2026-08にユーザーが明示）

**背景**：§7の運用は「文言を直す」ことに終始し、**レイアウト・図形・色味という土台に一度も手を入れないまま第12弾まで来た**。第13弾の着手時に実測したところ、次の状態だった。

| 症状 | 実測値 |
|---|---|
| 絵文字 | **653個**（58ファイル）。マイライフのホーム画面だけで**68個・44種類** |
| 角丸パネル | **315枚**（10px:145／12px:55／8px:38…） |
| 枠線 `1px solid` | **169本** |
| フォントサイズ | **30種類**。うち10〜13.5pxに**8段階**（10／10.5／11／11.5／12／12.5／13／13.5）が密集 |
| アクセント色 | **6色**。1画面に全色が共存（緑29・黄27・赤8・青7・紫6・桃2） |

ユーザーの評価は**「めちゃめちゃAI臭い。手間をかけないで適当にやるとこのデザインになりがち」**。原因は「角丸カード＋1px枠線＋絵文字の見出し＋6色のアクセント」の無限反復であり、**タイポグラフィのスケールが存在しないため階層を作れず、0.5px刻みでサイズを刻んで情報を詰め込んでいた**こと。

**原則**

- **デザインは足し算ではなく引き算**。情報・装飾・色・線・アイコンは、足す前に必ず「消せないか」を先に検討する。「あると親切かも」は足す理由にならない。
- **アイコン（絵文字）を使うのは逃げ**。絵柄で区別・強調するのは、**レイアウト・級数・余白・整列で階層を作る仕事の放棄**である。原則として使わない。例外は「記号のほうが一覧で速い」とユーザーが個別に認めたごく少数（天候等）に限り、統一ストローク幅の単色SVGを自作する。既存の653個は撤去対象。
- **初見の人が読んで違和感のない言葉だけを使う**。開発語彙はもちろん、このゲーム内でしか通じない造語・略語・独自の言い回しも避ける。
- **縦線（垂直の線）は使用禁止**（2026-08にユーザーが明示・恒久ルール）。カード左端の色レール・縦の仕切り線・区間境界の縦罫線など、装飾・区切り目的の縦線をあらゆる画面・部品で引かない。区切りは余白・面の明暗・横罫線で作る。図形そのものの縦成分（勾配シルエットの輪郭、レーダーの軸、バーの端）はこの禁止の対象外として扱っているが、ユーザーがより厳格な適用を指示した場合はそれに従う。既存の縦線（CourseProfileの区間境界線・レーダー2枚の間の仕切り等）は第32弾で撤去対象。

**手順（省略禁止）**

デザイン・UI・画面内の文章を変更するときは、**いきなり実装しない**。必ず次の順で進める。

1. **サンプル候補を複数提示する**（実際に目で見える形で。テキストモック／HTML／スクリーンショット等）
2. **ユーザーが選択し、編集の指示を出す**
3. **2を、合意に達するまで繰り返す**
4. **合意後に初めて実装に入る**（実装はSonnet。§2の規定に従う）

**「良さそうなので作ってみました」は禁止。作る前に見せる。** これは§1（完璧な設計図の先行作成）のデザイン版であり、機能の設計と同じ重さで扱う。

**設計の具体性（必須・2026-08にユーザーが明示）**

- 背景：Phase 3-A完了後、Phase 3-Bの設計依頼にあたりユーザーから「設計は極めて具体化することを前提としてね。なんならルールにも明記してね」と明示された。曖昧な設計案（「〜など」「詳細情報を表示」といった要約表現）は、実装段階で解釈のブレや手戻りを生む。§1の「完璧な設計図」は機能設計だけでなくデザイン設計にも同じ重さで適用される。
- **「サンプル候補を複数提示する」（手順1）の前提として、候補を作る前に必ず次を個別に確定させること**：
  - **画面内の要素を「〜など」で済ませず全て列挙する**。省略した項目は無いものとして扱われるため、載せる/載せないを1件ずつ判断する。
  - **各データ項目について、取得元（具体的な関数名・フィールド名・`ml.xxx`のようなプロパティパス）を明記する**。「実データを使う」ではなく「どの実データか」まで書く。
  - **画面内のレイアウト順序・グルーピング・優先順位を明記する**（上から何が来るか、どれとどれが1つのセクションにまとまるか）。
  - **空・0・未達成などの状態（empty state）の挙動を定義する**。「データが無い場合どう見えるか」を書き漏らさない。
  - **モックアップは要約テキストではなく実際のゲーム内の実データ・実文言をそのまま使う**（プレースホルダーや「〇〇選手」のような仮名は使わない）。
- 上記が1件でも未確定のまま候補提示（手順1）に進まないこと。ユーザーへの確認が必要な場合は候補提示より前にAskUserQuestion等で解消してから候補を作る。

### 9. 配色ルール（第13弾Phase3-D-5・ユーザー明示・恒久ルール）

**背景**：D-4-bまでで装飾（絵文字・角丸・枠線・多色）は撤去できたが、ユーザーから
「色味が少なすぎる」「アクセントカラーが欲しい」との指摘。実測（シーズン17画面・430万点の
ピクセル分類）で**無彩色93.18%／黄3.24%／他1.4%未満**＝実質**93:3:0**、さらにHSL変換で
`text`(H45.0°)も`sub`(H48.0°)もアクセントの黄(H45.2°)と同じ暖色系と判明し、「無彩色」の
93%自体が黄色寄りだった。**色数の少なさではなく、色相が1つしかないことが原因**。

**トークンの役割分担（`T.color`）**：
- **`accent`（黄）＝データ強調専用**。数値・見出し・状態語（成長フェーズ・調子など判断に
  使う値）に使う。「読むもの」の色。
- **`action`（バイオレット `#A76ADC`）＝操作専用**。主ボタン（1画面に1つが想定）・選択中の
  メニュー・リンクに使う。「押すもの」の色。
- **同じ色を両方の役割で兼任させない**。黄が「数値強調・主ボタン・見出し・選択中」を
  兼任していた状態が、読むものと押すものを見分けられなくしていた元凶だった。
- `good`（結果の良し悪し・勝ち）・`bad`（結果の良し悪し・負け）は既存どおり、良し悪しの
  判定にのみ使い、強調やアクセントの代用にしない。

**新しい色を足す／既存の色の使用範囲を変えるときの判断基準**：
- **その色に「他の色にはできない仕事」を1つ以上、事前に割り当てること。** 役割のない色は
  ただの装飾に戻る（撤去してきた6色アクセントの再来になる）。
- **色相は、既存の意味色すべて（`accent`・`good`・`bad`）からバランスよく離れているか
  をHSLの色相差で確認する。** どれか1つとでも40°未満しか離れていない色は、その意味色と
  混同されるため候補から外す（実例：マゼンタは`bad`の赤と42°しか離れず除外した）。
- **面積比の指定（例：ベース70%・メイン25%・アクセント5%）は、ピクセル面積そのものでは
  なく「視覚的な重み」の目安として扱う。** 暗い計器盤UIで面積比を額面どおり満たそうとすると
  （実例：黄をパネル地・罫線・本文まで広げて22%にした試作）、強調がすべて等価になり
  R4/R5（一覧行の階層・アクセントは少数の値に限る）で作った階層が壊れる。指定を受けたら
  必ず複数の強さの候補（弱・中・強）を実際に描いて見せ、どこまでなら階層が保たれるかを
  ユーザーと一緒に確認してから決める。
- **データ層の意味色（脚質・コース地形・系統図など、実データが持つ色）とUIクロームの
  アクセント色は別レイヤーとして扱う。** 図・チャートの中で識別のために使われている色は、
  UIの配色ルール変更の対象外（D-3で確立した原則の踏襲）。ただし新しいUIアクセント色を
  決める際は、既存のデータ層の色と色相が極端に近くないか（実例：`SEG_COLOR.hill`の紫と
  新アクセントの紫が4°しか違わなかった）を実装後に目視確認すること。
