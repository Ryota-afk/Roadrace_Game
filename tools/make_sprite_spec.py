"""AIへ渡すドット絵発注仕様書 tools/BIKE_SPRITE_SPEC.md を生成する。

参照用の現行フレームは pixelBike.jsx から実データを埋め込む（手写しによる転記ミスを避ける）。
スプライトを差し替えた後に再生成すれば、常に「現行」を基準にした仕様書になる。

    python3 tools/make_sprite_spec.py
"""
import os
import re

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SRC = os.path.join(ROOT, "src/components/sprites/pixelBike.jsx")
OUT = os.path.join(ROOT, "tools/BIKE_SPRITE_SPEC.md")

src = open(SRC, encoding="utf-8").read()


def extract_table(name):
    m = re.search(r"const %s = \{(.*?)\n\};" % name, src, re.S)
    out = {}
    for km in re.finditer(r"(\w+):\s*\[(.*?)\]", m.group(1), re.S):
        out[km.group(1)] = re.findall(r'"([^"]*)"', km.group(2))
    return out


A = extract_table("BIKE")


def block(pose):
    rows = A[pose]
    body = "\n".join(f'  "{r}",' for r in rows)
    return f"{pose} ({len(rows[0])} x {len(rows)}):\n{body}"


SPEC = f"""# 自転車ドット絵スプライト 発注仕様書

このファイルをそのままAIに渡してください。**この仕様に書かれていることが全てです。
仕様に無い判断を勝手に足さないでください。**

---

## 0. あなたへの依頼（要約）

自転車ロードレースのゲームで使う、**自転車に乗った選手のドット絵12コマ**を作ってください。
出力は画像ではなく、**ASCIIの文字列配列（JSON）**です。

現行のコマは既に存在しますが、以下の2点が問題で作り直しが必要です。

1. **ダンシング（立ち漕ぎ）が通常走行とほぼ同じシルエットで、見分けがつかない。**
   実測すると、通常走行の全高50マスに対しダンシングは51マス（**わずか+1マス**）しかなく、
   「サドルから腰を上げて立っている」ことが形として全く表現できていません。
2. **スプリントに「全力で踏んでいる感」が無い。**

---

## 1. 前提：この絵がゲーム内でどう見えるか（最重要）

**表示サイズが非常に小さい。** スマホ画面で1コマが数十px程度にしか表示されません。
つまり**細部は一切見えず、シルエット（輪郭の形）だけが読み取れます。**

したがって最優先事項は「3つの姿勢が、輪郭の形だけで瞬時に区別できること」です。
顔の描き込みやフレームのディテールを頑張っても意味がありません。**外形で語ってください。**

視点は**斜め上から見た3/4ビュー**（アイソメトリック風。真横でも真正面でもない）です。
現行コマの見え方を踏襲してください。

---

## 2. 出力フォーマット（厳守）

### 2-1. 全体構造

以下の構造の**JSONのみ**を出力してください。前置きや解説文は不要です。

```json
{{
  "BIKE":   {{ "normal_SE": {{...}}, "normal_NE": {{...}}, "dancing_SE": {{...}},
              "dancing_NE": {{...}}, "sprint_SE": {{...}}, "sprint_NE": {{...}} }},
  "BIKE_B": {{ "normal_SE": {{...}}, "normal_NE": {{...}}, "dancing_SE": {{...}},
              "dancing_NE": {{...}}, "sprint_SE": {{...}}, "sprint_NE": {{...}} }}
}}
```

各コマの中身：

```json
{{
  "rows": ["....KKK....", "...KJJJK...", "..."],
  "anchors": {{
    "head":        [row, col],
    "hip":         [row, col],
    "saddle":      [row, col],
    "hand":        [row, col],
    "crank":       [row, col],
    "front_wheel": [row, col],
    "rear_wheel":  [row, col]
  }}
}}
```

`anchors` は**あなたが描いた絵のどこに何があるかの申告**です。これを機械検査に使うので、
**実際に描いた位置と一致させてください**（row/colともに0始まり）。各アンカーの定義：

| アンカー | 指す位置 |
|---|---|
| `head` | 頭部（ヘルメット含む）の中心 |
| `hip` | 腰（胴と脚の接続点）の中心 |
| `saddle` | サドルの上面の中心 |
| `hand` | ハンドルを握っている手（手前側の手）の中心 |
| `crank` | クランク軸＝ボトムブラケット（ペダルの回転中心） |
| `front_wheel` | 前輪の中心 |
| `rear_wheel` | 後輪の中心 |

### 2-2. 使用できる文字（これ以外は使用禁止）

| 文字 | 意味 | 色 |
|---|---|---|
| `.` | 透明（背景） | — |
| `K` | 輪郭線・最も暗い影 | `#161616` |
| `D` | 暗いグレー（タイヤ等） | `#3e3e3e` |
| `M` | 中間グレー | `#707070` |
| `R` | 明るいグレー（リム・スポーク） | `#a8a8a8` |
| `W` | ハイライト（白に近い） | `#d8d8d8` |
| `J` | **チームカラー**（ジャージ・ヘルメット・フレーム） | 実行時に選手ごとに変わる |
| `j` | チームカラーの影（Jの68%の明度） | 同上 |
| `S` | 肌 | `#f2c18e` |
| `s` | 肌の影 | `#cd9260` |
| `H` | 髪 | `#463223` |

**重要**：`J`/`j` は実行時にチームカラーへ差し替わります。**ジャージ・ヘルメット・自転車の
フレームの3箇所に必ず使ってください**（ロードレースでは車体がチームカラーなのが自然なため、
フレームも同じ動的色にしています）。逆に、チームカラーであってほしくない部分に`J`を使わないで
ください。

**輪郭線は自動生成されません。** シルエットの外周を `K` で自分で描いてください。

### 2-3. グリッドサイズ（厳守）

| 姿勢 | キャンバス（幅 × 高さ） |
|---|---|
| `normal_SE` / `normal_NE` | **38 × 50** |
| `dancing_SE` / `dancing_NE` | **38 × 56** |
| `sprint_SE` / `sprint_NE` | **48 × 44** |

- `rows` の要素数＝高さ、各文字列の長さ＝幅。**同一コマ内の全行が完全に同じ長さ**であること。
- **BIKE と BIKE_B は、同じ姿勢どうしで完全に同じキャンバスサイズ**であること。

---

## 3. 座標・接地の規約（厳守）

スプライトは**下端中央**を基準点としてゲーム世界に配置されます。

- **接地**：下から2行目（`row = 高さ - 2`）に前後輪の最下端が来ること。
  **最下行（`row = 高さ - 1`）は必ず全て `.`（空）**にしてください。
  この余白1行が地面との位置合わせの規約です。ここがずれると自転車が地面から浮いたり沈んだり
  します。
- **水平中心**：描画内容の左右の中心が、キャンバス幅の中央（`幅 / 2`）から**±1マス以内**に
  収まること。
- **上端**：`row = 0` の行は空けてください（上に1行以上の余白）。

---

## 4. 向き（handedness）— **最も間違えやすい箇所**

各姿勢に `_SE` と `_NE` の2方向があります。これは**互いの鏡像ではありません。別々に描いて
ください。** そして**描く向きが SE と NE で逆**です。

- **`_SE` のコマ：自転車は「左向き」に描く。** 前輪とハンドルが**グリッドの左側**、後輪が右側。
  選手はやや手前（視聴者側）を向く。
- **`_NE` のコマ：自転車は「右向き」に描く。** 前輪とハンドルが**グリッドの右側**、後輪が左側。
  選手はやや奥（背中側）を向く。

> **なぜこれが重要か**：過去にこの規約を取り違えたコマが1つ混入し、ゲーム内で「その姿勢の
> ときだけ選手が逆を向いて進む」というバグが長期間発見されませんでした。コード側の反転処理は
> この規約を前提に組まれているため、**1コマでも向きを間違えると、そのコマだけ逆走します。**

**必ず、下記「7. 現行の参照コマ」の `normal_SE` / `normal_NE` を見て、前輪とハンドルが
どちら側にあるかを確認してから描き始めてください。** 6コマすべてで、対応する normal と
同じ側に前輪が来ていることを描き終わりに再確認してください。

---

## 5. 3つの姿勢の描き分け（この依頼の本題）

### 共通の考え方

前述の通り**シルエットだけが読み取れる**ので、3姿勢は**輪郭の縦横比が明確に違う**必要が
あります。狙う形は：

- **normal** … 基準。コンパクトで上体が起きている
- **dancing** … **縦に高い**。選手が立ち上がっている
- **sprint** … **低くて長い**。選手が前に伏せている

### 5-1. `normal`（通常走行）— 38 × 50

巡航中の自然な姿勢。**この姿勢だけは現行コマがほぼ良好なので、大きく変えないでください。**
参照コマ（7章）をベースに、輪郭の整理程度に留めてください。

- 腰は**サドルに乗っている**（`hip` は `saddle` とほぼ同じ高さ、±2マス以内）
- 上体は起き気味。手はハンドルの**上部**を握る
- 描画内容の高さ：**46〜48マス**、幅：**33〜37マス**

### 5-2. `dancing`（ダンシング／立ち漕ぎ）— 38 × 56 ★最重要

**現行コマの最大の失敗がここです。「立っている」ことが形で分からなければ失敗です。**

必須要素：

1. **腰がサドルから完全に離れて浮いている。** `hip` は `saddle` より**4マス以上高い位置**
   （＝row の値が4以上小さい）。サドルと尻の間に**明確な隙間**を作ってください。
2. **上体が高く持ち上がる。** 描画内容の高さは**52マス以上**（normal より5マス以上高い）。
   輪郭が縦に伸びていることが一目で分かるように。
3. **車体が左右に傾く。** ダンシングは車体を振りながら踏む動作です。自転車全体を
   **左右どちらかに5〜8度傾けて**描いてください（BIKE と BIKE_B で**逆向きに傾ける**
   ＝交互に切り替わることで車体が振れて見える）。
4. **腕が伸びてハンドルを引きつけている。** 肘は軽く曲がり、体重が前に乗る。
5. **脚**：片脚がほぼ伸び切って踏み下ろし、もう片脚が高く上がる。左右差を大きく。

### 5-3. `sprint`（スプリント／全力もがき）— 48 × 44 ★最重要

**「全力で踏んでいる」爆発力が伝わる形にしてください。**

必須要素：

1. **背中がほぼ水平になるまで伏せる。** `head` は `hip` より**8マス以上高い位置には
   来ない**（＝`head.row >= hip.row - 8`）。normal の起きた上体との差を明確に。
2. **頭が大きく前に出る。** `head` と `hip` の**横方向の距離が8マス以上**。前傾して
   前輪の上に覆いかぶさるように。
3. **低く長い輪郭。** 描画内容の高さは**42マス以下**（normal より5マス以上低い）、
   幅は**42マス以上**（normal より6マス以上広い）。
4. **ハンドルの下部（ドロップ）を握る。** 手の位置を normal より低く、前に。
5. **肘を張る。** 力を込めている緊張感を、腕のシルエットで表現。
6. **脚**：踏み込みの瞬間。太ももとふくらはぎの角度を大きく、力感のあるライン。

---

## 6. クランク位相 A / B（ペダリングのアニメーション）

`BIKE`（位相A）と `BIKE_B`（位相B）は、**同じ姿勢の、ペダルが半回転ずれた2コマ**です。
ゲーム中は約0.11秒ごとにA→B→A→…と切り替わり、これがペダリングのアニメーションになります。

厳守事項：

- **前輪・後輪・サドルは A と B で1マスも動かさないこと。** 車輪は地面に接しています。
  ここが動くと自転車全体がガタガタ揺れて見えます。`anchors` の `front_wheel` /
  `rear_wheel` / `saddle` は A と B で**完全に同じ座標**にしてください。
- **動かすのは脚・ペダル・クランク**です。A で右脚が踏み下ろしなら、B では左脚が踏み下ろし。
- **上体は姿勢ごとに扱いを変える**：
  - `normal` … 上体はほぼ不動（脚だけ動く）
  - `dancing` … 上体と車体が**左右に振れる**（A と B で傾きを逆にする＝これがダンシングらしさ）
  - `sprint` … 上体はわずかに上下（1〜2マス程度）。振りすぎない

---

## 7. 現行の参照コマ（向き・画風・パレットの基準）

以下は**現在ゲームで使われている実データ**です。**向き（前輪がどちら側か）と画風・
パレットの使い方の基準**にしてください。normal はこれをベースに整えてください。

```
{block("normal_SE")}
```

```
{block("normal_NE")}
```

参考までに、**作り直す対象**である現行の dancing も載せます。
**これが「normal と見分けがつかない」失敗例です。同じ轍を踏まないでください。**

```
{block("dancing_SE")}
```

---

## 8. 提出前セルフチェック（必ず全項目を確認してから出力）

- [ ] 12コマすべてある（normal/dancing/sprint × SE/NE × A/B）
- [ ] 使用文字は `.KDMRWJjSsH` のみ
- [ ] 各コマ内の全行が同じ長さ
- [ ] キャンバスサイズが §2-3 の表と一致（A と B で同じ）
- [ ] 最下行が全て `.`、その1つ上の行に車輪の最下端がある
- [ ] 描画内容の左右中心がキャンバス中央±1マス以内
- [ ] **`_SE` は前輪・ハンドルが左、`_NE` は前輪・ハンドルが右**（全6姿勢で確認）
- [ ] `J`/`j` をジャージ・ヘルメット・フレームの3箇所に使った
- [ ] シルエットの外周を `K` で囲った
- [ ] **dancing：腰がサドルより4マス以上高く、隙間が見える。全高52マス以上**
- [ ] **sprint：背中がほぼ水平、頭が前に8マス以上出ている。全高42マス以下・幅42マス以上**
- [ ] A と B で前輪・後輪・サドルの座標が完全に一致
- [ ] `anchors` の申告座標が、実際に描いた絵の位置と合っている
- [ ] 出力はJSONのみ（解説文を混ぜない）

---

## 9. 受け取り側の検査について

提出されたJSONは、以下のコマンドで**機械的に全項目検査**されます。
不合格項目は具体的に指摘されるので、その場合は該当箇所だけ直して再提出してください。

```bash
python3 tools/bike_sprites.py validate candidate.json   # 全ルールを検査
python3 tools/bike_sprites.py preview  candidate.json   # 画像化して目視確認
python3 tools/bike_sprites.py tojs     candidate.json   # 合格後、JSXへ変換
```
"""

os.makedirs(os.path.dirname(OUT), exist_ok=True)
with open(OUT, "w", encoding="utf-8") as f:
    f.write(SPEC)
print("wrote", OUT, f"({len(SPEC)} chars)")
