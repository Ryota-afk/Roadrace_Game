# TODO — 次のアクション（未着手のみ）

DEVLOG.mdから分離（2026-09・第95弾）。**分離の理由**：DEVLOGへのReadは上限超過時に
**末尾から切り捨てられる**ため、末尾に置いていたこのリストが真っ先に読めなくなる事故が
構造的に起きる（CLAUDE.md §4に実測記録）。TODOは変更頻度も性質（随時追加・完了で削除）も
履歴と異なるため、独立ファイルにする。

運用はDEVLOG時代と同じ：
- ユーザーから明示的に指示された、今後着手すべき項目のメモ。優先度・着手順は未確定。
- **完了項目はここから削除する**（詳細は各弾の§番号・`devlog/waveNN.md`に残る。
  番号の欠番は削除済みの完了項目の跡）。
- ⚠️DEVLOGの§番号は統合された索引の範囲で読むこと（例：§79は「§69〜81」）。
- ⚠️**弾を締めるとき、その弾で先送りにしたものは`devlog/waveNN.md`に「上げる」と書かずに
  このファイルへ直接書く。** 締め前に
  `grep -rn "「次のアクション」へ上げる\|TODO.mdへ上げる\|TODOへ上げる" devlog/` が
  **0件**であることを確認する（#27・#29の再発防止。旧い言い回しも新しい言い回しも、
  「後で上げる」と書いた時点で同じ失敗）。

## やらないと決めたこと（再提案しないこと）

一度検討して**却下・完了**した項目。新しいチャットが蒸し返さないための一覧。

- ⚠️**#29-e：シンクに注ぎ込んだ結果として破産する経路は確認済み・コード対応不要（第94弾）**。
  `builder`方策（買えるだけ買う極端な貪欲さ）の実測で資金マイナス12ヶ月連続が成立し得るが
  （2/5→1/5→0/5とばらつき）、実プレイヤーが赤字時に買い物を続けることは考えにくい。
  ⚠️ただし第95弾の総点検で「**経済に脅威が無い**」symptom として関連論点が再オープンされた
  （TODO #31-B・`devlog/wave95.md`）。「破産バグ修正」としては閉じたまま、「緊張の設計」
  としては生きている——混同しないこと。
- ⚠️**「能力値がキャップを超えている」は不具合ではない（第92弾）**。
  `domain/mylife/growthCap.js:49-50`に明文の設計意図あり（capはソフトな減速の目安。
  addAbはクランプしない）。simcareerのA4チェックは削除済み（`devlog/wave92.md`訂正3）。
  第33・36弾の「早期カンスト」はcap算出式が際限なく伸びる別種の問題であり混同しないこと。
- ⚠️**逃げの「速度」を触る案は入れない**——4回失敗し軸そのものが行き止まりと確定（DEVLOG §82〜85）。
  詳細と代わりに残る軸（`brk`＝エネルギー経済）は`ARCHITECTURE.md`のレースsim節が正本。
- ⚠️⚠️**`attack−hold`の数値を追う仕事はもう再開しない**（第58〜59弾打ち切り・DEVLOG §88〜89）。
  同じ問題に7回挑戦して効いたのは1回。実測では成功率42〜47%・決まれば−3.39着で
  「平均では損だが決まれば勝てる」諸刃の剣として正しい姿の可能性が高い。**再開する場合は、
  数値ではなく「実際にプレイしてfelt problemとして観測されたか」を先に示すこと。**
- **バッジの退行（使わないと段階が落ちる）は入れない**——恒久確定（DEVLOG §69〜81・`devlog/wave43.md`）。
- **AIへのバッジ段階配布はやらない**（DEVLOG §69〜81・第49弾実測。勝率が測定上動かない）。
- **バッジ所持上限の撤廃はやらない**（DEVLOG §69〜81。上限こそが個性の源）。
- **スキルポイント制（旧14番）は廃止**。
- ⚠️**「12px未満は漢字が潰れる」という旧メモは失効**（下限は`micro:8`/`nano:6`/`digit:5`。
  `data/theme.js`）。この失効した制約を根拠に設計しないこと。

## 育成の方向転換（2026-08・ユーザー明示・以降の設計はこれに従う）

**「能力値で個性を出す」のをやめ、「バッジで個性を出す」へ舵を切る。** 能力値が横並びでも
持っているバッジが違えば別の選手になる、という構図に寄せる。「極めると全員同じ万能型」問題を、
能力値側の上限調整ではなくバッジ側で解く。

## 未着手のTODO

32. **第96弾の後始末：計測バグの汚染を受けた過去の検証をやり直す**
   （2026-09・第96弾§7。全文は`devlog/wave96.md`）。
   ⚠️**「難易度つまみが逆転している」という第96弾の前提は誤りだった。** 全プローブと
   `tools/decisioncard_ev.mjs`が`es.find(e=>e.isPlayer) || es[0]`でプレイヤーを取得して
   おり、実フィールドは`isPlayerChar`のため`.find()`は常に失敗し、⚠️**毎回`es[0]`＝
   AI選手を測っていた**（プレイヤーは添字30前後・一致率0%）。難易度は正しく効いている
   （円熟/PRO: easy 勝率65% → oni 0%／新人/B1: easy 35% → oni 0%、いずれも単調）。
   - **32-f. 第95弾のEV計測・finaleカード検証をやり直す**（⚠️最優先）。
     `tools/decisioncard_ev.mjs`は`resumeSim(forked, fromTick, me.id, ch.move)`で
     ⚠️**AI選手にプレイヤーの手を適用**していた。第95弾§4.5〜§4.5eの全EV値と、
     finaleカード改修の合格判定（「10択中9択が非支配」「脚の層別で溜める/踏むが
     入れ替わる」）は**すべて未検証に戻る**。ツール側の取得は第96弾§7.5で修正済み
     なので、⚠️**再実行して結論が維持されるかを確認する**こと。維持されなければ
     `KICK_ENERGY_COST`/`KICKBIG_ENERGY_COST`とfinaleカードの3択構成を再設計する。
     ⚠️**一次データは取得済み**（`devlog/wave96.md` §7.7）。判明したこと：
     ①easy・脚十分では`kick`が勝率8%→44%と跳ね上げつつ着順を損なわない優秀な一手だが、
     ⚠️**`kickBig`は`kick`に両軸で劣り支配されている**（合格条件③を満たさない）。
     normalでは支配関係が逆転するため⚠️**n=35〜48では結論不能・nを増やすこと**。
     ②⚠️**ツールは新人/B1しか測れず、finaleカードが最も効く円熟期を測っていない**。
     `--ability`/`--class`を追加すること（`tools/difficulty_check.mjs`の`scaleTo`を流用）。
   - **32-i. 判断カードが不活性な区間が実在する**（第96弾§7.7で発見。⚠️難易度の逆転とは
     別問題で、正しく測って初めて見えた）。新人がhard/oniに出ると集団から千切れ、
     ⚠️**oniでは全スロット・全選択肢がΔ0.00**（hardも脚「限界」がn=34/50）。
     CLAUDE.md §0-1「意味のある判断」が成立していない。⚠️**「もう何をしても無駄」な
     区間をどう扱うか**——①そもそもその状況に入りにくくする②入ったときに別の目標
     （完走・チームへの貢献・経験値）へ切り替える、のどちらかの設計判断が要る。
   - **32-j. terrainカードは未分化ではなく有害**（第96弾§7.7）。easyで
     ⚠️**全選択肢が悪化**（attack Δ+15.13／tempo +6.25／hold +3.38）＝「触らないのが最善」。
     `mid/attack`も Δ+9.60・勝率0%。第95弾 #31-D-2は「未分化」と書いたが実態はより深刻。
   - **32-b. マイライフの難易度説明がシーズンの仕様を説明している**（⚠️**これは有効**。
     計測に依存せずコード読解で確認済み）。`screens/mylife/create.jsx:98`が出す
     `DIFFICULTIES[].desc`の「成長上限も上がる」の根拠`growthCap`（88/94/102/112）は
     **シーズン専用**。マイライフは`ML_GROWTHCAP_DIFF_MUL = {easy:1.3, normal:1.0,
     hard:0.75, oni:0.5}`で、⚠️**鬼は上限が下がる**。文言と実装が逆。
   - **32-g. `es[0]`フォールバックの横断チェック**。今回の事故は`|| 配列[0]`が
     `.find()`の失敗を静かに隠したことが本体。⚠️**この種のフォールバックを
     プローブ・ツールに書かないこと**（取れなければskip）。`tools/`は第96弾で
     監査済み（該当は`decisioncard_ev.mjs`の1件のみ・修正済み）だが、
     `src/`側で同型の取得をしていないかは未監査。
   - **32-d. 「本気度」システム（悪魔の釜・`design/sakurai-gameplay.md` #05）**——
     ⚠️**着手をブロックしていた理由（難易度の逆転）は消滅した。** 難易度つまみは
     正しく効いているので、その上に「上げると難しくなる代わりに報酬が増える」を
     載せられる。ただし⚠️**32-fの再検証を先に済ませること**（判断カードのEVが
     未検証のまま、その上に賭けの層を積まない）。設計方針は従来どおり：
     賭けるのは金（#16の恒久シンク不足を埋める）、見返りは成長・バッジ進捗・人気度
     （賞金を増やすと金を刷る装置になり#16の解決が台無しになる）。
   - **32-e. 進行中の難易度変更**——桜井#24に照らして**あった方がよい**。
     ⚠️**永続的な`difficulty`を書き換える形では実装しない**（`mlGrowthCap`は毎回
     その場で計算するため、終盤にeasyへ切り替えると蓄積済みの実績ボーナスが
     遡って1.3倍になり天井が跳ねる）。レース単位で完結する32-d（本気度）が上位互換。
   - ⚠️**32-a（案A・集団ペース押し上げ）と32-h（案H・強度ベースの消耗）は
     「効かなかった」という評価自体がAI選手基準だったため、是非は未評価に戻る。**
     ただし⚠️**そもそも直すべき問題が無かった**ので、再挑戦する理由も現時点では無い。
     経緯は`devlog/wave96.md` §5.5／§5.7に残す。


31. **第95弾：面白さの総点検で洗い出した50項目**（2026-09・全文と根拠は`devlog/wave95.md`）。
   桜井130テーマ照合＋全体評価の結論。◎＝構造問題11件を個別に挙げる。○/△の39件は
   `devlog/wave95.md`の表を正とし、着手時にここへ昇格させる。
   - **31-A. 人間による1キャリア通しプレイ（時間計測付き）**——最優先。他の全項目の
     要否判断がこれに依存する。⚠️**計測基盤は実装・検証済み（本弾）**：`src/dev/telemetry.js`
     （既定OFF）。使い方はブラウザのdevtoolsコンソールで
     `__telemetry.enable()` → 普通に遊ぶ → `__telemetry.summary()`（集計を見る）／
     `__telemetry.exportText()`（クリップボードへコピー、Claudeに貼り付け）。
     画面滞在時間・種別（hub/race/shop/reading/menu等）ごとの時間比率・月次アクションの
     内訳・判断カードの表示/選択回数を記録する。⚠️**残作業は「実際に人間が遊ぶこと」だけ**
     ——コード側の作業はここで完了。Playwright自動プレイ（スキップ優先）で配線を検証済みだが、
     判断カードはスキップ中は発火しない仕様のため`decisionShown/decisionChosen`は
     人間のプレイでしか埋まらない。
   - **31-B. 「奪う器官」の設計**——プレイヤーの判断と結びついた継続的な下振れが1つも無い
     （ギュッとパッの欠如）。フォーム崩壊・契約喪失・加齢との攻防など、どれか1つ。
   - **31-C. 経済の再設計**——恒久需要が13.7年で枯渇（第93弾実測）。使い道を「足す」のでは
     なく「競合させる」（両方は買えない／今しか買えない／外れるかもしれない）。
   - **31-D. 判断カードの選択肢EV計測**——⚠️**完了（本弾）**。第一次（RUL・80レース×2難易度、
     `devlog/wave95.md` §4.5）→脚質5種への一般化（`tools/decisioncard_ev.mjs`・
     5脚質×2難易度×40人＝800レース、§4.5b）まで実施。**恒久ツール化済み**
     （`tools/decisioncard_ev.mjs`。フォーク方式＝courseを共有し`entrants`だけ深クローン。
     ⚠️`sim.course`は関数を含み`structuredClone`不可）。結論：`finale/send`は5脚質×2難易度
     の全10セルで一貫した罠、`mid/attack`の難易度反転（easy最悪→hard改善）も全脚質で
     再現——**RUL固有ではなく構造的**と確定。残作業は②のみ：**キャリア中盤/後半
     （バッジ所持後）の再測定**——月送りの土台が別途要るため未着手のまま。
   - **31-D-2. finaleカードの設計是正**——⚠️**実装・検証済み（本弾。案A・2-b、
     `devlog/wave95.md` §4.5e）**。`kick`（安全側）と`kickBig`（賭け側）を全員に必ず
     両方提示し、3つ目を`hold`→`conserve`（脚を溜める）へ。`kick`/`kickBig`に
     エネルギーコスト（8/12）を新設し「脚が十分なら踏む・苦しければ溜める」判断にした。
     800レース再検証：**10セル中9セルで両軸支配が消滅**（kickが平均で勝ち、kickBigが
     勝率で勝つ階段。例外1セル=SPR/hardはn=80のノイズの可能性）。`moveEdge.js`の
     `kickBig`分類も金→虹（大勝負）に是正。
     - ⚠️**残る懸念（次に触る人へ）**：「やや消耗」層（エネルギー0〜40）で`kick`/`kickBig`が
       想定より大きく悪化するセルが複数見つかった（CLM/easy等でΔ+9〜+13）。
       `energyPenaltyMul`のenergy=20の崖を越えてしまうため。n=4〜10と小さくノイズの
       余地はあるが方向は揃っている。次にfinaleを触るときはこの層を優先的に再測定し、
       必要なら`KICK_ENERGY_COST`/`KICKBIG_ENERGY_COST`を較正し直す。
     - ⚠️**未検証**：ブラウザでの実プレイ確認はテストハーネスの限界で未達（`composeCard`を
       直接呼ぶ代替検証は実施済み・§4.5e）。次に人間が遊ぶ機会（TODO #31-A）で
       finaleカードの実際の見え方も併せて確認すること。
     - `terrain`カード：3択の差が無く報酬になっていない（n小のため要再測定・未着手のまま）。
     - `sprint`カードは対象外（機能している）。
   - **31-E. 判断カードの拡充**——選択肢バリエーション（数百回同じ3択の解消）と、
     反実仮想フィードバック（選ばなかった側の顛末を実況で見せる）。⚠️31-Dで
     **midの最適手が難易度で反転する（easyでattackは最悪・hardで最良）**と判明したが、
     プレイヤーがそれを知る手段が画面に無い＝反実仮想の不在が実害として確認された。
   - **31-F. 最初の3分の設計**——見どころ（集団戦レースsim）到達までの導線が未設計・未計測。
   - **31-F-2. 「今月、どうする？」の同格4択が競っているか**（本弾で#19を訂正して発生）。
     推奨の大ボタンは第61弾Phase2で撤去済みで、現在は同格4択。⚠️問題は「推奨がある」ことでは
     なく「4択が実際に競っているか（レースに出る一択になっていないか）」。練習メニュー側の
     推奨（`hub.jsx`の`recKey`）は残存。副次：`hub.jsx:95`の`recWhy`はデッドコード。
   - **31-G. 製品としての見どころ宣言**——2モードの主従、「最初の3分で何を見せるゲームか」の
     一文が無いことが31-Fに答えられない根因。
   - **31-H. 世代交代メタの検証**——配合・血脈レシピ・王朝NG+が人間未体験のまま巨大実装。
     31-Aの結果を待って評価。

29. **第83〜92弾から回収した積み残し**（第92弾の締めで機械的に洗い出し）。
   - **29-a. チームTTの監督評価（`managerEval`）**（第84弾①-4）。指示文がすべて個人順位
     前提の文言。⚠️チームTTに当てるには指示カード側の設計から要る。
   - **29-c. `sciPendingId`の導線がショップ画面以外に無い**（第88弾）。科学トレーニングの
     結果待ち（バッジを1つ手放す選択）に気づけない。
   - **29-d. `POP_MILESTONES`がレース由来の増加でしか判定されない**（第90弾①）。
     ⚠️直すと到達ボーナス（80〜400万円）の入り方が変わる＝バランス変更を伴う。
   - **29-f. シード再現性**（第92弾）。`createChar.js:18,25`・`career.js:18`が
     `Date.now()`/`Math.random()`起源で同じキャリアを再現できない。⚠️simの挙動に触れる。

27. **`devlog/`から回収した積み残し**（全62ファイルの機械的洗い出し）。
   - **27-c. `segs`の死んだ`base`列の除去**（第72弾）。13テンプレート×全区間の書き換え。
   - **27-f. `.ml-enter-*`→`.app-enter-*`の一括改名**（第67弾・cosmetic）。
   - **27-g. `タイトルに戻る`の置き場所**（第64弾）。⚠️`最初からやり直す`（データが消える）の
     真下にあり押し間違いの危険。
   - ⚠️**27-h. 出走表・観戦画面への「実順位」バッジ**（第11弾Phase2から持ち越し）。
     当時の案は絵文字前提でCLAUDE.md §8と衝突。着手するなら見せ方から設計し直す。
   - **27-i. 透明度差・同色相による階層表現**（第13弾・ユーザーの留保付き提案）。
   - **27-j. 同じものを2つの言葉で呼んでいる箇所の統一**（第85弾・§7）。
     「キャプテン/主将」「ポイント/チームポイント」。着手時は§8の手順で語を選ぶ。

26. **初期のまま触っていない要素・残る候補**（第72弾の棚卸し・`devlog/wave72.md`）。
   - **会場に個性を与える**：国内12・海外8の会場が名前だけ。⚠️着手時は
     `design/sakurai-planning.md` #24（ピーキーに）・#21（カスタマイズ5条件）を必読
     （第95弾#50としても再指摘）。
   - **配合データの新旧重複を調査**：`data/breeding.js`と`breeding/recipes.js`の併存。

11. **スプライト（スプリント／ダンシング）モデルの描き直し**（⚠️凍結中→**再開方針確定**）。
   試した3方式は全て失敗＝再挑戦しないこと。**再開条件が第95弾で更新された**：
   「解剖学的に正しい姿勢」ではなく`design/sakurai-motion.md` #02/#07に従い
   **破綻するほど大げさな**dancing/sprint 6コマ分の参考画像を新規に用意し、
   黒塗りシルエットで判別検証（`design/sakurai-graphics.md` #19）してから
   CLAUDE.md §6の手順で抽出する。

## 恒常タスク（終わりが無い）

9. **UIテキスト・ビジュアルの作り込み**（CLAUDE.md §7・§8）。新画面・新機能を作るたびに
   点検する。経緯は文言面＝DEVLOG §13〜23・§24〜31、土台面＝§38〜49・§50〜68。
8. **マイライフのイベント本文の拡充**（Phase 1〜3完了・現在76件、長期目標「数百種類」）。
   既存5分類のタグ付け方針で`data/events.js`の`ML_EVENTS`へ追記。
