# 第99弾 — 本気度（TODO #32-d）

**状態：設計中（計測待ち）**

## なぜ今この弾なのか

TODO #32-d は長らく2つの理由でブロックされていた。両方とも解消済み：

| ブロッカー | 解消 |
|---|---|
| 「難易度つまみが逆転している」 | 第96弾§7で**計測バグだったと判明**。つまみは正しく効く（PRO easy 勝率67% → oni 2%） |
| 「判断カードのEVが未検証」 | 第96弾§7.9で再検証完了。第97〜98弾で不活性区間も潰した |

第96〜98弾は「レース中の判断が本当に結果を動かすか」を確かめる作業だった。本気度は
その上に載る**増幅器**である——「今日の判断はどれだけ重いのか」をプレイヤー自身が決める。
これが第95弾§0-2（奪う側の設計）に対する直接の回答になる。

## 参照した外部知見（CLAUDE.md §11）

`design/sakurai-gameplay.md` より：

- **#05 悪魔の釜**（『新・光神話 パルテナの鏡』）。ステージ開始時に「本気度」（基準2.0）を
  ハート（お金）で上げ下げ。上げると敵が手強くなる代わりに報酬が増える。ミスすると釜の
  ハートをこぼし、難度が下がる代わりに報酬も減る。**リスクとリターンを難易度調整に
  落とし込むことで、行き詰まりにくく「イージーにしますか」の屈辱感もない。**
  - 桜井自身の反省点も収録されている：**常にギリギリを突きつけられカジュアルになりにくい／
    「下げれば楽になる」の周知不足**。本弾の設計はこの2点を回避対象として扱う。
- **#01 リスクとリターン**：リスクとリターンは**近い位置に、適切な大きさで**織り込む。
- **#27 どれぐらい良いことをしたか**：ペナルティを与えるなら挽回経路と回復時間まで見直す。

## ユーザーが決めた2つの分岐（本弾の前提）

### 分岐1：賭けるものは「金と活力の両方」

提示した3案（活力のみ／金のみ／両方）のうち**両方**を選択。

- **金**：31-C の「使い道を足すのではなく競合させる」に乗る。開発・科学トレと同じ財布を取り合う。
- **活力**：`player.vitality`（0〜100）。`vitMul = 0.55 + 0.45*min(1, vit/70)`
  （`controllers/mylife/month.js:36`）で、**70を割ると練習・出走経験の伸びが鈍る**。
  現状の消費はレース1回で `5 + grade*2`（7〜13）、練習3、完全休養+22、オフシーズン+20〜40。
- ⚠️**この組み合わせが持つ自己矛盾が、そのまま緊張になる**——本気度の見返りは成長であり、
  支払うのは「成長の芯」である。育てるために育つ力を削る。

⚠️**第93弾の実測が従来方針の根拠を崩していたことを記録しておく。** TODO 32-d には
「賭けるのは金（#16の恒久シンク不足を埋める）」とあったが、第93弾では
**シンクを使うプレイヤーは吸収力3,100万/年 対 純収入1,600万/年＝常に資金不足**と判明して
いる。つまり「余った金の捨て場」という動機は成立しない。金を賭ける意味は
**「余りを捨てる」ではなく「開発と取り合う」**である。

⚠️**懸念（未解決・UI設計で解く）**：コストが2種類あるとUIが重くなり、CLAUDE.md §7の
引き算原則と衝突する。**「本気度」というつまみ1つに対して結果が2行出る**形にはしない。
候補提示（§8手順1）の段階でここを主要な争点として扱う。

### 分岐2：構造は「賭け型（閾値で払い戻し）」

提示した2案（購入型／賭け型）のうち**賭け型**を選択。

- 出走前に本気度を上げると、**目標**（例「3位以内」）が宣言され、相手が強くなる。
- 達成すれば大きな見返り。**外せば賭けたものは没収され、見返りはゼロ。**
- 購入型（着順に関わらず倍率が付く）を却下した理由：CLAUDE.md §0-1 の
  「分散ゼロの購入は判断ではない」に近づくため。

## 実装レバー（新しいsim機構は不要）

第96弾で「難易度つまみは正しく効く」と確認済みの経路をそのまま使う：

- `buildMyLifeSim(raceMeta, player, team, classIdx, difficultyId, ...)` は
  `DIFFICULTIES.find(d => d.id === difficultyId)` から `aiMul` と `abilCap` を引くだけ
  （`sim/buildMyLifeSim.js:28`）。**本気度は「そのレースだけ aiMul を上乗せする」で表現できる。**
- 賭け金の年次スケールは既存の `mlPrivateCampCost`（`120 + (year-1)*40 + classIdx*60`）と
  同じ形が使える。y1/B1で120万、y9/PROで560万、y20/PROで1,000万。
- ⚠️**`abilCap`は動かさない**方針で計測している（`mlAiCapFor`の92/96/102/112は据え置き）。
  「別の（もっと強い）選手が来る」ではなく**「同じ顔ぶれが本気を出す」**という意味論に
  合わせるため。

### 置き場所の技術的制約（調査済み）

- `mlStartRace`（`hooks/useMyLifeGame.js:202`）が **出走表を出す前に sim を組み終えている**
  （`ml.result = sim`）。
- したがって本気度を**出走表の画面で**動かすなら、`buildMyLifeSim` の再実行が必要。
  ⚠️**これは既に確立した挙動**——`RaceView`の判断カードが選択のたびに再simしている
  （`resimBusy`）。1回のsim再構築は実測 **約315ms**。
- 出走表で決められると、**本気度を上げると出走表の顔ぶれが実際に強くなるのが見える**
  ＝説明文を書かずに伝わる（§7・§8の「レイアウト・ビジュアルで伝える」）。
  ホーム（`hub.jsx`）で決める形はこの利点を失う。

## 計測（§10・before）

設計が2つの数値に依存するため、候補提示より前に測る。プローブは
`scratchpad/intensity_probe.mjs`（srcは一切変更せず、`DIFFICULTIES`へ計測用の行を
一時追加して `aiMul` だけを動かす）。

- **M1. 感度**：aiMul を +0/+0.08/+0.15/+0.25/+0.40/+0.55 と回したとき、プレイヤーの
  平均着順・勝率・3位以内率・Δ秒はどれだけ動くか。**動かなければ本気度は見た目だけの装置**。
- **M2. 素の達成率**：各キャリア段階（新人B1/y1 能力60、中堅A/y5 能力80、円熟PRO/y9 能力92、
  極みPRO/y9 能力100）で、素の3位以内率・10位以内率は何%か。
  ⚠️**閾値を置くにはこれを知らないと「必ず成功」か「絶対無理」になる。**

第98弾§4.2の教訓に従い、**着順とΔ秒を必ず併記**し、素の勝率が40%以上の条件には
飽和の警告を出す（着順は1着が天井なので、強い選手では判断の効果を隠す）。

### 結果・第1次（粗いつまみ）

`scratchpad/intensity_probe.mjs`。3段階×5つまみ位置×8キャラ×20〜25レース。

⚠️**計測のやり直しを1回した**。初回は `node probe.mjs | tail -50` としたため
**`tail`が出力を全部バッファし、`timeout 900`で殺される寸前まで0バイトだった**
（プロセスは生きていたので気づくのが遅れた）。長時間プローブはパイプを挟まず
直接ファイルへ書くこと。

| aiMul差 | 新人 3位以内 | 中堅 3位以内 | 円熟 3位以内 | 新人 平均着順 |
|---|---|---|---|---|
| +0.00 | 8% | 28% | 56% | 18.77 |
| **+0.15** | **0%** | **8%** | **18%** | **28.45** |
| +0.30 | 0% | 1% | 4% | 34.80 |
| +0.60 | 0% | 0% | 1% | 39.84 |

⚠️**①つまみは崖だった。** `+0.15`（normal→hardの差の6割）**1段で壊滅する**。
「本気度3段」のような素朴な刻みは作れない。段は **+0.03〜+0.10のオーダー**。

⚠️**②固定閾値は成立しない。** 素の3位以内率が**新人8% / 中堅28% / 円熟56%**。
「3位以内で成功」は新人には実質不可能で円熟にはコインより有利。

⚠️**③Δ秒はこの問いには使えない。** 複数の行でΔ秒が**マイナス**になった
（中堅+0.15で−28.5秒、円熟+0.15で−14.3秒）。矛盾ではない——**強い集団は速く走るので、
後方に沈みながら絶対タイムは速くなる**。第98弾§4.2でΔ秒を足したのは
「同じ顔ぶれの中で自分の選択の効果」を測るためであり、⚠️**顔ぶれ自体を変える本問では
Δ秒は「うまくいったか」を測れない。** 第98弾の是正をそのまま流用すると
4度目の計測ミスになっていた。ここでは着順が正しい指標（勝率は最大29%で飽和せず、
ツールの自動警告も正しく沈黙した）。

### 結果・第2次（細かいつまみ＋「予想超え」）

`scratchpad/forecast_probe.mjs`。第1次の②を受け、閾値を
`raceForecast`（`domain/shared/forecast.js`・**既に出走表に出ている**＝`panels.jsx:73`）の
予想着順に置く案を測った。

| aiMul差 | 新人 予想超え | 中堅 予想超え | 円熟 予想超え |
|---|---|---|---|
| +0.00 | **78%** | 39% | 43% |
| +0.06 | 86% | 48% | 51% |
| +0.15 | **88%** | 66% | 64% |

⚠️**「予想を上回れ」は閾値にできない（案を却下）。** 新人は素で78%通り、しかも
**つまみを上げるほど通りやすくなる**（賭けとして逆向き）。原因は`raceForecast`が
能力だけで順位付けするため、AIを強くすると**プレイヤーの予想着順が実着順より速く落ちる**こと。

⚠️**副産物・本気度とは独立した既存UIの正直さの問題**：新人（B1）は実着順17.29に対し
予想24.32で**約7着ぶん悲観的**。中堅（10.52 vs 9.75）・円熟（7.92 vs 9.08）ではほぼ公平
なので、**B1帯だけの偏り**。出走表は「24位予想」と告げて実際は17位になる。→ TODOへ起票。

**細かい刻みでの絶対達成率**（これが設計に使う本体）：

| aiMul差 | 新人 10位以内 | 中堅 3位以内 | 円熟 3位以内 |
|---|---|---|---|
| +0.00 | 30% | 31% | 43% |
| +0.03 | 25% | 30% | 37% |
| +0.06 | 18% | 24% | 35% |
| +0.10 | 16% | 11% | 23% |
| +0.15 | 5% | 6% | 12% |

⚠️**使える範囲は +0.00〜+0.12 程度。** そして素の達成率が**新人30% / 中堅31% / 円熟43%**と
揃うため、⚠️**クラス別に固定の目標**（B1＝入賞/10位以内、A・PRO＝表彰台/3位以内）を置けば、
相対閾値を発明せずにコインに近い賭けが成立する。第1次の②は
「単一の固定閾値は不可」だが「クラス別の固定閾値は可」だった。

### 計測の限界（正直に記録）

- 能力は `scaleTo` で**比例**スケール（`p[k] *= t/cur`）なので**脚質のシルエットは保たれる**が、
  実際の成長は上限（`mlGrowthCapFor`・`ML_TYPE_CAP_OFFSET`）で圧縮されるため、
  ⚠️**水準は近似**として読むこと。段の相対的な並びは信頼できる。
- 脚質はRULのみ。第95弾の実績（5脚質で構造が再現した）から一般化できると見ているが、
  実装後の検証では他脚質も測ること。

## 設計上の未決事項（計測後に確定する）

1. **閾値の置き方**。固定（3位以内）か、レースのグレード・適性で動くか。M2の素の達成率が
   段階によって大きく違えば固定は成立しない。
2. **つまみの段数**。悪魔の釜は連続値だが、§7の引き算原則からは2〜3段が上限に見える。
3. **見返りの通貨**。出走経験の倍率は1レースでは**目に見えない**（+1能力が+1.6になるだけ）
   ＝§7の可読性問題。人気度（`popGain`・マイルストーンで金にもなる）とバッジ進捗は離散で見える。
   ⚠️**「見返りが見えない賭け」は賭けにならない**ので、ここは計測より設計の問題として扱う。
4. **桜井の反省点2つへの対処**。①常にギリギリを突きつけない（＝本気度0が常に正当な選択で
   あること）②「下げれば楽になる」の周知（＝本気度を上げない選択が罰されないこと）。
