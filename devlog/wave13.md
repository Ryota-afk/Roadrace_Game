# 第13弾：デザインの作り直し（AI臭さの除去）＋`logic/support.js`の分割

## この弾の位置づけ

第2波（§23）〜第3波（§31）で**UIテキストは何度も直してきたが、レイアウト・図形・色味という
土台には一度も手を入れていなかった**。第12弾までの改修は全部その上に載っていた。
ユーザーからの指摘：

> 今の画面のレイアウトとか図形とか色味とかってめちゃめちゃAI臭いんだよね。
> 手間をかけないで適当にやるとこのデザインになりがちだわ。
> 面倒くさがらないで、スタイリッシュなデザインやUIを目指せ。
> まず、アイコンを使うのは逃げだと思って欲しい。
> デザインは、足し算ではなく引き算です。

## 着手前の実態調査（実測）

感覚ではなく数えた。「AI臭さ」には構造的な原因がある。

### ① アイコン（絵文字）

| 範囲 | 個数 |
|---|---|
| ユーザー添付のスクリーンショットで**見えている範囲だけ** | **23個** |
| `screens/mylife/hub.jsx`（マイライフのホーム） | **68個・44種類** |
| ソース全体 | **653個・58ファイル** |

hub.jsxの44種類：🔥🎓⭐🌍📊🏆🏁🏛🔒🎯📈🗂🏅🎖🚴😴💪✏🧬🩸🌟💍🎬🎤📸🎌📻🎉💚📰📋🔀🧑🏫📂🛍👥📖⚙🚪🎭🔄🤝👑
→ **1画面に44種類の絵柄が並ぶと、どれも目印として機能しない。**

ファイル別の上位：`RaceView.jsx` 111／`mylife/hub.jsx` 68／`mylife/race.jsx` 49／
`mylife/career.jsx` 45／`season/race.jsx` 36／`logic/support.js` 33／`mylife/help.jsx` 33。
判断カードのアイコンは`DecisionCard.jsx`ではなく**`sim/race.js`の`RACE_MOVES`側**に11個。
他にデータ層にも点在（`data/economy.js` 9／`data/abilities.js` 4／`data/progression.js` 4）。

### ② 「AI臭さ」の構造的な正体

| 症状 | 実測 | 何が起きているか |
|---|---|---|
| 角丸パネル | **315枚**（10px:145／12px:55／8px:38／6px:31…） | 「角丸カード」を貼るだけの単調な反復 |
| 枠線 `1px solid` | **169本** | 全部を線で囲む＝階層が無い |
| `background: C.panel` | **171箇所** | 同じ色の箱が171枚積んである |
| フォントサイズ | **30種類**。10〜13.5pxに**8段階**（10／10.5／11／11.5／12／12.5／13／13.5） | **タイポグラフィのスケールが無い**。階層を作れないから0.5px刻みで詰め込んでいる |
| アクセント色 | **6色**。hub.jsx単体で全色登場（緑29・黄27・赤8・青7・紫6・桃2） | **アクセントが6色＝アクセントが無い** |

現状の`data/theme.js`は色8個＋フォント3種のみで、**余白スケールも型階層も存在しない**。
これが「各画面が好き勝手に0.5px刻みでサイズを決める」状態を許してきた根本原因。

### ③ 対象範囲

**全52画面**（マイライフ31／シーズン・共通21）＋シーズンhub内の17小分類。
ユーザー要望は「これまで実装したもの全てに関しても考え直したい」＝全画面が対象。

## 決定事項（2026-08・ユーザー選択済み）

| 論点 | 決定 |
|---|---|
| 絵文字を撤去した後の代替 | **原則廃止＋ごく少数だけ自作の線画**。天候など「記号のほうが一覧で速い」ものだけ、統一ストローク幅の単色SVGを自作する。それ以外は撤去し、階層は級数・余白・整列・罫線で作る |
| デザインの方向 | **計器盤とエディトリアルを使い分ける**。能力値・レース結果・順位表は等幅数字＋極細罫線の「計器盤」として、監督指示・ニュース・イベントは「読み物」として組む。**1つのトークン体系の中で役割を分ける**（別々のデザインを2つ作るのではない） |

さらにユーザー指示により、**CLAUDE.md §8を新設**して以下を恒久ルール化した。
- デザインは足し算ではなく引き算
- アイコンを使うのは逃げ
- 初見の人に通じる言葉だけを使う
- **サンプル候補提示 → 選択・編集 → 合意 → 実装**の手順を省略禁止

## 進め方

```
Phase 0  A-1：logic/support.js（1,844行）の分割
         ロジック層なのでUI改修と衝突しない。先に土台を整理する
Phase 1  デザイントークンの設計 ★サンプル候補を提示して選んでいただく
         型階層／余白スケール／ニュートラル／単一アクセント／罫線の太さ
Phase 2  マイライフのホーム画面だけを新トークンで作り直す ★候補提示→編集
Phase 3  合意が取れた形を全52画面へ横展開＋絵文字653個の撤去
```

**Phase 2で1画面だけ作って見ていただくのが要**。全画面を一気に変えると、方向が外れていた
場合の損失が大きすぎる。Phase 3はPhase 2の合意が取れるまで着手しない。

### Phase 0 の対象（`logic/support.js` 1,844行）

同階層に他ファイルが無く、`domain/`配下の最大300行・平均約100行と比べて突出している。
CLAUDE.md §5が名指しで警戒している「肥大化の受け皿」そのもの。
第11弾の`domain/shared/scouting.js`、第12弾の`domain/season/salary.js`と切り出してきた
流れの延長で分割する。分割の内訳はPhase 0着手時に確定する。

## Phase 1 の反復記録（候補提示 → 選択 → 編集）

CLAUDE.md §8の手順に従い、実際に目で見える候補を出して選んでいただく往復を重ねた。

### 1回目：方向性の三案（`design_candidates.html`）
計器盤／エディトリアル／紙面の三案を、同じ画面・同じスマホ幅で描き分けて提示。
→ **ユーザー選択：B案（計器盤）を土台に。** ただし「フォントと色味は変更の余地」
「あまりに簡素・直線的・画一的だと逆に手抜きで味気ない」との指摘。参考として
ポケモンチャンピオンズ／プロ野球ペナント／BMJP／ダビマスの4本を提示された。

### 2回目：作り込みと配色三案（`design_b_refine.html`）
**引き算の線引きをここで明文化**：「消しても意味が失われないか」で判定する。失われるなら
情報（＝足してよい）、失われないなら装飾（＝削る）。絵文字が逃げだったのは🏔が「高地合宿」に
意味を1文字も足していなかったからで、コース断面図は「登り続ける」を文章より速く伝えるので
足す価値がある、という基準。配色は計器シアン／マイヨ・ジョーヌ／クラシックの三案。
→ **ユーザー選択：マイヨ・ジョーヌ。** 併せて6点の修正指示。

### 3回目：ドット絵の主人公を投入（`design_v2.html`）
ユーザーからの問い「**現行のゲーム達やトップ層のソシャゲと比べて足りない部分はもう無いと
考えるか。繰り返し考えるべきではないか**」に対し、洗い直して7つの不足を特定した。
最大のものは**主人公が画面にいないこと**。前案は文字と数字と罫線だけで、誰の物語かが
消えていた。**既存のドット絵アセット（`pixelBike.jsx`の`normal_SE`、35×47マス）を
そのまま抽出してヒーロー領域に配置**。これは「アイコンは逃げ」と矛盾しない——絵文字は
意味を運ばない汎用記号だが、このゲームのために描かれた選手の絵は主役である。
→ **ユーザー評価：いい感じ。**

**この回で見つけた自分の不具合**：能力バーが`display:inline`のままで実測0×0、
完全に非表示だった。ユーザーに「見えていないが正常か？」と指摘されるまで気づかなかった。
以後、**公開前に必ずPlaywrightで描画して目視確認する**ことを手順に加えた。

### 4回目：点々体の検証と下部タブ（`design_v3.html`）
- **点々体（ピクセルフォント）の実証**：canvasに極小サイズで描いて平滑化なしで拡大する
  手法を実験。**日本語の漢字は12pxでも破綻**（「澪」「富」が判読不能）。画数の多い漢字は
  汎用フォントの縮小では形が保てず、美咲フォントやPixelMplusのような**専用設計の
  ビットマップフォントが必須**と判明。一方**数字は成立**するため、5×7マスの数字グリフを
  自作してSVG矩形で描画した（フォントファイル追加不要）。日本語も揃えるなら
  PixelMplus同梱が必要だが、単一HTMLインライン構成のため**ビルドが907KB→2MB前後**になる。
- ニュースは「世界」タブへ隔離、メニューは下部タブ5分類（ホーム／選手／世界／ショップ／記録）、
  サブ能力はレーダーチャート復活（メイン5角＋サブ4角）。
- **繰り返し考えた結果の追加提案3件**：①二つ名（`riderNickname()`は実装済みなのに
  未表示）②引退までの残り（マイライフの核心なのに残り時間の意識が無い）③次のレースの予告。
  ①のみプレビューに反映。

## 決定済み（Phase 2以降はこれを前提にする）

| 項目 | 決定 |
|---|---|
| 骨格 | B案（計器盤）。極細罫線のグリッド＋等幅・桁揃えの数値 |
| 配色 | マイヨ・ジョーヌ（地`#0E0E10`／面`#171719`／罫`#2C2C31`／文字`#F4F2EC`／アクセント`#F2C94C`単色） |
| 主人公 | ドット絵の選手をヒーロー領域に配置（既存アセットを流用） |
| 絵文字 | 全廃。653個は撤去対象 |
| 角丸 | 0px |
| 型階層 | 5段のみ（0.5px刻みは廃止） |
| ニュース | ホームから外し「世界」タブへ |
| メニュー | 下部タブ5分類 |
| サブ能力 | レーダーチャート（メイン5角＋サブ4角）で復活 |


## フォントの確定（2026-08）

### 採用：チェックポイント．（ピリオド）

ユーザーが「綜藝体」「チェックポイント．」を候補として挙げ、配布サイトが本環境の
egress許可リストに無いため取得できず（`Host not in allowlist: yokutobanaitori.web.fc2.com`）、
**ユーザーが手元でダウンロードしてアップロードする形で入手**した。

| ファイル | 判定 |
|---|---|
| `cp_period.ttf`（本体） | **採用。**14,964字収録（うち漢字12,204字） |
| `Append_cp_period.ttf` | 不使用。ほぼ空で、本体に無い字を補う追加パーツ |
| `Scream_cp_period.ttf` | 不使用。一部の字のみの派生版 |

比較対象のDotGothic16（Fontworks・SIL OFL・GitHubから取得可）も検証したが、
**チェックポイント．の方が太くドット絵と粒度が揃い、かつ軽い**ため採用。

### ライセンス（確認済み・`src/assets/fonts/LICENSE-CheckpointPeriod.txt`に全文を保存）

作者：マルセ / プロジェクトU-Fo（よく飛ばない鳥）。**CC BY-ND 相当。**

- **商用利用・ゲームへの組み込みとも可**（営利/非営利ともに用途制限なし）
- **「Webフォントとしてのご利用には制約を設けておりませんので、サブセット化やWOFFコンバートは
  自由となります」と明記**——本弾でやるサブセット化が名指しで許可されている
- 禁止：①字形あるいは著作権表示を**改変しての**二次配布 ②このフォントで作ったロゴの商標登録
- クレジット表記は**任意**（「可能な場合」であり必須ではない、と明記）

**対処**：サブセット時に`--name-IDs='*' --name-legacy --name-languages='*'`を付け、
日本語の著作権表示（「2010-2026 マルセ / プロジェクトU-Fo」）が落ちないようにした。
初回のサブセットでは英語版だけが残り日本語版が消えていたため、禁止事項の
「著作権表示を改変して配布」に触れないよう作り直した。
**クレジット4項目（フォント名／マルセ／よく飛ばない鳥／URL）をヘルプ画面へ載せる**ことでユーザー合意。

### サイズの実測（3案）

| 収録範囲 | 文字数 | woff2 | ビルド合計（現907KB＋） |
|---|---|---|---|
| `CheckpointPeriod-min.woff2` ゲーム内で使う文字のみ | 2,233 | **64KB** | 約971KB |
| `CheckpointPeriod-jis.woff2` JIS第1・第2水準（cp932全域）∪ゲーム使用文字 | 9,577 | **227KB** | 約1.13MB |
| （参考）全収録をwoff2化しただけ | 14,964 | 547KB | 約1.45MB |

### 文字の網羅性（実測）

ゲームが自動生成する名前は**すべて収録済み**（選手の名字243字種・名前155字種・チーム名98/37字種・
国内会場26字種・海外会場25字種・地域6字種、いずれも未収録ゼロ）。
ソース全体で未収録は161字あるが、**その大半は絵文字**（本弾で全廃予定）。
残りはコメント内に紛れた誤入力（ハングル「강호」等）で画面には出ない。

## ドット絵の白い縁取り（原因特定・修正済み）

ユーザー指摘「選手のドット絵の周りに不自然に白い縁取りが見える」。**推測どおり抽出の問題**だった。

**原因**：`pixelBike.jsx`のデータは参考画像から「面積平均(BOX)で縮小→パレットへ最近傍写像」で
抽出したため、**元画像の輪郭のアンチエイリアスが中間グレーのドットとして混入**していた。
`normal_SE`で実測すると**輪郭236ピクセル中52個（22%）が中間グレー`M`(#707070)**で、
本来の輪郭`K`(#161616)はわずか37個。暗い背景では目立たないが明るい背景で白い縁に見える。

**修正方針（ユーザー選択：B）→ 実装済み**：透明ピクセルに隣接する`M`/`R`/`W`を`K`（黒）へ置換する。
- 案C（該当ピクセルを透明化）は輪郭が痩せ、車輪が細く貧弱になるため不採用。
- 手動で作った参照実装（`M`は4方向隣接に透明があれば`K`化）とロー・列で1件の差分もなく完全一致
  することを確認したうえで、同じ規則（4連結隣接に`.`があるM/R/Wを`K`へ）を`BIKE`/`BIKE_B`両テーブル・
  全6ポーズ×2＝12コマへスクリプトで一括適用（417セル変更）。過去にhandedness不具合が長期化した
  `dancing_NE`も含め、明るい背景で目視確認して白い縁取りが解消したことを確認。
- `pixelPerson.jsx`は別の抽出方式（参考ドット絵から直接抽出、輪郭`K`込み）で、そもそも`M`/`R`/`W`の
  ような中間グレーの縁取り文字がlegendに存在しない。**同じ不具合は無し**（確認済み・修正不要）。

## ユーザー入力の文字制限（実装済み）

**入力箇所は4つ**（ユーザー指摘で1箇所訂正——マイライフの選手名も改名可能だった）：
- `components/chrome.jsx` 汎用の改名モーダル`RenameModal`（シーズンの選手名／マイライフの選手名の2箇所がここを共有）
- `screens/season/hub/misc.jsx` チーム名の変更（`maxLength=16`）
- `screens/season/intro.jsx` 開始時のチーム名（`maxLength=16`）

共通の判定関数を1つ作り、上記すべてから呼ぶ形で実装した。

**実装**：
- `src/data/allowedChars.js` — 埋め込みフォント`CheckpointPeriod-jis.woff2`の実際のcmapから
  fontTools経由で機械生成（7,532字）。手計算のJIS範囲ではなく実際に埋め込んだwoff2そのものを
  正とすることで、判定とレンダリング可否のズレを構造的に防止。
- `src/domain/shared/textInput.js` — `findUnsupportedChars()`/`isNameSupported()`の純関数のみ。
  半角/全角スペースは常に許可。
- `RenameModal`（`chrome.jsx`）はリアルタイムに不可文字を検出し、赤枠＋「「◯◯」は使えません」の
  インライン警告＋決定ボタンのdisableで弾く（ユーザー選択：案C）。`maxLength`は呼び出し元ごとに
  可変化（`openRename(title, current, onCommit, maxLength=12)`）——チーム名は16、選手名は既定の12。
- `intro.jsx`の開始時チーム名も同じ判定関数で同様のインライン警告＋開始ボタンdisable。
- `misc.jsx`のチーム名変更は、独自のライブ入力欄を廃止し既存の`openRename`共有モーダルを呼ぶ形に
  統一（このコードベースの`screens/`は`useState`を持たずctx経由で状態を扱う規約のため、重複実装を
  避けた）。

**検証**（Playwrightで実際のゲーム操作を通して確認）：チーム名変更モーダルの`maxlength`属性が16、
16文字の入力がそのまま通ること、絵文字を含む入力で警告文が出て決定ボタンがdisabledになることを確認。
既存の選手名変更（シーズン・マイライフ両方）は`maxLength`既定値12のまま無変更で動作。

## フォントクレジット表記（実装済み）

ライセンスで「必須ではないが可能なら」と定められた4項目（フォント名／作者名／サイト名／サイトURL）を、
シーズン・マイライフ両方のヘルプ画面末尾に「使用フォントについて」の小さな1行として追加。
`screens/season/hub/help.jsx`・`screens/mylife/help.jsx`の2箇所に配置し、Playwrightで両画面とも
表示を確認済み。

## 決着済み（4〜6回目の往復で確定）

| 論点 | 結論 |
|---|---|
| 点々体（数字だけ／日本語も） | **日本語ごとドットにする。**専用フォント（チェックポイント．）を導入したため、自作5×7グリフは不要になった |
| フォント同梱のコスト | 当初「2MB前後」と見積もったが**過大だった**。サブセットで64〜227KB |
| 下部タブ5分類 | **承認**（ホーム／選手／世界／ショップ／記録） |
| 追加提案1・2・3 | **3件とも採用**（二つ名／引退までの残り／次のレース予告2ヶ月分） |
| ドット絵の縁取り | **案B（輪郭の中間グレーを黒へ置換）を採用** |
| クレジット表記 | **ヘルプ画面に4項目を載せる**（任意だが載せる） |
| フォントのサイズ | **227KB（JIS第1・第2水準・中サイズ）**を採用。`CheckpointPeriod-jis.woff2`として同梱済み |
| 入力制限の弾き方 | **案C：リアルタイムの赤枠＋インライン警告＋決定ボタンdisable。実装・検証済み** |

## Phase 1: デザイントークンの確定と実体化（完了）

CLAUDE.md §8の手順（候補提示 → 選択 → 合意 → 実装）に従い、3案を実際にレンダリングして提示した。

**提示した3案**：A案「計器盤」（見出しもドット、4pxグリッドで高密度）／B案「エディトリアル」
（ドットは数字のみ、余白広め）／C案「ハイコントラスト・ミニマル」（枠線を使わず面の濃淡だけで区切る）。

**ユーザーの選択**：**C案の処理方針**（枠線なし・面の濃淡で区切る）＋**全要素をドットフォントに統一**。

**この時に起きた手戻り（記録として残す）**：3案を作る際、本ファイルの§「決定済み」に既にあった
土台（配色＝マイヨ・ジョーヌ／角丸0px／型階層5段）を参照せずに、色・角丸・段数を独自に作り直して
提示してしまった。ユーザーの確認により、**C案の新規部分（面の濃淡・全ドット化）だけを採用し、
色・角丸・段数は既存の決定に合わせて作り直す**方針で再提示した。
→ **教訓：デザイン案を出す前に、まず本ファイルの「決定済み」を読むこと。**

### フォントの実測（重要・一度誤った値を出したので経緯ごと残す）

| 対象 | 実測値（font-size に対する比） |
|---|---|
| 和文（かな・漢字） | **1em ちょうど**（`国`『字』『漢』『鬱』『一』すべて同幅） |
| 数字 0〜9 | **0.75em**。**10字すべて完全に同幅**＝桁が必ず揃う |
| ラテン文字 | **プロポーショナル**（A=0.75em／i=0.25em／W=0.875em）。桁揃えの対象外 |
| 漢字の可読下限 | **12px**。9〜11pxでは潰れて判読できない（目視確認） |

**注意**：最初の計測で数字を「0.5em」と誤って報告した。原因は`document.fonts.ready`を待たずに
測定したため、**Webフォント適用前の代替フォントの値を拾っていた**こと。ブラウザで幅を実測する時は
必ず`await document.fonts.load(...)`＋`await document.fonts.ready`を挟むこと。
なお設計の根拠にしていた性質（数字が全て同幅＝桁が揃う）自体は正しく、計器盤の表組みは成立する。

### 確定したトークン（`src/data/theme.js` の `T` / `FONT_DOT`）

| 分類 | 値 |
|---|---|
| 地 | `#0E0E10` |
| 面 | `#171719`（パネル。**外周に枠線を引かない**） |
| 面・上 | `#1E1E21`（パネルの中に置く入れ子の面） |
| 罫 | `#2C2C31`（**表の行区切り専用**。パネルの輪郭には使わない） |
| 文字 | `#F4F2EC` |
| 副文字 | `#8C8A82`（**今回追加**。ラベル・単位・補足。全部を文字色にすると階層が作れないため） |
| アクセント | `#F2C94C`（数値の強調と主ボタン。UIの装飾はこの1色だけ） |
| 好 / 不 | `#7FB069` / `#C0554D`（**今回追加**。勝敗・増減など結果の良し悪しにのみ使う） |
| 型階層 | **28 / 20 / 16 / 14 / 12px の5段**。すべて偶数pxでドットの格子を崩さない。0.5px刻み禁止 |
| 余白 | 4 / 8 / 12 / 16 / 24px（4の倍数のみ） |
| 角丸 | **0px** |

**「極細罫線のグリッド」と「枠線なし」の統合**：既存決定（B案・計器盤）は罫線のグリッド、今回選ばれた
C案は枠線なし、で一見矛盾する。**パネルの外周には罫線を引かず面の濃淡だけで区切り、罫線は表の行区切り
だけに使う**形で統合した。

### 実装（`463bfb7`）

- `src/styles/fonts.css` 新設——`@font-face`で`CheckpointPeriod-jis.woff2`を宣言。`main.jsx`からimport。
  `vite.config.js`の`assetsInlineLimit`により**ビルド時にbase64で単一HTMLへインライン化**される
  （単一ファイル配布のため外部フォント取得を前提にできない）。実測でビルドは931KB→**1,243KB**
  （+311KB＝227KBのwoff2をbase64化した分）。
- `src/data/theme.js` に `T`（色・型階層・余白・角丸）と `FONT_DOT` を追加。
  **旧トークン（`C`/`FONT_D`/`FONT_B`/`FONT_M`）は無変更で残す**——Phase 2以降で画面を1つずつ
  移行し、全画面が終わった時点で旧トークンを削除する。移行期間中は両方が同居するが、
  **新規のUIは必ず`T`を使う**。
- **この段階では画面の見た目は一切変えていない**。Phase 1はトークンを使える状態にするまでが範囲で、
  実際の適用はPhase 2（マイライフのホーム1画面で方向確認）から。

**検証**：ビルド成功。Playwrightで`document.fonts`が`status:"loaded"`になること・
`document.fonts.check('20px CheckpointPeriod')`が`true`になることを確認。上表の字幅もこの実機上で実測。
タイトル／マイライフ作成／シーズン導入の各画面がコンソールエラー無しで従来どおり表示されることを確認。

## Phase 2: マイライフのホーム画面を作り直す（完了・`dcabcb9`）

**手順**：CLAUDE.md §8に従い候補提示から入った。まず「勾配（コース断面図）はホームに要るか」を
引き算の基準（消しても意味が失われないなら装飾、失われるなら情報）で検討し、実データ（`tmpl.segs`）
から断面図を描く案を提示 → **採用**（平坦なレースでも枠は必ず確保する、とユーザー指示）。

**実装前に判明した規模のずれ**：実際の`mylife_main`（593行）は、これまでの提示モックアップより
遥かに情報量が多かった（作戦選択・練習フォーカス・成長レポート・弟子・世界ランキング＆アンビション・
個人スポンサー・ライバル対戦成績・サブメニュー8個・キャリア管理、計10種類以上）。モックアップ通りに
作ると機能が消えるため、進め方をユーザーに確認：**「最小限だけ残し、残りは仮の場所へ退避」**を選択
（作戦・練習フォーカス・成長レポート・弟子・ランキング等は「その他」の折りたたみへ機能そのまま移動、
色・トークンは旧仕様のまま。世界ランキング・ライバル等の行き先の再設計はPhase3で選手/世界タブが
決まってから）。

**新設**：
- `components/RiderPortrait.jsx` — 既存の`PixelBike`（レース描画用）を静止画1コマとして使う
  薄いラッパー。新規のドット絵は描いていない。
- `components/CourseProfile.jsx` — `data/course.js`の`tmpl.segs`（実データ）からコース断面図を
  描く純表示コンポーネント。論理座標100幅+`viewBox`で親要素の実幅に追従（呼び出し側でpxを
  推測しなくてよい）。平坦なレース（`maxEle<=0`）でも高さは必ず確保する。

**新ホーム画面の構成**（新トークン`T`/`FONT_DOT`）：選手のドット絵＋名前＋総合力のヒーロー →
疲労/フォーム/活力バー → 今月のレース（断面図・種目・有利脚質・天候） → 今月の行動（推奨1つを
塗り＋他の選択肢を並べる、旧デザインの「黄塗り＝おすすめ」の考え方を踏襲） → 監督から、の5ブロック。
絵文字は一切使っていない（改名ボタンも✏️→「変更」に置き換え）。

**検証**：ビルド成功。Playwrightで実際に操作——新ホーム画面の表示・「その他」を開いての
全項目表示（作戦/練習/成長/新聞/弟子/ランキング/スポンサー/ライバルいずれも表示確認）・
実績や選手成績など既存サブ画面への遷移・「完全休養」での月送り（4月→5月、疲労23→2に反映）・
たまたま次の月がモニュメントレース（石畳の古典）に当たり実データの起伏（登り基調→山頂）が
断面図に正しく反映されることを確認。いずれもコンソールエラー無し。

## 未確定（次の往復で決める）

- 自作する線画SVGの上限数（天候3種程度を想定）

（「その他」退避内容の行き先はPhase 3-A・3-Bで解決済み。下記参照）

## 手つかず（Phase 3-C以降）

動きと手応え（タップ反応・カウントアップ・順位変動の演出）／音（効果音・BGMは現状ゼロ）／
**RaceView.jsx本体**（1558行・最も「熱」が要る画面・Phase 3-Eで単独の弾として着手予定）／
シーズンのダビマス式メニュー配置／マイライフ残り4画面（career/events/create/help）／
シーズン21画面（旧デザインのまま）。

## 選手ドット絵の顔（目・口）修正（実装済み）

**背景**：ユーザー指摘「顔がはっきりしてない。左右の目の大きさが違う、目と口の形が不明瞭、輪郭が
ぼやけてる」。原因は元の抽出パイプラインに目・口専用の色が無く、肌の陰影(`s`)や中間グレー(`M`)の
ムラで代用されていたため、左右非対称でぼやけた印象になっていたこと。

**対応**：`bikeLegend()`に新色2つを追加——`e: "#3a2418"`（目・落ち着いた濃茶。純黒`#1a1006`は
「黒すぎ」で不採用）、`m: "#b06a52"`（口・肌寄りローズ、3案中「案C」を採用）。前面(SE方向)の
6コマ全て（`BIKE`/`BIKE_B` × `normal_SE`/`dancing_SE`/`sprint_SE`）で目・口を専用色に置き換え、
周辺の残存M/D/H斑を肌色(`S`)へ均した。NE方向は横顔中心で左右対称な顔が写っていないため対象外。

**作業手順（途中で確立し、以降すべてのコマで厳守）**：独断で座標を推測せず、①番号付きグリッド画像を
先に提示 → ②ユーザーが行/列で正確な位置を指定（またはユーザー手描きの色分け注釈画像：紫=帽子外枠→K、
ピンク=顔外枠→H等、赤=目専用色、緑=口専用色）→ ③その通りに反映 → ④レンダリングして確認、を1コマずつ
繰り返す。一度、独断で`BIKE.dancing_SE`/`sprint_SE`に手を入れて「顔面が完全に崩れる」失敗をし、
以降この手順を徹底することで解決した。

**具体的な学び**：
- 目は縦2px・口は横2px幅で統一。
- 顔の内側（ピンクで囲われた輪郭の内側）は基本すべて肌色に統一するが、**後ろ髪・耳の部分
  （輪郭より外側の列）は誤って肌色化しないよう注意**——一度全部フラット化してしまい
  「後ろ髪や耳が消えた」と指摘され、該当列のみ復元した。
- 単一の1色で塗りつぶすと「耳・後ろ髪の質感が無い」と感じられるため、確定済みコマ
  （`BIKE.normal_SE`の`...HSeSSeSsHHHD..`のような、肌→肌陰(`s`)→髪(`H`)→輪郭(`D`)と段階的に
  変化する並び）を参考に、輪郭付近は`H`/`D`/`s`を混ぜて段差をつける。
- 抽出済みシルエットの外側に意図せずはみ出た1マスは、元データとの差分（`'.'→非'.'`の変化箇所）を
  取ると機械的に発見できる（`BIKE_B.dancing_SE`の6行目20列目、`BIKE_B.sprint_SE`の5行目7列目で
  この方法により発見・修正）。
- 座標のやり取りは「◯行目◯列目」の日本語表記から、途中で**列をA/B/C…・行を0,1,2…**とする
  スプレッドシート形式（例：`B14`）に切り替えた。ユーザーからの依頼により今後の全コマ作業も
  この形式を使う。

**確認・反映済み**：`BIKE.normal_SE`（`e592cba`）／`BIKE.dancing_SE`（`bb31a94`）／
`BIKE.sprint_SE`（`d4d1b31`）／`BIKE_B.normal_SE`（`eb4bda6`）／`BIKE_B.dancing_SE`（`188a9c8`）／
`BIKE_B.sprint_SE`（`6f941b0`）。対象6コマすべて完了。

## Phase 0: `support.js`分割（完了・`0d14999`）

**背景**：`src/logic/support.js`が1844行・100件超のexportを抱える巨大ファイルになっていた。
CLAUDE.md §5が定める`data → core/sim → domain/state/view → controllers → screens → app`という
依存階層のどこにも属さない孤立したディレクトリ（`logic/`）に、成長計算・CP・血統・スタッフ・
イベント効果・ライバル演出・実績・育成上限・世界ランク・キャリア年表・野望・セーブ入出力・
`buildSim`という性質の異なる十数種類の機能が同居していた。呼び出し元は37ファイル
（`controllers/`・`domain/`・`screens/`・`data/`・`components/`）。

**分割案**（ユーザー合意済み）：

| 新ファイル | 中身 |
|---|---|
| `domain/shared/growth.js` | 成長計算（growthFactor/softFactor/breakthroughMul/growthPhase/potentialHint/disciplineScore/riderAptitudes/aptGrade/t_label等） |
| `domain/mylife/cp.js` | CP・アビリティ習得（CP_MILESTONES/applyCpMilestones/loadAbilityFile等） |
| `domain/shared/courseRecords.js` | コースレコードの保存・照合 |
| `domain/mylife/lineage.js` | 血統表・配合表記・因子図鑑 |
| `domain/season/staff.js` | スタッフ名・効果・給与 |
| `domain/season/events.js` | イベント効果適用・性格イベント |
| `domain/season/rival.js` | ライバルの熱量・セリフ生成・殿堂判定 |
| `domain/season/achievements.js` | 人生の岐路・オフシーズン選択・シーズン実績・指令 |
| `domain/mylife/growthCap.js` | 育成上限・私生活イベント抽選・生活費 |
| `domain/mylife/worldRank.js` | 世界ランキング・選手成績台帳・メディア記事 |
| `domain/mylife/career.js` | キャリア年表・弟子・素質ランク・エピローグ |
| `domain/mylife/ambition.js` | 野望パス |
| `state/saveGame.js` | セーブ有無判定・削除（localStorage I/O） |
| `sim/buildSim.js` | `buildSim()`・`groupModeFor`・`raceIsHome`・`teamChemistryTier` |

**実装時に判明した層の制約（計画時点の想定から1点調整）**：`teamChemistryTier`は元々
`SEASON_ACHIEVEMENTS`のchemistry_max判定にも`buildSim`にも使われていたが、sim層はdomain層に
依存できない（data→core/sim→domain の一方通行）ため、`domain/season/achievements.js`に
置くとbuildSim側から参照できず層違反になる。`groupModeFor`・`raceIsHome`と合わせて
`sim/buildSim.js`側に置き、`achievements.js`・`rival.js`はそこからimportする形に変更した
（domain→simの参照は一方通行と整合するため問題なし）。

**安全策**：`support.js`自体は互換シムとして残し、新ファイルからre-exportするだけにした。
37箇所の既存import文は無変更で動く。

**検証**：
- ロジックは全関数本体を無編集で移送（式・分岐・コメント含め一字一句変更なし）。
- ビルド出力サイズが移送前後で一致（931.17kB→931.16kB）。
- Node上で`logic/support.js`の全175 export（既存のdata/*再エクスポート含む）を読み込み、
  undefinedが無いことを確認。`buildSim`・`teamChemistryTier`・`groupModeFor`・`raceIsHome`・
  `mlUpdateRiderStats`・`mlWorldBoard`・`SEASON_ACHIEVEMENTS`のchemistry_max判定など、
  分割ファイル間を跨ぐ依存（rival.js↔worldRank.js、growth.js↔growthCap.js↔achievements.js↔
  sim/buildSim.js）を個別に呼び出して正しく動作することを確認。
- Playwrightでシーズンモード・マイライフモードの両方を実際に操作して検証：マイライフは
  デビュー〜ハブ画面〜実績／チーム名鑑／選手成績／キャリアグラフ／特殊能力図鑑／
  コースレコードの全画面遷移、シーズンは開幕〜レースカレンダー〜出走表（`buildSim`を
  実際に呼び出すプレビュー機能）まで、いずれもコンソールエラー無しで動作を確認。

## Phase 3-A: 下部タブ5分類の新設（完了・`33b9ae7`）

**内容**：マイライフの画面遷移を「ホーム／選手／世界／ショップ／記録」の5タブへ集約。
`components/BottomTabs.jsx`を新設し、`TAB_MEMBERS`で各サブ画面を親タブへマッピング、
`TAB_HIDDEN_SCREENS`（chrome.jsx）でキャラ作成・レース中・イベント中はタブを隠す。
Phase 2で「その他」へ仮置きしていた内容の行き先をここで決定：
- `screens/mylife/rider.jsx`（新設）— 能力・コース適性・素質・性格と成長・特殊能力・経歴。
  レーダーチャート（`RadarChart.jsx`）は形はそのまま、フォント・色だけ新トークンへ寄せた
  （ユーザー指示「レーダーのままフォントのみ変更」・単色アクセントへ統一）。
- `screens/mylife/world.jsx`（新設）— アンビション進捗・世界ランキング表・好敵手・メディア記事。
- `screens/mylife/archive.jsx`（新設）— 実績・記録・殿堂・系譜・因子・特殊能力図鑑への索引。
- ホーム画面（`hub.jsx`）に残したのは「今月の決断」系（作戦・練習フォーカス・成長レポート）のみ。
  8ボタンのメニューグリッドは撤去（タブに代替されたため）。

**検証で見つけた不具合**：世界タブで初レース前に`ml.worldRank`がnullのままヘッダーとランキング表の
順位が食い違う（`board.myRank`へフォールバックして解消）、選手タブで「伸びしろ中」の重複表示
（`potentialHint().label`が既に「伸びしろ」を含んでいたため`.replace()`で剥がす）。

## Phase 3-B: マイライフ「レース」画面の作り直し（進行中・race.jsx完了）

**設計プロセスの変更**：この弾の着手時にユーザーから「設計は極めて具体化することを前提に、
ルールにも明記して」と明示され、CLAUDE.md §8へ「設計の具体性」節を追加した（要素の全列挙・
データ取得元の明記・レイアウト順序・空状態の定義・実データでのモック作成を候補提示前に必須化）。

**スコープの切り分け**：`race.jsx`は6状態（出走表／LIVE中継／結果・通常／結果・チームTT／
ライバル対話／号外）を持つ最も情報量の多い画面。うち出走表の中身（`StartListPanel`）はseason側
と共有のためPhase 3-D（共有コンポーネント）の担当、LIVE中継の中身（`RaceView.jsx`本体・1558行）
は最も「熱」が要る画面としてPhase 3-Eで単独着手予定——今回はどちらも中身を据え置き、外枠だけを
新トークンで作り直した。

**設計の反復**：
1. 結果・通常画面の候補をA(数字主役)/B(見出し主役)の2案で提示 → ユーザーがA選択。
2. 「全体的なフォントサイズを調整するもしくは改行位置を調整して見やすくして」との指摘 →
   長い値を無理に1行の右カラムへ詰めていたのが原因と判断し、「見出し＋短い値（必ず1行）」＋
   「補足説明は下段にキャプションサイズで分離」という2段構成へ変更。あわせて、順位数字が
   独自に48pxを使っていた（§8の「型階層は5段のみ」違反）のも発見・是正（`T.size.display`の
   28pxへ統一）。
3. 「順位表は開けるようにもしといて欲しい」との指摘 → 全順位表を既定で閉じ（`ml.resultTableOpen`）、
   見出し行タップで開く形に変更（モックアップ上でも実際にクリックで開閉することを確認してから提示）。
4. 残り4状態（出走表・LIVE枠・チームTT結果・ライバル対話・号外）を実データ（`directives.js`・
   `rival.js`・`view/news.js`・`course.js`の実際の文言）で作成して提示 → 承認。
5. ライバル対話の返答2択の色分け——単色アクセントの原則と、`tone`（fire/calm）による性格差を
   両立させる方法をユーザーに確認。「片方を控えめな面色にする」（fireのみアクセント、
   それ以外は`surfaceUp`背景の控えめなボタン）で決着。

**実装（`src/screens/mylife/race.jsx`全面書き換え）**：
- 共通ヘルパー：`Section`（見出し+面）、`Item`（見出し語+短い値+任意の補足行）、`PrimaryBtn`/
  `QuietBtn`、`ResultHero`（順位ヒーロー、rank===1のみgoodで強調・他は色分けしない）、
  `Bubble`（吹き出し・話者は面の濃淡と寄せのみで区別、色は使わない）、`RivalBlock`（好敵手の
  結果+対話への導線を一本化。`scene`ありは専用画面への導線のみ、`scene`なし`dialogue`ありは
  その場でVN風に表示——という元のロジックをそのまま踏襲）。
- `ML_MILESTONE_LABEL`（`useMyLifeGame.js`）から絵文字を除去（"🌍 世界選手権"→"世界選手権"）。
  この定数はrace.jsxからしか参照されないため、影響範囲はこの弾のみ。
- 号外（新聞）画面は紙面という別世界の見た目を意図的に維持（新聞であることが構造で伝わるため）が、
  `np.accent`（イベント種別ごとに変わる6色）は使わず、単色アクセントの原則をここにも適用。

**検証**：ビルド成功。絵文字カウント58→7（残る7個は`→`/`★`/`●`——ボタンの遷移矢印・グレード表示・
選手マーカーの凡例で、他画面（world.jsx/rider.jsx）でも同じ用途で使われている記号であり、
CLAUDE.md §8が対象とする装飾絵文字ではない）。Playwrightで実際にキャラ作成→出走→スキップ→結果→
ライバル対話（回答前後）→号外→翌月、の一連を実データで動かし、6状態中5状態（出走表・LIVE枠・
結果・通常・ライバル対話の前後・号外）をコンソールエラー0・NaN/undefined無しで確認。チームTT結果
画面はCP30アンロック条件のため今回のPlaywright走行では実際には遭遇できておらず、コードレビュー
（個人結果と同じ`Section`/`Item`ヘルパーを再利用）のみで担保——実プレイでの確認は今後の課題。

## Phase 3-C: マイライフ`career.jsx`8画面の作り直し（完了）

**設計プロセス**：3-C-1（引退勧告・チーム名鑑・キャリアグラフ・世界ランキング詳細）と3-C-2
（引退セレモニー・殿堂）の2ラウンドに分けて候補提示。`mylife_ranking`が下部タブ新設（Phase 3-A）
で導線を失い到達不能になっていたのを発見・`world.jsx`へ「ランキングをすべて見る」を追加して復活。

**引退セレモニーへのユーザー指摘と対応**：候補提示後、ユーザーから「内容が変だし面白くなさげ。
かなりの間アプデされてないのでシンプルに不要なものとか追加すべきものがある気がする」との指摘。
実コードを再点検して以下を特定・修正した。
- **重複の削除**：通算成績の表（118戦22勝・表彰台41回）と「生涯」要約文が同じ数字を繰り返して
  いた→要約文を撤去（殿堂カード側の`riderCareerSummary`は維持）。称号が二つ名（`riderNickname`）
  と生き様（`mlCareerArchetype`）で2種類あり紛らわしい→ヒーローは生き様のみに統一。
- **計算済みなのに未表示だったデータの追加**：`computeAchievements()`で計算済みの実績14件
  （`ML_ACHIEVEMENTS`）が引退画面に一切出ていなかった→「成し遂げたこと」として掲載（未達成は
  折りたたみ）。到達クラス・最高総合力・最高世界ランク（`careerHistory`から算出可能）も追加。
- **「監督」の意味が2つあって紛らわしい問題**：エピローグ選択の「監督としてチームに残る」
  （文章が変わるだけ）と、実際にシーズンモードへ遷移する「監督として新チームを率いる」ボタンが
  並んでいた→エピローグの選択制を廃止し、弟子の有無で自動生成（`useMyLifeGame.js`のCP付与
  useEffectに統合、CP/殿堂登録と同じタイミングで一度だけ確定）。
- **勝利専用だった年表への挫折の追加**：`mlCareerTimeline()`（`domain/mylife/career.js`）が
  優勝・表彰台しか拾わない仕様だったため、大舞台での二桁着順（rank≥11）と3年以上の勝利空白を
  挫折として拾うロジックを追加。あわせてiconフィールドを撤去（絵文字ゼロの原則。表示側は
  `down`フラグの有無で文字色を分ける：通常＝`T.color.text`、挫折＝`T.color.sub`）。

**実装（`src/screens/mylife/career.jsx`全面書き換え・404行→560行程度）**：
- `race.jsx`と同じ`Section`/`Item`/`PrimaryBtn`/`QuietBtn`パターンをこのファイル内に再定義
  （画面ファイルごとに小さなヘルパーを持つ既存の慣習を踏襲。共有化はPhase 3-Dの判断）。
- 殿堂カードは1件16要素が常時全開だったのを折りたたみ式に変更（`ml.expandedLegend`を「どの
  カードが開いているか」のトグルへ転用。血統ツリーの親子表示は開いたカード内へ統合し、
  別トグルを増やさなかった）。
- `mylife_lineage`/`mylife_factors`（`dynasty.jsx`＝`meta.jsx`と共有）は中身を据え置き。

**検証**：ビルド成功。絵文字カウント`career.jsx`46→0、`domain/mylife/career.js`10→3（残る3個は
`protegeMilestoneNews`という今回対象外の別関数内・据え置き）。Playwrightで実データに近い合成
セーブ（12年分のraceLog・rivalRecord・protege・careerHistory等）を`roadrace_v12_mylife_save`へ
注入し「続きから」でロードする手法で、8画面全て（引退勧告・引退セレモニー・実績折りたたみ・
殿堂カードの折りたたみ/展開・キャリアグラフ・世界ランキング詳細・チーム名鑑）を実データで動かし、
コンソールエラー0・NaN/undefined無しを確認。挫折年表（大舞台二桁着順・3年勝利空白）・自伝3択・
エピローグ自動生成・好敵手フォールバック表示（ランキング表に載らない順位の好敵手/ライバル）も
すべて意図通りの分岐で表示されることを確認済み。

## Phase 3-D: 共有部品の新トークン化＋マイライフ残り4画面（完了）

**設計の経緯**：当初「共有`ui.jsx`/`panels.jsx`は据え置き、season側との共有を理由に触らない」と
提案したところユーザーから「再質問して」と差し戻された。実測せずに下した判断だったため、
season側のAI臭さの発生源を`components/`と`screens/season/`で分けて実測（色/角丸/枠線/絵文字の
出現箇所を集計）→ season側の装飾密度の85〜90%は`screens/season/`自身のインライン`style`が
原因で、共有部品の寄与は10〜15%と判明。この実測を踏まえ再度AskUserQuestionで提示し、
以下が決定された：①共有`ui.jsx`/`panels.jsx`は今すぐ着手する ②`career/race/rider/world.jsx`に
4つに分岐していた`Section`等を`mlUi.jsx`へ集約し、同じラウンドでマイライフ残り4画面
（`hub.jsx`の旧5画面・`events.jsx`・`create.jsx`・`help.jsx`）も変換する ③装飾色は紫のみ例外で
アクセントに残す ④スコープが大きいのでD-1/D-2/D-3の3ラウンドへ分割する。

**D-1（`src/components/ui.jsx`）**：`Btn`/`Eyebrow`の呼び出し側231箇所（Btn111・Eyebrow120）は
無改修のまま、`color`プロップの写像シム（`COLOR_MAP`/`mapColor()`）を追加。旧`C.yellow`→
`T.color.accent`、`C.purple`→`T.color.accent`（紫のみの例外）、`C.green`→`good`、`C.red`→
`bad`、それ以外（青・桃・橙・sub等）はすべて`T.color.sub`へ収束。形状（角丸・枠線・フォント）は
このラウンドでは変えない（season側の装飾密度の主因ではないと実測済みのため）。

**D-2（`src/components/mlUi.jsx`新設＋マイライフ残り4画面）**：
- `mlUi.jsx`：`career/race/rider/world.jsx`にファイルごとに再定義されていた`Section`/`Item`/
  `PrimaryBtn`/`QuietBtn`/`Prose`/`Screen`を1箇所へ統合。`Section`のpadding差異（career/raceは
  `0 mdpx`の非paddedスタイル、rider/worldは`md`全周のpaddedスタイル）は`padded`propで両立。
  加えて`create.jsx`の選択式UI（脚質・経歴・難易度等）向けに`SelectRow`を新設。
- `career/race/rider/world.jsx`は各ファイルのローカル定義を削除し`mlUi.jsx`からimportするよう
  差し替え（race.jsxは同一パターンの`Screen`相当インラインdivが6箇所あり、すべて`<Screen>`に
  統一）。
- `hub.jsx`の旧5画面（実績・特殊能力図鑑・選手成績・全チーム名鑑・タイトル/コースレコード）を
  新トークンへ変換。あわせて「戻る」導線のバグを修正——下部タブ新設（Phase 3-A）後もこれらの
  画面は一律`mylife_main`へ戻る旧仕様のままで、タブの現在地表示（`BottomTabs.jsx`の
  `TAB_MEMBERS`）と食い違っていた。実績/特殊能力図鑑/コースレコードは`mylife_archive`（記録タブ）
  へ、選手成績/全チーム名鑑は`mylife_world`（世界タブ）へ戻すよう修正。
- `events.jsx`（ショップ9カテゴリ＋できごと/弟子イベント/オフシーズン/人生の岐路/契約の8画面）：
  `Item`では表現できない「購入ボタン付き行」用にファイルローカルの`ShopRow`/`ShopBtn`を新設。
  複数行の生成テキスト（`\n`区切りの`eventResultText`等）は`Prose`だと改行が潰れるため
  `whiteSpace:pre-wrap`を明示したファイルローカルの`ResultText`で対応（`Prose`本体は変更せず、
  他画面での挙動に影響を与えない）。
- `create.jsx`（キャラ作成＋配合プレビュー＋素質診断）：脚質/経歴/難易度/師匠/配合相手の選択UIを
  すべて`SelectRow`へ統一。配合の相性パネル（爆発力・配合評価・危険度・配合相性・血統ボーナス・
  受け継ぐ特能・金の特殊能力・配合限定特能・継承する系統・血の格）は情報を一切削らず`Item`の
  縦積みへ再構成し、旧デザインの8色（黄/紫/桃/赤/橙等）を単一アクセント＋`bad`（危険度のみ）へ
  収束。
- `help.jsx`（20セクション）：局所`Section`/`Card`をファイルローカルの`HelpCard`へ置き換え。
  絵文字（📖🌍🥇🏆🎌🤝🔩✨🔀💚🏔️🎯🧬🌳🪨等、約35個）を全廃し、強調用の6色の装飾色（黄緑赤紫青橙桃）
  を「太字＋`T.color.text`」への統一と「降格の警告のみ`T.color.bad`」の2択へ収束。文章量・
  説明内容そのものは変更していない（トークン移行のスコープであり、§7の内容見直しは別タスク）。
- 画面変換中に見つかったデータ層の絵文字漏れ（`data/events.js`の`ML_BACKGROUNDS.meritLabel`
  3件：🌱叩き上げ／🎓文武両道／💼即戦力）は`create.jsx`の経歴選択カードに直接表示されるため
  合わせて除去。データ層全体の絵文字一掃はPhase 3-F（最終掃討）の担当範囲。

**検証**：ビルド成功（4回、各ファイル変換後に都度確認）。絵文字カウント（コメント行除外）：
`hub.jsx` 13→0（→/★等の機能記号のみ残存）、`events.jsx` 11→0、`create.jsx` 14→0、
`help.jsx` 35→0（B1→A→PRO等の文中の→矢印のみ残存、装飾ではなく文脈上の遷移記号）。
Playwrightで実プレイ（マイライフ開始→キャラ作成→デビュー→ショップ3カテゴリ→記録タブ
（実績/特殊能力図鑑/コースレコード）→世界タブ（選手成績/全チーム名鑑）→ヘルプ）を通し、
コンソールエラー0・NaN/undefined無しを確認。D-1の`ui.jsx`シムは既存231箇所のBtn/Eyebrow
呼び出しを無改修のまま通した後にスクリーンショットで確認済み。

**残課題（Phase 3-D-3へ）**：`panels.jsx`（15コンポーネント）・`dynasty.jsx`・`chrome.jsx`は
まだ旧トークンのまま。これらはseason側と共有のため影響範囲が広く、次ラウンドへ持ち越し。

## Phase 3-D-3: 共有部品17個（panels.jsx/dynasty.jsx/chrome.jsx）の新トークン化（完了）

**実測でわかった構造的な問題**：D-3着手時に改めて実測したところ、D-2で「マイライフは全画面が
新トークンで閉じた」としたのは画面ファイル単位の話に過ぎず、`panels.jsx`の15部品は**マイライフ
専用のものが1つも無く全て`season`とも共有**（`panels.jsx`にマイライフ専用部品は0個。
`AbilityGrid`はマイライフのcreate.jsx／season の scout.jsx・transfer.jsx・race.jsx から、
`TitlesPanel`はマイライフのhub.jsx／season の archive.jsx・scheduleBoard.jsxから、という具合に
全部品が両モードから呼ばれている）。つまり画面の中にはまだ絵文字16個（panels.jsx）・8個
（dynasty.jsx）が埋まったままだった。season側画面（riders/list.jsx・scheduleBoard.jsx・
meta.jsx・race.jsx）は装飾密度（`C.xxx`参照48〜165箇所）が高く、部品だけ新トークン化すると
「部品だけ浮く」問題が起きる一方、`records/archive.jsx`（`C.xxx`参照1箇所＝中身の100%が
共有部品）はほぼそのまま完成することも実測で判明。詳細な比較表は当時の設計仕様（Artifact）に
記載。

**設計プロセス**：Opusで実測→AskUserQuestion3問（①共有部品はD-1同様シムのみか全面新トークン化
か→**全面新トークン化を選択**（マイライフ専用の新規部品を別建てすると二重実装になり、
`records/archive.jsx`のように部品だけで画面の大半が完成する例もあったため）②絵文字の線引き
→D-2と同じ基準（装飾のみ撤去、→★●等の機能記号は残す）③スコープ→共有部品まで、season画面
本体は次弾）。続けて実データ（`ABILITIES`・`APT_GRADE_COLOR`・`raceForecast()`の`mark`構造・
`TITLE_DEFS`・`mlLineageForest()`の戻り値等）を実測し、17部品全てのデータ取得元・空状態・
撤去する絵文字を確定した設計仕様をArtifactとして提示（現状/新案の比較モック・実機ズーム機能
つき）。

**ユーザーからの追加指示（3点）**：①能力バーの5色（AB_COLOR）は維持 ②適性グレードの8色
（APT_GRADE_COLOR）は維持 ③金の特殊能力・「★2倍」表記の黄金色は必ず維持——単一アクセント
原則の中で「実データが意味を持つ色」は例外として残す方針が確定した。④系譜ツリーはnetkeibaの
血統表（5代・罫線グリッド）を参考にすることを指示され、現在のデータで再現可能な範囲
（`mlLineageForest()`が返す1系統内1本の世代チェーン。父方/母方の区別は無く`parents`は
[師匠,配合相手]の役割）を確認のうえ、罫線グリッドの表形式へ再設計する方針で合意。

**実装（`src/components/panels.jsx`全面書き換え）**：
- 構造上の分類：「ゲージ型・バッジ型」9部品（FatigueBar/SubStatLine/TraitLine/PersonaLine/
  AbilityGrid/DisciplineGrid/ScoutBadge/BlurGrid/CondFc）は元々自前の背景枠を持たない断片
  ——season/mylife双方の呼び出し元が用意した面に乗せる前提のまま維持（変更なし）。
  「リスト型」4部品（StartListPanel/TitlesPanel/CourseRecordsPanel/AbilityFileList）は
  自前の面（`T.color.surface`）を持ち続ける設計に据え置いた——season側の呼び出し元
  （`records/archive.jsx`等）はSectionのような外枠を持たないため、面を剥がすと素のテキストに
  なってしまうため。マイライフ側はこれらをSectionで二重に囲わず直接呼ぶよう`hub.jsx`を修正
  （`mylife_abilityfile`/`mylife_records`の外側`Section`ラップを撤去）。
- 色：`AB_COLOR`（5色）・`APT_GRADE_COLOR`（8色）は既存の値をそのまま維持（新トークンの背景
  でも視認性は変わらないため変更不要と実測）。金の特殊能力・★2倍は`T.color.accent`。
  下馬評◎○▲の3色は撤去し「自分の行のみaccent」に統一。絵文字（👑🏛📊🔒⚠️✦）は全廃し、
  「エース」「殿堂」「未発見」等の文字ラベルに置換。`ScoutBadge`の「OVR」→「総合力」
  （§7・他画面と表記統一）。`AbilityFileList`の未発見行にあった「まだ発見されていない特殊
  能力です。」という全行同一の説明文を削除し、右端「未発見」の状態語のみに整理。

**実装（`src/components/dynasty.jsx`全面書き換え）**：
- `LineageForestView`：netkeiba風の罫線グリッド表へ再設計。系統ごとに見出し行（系統名／確立
  段階／因子/人数）＋世代・名前・総合力の3列テーブル。ニックネーム・継承元（師/配合相手）は
  行の下にキャプションで表示。5代・両親再帰グリッドは次弾送り（データ層拡張が必要）。
- `FactorCollectionView`：因子カテゴリごとに★カウントの一覧。特能因子の色は`mlFactorCollection()`
  側で意味なく`C.purple`が割り当てられているだけだったため、表示層で`T.color.text`へ上書き
  （脚質因子＝TYPES色、適性因子＝グレード色はそのまま——実データが持つ意味色のため）。
- 旧`variant`の`headingAsH2`/`descMarginTop`/`memberPosition`差分吸収を廃止——見出しは
  `mlUi.jsx`の`Screen`が担うようになったため呼び出し側での出し分けが不要になった。`variant`は
  空状態文言の出し分けだけに縮小。`career.jsx`（mylife）・`meta.jsx`（season非依存）どちらの
  呼び出しも無改修で動作（`Screen`のマージン相殺値が両者の外枠paddingと一致するため）。

**実装（`src/components/chrome.jsx`）**：`RenameModal`/`ConfirmDialog`を新トークン化
（角丸撤去・`T.color.surface`・ボタンをフル幅2択へ）。入力欄のフォントだけは入力中の文字が
読める必要があるため`FONT_B`のまま維持。season専用の`SeasonHeader`/`makeWrap`は無改修
（`makeMlWrap`は既にPhase 3-Aで新トークン化済みと判明）。

**`askConfirm`に`confirmLabel`引数を追加**（`useAppShell.js`）：取り返しのつかない操作
（引退・データ消去・殿堂記録削除等）でも従来は確認ボタンが一律「OK」だった問題への対応。
未指定時は従来通り「OK」を維持するため既存の全呼び出しは無改修で動く。マイライフの
destructiveな呼び出し6箇所（`hub.jsx`5箇所・`create.jsx`1箇所・`career.jsx`2箇所）に
「メンターになる」「出走する」「引退する」「最初からやり直す」「タイトルに戻る」「削除する」
「新しく始める」を指定。season/meta側の呼び出しは今回のスコープ外のため無改修。

**データ層の修正**：`data/progression.js`の`TITLE_DEFS`から絵文字（🌍🏆🌐🥇）の`icon`
フィールドを削除（`TitlesPanel`が直接表示していたため）。

**検証**：ビルド成功（各ファイル変更後に都度確認）。絵文字カウント（コメント行除外・機能記号
除く）：`panels.jsx`16→0、`dynasty.jsx`8→0、`chrome.jsx`2（`SeasonHeader`内・対象外の据え置き
分）。Playwrightで①マイライフ実プレイ（デビュー→選手タブ→ショップ→記録タブの実績/特殊能力
図鑑/コースレコード）②合成セーブ注入（`roadrace_v12_mylife_legends`に殿堂データ2件を注入）で
系譜ツリー・因子図鑑・全チーム名鑑を検証③season側はオンボーディング（スカウト方針→契約）を
実際にクリックで通し、記録メニュー→通算タイトル・コースレコード・特能図鑑、選手メニューを
確認——3系統すべてでコンソールエラー0・NaN/undefined無しを確認。season の
`records/archive.jsx`は実測どおり大きく改善（旧`Eyebrow`見出しのみ旧デザインで残存、
中身の共有部品は新トークンで統一されて見える）。

**残課題（Phase 3-D-4以降）**：season画面本体（21画面のインラインstyle、特に`race.jsx`の
`C.xxx`参照165箇所）とdata層の残り絵文字（`ML_ACHIEVEMENTS`のicon等）は次弾以降。系譜ツリーの
5代祖先グリッド（netkeiba完全準拠）は`buildBloodMap`の再帰的な祖先探索というデータ層拡張が
要るため、需要があれば別タスクとして起票する。

## Phase 3-D-4-0: 可読性・情報のメリハリ改修（軸2、完了）

**背景**：D-1〜D-3は「AI臭さ」（絵文字・多色・角丸・枠線の装飾過多＝軸1）の除去だったが、
ユーザーからD-3完了後に「情報にメリハリがついていない部分が多々ある」との指摘を受けた。
具体例として①金特（特殊能力図鑑）の表現が「右側に金という文字を金色で入れる」形になって
いるが意図は「能力名自体を金色にする」だった、②ショップでパーツ名と数値のフォント差が
無く、「所持」「空き」という表現が縦の整列も崩れている、の2点。ユーザーは「ショップは
一例にすぎず、AIらしさは無くなったがシンプルに遊ぶ際にわかりづらい・認知負荷が高い部分が
ありえないほど大量にある。一度全体を通して修正すべき」と明言——**装飾除去（軸1）と
可読性・階層（軸2）は別軸であり、軸2はD-1〜D-3で未着手だった**と判明。

**ルール確定の手順**：CLAUDE.md §8に従い、いきなり全画面に展開せず、まずD-2/D-3で自分が
書いたコード（マイライフ既存64行）の中で軸2に違反している箇所を実測（grep）で洗い出し、
小さいスコープでルールを確定・実証してから残りへ展開する方針をユーザーに提示し、
「先に小さく確定して実証（推奨）」で承認された。grepの結果、ユーザーが指摘した2件
（型A：ショップ、型B：金特表示）に加え、`hub.jsx`の選手成績／全チーム名鑑にも同種の問題
（型C：3個以上の可変幅の値を`space-between`で並べる一行レイアウトが縦に整列しない）が
見つかった。

**確定した可読性ルール（R1〜R5、今後の全画面改修に適用する恒久ルール）**：
- **R1**：数値を`label`文字列へ連結しない（例：`` `${p.label}　所持${n}（空き${m}）` ``）。
  文字列長が変わると列が揃わなくなる。数値は`count`/`countLabel`のような独立したprop/要素に
  分離する。
- **R2**：3個以上の可変幅の値をリストの各行で`space-between`のみで並べない。見出し行＋
  固定幅・右揃えの列（`fontVariantNumeric: "tabular-nums"`）にする。
- **R3**：状態は「対象物そのもの」で表現し、横に別バッジを足さない（例：金特は能力名自体を
  `T.color.accent`にする。「★ 金」のような右側の別ラベルは追加しない——対象を変える方が
  バッジを追加するより視線移動が1回少ない）。
- **R4**：1行リストの主役（値・答え）は`T.size.head`(16px)、脇役（ラベル）は`T.size.caption`
  (12px)にする。太字が使えない（`CheckpointPeriod`フォントは`font-weight: normal`のみ。
  `fontWeight: 700`は疑似ボールドでドット絵グリッドが崩れるため使用不可と判明）・
  `T.size`にheadより大きい実用サイズが無い制約下では、head/caption比1.33が達成できる
  最大の階層コントラスト（body/captionは1.17で階層として認識されないと判断）。
- **R5**：アクセント色（`T.color.accent`）は画面内で本当に目立たせたい少数の値だけに絞る
  （例：最高順位1位はaccent、8位はaccentにしない。全て0の行は`T.color.sub`へ丸ごと沈める）。

**ユーザーからの承認内容（`/model claude-sonnet-5`切替時に明示）**：①ショップのパーツ行は
「所持」表記を廃止し「未装着 N」のみにする（②消耗品・恒久投資行は元々「所持」の意味が
異なるため実装者判断で「所持」表記を維持）。②`hub.jsx`の選手成績・全チーム名鑑は
勝敗数を軸にした固定幅列レイアウトへ再設計。**将来の検討事項として**（今回は未実装・
ユーザーの言葉では「かもね」「とかでも」という留保付きの提案）透明度差・同色相
（黄色の彩度・明度のみ変更）による階層表現も挙げられた——正式なトークン化はせず、
次のデザイン検討時の材料としてここに記録する。

**実装**：
- `src/components/mlUi.jsx`：`Item`をR4に沿って改修。`label`を`caption`、`value`を`head`
  ＋`textAlign: right`＋`tabular-nums`へ。
- `src/components/panels.jsx`：`TraitLine`の「★ 」接頭辞を削除し「（★効果2倍）」を
  「（金は効果2倍）」に変更。`AbilityFileList`はR3に沿い、能力名自体をgold時accent／
  未発見時sub／bad時bad／通常時textに変更し、右側の「★ 金」ラベルを削除
  （残る右ラベルは「未発見」「金に進化する」のみ）。名前のフォントサイズを`head`へ拡大。
- `src/screens/mylife/events.jsx`：`ShopBtn`に`minWidth`（指定時`textAlign: center`）を追加。
  `ShopRow`をR1に沿って`count`/`countLabel`propで再構成——1行目はlabel（head・省略記号
  つき）＋バッジ／ロック＋副ボタン＋購入ボタン（`minWidth: 56`で固定幅）、2行目は
  detail（caption）＋`{countLabel} {count}`（caption・tabular-nums・右揃え）。呼び出し
  6箇所を全て`count`/`countLabel`へ移行（パーツ＝「未装着」、パーツ強化＝「Lv」、
  消耗品＝「所持」、成長力＝「現在」、成長タイプ＝「現在」）。
- `src/screens/mylife/hub.jsx`：R2に沿い`STAT_COL_W`（列幅定数）・`StatColHeader`
  （見出し行）・`StatRow`（固定幅・右揃え・tabular-numsの1行）を新設。`mylife_riderstats`
  （自分の今季/通算/表彰台/最高をR5でaccent、rowが2件以上の時のみ見出し表示）・
  `mylife_worldstats`（全チーム名鑑、自分の行と最高順位1位をaccentで強調）を
  この2部品で再構成。

**検証**：ビルド成功。Playwright（マイライフ実プレイ→ショップ→記録タブ→特殊能力図鑑、
および合成セーブ注入で選手成績・全チーム名鑑を検証）でコンソールエラー・警告0件、
`所持`/`空き`の残存文字列0件、NaN/undefined 0件を確認。スクリーンショットで
①ショップのパーツ行が「未装着 0」で右揃え・整列 ②`AbilityFileList`の金特（逃げ屋）が
能力名自体アクセント色・右ラベルは「金に進化する」のみ ③選手成績・全チーム名鑑が
今季/通算/表彰台/最高の固定幅列で揃い、自分の行と最高順位1位がアクセント色、を目視確認。

## Phase 3-D-4-a: シーズンの額縁（ヘッダ・メニュー・敷地ストリップ）の作り直し（完了）

**設計案**：https://claude.ai/code/artifact/6a0b617b-af43-43d4-96b4-c10934d83c75
（実データ・実フォント埋め込みで候補3案を提示。CLAUDE.md §8の手順1〜3を実施し合意済み）

### 実測（着手時）

シーズンを実際に開始して撮影したところ、`SeasonHeader`（`chrome.jsx:14-40`）は**21画面すべてに
出続ける常設クロームでありながら18項目を詰め込んでいた**。実データ（クラス B1／1年目 4月／
予算300万／スポンサー「カワセミ電工」・月18万・ノルマ103pt・違約金30万・指定レース1回）で確認：

- **実バグ：幅420pxで右カラムが狭すぎ、値の途中で改行していた**。「`/ 出場権113pt`」が
  「/ 出場権」＋「113pt」に、「`順位 10/10位`」が「10/10」＋「位」に割れ、数字と単位が
  別の行に離れていた。
- R1違反：項目6〜10・12〜14が1本の文字列に連結（`月+18万／ノルマ103pt／未達-30万／指定レース0済`）。
- R4違反：10.5px・11px・12pxが混在。3段あるのに全て「小さいグレー」で階層になっていない。
  加えて`theme.js`が実測で「9〜11pxは漢字が潰れて読めない」と記録しているサイズを使っていた。
- R5違反：黄・緑・金・白・灰が1つのヘッダに同居。
- 絵文字2個（🏆順位・🔁ダイナスティ）。

### 判断：18項目のうち常時表示に値するのは7項目

判断基準は「この画面でも次の画面でも、判断のために参照し続ける値か」。

- **残す（7項目）**：クラス／年月／チーム名／予算／ポイント／昇格必要pt／順位
- **移す（10項目）**：スポンサー4項目（月給・ノルマ・未達・指定レース）＋支出内訳3項目
  （選手年俸・人数・スタッフ月給・OBコーチ）＋ダイナスティ周回。
  **「移す」は「消す」ではない**——移設先は`race_status`（レース→シーズン状況・目標）。
  既に「中期目標」「タイトル争い」「今月のチーム状態」を持つ"今の状況"画面であり、
  受け皿セクションを同じラウンドで新設する。
- **注釈「（昇格ボーダー緩和圏）」は撤去してよいと確認済み**：このルールは
  `scheduleBoard.jsx:49,71`（年間プログラム）で本文として説明済みで、`yearend.jsx:26`でも
  年度末に表示される。ヘッダから消しても情報は失われない。

### 確定した仕様（ユーザー選択：案C 計器盤3列）

候補3案（A最小68px／B進捗ゲージ80px／C計器盤3列92px）を実データ・実フォントで提示し、
**案C**が選ばれた。D-4-0で確定した`StatRow`の固定幅列の思想をヘッダにも適用し、順位も残す形。

**`SeasonHeader`の構造**（3行）：
1. クラス（左・caption・sub）／年月（右・caption・sub）
2. チーム名（head 16px・text・`whiteSpace:nowrap`＋`textOverflow:ellipsis`で1行固定。
   長い名前でもヘッダの高さが変わらない）
3. 3列（`flex:1`ずつ・`minWidth:0`）。各列＝ラベル（caption・sub）＋値（head・
   `fontVariantNumeric:"tabular-nums"`）＋単位（caption・sub）
   - 資金（左揃え）：`g.budget` ＋「万円」。色は`T.color.accent`、**負のとき`T.color.bad`**
   - 昇格まで（右揃え）：`g.points` ＋「/{seasonNeed(g.classIdx)}pt」。色は`T.color.text`、
     **`g.points >= need`のとき`T.color.accent`**
   - 順位（右揃え）：`seasonRank(g).rank` ＋「/{total}」。色は常に`T.color.text`

**共通ルール**：絵文字0個／色は`T.color.accent`1色のみ（緑は使わない。例外は上記の`bad`）／
級数はcaption(12)とhead(16)の2段だけ（10.5・11pxは廃止）／数字は全てtabular-nums。

**空・特殊状態**：
- `g.teamName`が空 → 「あなたのチーム」（現行と同じ）
- `g.budget < 0` → 数値を`T.color.bad`。「（借金）」の文字は付けない（マイナス記号＋赤で
  足りており、買い物不可という帰結は`race_status`の警告文が既に説明している）
- `g.points >= need` → 値を`T.color.accent`
- `seasonRank(g).rank === 0`（算出不能） → 「—」を表示し単位は空
- `g.dynastyLevel > 0` → **ヘッダには出さない**。`race_status`に「N周目」として表示

### 同じラウンドで直す残り3箇所

**`MenuShell`（`components/menu/MenuShell.jsx`）**：全画面から開く常設UI。
- 大ジャンル6個の絵文字🏠🚴🏗🛒📜⚙️を撤去（`data/seasonMenu.js`の`icon`フィールドごと削除）。
  ラベルだけで判別できる。
- 丸ボタン（56px・`borderRadius:"50%"`・`boxShadow`）→角丸0・影なしの矩形へ。
  「✕/☰」は開閉の記号として機能しているため残す。
- `C.panel`/`C.line`/`FONT_B`→`T`/`FONT_DOT`。小ジャンルの角丸8px・1px枠線を撤去し
  面の濃淡と罫線に置換。
- フォントサイズ13/13.5/14.5/16pxの4段→`body`(14)と`caption`(12)の2段へ。

**BaseView下部の施設ストリップ（`components/base/BaseView.jsx:294-300`）**：
- **10pxで表示されている**（`theme.js`が「9〜11pxは漢字が潰れる」と実測記録しているサイズ）→`caption`(12)へ。
- 絵文字6個（🏠💪🔧🏥🔍🌳）を撤去。
- `FONT_M`（欧文等幅）→`FONT_DOT`。
- 初期状態ではLv0が6個並ぶだけの行になるため、**Lv1以上の施設だけを出す**。
  空状態：全部Lv0なら行ごと非表示。

**`makeWrap`/`makeMetaWrap`（`chrome.jsx:91-124`）**：
- 背景`C.bg`(#14171d・青紫寄り)→`T.color.bg`(#0E0E10)。マイライフ側は移行済みで、
  モードを行き来すると背景色が変わってしまう状態だった。
- `fontFamily: FONT_B`→`FONT_DOT`。
- **padding・maxWidthは変更しない**（`mlUi.jsx`の`Screen`のマージン相殺値がこの値に依存）。

### `race_status`に新設する受け皿（ヘッダから外す10項目の移設先）

既存の「中期目標」カードの直後・「タイトル争い」の前に置く。`mlUi.jsx`の`Section`＋`Item`で
書く（D-3で`Screen`がseason側でもそのまま動くことを確認済み。**mlUi.jsxのseason共有の第一歩**）。

- **「スポンサー契約」**（`g.sponsor`があるときのみ。無ければセクションごと非表示）
  - スポンサー名：`g.sponsor.name`
  - 月々の収入：`g.sponsor.monthly`
  - 年間ノルマ：`g.sponsor.norma`
  - 未達の違約金：`g.sponsor.penalty`
  - 指定レース：`g.sponsor.mandates`（総数）・`g.sponsor.mandatesMet`（出場済）・
    `g.sponsor.mandatesMissed`（見送り。0なら出さない）
- **「今月の支出」**（常に表示）
  - 選手年俸：`teamPayroll(g.roster, g.salaryDiscountMul || 1)`＋`g.roster.length`名
  - スタッフ月給：`staffSalaryTotal(g.staff)`（0なら行を出さない）
  - OBコーチ：`OB_COACH_SALARY`（`g.obCoach`が無ければ行を出さない）
  - 合計：上記の合計（**スタッフかOBがあるときだけ出す**。他が0なら合計＝選手年俸で冗長なため）
- **「ダイナスティ」**（`g.dynastyLevel > 0`のときのみ）：「N周目」

### スコープ

当初のD-4-a案に含めていた`meta.jsx`3画面（モード選択・生涯評価・クリアポイント交換所、
絵文字18個）は**D-4-a2として次ラウンドへ分離**（ユーザー選択：「額縁に絞る」）。
額縁は21画面すべての土台なので先に確定させる。

実装対象5ファイル：`components/chrome.jsx`／`components/menu/MenuShell.jsx`／
`data/seasonMenu.js`／`components/base/BaseView.jsx`／`screens/season/hub/race/status.jsx`。

### 実装

設計どおり5ファイルを実装。`SeasonHeader`に`HeaderStat`ローカル部品（label=caption・
value=head+tabular-nums・unit=caption）を新設し3行構成へ。旧`teamPayroll`/`staffSalaryTotal`/
`OB_COACH_SALARY`のヘッダ内呼び出しは撤去し、`race/status.jsx`の`SponsorAndSpendingSections`
（新設）へ移設——「スポンサー契約」「今月の支出」「ダイナスティ」の3セクションを
`mlUi.jsx`の`Section`/`Item`で構成した（**season側での`mlUi.jsx`共有の第一歩**。D-3で
`Screen`がseason外枠のpaddingと相殺値が一致することを確認済みだったのが活きた）。
`MenuShell`/`seasonMenu.js`は`icon`フィールドを削除し新トークン化、`BaseView.jsx`下部
ストリップはLv1以上の施設だけを表示する条件分岐を追加。`chrome.jsx`の未使用化した
`C`/`FONT_D`/`FONT_M`インポートも削除。

### 検証

ビルド成功（各ファイル変更後に都度確認）。Playwrightでシーズン実プレイ（オンボーディング
→敷地画面→メニュー開閉→選手/施設/市場/レース/記録の各セクション）を一通り確認：
コンソールエラー0件・NaN/undefined 0件・「昇格ボーダー緩和圏」の残存文字列0件。

高DPIでヘッダ単体を再撮影し、**実測で見つかっていた「値の途中で改行するバグ」が解消**
していることを確認（`資金 300万円`／`昇格まで 0/113pt`／`順位 10/10`の3列が折り返さず
1行に収まる）。当初の通常解像度スクリーンショットでは数字が二重露光のように見えたが、
高DPI再撮影で圧縮アーティファクトと判明——実バグではなかった。

撤去対象の絵文字（🏆🔁🏠🚴🏗🛒📜⚙️💪🔧🏥🔍🌳）はヘッダ・メニュー・下部ストリップの
テキストからは0件。ボディに残っていた🏠💪🔧🔍は`Room.jsx`/`Station.jsx`が敷地の建物・
持ち場に描く**シーン内のラベル**（アイソメ図の上に描く記号で、UIテキストの階層とは別物）
と、D-4-bスコープの`staff.jsx`由来——今回の対象（額縁）ではないため意図した残存。

`riders`/`facility`/`market`/`records`の各セクション本体（hub配下17画面）は旧`C`トークン
のまま残っており、ヘッダだけが新トークンで浮いて見える状態だが、これはD-4-bで解消する
想定どおりの中間状態。

## Phase 3-D-4-b: hub配下17画面の作り直し（完了）

**設計案**：買い物行 https://claude.ai/code/artifact/b9d8a5da-e48d-4c71-9be1-ffcaa5457871
／選手カード・記録 https://claude.ai/code/artifact/c4cf93b2-6dd8-433b-a420-77d6ee3fdf7d
（実データ・実フォント埋め込み。CLAUDE.md §8の手順1〜3を実施し合意済み）

### 実測（着手時）

`screens/season/hub/`配下17画面＝**1,292行・旧トークン`C.xx`431箇所・絵文字86個・
角丸73枚・1px枠線58本**。共有部品の使用状況を数えると`Eyebrow`39回・`Btn`29回だけで、
**残りは全て生のインラインstyle**——同じ形の行が17回書かれている状態だった。

**D-4-0で直したはずの表現がシーズン側に残っていた**（`market/shop.jsx:26`）：
`{p.label} <span>所持{g.partsInv[pid]}（空き{availParts(pid)}）</span>`。
ユーザーが最初に指摘した「所持と空きの表示」「パーツ名の後ろにつくので長さ次第で位置が
変わる」そのもので、マイライフ側だけ「未装着 N」に直しシーズン側は手つかずだった。

### パターン分類（17画面は実質3つの繰り返し）

| | 形 | 画面 | 行 |
|---|---|---|---|
| **P1 買い物行** | 名前＋現在値／説明／価格ボタン | facility/equip・staff・ob・room、market/shop | 252 |
| **P2 選手カード** | 名前＋脚質＋OVR／属性／特能／能力図／ボタン | riders/list、market/scout・transfer、records/hall | 326 |
| **P3 記録・状況** | 一覧・カード・設定など個別色が強いもの | race/status・calendar、riders/youth・team、records/career・archive・standings、misc | 500 |

**ユーザー選択：3ラウンドに分けず17画面を一括で実装する**。

### 確定仕様

**共有部品の置き場所**：`components/mlUi.jsx` → **`components/kit.jsx`へ`git mv`**。
D-4-aで`Section`/`Item`をseasonの`race/status.jsx`が使い始めており、現在10ファイルが
importしていて既に「マイライフ専用」ではない。名前が実態と食い違ったまま増築するのは
CLAUDE.md §5が防ごうとしている構造の腐りなので、実態に合わせる。import元10ファイルの
パスも同時に書き換える（互換シムは不要な件数）。

**P1 買い物行**：`ShopRow`/`ShopBtn`を`screens/mylife/events.jsx`のローカル定義から
`kit.jsx`へ移設し、season 5画面でも使う。`gauge={{lv, max}}`（任意）を追加。
- **ユーザー選択：案C（施設ダッシュボードを買い物行に統合）**。`facility/equip.jsx`の
  「施設・投資の状況」7行は機材3種のLv・効果を同じ画面の上下2箇所に重複表示していた。
  ダッシュボードは廃止し、失われる「進み具合が図で見える」性質はセグメントバーを
  買い物行の3段目に置いて残す。重複ゼロ・導線と現在地が同じ行に揃う。
- **`market/shop`のパーツ行は`countLabel="未装着"`/`count={availParts(pid)}`へ**
  （マイライフと同じ表現に統一）。消耗品行は`countLabel="所持"`。
- 撤去する絵文字9個：🏭🎓🛋⛺🔒🧑‍💼ほか。ロックは「Aで解禁」の文言自体が状態を語るため記号不要。
- 色：現在は紫（market）・赤（facility）・金（equip/ob/room）と画面ごとに違うが、これは
  ジャンルの色分けではなく**ただ揃っていなかっただけ**（同じ「買う」操作が3色ある）。
  `T.color.accent`1色へ統一。

**P2 選手カード**：`components/riderCard.jsx`を新設。**ユーザー選択：案B**。
- 実測で`riders/list.jsx`のカードは**1枚に25要素・1人約560px・6人で3,399px**。
- **見つかった重複**：①「どれだけ伸びるか」の指標が3つ（成長ランク`POW[r.growthPow]`・
  伸びしろ`potentialHint(r)`・成長フェーズ`growthPhase(r).tag`）同じ行に並び、しかも
  成長フェーズは属性行にもう一度出ていた。②年齢も2箇所（ユースバッジ`age<=18`と「27歳」）。
- **常時表示**：名前・主将・OVR・脚質・成長フェーズ・調子・疲労・能力レーダー・練習指定。
  シーズンの毎月の操作は練習指定であり、そのために見る必要があるのは「今どこが低いか
  （能力）」「休ませるべきか（疲労・調子）」「伸びる時期か（フェーズ）」だけ。
- **展開（「くわしく見る」）**：改名・お気に入り・主将任命・解雇・性格・特能・コース適性・
  パーツ4スロット・ニックネーム・フレーバー・戦績。
- 1人300px・6人で約1,800pxになる（現状の約6割）。
- 成長指標3つ→**成長フェーズのみ常時**、他2つは展開へ。ユースバッジは廃止し年齢に一本化。
- 絵文字11個（✏️🎖🌱🌳★☆☘⛰🍂🏥）撤去。色6種→`accent`（OVR・主将）と`bad`（故障・連闘）のみ。
- 他3画面は同じ`RiderCard`を表示要素を絞って使い回す（scout＝推定OVR`sc.ovrMin〜ovrMax`、
  FA＝確定OVR、transfer＝提案文＋相手カード、hall＝引退年・`riderCareerSummary(r)`）。
  `market/transfer`の「引き抜き市場」は説明文＋ボタン1つなので`RiderCard`は使わず
  `Section`＋ボタンへ。

**P3 記録・状況8画面＋help**：候補ではなく変換ルール6つ＋画面ごとの個別指示。
`Eyebrow`→`Section`／カード（角丸＋枠線＋背景）→`Section`の面＋行の罫線／ラベル値の
並び→`Item`／5色→`accent`1色（良し悪しのみ`good`/`bad`）／`Btn`→`PrimaryBtn`/`QuietBtn`／
級数6種→`caption`(12)・`body`(14)・`head`(16)のみ。
- `race/status`：既存4カードの`borderLeft:3px solid`色分け（緑/赤/紫/青）を撤去
- `race/calendar`：レース名の前の絵文字接頭辞4つ（👑🌍🎯🏠）を名前の下のcaption行に文字で
- `riders/youth`：配合の「累代ボーナス／インブリード／金特／専用特能」1行連結（R1違反）を`Item`へ
- `riders/team`：スタッフ陣の`join("・")`連結を`Item`へ。ケミストリー進捗バーは残す
- `records/career`：実績の`a.icon`（22px絵文字）撤去、達成/未達は文字色と`opacity`で（R3）。
  年度別記録は固定幅列（R2）
- `misc`：✏️を「変更」の文字ボタンへ。「いつでも変更できます。」は自明なので削除
- `help`（214行・角丸27・枠線27・`C.xx`128）：`Prose`と`Section`への機械的変換

**絵文字を残す例外は2つだけ**：`race/calendar`のコース断面セグメントバー（絵文字ではなく
色分けした図形で、地形の起伏を示す本来の役割を果たしている）と、レースグレードの「★★★」
（数の多寡が一目で分かる記号）。CLAUDE.md §8の「記号のほうが一覧で速い」少数例外に当たる。

### 実装

設計どおり17画面＋共有部品を実装。

- `components/mlUi.jsx` → **`components/kit.jsx`へ`git mv`**。冒頭コメントを
  「season/mylife両モード共有」に改め、import元10ファイルのパスを一括書き換え。
  `ShopRow`/`ShopBtn`を`events.jsx`のローカル定義から`kit.jsx`へ移設し、
  `gauge={{lv, max}}`（任意プロパティ・段のセグメントバー）を追加。
- `components/riderCard.jsx`新設：ヘッダ（名前・バッジ・OVR）＋副次行（脚質・sub文字列・
  調子）＋`children`（本文はscreenごとに異なる内容を渡す）＋任意`footer`＋任意の展開領域、
  という5スロットの骨格だけを共有する設計。中身（性格・特能・適性・戦績等）は呼び出し側が
  既存の共有部品（`PersonaLine`/`TraitLine`/`DisciplineGrid`等）で組み立てる。
- **P1買い物行5画面**：`facility/equip.jsx`はダッシュボードを削除し`ShopRow`＋`gauge`のみに
  （機材3種のLv重複表示を解消）。`facility/staff.jsx`・`ob.jsx`・`room.jsx`・
  `market/shop.jsx`も`ShopRow`へ。`market/shop`のパーツ行は`countLabel="未装着"`
  （旧「所持N（空きN）」を撤去）。
- **P2選手カード4画面**：`riders/list.jsx`は`RiderCard`＋常時9要素（名前・主将・OVR・脚質・
  成長フェーズ・調子・疲労・能力レーダー・練習指定）＋展開領域（改名・お気に入り・主将任命・
  解雇・性格・特能・コース適性・パーツ4スロット・ニックネーム・フレーバー・戦績）へ全面書き換え。
  `market/scout.jsx`（新人＝推定OVR範囲＋タグ／FA＝確定OVR）・`market/transfer.jsx`
  （トレード提案＋相手カード。引き抜き市場は`Section`＋ボタンのみで`RiderCard`不使用）・
  `records/hall.jsx`（引退年・`riderCareerSummary`）も`RiderCard`で統一。
- **P3記録・状況8画面**：`race/status.jsx`は4カードの`borderLeft`色分けを撤去し
  `Section`/`Item`へ（D-4-aの`SponsorAndSpendingSections`はそのまま維持）。
  `race/calendar.jsx`は絵文字接頭辞4つを名前下のcaption行の文字列へ、セグメントバーと
  ★グレードは維持。`riders/youth.jsx`は配合詳細の1行連結を`Item`の行へ分離。
  `riders/team.jsx`はスタッフ陣の`join`連結を`Item`へ（未雇用時はセクション自体を非表示）。
  `records/career.jsx`は実績アイコンを撤去し達成/未達成を文字色+`opacity`で表現、年度別
  記録を固定幅列へ。`records/standings.jsx`・`records/archive.jsx`（list-type共有部品の
  二重包装を避けるため`Section`ではなく軽量`Heading`を使用）・`misc.jsx`も変換。
- **help.jsx**（214行）：15トピックすべてを`Section`＋`TextRow`（プレーンな段落行、
  `first`以外は罫線区切り）へ機械的変換。役割・作戦のpros/consは「◎/▲」記号ではなく
  「強み：」「弱点：」の文字ラベル＋`good`/`bad`色に置換（記号より明示的で、CLAUDE.md §7の
  「開発語彙ではなく初見の人に伝わる言葉」の精神に合う）。

**実装中に見つけた副次的な修正**：`facility/staff.jsx`の雇用済み表示名が
`STAFF_META[k].title`（正式な役職名「チームドクター」等）ではなく`STAFF_ROLES[k].label`
（短い呼称「ドクター」）を使っていた実装ミスを、旧コードの挙動を再確認して踏襲・修正。

### 検証

ビルド成功（各バッチ変更後に都度確認、最終dist 1,204.76kB≒旧比-23kB）。dev serverが
コンテナ再起動で停止していたため再起動して検証。Playwrightでシーズン実プレイ
（オンボーディング→敷地画面→メニュー経由で全17画面を実データで巡回）を実施：
コンソールエラー0件・NaN/undefined 0件・「所持N（空きN）」の残存0件・撤去対象絵文字
（🏭🎓🛋⛺🔒🧑‍💼🌱🧬✨💎🌟⚠️🩸🎖🌳☘⛰🍂🏥✏️🎯🏆🔁👑🌍🏠🚩🌧🥵🟢🔴⚪📅📊🔍🏳）の残存0件を確認。
スクリーンショットで①`riders/list`の選手カードが1人あたり約340px（旧560pxから4割減、
6人でも1画面弱に収まる）②`market/shop`の「未装着 0」が右揃え・パーツ名の長さに関係なく
整列③`race/calendar`のセグメントバー・★グレードが維持されたまま絵文字接頭辞のみ撤去
④`records/hall`・`riders/team`の空状態（殿堂0名／スタッフ未雇用でセクション非表示）が
正しく動作、を目視確認。

**verify_d4a.mjsで踏んだ落とし穴の再発**：メニューのカテゴリボタン「レース」への
`hasText`部分一致クリックが、背後の「このレースに出場」ボタン（同じく「レース」を含む）に
誤ってマッチしオーバーレイに阻まれてタイムアウトした。メニュー系のクリックは正規表現の
完全一致（`^...$`）に切り替えて解消——アプリ側のバグではなくテストスクリプト側の問題。

## Phase 3-D-4-a2: meta.jsx 3画面の作り直し（設計確定・実装待ち）

**設計案**：https://claude.ai/code/artifact/9e55822b-6b7a-453b-9460-31b63d196b11
（実データ・実フォント埋め込み。CLAUDE.md §8の手順1〜3を実施し合意済み）

### 実測（着手時）

`screens/meta.jsx`は207行で**フォントサイズ14種類**——プロジェクト最悪の密度だった。
9.5 / 10.5 / 11 / 11.5 / 12 / 12.5 / 13 / 13.5 / 20 / 21 / 26 / 30 / 32 / 40px。
うち9.5〜13.5pxに8段階が密集しているが、`theme.js`が実測で「このフォントは9〜11pxだと
漢字が潰れて読めない」と記録しているサイズであり、9.5/10.5/11pxはそもそも表示として
成立していない。一方で40/32/30/26pxという特大値もこのファイルにしかない。
ほか：旧トークン`C.xx`78箇所・絵文字18個（🏢🚴🏆🛒🌍🌳🧬🔓✓）・角丸14枚・1px枠線12本・
グラデーション2箇所。

**実データの取得方法**：殿堂3名・生涯CP128pt・使用済み70ptを`localStorage`へ注入して
実測（生涯評価スコア732／通算42勝・表彰台89回・実績26／残高58pt／次の解禁まであと2pt）。
空状態（未プレイ）も別途取得（全項目0／次の解禁まであと5pt）。

### 見つかった問題

1. **クリアポイント交換所は「買い物行」そのもの**。19件すべてが「カテゴリ・名前・説明・
   価格ボタン／購入済み」の形で、D-4-bで共有化した`ShopRow`がそのまま当てはまる。
2. **生涯評価の数値が全部「ラベル：値」の文字列連結**（R1違反）。
   「通算勝利数：42勝／通算表彰台：89回」のように1行に2値を詰めた行まである。
3. **解禁一覧30行が高さ300pxのスクロールボックスに閉じ込められている**。1行約28pxなので
   一度に見えるのは10行だけ。実データでは30行中23行が既に解禁済みで、遊び込むほど
   読み飛ばす行が上に溜まる構造だった。

### 確定仕様

**モード選択＝案C「タイトルらしく組む」**（ユーザー選択）。
- **ゲーム名を画面に出す**。既存の名前は`index.html`の`<title>`＝「ロードレース・プロチーム運営 v12」
  ／`package.json`のdescription／DEVLOG見出しに存在したが、①「プロチーム運営」は
  シーズンモードしか指しておらずマイライフ（選手キャリア）が名前に入らない ②`<title>`に
  「v12」が残っている（以前「MODE SELECT — v14」を撤去したのと同じ、バージョン番号が
  ユーザーに見えている状態）という2点があった。**ユーザー決定により
  「ロードレース・シミュレーション」に確定**し、`index.html`の`<title>`もこれに揃える
  （v12を落とす）。
- **【実測・重要】このフォントは完全な等幅（和文=1em）なので、28px×15文字＝420pxとなり、
  実機幅420px−padding28px＝使える幅392pxに1行では収まらない。** 2行に割って28pxを維持する
  （「ロードレース」168px ／「シミュレーション」224px）。中黒は改行が区切りを担うため落とす。
  20px1行（300px）でも収まるが、案Cを選んだ理由である「玄関らしさ」が弱まるため採らない。
- 構成：ゲーム名2行（28px・accent）＋タグライン「チームを率い、世界の頂点へ」（caption・sub）
  ／本編2つ（シーズン／マイライフ）は`Section`の面に20px見出し＋説明caption
  ／付属2つ（生涯評価／使えるCP）は横並び2枠で**今の値を添える**（生涯評価732・58pt）。
- 案Cのモックで使った左3pxのアクセント線は、D-4-bで他画面から撤去したばかりの装飾なので
  **実装では使わない**（面の濃淡だけで区切る）。

**解禁一覧＝案A「未解禁だけ出す」**（ユーザー選択）。
- 済んだ行は見出しの`right`に「23 / 30 解禁済み」として畳み、未解禁だけを出す。
- **絶対値（130pt）ではなく差分（あと2pt）で書く**。`cpUnlockRows()`の`cp`から
  `totalEarnedCP`を引いた値を表示する。
- カテゴリ（コース／シーズン開幕／マイライフ）は**どのモードで効くかを示す情報なので残す**が、
  枠線つき3色バッジではなく`detail`行の文字へ。
- スクロールボックス（`maxHeight:300px`）は廃止。
- 空状態：`rows.every(r => r.unlocked)`なら「すべて解禁済み」1行。

**候補によらず確定していた変更**：
- CPショップ19件を`ShopRow`へ（カテゴリ＝`badge`、「✓ 解禁済」＝`locked="解禁済み"`、
  残高不足は価格ボタンdisabledで価格表示は残す）。**D-4-bのショップ5画面と完全に同じ形**。
- 生涯評価の統計を`Item`へ。「主要タイトル獲得数：0回（グランツール・…）」は
  label「主要タイトル」／value「0回」／detail「グランツール・グランファイナル・世界選手権・
  オリンピック」へ分解。「通算勝利数：42勝／通算表彰台：89回」は**2行に分離**。
- スコアだけは画面の主役なので`display`(28px)。40pxの🏆と32pxの数値は廃止。
- **絵文字18個すべて撤去**。「←」は戻るボタンの記号として残す。
- **フォントサイズ14種→`T.size`の5段から必要なものだけ**。9.5/10.5/11pxは全廃。
- 角丸14枚・枠線12本・グラデーション2箇所を撤去し`Section`の面＋行の罫線へ。
- ダイナスティのカード（緑枠＋グラデーション）は`Section`へ。説明文
  「歴代の名選手・系統・因子は1つの世界として、両モード・全周回で受け継がれます。」は
  **残す**（この仕組みは説明がないと伝わらないため）。
- 「クリアポイントをリセット」の2段確認は**そのまま維持**（取り返しがつかない操作）。
  `askConfirm`の`confirmLabel`に「消去する」を指定。

**空・特殊状態**：
- 初回起動（`totalEarnedCP === 0`かつ殿堂0名）→ モード選択の付属2枠を非表示にし本編2つだけに
- 生涯評価が全て0 → スコア0を出す（隠さない）。「まだプレイ履歴がありません」等の注釈は足さない
- CP残高0 → 全商品の価格ボタンをdisabled（価格表示は残す）
- 全商品購入済み → 全行「解禁済み」。特別な文言は足さない
- 殿堂0名 → 系譜ツリー・因子図鑑のボタンは出す（各画面はD-3で空状態対応済み）

### 実装対象

`screens/meta.jsx`（3画面すべてを`kit.jsx`の`Section`/`Item`/`ShopRow`/`PrimaryBtn`/
`QuietBtn`へ）。

**`src/index.html`も同時に直す**（リポジトリ直下の`index.html`はビルド生成物であり、
真のテンプレートは`src/index.html`。`vite.config.js`の`root: "src"`）。設計中に発見した
3点：
- `<title>ロードレース・プロチーム運営 v12</title>` → 「ロードレース・シミュレーション」
  （バージョン番号がユーザーに見えている状態の解消も兼ねる）
- `body { background: #14171d }` → `#0E0E10`。**これは旧`C.bg`のままで、D-4-aで
  `makeWrap`/`makeMetaWrap`を`T.color.bg`(#0E0E10)へ移行した結果、アプリがマウントされる
  までの一瞬だけ旧配色が見えてちらつく状態になっていた。**
- `#loading { color: #9aa3b5 }` → `#8C8A82`（旧`C.sub`→`T.color.sub`）

### 実装

`src/screens/meta.jsx`を全面書き換え（旧`C`/`FONT_D`/`FONT_M`/`Btn`/`Eyebrow`を廃し
`T`/`kit.jsx`へ一本化）。

- **モード選択（案C）**：ゲーム名2行「ロードレース」「シミュレーション」（`display`28px・
  `accent`）＋タグライン「チームを率い、世界の頂点へ」（`caption`・`sub`）。本編2つは
  `Section`（`title`＝モード名の小見出し、内部に`title`(20px)の具体語「チーム運営」
  「選手キャリア」＋説明`caption`＋`PrimaryBtn`（シーズン）／`QuietBtn color={action}`
  （マイライフ））。付属2枠（生涯評価スコア／使えるCP）はローカルの`<button>`2つを
  `flex`で横並びにし、`loadMeta().totalEarnedCP===0 && loadMlLegends().length===0`で
  丸ごと非表示。
- **生涯評価（Item化＋案A）**：スコアのみ`display`(28px・`accent`)、他は`Section`＋`Item`。
  「通算勝利数：42勝／通算表彰台：89回」の1行2値連結を`Item`2行へ分離。解禁一覧は
  `lockedRows = rows.filter(r => !r.unlocked)`を`ShopRow`（`label`=r.label・`detail`=
  r.category・`locked`=`あと${cp}pt`）で列挙、`Section`の`right`に
  `${unlockedCount} / ${rows.length} 解禁済み`。全解禁時は「すべて解禁済みです。」1行。
  ダイナスティは`Section`の`right`に「世界○年目」を移設（🌍絵文字を廃止）。
- **CPショップ（ShopRow化）**：D-4-bの5画面と同型で`CP_SHOP`19件を`ShopRow`へ
  （`badge`=category・`detail`=desc・`locked`="解禁済み"／`buyLabel`=`${cost}pt`・
  `buyDisabled`=残高不足）。リセットボタンは`QuietBtn color={T.color.bad}`（既存の
  `releaseRider`等と同じ「破壊的操作はQuietBtn+bad」パターンを踏襲）。2段確認の内側
  `askConfirm`に`confirmLabel="消去する"`を追加（未指定だった）。
- 絵文字18個（🏢🚴🏆🛒🌍🌳🧬🔓✓）を全廃、戻るボタンの「←」のみ残す。角丸14枚・枠線
  12本・グラデーション2箇所も`Section`の面・行の罫線に統合され消滅。

`src/index.html`：`<title>`を「ロードレース・シミュレーション」へ、`body`の起動前背景を
`#14171d`→`#0E0E10`、`#loading`の文字色を`#9aa3b5`→`#8C8A82`へ修正（D-4-aで本体側は
`T.color.bg`へ移行済みだったが、マウント前の一瞬だけ旧配色が見えていたちらつきを解消）。

### 検証

`npm run build`成功後、Playwrightで3状態を実撮（`/tmp`のスクラッチに保存・目視確認）：

1. **空状態**（`localStorage`未設定）：モード選択の付属2枠が正しく非表示。本編2つのみ表示。
2. **全項目0状態**（`totalEarnedCP:0`・殿堂1名・全stats0）：スコア15（殿堂1名分の
   基礎点のみ）を隠さず表示。解禁一覧は28行中0行解禁で全件が未解禁として列挙される
   （スクロール閉じ込めが無いことを確認）。CPショップは残高0円で19件全ての購入ボタンが
   disabled、価格表示は残る。
3. **遊び込み状態**（生涯CP128pt・使用済み70pt・殿堂3名）：スコア732・残高58pt・
   解禁19/28が正しく算出・表示。解禁一覧は未解禁9行のみ表示され差分表記（あと2pt〜
   あと272pt）を確認。

5画面（モード選択・生涯評価・系譜ツリー・因子図鑑・CPショップ）×3状態＝15回の遷移で
console/pageエラー0件（faviconの404のみ、無関係）。ブラウザ`document.title`が
「ロードレース・シミュレーション」に、起動前`body`背景が`rgb(14,14,16)`(`#0E0E10`)に
変わったことも確認。

## Phase 3-D-5: 配色トークンの見直し（アクセントカラー導入・設計確定・実装完了）

**設計案**：https://claude.ai/code/artifact/44316e43-969e-4f28-bb2b-6bc0b0156c8b

**発端**：ユーザーから「色味が少なすぎる。ベースカラー：黒／メインカラー：黄となっているが
アクセントカラーが欲しい。アクセントは他と彩度・明度・色相が大きく離れているもの。
ベース：メイン：アクセントが70：25：5ぐらいの比率になると嬉しい」との指摘。

### 実測

シーズン17画面のスクリーンショット18枚・430万点をサンプリングしてピクセルを分類：
**無彩色93.18% ／ 黄3.24% ／ 赤1.17% ／ 緑0.25% ／ その他すべて1.4%未満**＝実質 **93:3:0**。

**根の原因は「色数」ではなく「色相が1つしかない」ことだった**（HSL変換で判明）：

| トークン | H | S | L |
|---|---|---|---|
| `accent` #F2C94C | 45.2° | 86.5% | 62.4% |
| `text` #F4F2EC | **45.0°** | 26.7% | 94.1% |
| `sub` #8C8A82 | **48.0°** | 4.2% | 52.9% |
| `good` #7FB069 | 101.4° | 31.0% | 55.1% |
| `bad` #C0554D | 4.2° | 47.7% | 52.7% |

**`text`も`sub`もアクセントの黄とほぼ同じ色相（45〜48°）の暖色**。「無彩色」に見えている93%も
実際は黄色寄りで、画面全体がひとつの色相の濃淡でできていた。

### 70:25:5 についてユーザーへ伝えた見解

**面積比で黄を25%にすると画面の1/4が黄に染まり、R5（アクセントは少数の値だけ）で作った
階層が壊れる**。候補「強め（黄22%）」を実際に描いて示し、名前も数値も黄になって主役が
消える実例を提示した。比率は面積そのものではなく視覚的な重みの目安として扱う方針で合意。

### 確定仕様

**①アクセントに仕事を与える（色を装飾に戻さないための前提）**
現在の黄は4つの仕事を兼任していた：数値の強調／主ボタンの地色／見出しの右ラベル／選択中の
状態。**データと操作が同じ色**で「読むもの」と「押すもの」が見分けられない状態だった。
- **黄（メイン）＝データ**：数値の強調（資金・OVR・pt・順位・達成/上限）
- **新アクセント＝操作**：押せるもの（主ボタン・選択中のメニュー・リンク）。主ボタンは
  1画面1つなので自然と5%前後に収まる

**②アクセントの色＝バイオレット #A76ADC（H272 / S62% / L64%）**（ユーザー選択）
色相距離：黄と133.2°／緑(good)と170.6°／赤(bad)と92.2°——**既存の意味色3つすべてから
バランスよく離れている唯一の候補**。マゼンタ(H322)は赤(bad)と42°しか離れずエラー色と
紛らわしいため候補から除外した。ブルー(H214)は黄と168.8°で最遠だが個性が弱い、
シアン(H190)は緑(good)と88.6°しかなく「調子が良い」の緑と紛らわしい、と判断。

**③黄の使用範囲＝「中間」（約12%）**（ユーザー選択）
数値に加えて、**セクション見出し**と**状態を表す語**（成長フェーズ・調子など、プレイヤーが
判断に使う値）も黄にする。パネル地・罫線・本文までは寄せない（＝「強め」案は採らない）。

### 実装前に解消した懸念：既存の紫との衝突

設計時に「旧デザインで紫は『特殊・レア』の意味で使われていた」と指摘した点を実測。
残存する紫は**性質の違う2種類**に分かれ、いずれも致命的な衝突にはならないと確認した。

- **残すもの（データ層の意味色）**：`TYPES.PUN`パンチャー／`AB_COLOR.solo`独走／
  `SEG_COLOR.hill`丘陵／`baseViewRoomGrade.scout`／`breeding`の称号色／`lineage`の系統色。
  **いずれも図・チャートの中の識別色**であり、D-3で確立した「実データが持つ意味色は
  そのまま」の原則に該当する。UIクロームとは層が違う。
- **撤去されるもの**：`scheduleBoard`/`transferEvents`/`intro`/`race`の見出し・枠線に残る
  `C.purple`（31箇所）。これらはD-4-c／Phase 3-Eの対象で、そこで消える。

**【実装時に目視確認が必要な1点】**`SEG_COLOR.hill`の紫`#c98bf0`（H≈276）と新アクセント
`#A76ADC`（H272）は**色相が4°しか違わない**。レースカレンダー画面ではコース断面の
セグメントバー（残すと決めた図）と「このレースに出場」ボタンが近接するため、
実装後にこの画面のスクリーンショットで見分けがつくか確認すること。区別がつかない場合は
`SEG_COLOR.hill`側のS/Lをずらす（アクセント側は動かさない）。

### 実装対象

1. `data/theme.js`：`T.color.action = "#A76ADC"` を追加。`accent`の定義コメントを
   「データ強調専用」へ改訂。
2. `components/kit.jsx`：`PrimaryBtn`の地色・`QuietBtn`の文字色・`ShopBtn`の購入ボタンを
   `action`へ。
3. `components/menu/MenuShell.jsx`：開閉ボタン・選択中の項目を`action`へ。
4. `components/ui.jsx`：`BTN_ROLE.primary`/`month`を`action`へ（旧`Btn`経由の呼び出しにも効く）。
5. 各画面：「中間」案に沿って`Section`の見出しと状態語（成長フェーズ・調子）を`accent`へ。

**この変更はD-4-a2（meta.jsx）より先に入れる。**タイトル画面は配色が最も目立つ場所なので、
パレットを確定してから作るほうが二度手間にならない。

### 実装

設計どおり5ファイル＋呼び出し側の該当箇所を実装。

- `data/theme.js`：`T.color.action = "#A76ADC"`を追加。`accent`の定義コメントを
  「データ強調専用」に改訂。
- `components/kit.jsx`：`PrimaryBtn`の地色・`ShopBtn`の購入ボタン（塗り/枠線とも）・
  `SelectRow`の選択中の文字色を`action`へ。`Section`の見出し文字色を`sub`→`accent`へ
  （黄の「中間」の量。データを読む場所という位置づけのため）。
- `components/ui.jsx`：`BTN_ROLE.primary`/`month`（旧`Btn`のrole指定）を`action`へ。
  `mapColor`のCOLOR_MAP（`Eyebrow`と兼用）は今回変更していない——役割の異なる2部品が
  同じ写像関数を共有しているため、Btnのrole経由だけを対象にした。
- `components/menu/MenuShell.jsx`：開閉ボタン（☰/✕）の地色を`action`へ。
- `components/riderCard.jsx`：`subColor`プロパティを新設。`sub`文字列を単純な文字列連結
  ではなく色分けできる構造に変更（デフォルトは従来どおり`sub`色、呼び出し側が上書き可能）。
- `screens/season/hub/riders/list.jsx`：`sub={ph.tag}`（成長フェーズ）に`subColor={T.color.accent}`
  を指定——D-4-0/D-4-bで確立した「判断に使う状態語」の実例。`cond`（調子）は既存の
  `COND_COLOR`（5段階の意味ある色グラデーション）をそのまま維持し、accentで上書きしない
  （D-3の「実データが持つ意味色はそのまま」の原則を踏襲。設計時のモックはあくまで
  「状態語を黄にする」という考え方の例示であり、既に固有の意味色を持つcondは対象外と判断）。
- `screens/season/hub/riders/list.jsx`・`riders/team.jsx`・`misc.jsx`：`QuietBtn`等に
  ボタンの強調として`color={T.color.accent}`を渡していた5箇所を`action`へ修正
  （お気に入りボタン・サプリ/調律ボタン・キャンプ券ボタン・チーム名「変更」リンク）。

### 検証

ビルド成功。dev serverを再起動しPlaywrightで17画面を再検証：コンソールエラー0・
撤去対象絵文字残存0・「所持N（空きN）」残存0（D-4-b時点の回帰なし）を確認。

**配色比率の再実測**（同じ手法・シーズン17画面430万点）：

| | 変更前 | 変更後 |
|---|---|---|
| 無彩色 | 93.18% | 93.06% |
| 黄（accent） | 3.24% | 1.37% |
| バイオレット（action） | — | 2.05% |
| 緑（good） | 0.25% | 0.26% |
| 赤（bad） | 1.17% | 1.17% |

黄の単純な比率は3.24%→1.37%へ下がったが、これは**ボタンの塗り（従来は大面積の黄）が
actionへ移った結果**——黄+action の合計（3.42%）は変更前の黄単独（3.24%）とほぼ同水準で、
同じ量の色を2つの役割に分けて塗り直した形になったと判明。狙いどおり「読む＝黄／押す＝紫」
の分離ができている。

**目視確認（設計時に洗い出した懸念点）**：`SEG_COLOR.hill`の紫`#C98BF0`と`action`の紫
`#A76ADC`は、レンダリング後の実ピクセルをクラスタリングして比較したところ色相こそ4.7°しか
違わないが、**明度が10.4ポイント（63.9%→74.3%）・彩度が15.1ポイント（62.0%→77.1%）
離れており**、race/calendarのスクリーンショットで両者は目視でも区別できることを確認した。
修正不要と判断。

riders/listのスクリーンショットで、成長フェーズ（「全盛期」等）が灰色の脚質ラベルから
明確に黄で浮き上がって見えることを確認。race/calendar・race/statusでは主ボタンが紫、
セクション見出し・数値が黄という統一感を確認。

## Phase 3-D-4-c: intro・scheduleBoard・transferEvents・yearend 15画面（設計確定・実装待ち）

**設計案**：https://claude.ai/code/artifact/c5f2bc1e-983e-4e2a-abf6-f441594fe21e
（実データ・実フォント埋め込み。§8の手順1〜3を実施し、争点3件すべて合意済み）

### 実測（着手時）

| ファイル | 行数 | 画面 | 旧`C.xx` | fontSize | 角丸/枠線 | 絵文字 |
|---|---|---|---|---|---|---|
| intro.jsx | 264 | intro/newgame_setup/scoutpolicy_initial/sponsor | 80 | 12種(10〜23) | 12/6 | 11個 |
| scheduleBoard.jsx | 149 | program/standings/trophy/rivals | 52 | 12種(9.5〜34) | 8/6 | 11個 |
| transferEvents.jsx | 145 | event/transferRequest/poachOffer/poachMarket/event_result | 41 | 11種(9〜20) | 6/1 | 1個 |
| yearend.jsx | 82 | yearend/clear | 20 | 9種(10.5〜44) | 3/0 | 7個 |

4ファイルとも`T`使用0。本ラウンド完了で旧`C`の残りはPhase 3-E対象（race.jsx/RaceView/
DecisionCard）＋ui.jsx本体＋BaseView.jsxズームボタン3箇所のみになる（season.jsx/
mylife.jsxの「読み込み中…」各1箇所は本ラウンドで移行）。

### 確定仕様（ユーザー合意・2026-08）

**争点1＝案A「今回の開幕内容だけを要約」**（newgame_setup）
- CP_MILESTONES16件＋コース3件の全列挙を廃止。解禁済み特典を**集計した要約Item**
  （開幕資金+800万円／初期選手の能力+23／チーム設備Lv+3／逸材新人2名／開幕アイテム各2個
  ——累計128pt時の実値）＋「次の解禁まであと2pt（開幕資金+600万円）。全一覧は「生涯評価」に
  あります。」1行に置き換える。
- **要改修**：`CP_MILESTONES`の効果が`apply`クロージャで不透明なため、各件に集計用の
  構造化フィールド`fx`を併記（例：`fx:{budget:100}`／`fx:{abAll:8}`／`fx:{equipLv:1}`／
  `fx:{rookie:1}`／`fx:{items:1}`）し、`cpMilestoneSummary(totalCP)`（domain/mylife/cp.js）で
  解禁済み分を合算する。0の項目は行ごと非表示。CP0なら開幕ボーナスセクション自体を出さない。
- 上部は共通確定：累計CP Item→チーム名input（surfaceUp・使用不可文字はbad色caption＋開始
  disabled）→難易度SelectRow×4（ロック行は薄く＋「累計Npt必要」）→レジェンド招聘SelectRow
  （殿堂0名ならセクション非表示）→開幕ボーナス→PrimaryBtn「この内容でゲーム開始」→戻る。

**争点2＝案B「表で比較して選ぶ」**（sponsor）
- 3社を固定幅列の比較表へ（R2）：見出し行=社名＋タイプ語、行=月額/ノルマ/達成/未達/指定。
  列幅実測：値最長5文字=70px、3列＋左見出し52px=388px<392pxで収まる。
- 中期目標は表の下に3行（label＋right=社名＋desc・達成/未達の数値）。
- 選択は**SelectRow×3＋PrimaryBtn1つ**（「クレセント自転車と契約する」と社名を入れた動的
  ラベル）。従来の「各社に契約ボタン」を廃止し、D-5の「主ボタンは1画面1つ」に揃える。
- 選択状態は`g.sponsorChoiceIdx`（初期値0・契約確定時に破棄される一時フィールド）。
- 金額は符号のみで色分けしない（good/badは結果専用。契約条件は「これから」の値）。

**争点3＝案A「言葉が主役」**（yearend/clear）
- 絵文字（🎉😞🏆44px等）と色付き上枠線を廃止し、**出来事の言葉をdisplay(28px)に**：
  yearend=「{cls.label}へ昇格」(good)／「{cls.label}へ降格」(bad)／「残留 — 来季へ」(text)、
  clear=「グランファイナル制覇」(accent・10文字280px<392pxで1行)。
- yearend構成：hero（caption「N年目 終了」＋display＋caption CS結果）→Section最終成績
  （Item：最終順位 N/M位・detail順位ボーナス＋昇格ライン緩和）→Sectionスポンサー精算
  （right=社名。達成/未達は**結果なのでgood/bad**で塗る）→中期目標Item→Section引退セレモニー
  （「{名前}（N歳）が引退」行）→新年度caption→PrimaryBtn「スポンサー契約へ」。
  各データ無し（CS不出場・精算無し・引退者0）は該当セクション非表示。
- clear構成：hero（caption「PROクラス N年目」＋display＋経緯body）→Sectionクリア報酬
  （Item：獲得クリアポイント +Npt accent・detail自動反映説明）→PrimaryBtn「この轍を継いで
  さらなる高みへ」＋caption「N周目へ。他チームがさらに強化されます」→QuietBtn「新たな
  チームで最初から」＋確認。

**争点によらず確定（変換ルール適用）**
- 天候はアイコン→**文字**（「雨・」「猛暑・」accent色）。D-4-b済みcalendar.jsxと同形式。
  ★グレードは維持（データ層の記号）。
- イベント通知3画面（event/transferRequest/poachOffer）の色付き上枠線廃止。種別は見出しの
  言葉で伝える。選択肢：推奨行動=PrimaryBtn／失うものがある行動=QuietBtn(bad)＋確認／
  等価な選択肢（event）=QuietBtn(action)縦並び。
- 順位表=R2固定幅列（順位・チームカラー■(データ層維持)・名前・特色・pt右揃え。自チーム=
  accent）。最終順位ボーナスはItem×3（1位+150万/2位+90万/3位+40万・B1実値）＋昇格ライン
  caption。
- trophy=D-4-a2生涯評価と同型（スコアdisplay 28px＋TitlesPanel＋殿堂行。殿堂0名の空文言
  維持）。rivals=チームごとSection＋ScoutBadge行。poachMarket=RiderCard（market/transferと
  同型・footer=ShopBtn「移籍金N万で引き抜く」）。
- introの「最初から」確認に`confirmLabel="消して始める"`。セーブ無し時は「スカウト方針の
  確認へ」をPrimaryBtnに昇格（続きからが無い画面の主ボタンは開始のみ）。
- 全15画面の要素列挙・データ取得元・空状態は設計Artifactの§6仕様表が正。
