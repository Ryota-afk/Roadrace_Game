# 第102弾：結果画面（A-1）— まず報酬の実態を測った

## この弾の発端

第101弾の締めで出したネクストアクション候補からユーザーが選択：

- **A-1（結果画面の作り直し）**に着手する
- **(b)**：「予想を上回る快走」は主役にしない（TODO #32-kにより新人では高確率で出るため）

候補を作る前に、結果画面が実際に受け取る`resultInfo`をヘッドレスで26レース分捕まえた
（`scratchpad/result_dump.mjs`。`tools/simcareer.mjs`と同じディスパッチ表を使い、
`src/`は一切変更していない）。

## 実測：⚠️「0と0」はデビュー戦だけの話ではなかった

キャリア開始から26レース連続の`resultInfo`：

| # | 年/月 | 着順 | pts | 賞金 | 予想 | 世界ランク(前回) | 監督指示 |
|---|---|---|---|---|---|---|---|
| 1 | 1/0 | 36/43 | **0** | **0** | 43番手 | 21(—) | 達成 |
| 2 | 1/1 | 29/44 | **0** | **0** | 31番手 | 135(21) | 未達 |
| 3 | 1/2 | 25/38 | **0** | **0** | 38番手 | 160(135) | 未達 |
| … | … | … | **0** | **0** | … | 22x台 | 達成/未達が半々 |
| 25 | 3/0 | 29/44 | **0** | **0** | 10番手 | 232(231) | 未達 |
| **26** | **3/1** | **4/42** | **6** | **12万円** | 9番手 | 219(232) | 達成 |

⚠️**このランでは26レース中25レースが「獲得ポイント +0pt」「賞金 +0万円」で、
初めて0でなくなるのは26レース目＝3年目2月（4位）だった。**

⚠️⚠️**【訂正】この「25戦ゼロ」は単発の実行の値であり、代表値ではない。**
同じプローブをもう一度回したところ、⚠️**2本目のキャリアは1年目6月に既に5pt・16万円**を
得ていた。`mlCreateChar`は`Date.now()`系のrngで実行ごとにキャリアが変わる（TODO #29-f）。
⚠️**第99弾で4回撤回したのと同じ「単発の実行から結論を出す」誤りを繰り返すところだった。**
複数キャリアでの分布は下の「実測6」に差し替える。

### 構造的な原因

`data/economy.js`：

```
export const PRIZES = [100, 60, 40, 30, 22, 16, 12, 9, 6, 4];
export const PTS    = [ 10,  7,  5,  3,  3,  1,  1, 1, 1, 1];
```

どちらも⚠️**10要素の配列＝11位以下は一律ゼロ**。
新人のB1での着順帯は本計測で14〜44位（26戦中、10位以内は1回だけ）。

⚠️**計測の限界（明示）**：この方策は毎月必ずレースに出て**練習を一度もしない**
（`tools/simcareer.mjs`の既定方策と同じ）。難易度は**easy**
（`initMyLife()`が`mlDiffChoice`を持たないため`createChar`が`"easy"`へ倒れる）。
実プレイヤーは練習するのでゼロを抜ける時期は早まる。⚠️**「25戦ゼロ」はこの方策での値。
ただし「11位以下は一律ゼロ」という構造自体は方策に依存しない。**

## 実測2：⚠️「予想を上回る快走」は26回中24回（92%）出る

`beatForecast = forecast && rank < forecast.rank - 1`（`race.jsx:198`）。
TODO #32-kの「新人は78%の確率で予想を上回る」より⚠️**さらに高い**。

⚠️**ほぼ無条件に出る称賛は情報量がゼロ**（CLAUDE.md §0-1）。
ユーザーの選択(b)——「予想超えを主役にしない」——は実測に照らして正しかった。

## 実測3：⚠️色の割り当てが逆になっている

デビュー戦の結果画面で、

| 行 | 出現頻度 | 実際の色 |
|---|---|---|
| 「予想を上回る快走」 | **92%（ほぼ毎回）** | ⚠️**good（緑）** |
| 「初めてランキングに載った」 | **キャリアで1回だけ** | ⚠️**sub（灰）** |

`worldRankUp = worldRank != null && worldRankPrev != null && worldRank < worldRankPrev`
——初掲載は`worldRankPrev == null`なので`worldRankUp`が`false`になり、
⚠️**一度きりの出来事が「上昇でも下降でもない」灰色で描かれる**。

## 実測4：⚠️世界ランキングは最初の2年ほぼ毎回「後退」

21位（初掲載）→ 135 → 160 → 223 と**4戦で急落**し、以後2年間222〜232を漂う。
26戦目（4位）でようやく232→219へ上がる。

つまり⚠️**「世界ランキング」の行は、最初の2年間ほぼ毎回「◯位から後退」を出し続ける。**
（初掲載時に21位という高い数字が出るのは、その時点でランキングに載っている母数が
少ないため。プレイヤーから見ると「21位でデビュー→すぐ223位に転落」と読める。）

## 実測5：⚠️「この一戦の成果」に他人の記録が入る

`courseRecord`は`isNew`だけで出し分けており、`isPlayer`は色と文言にしか使っていない。
実測では#1が「牧野 奏太が樹立」、#26が「松井 遼が樹立」——
⚠️**どちらも他人の樹立が「あなたの成果」欄に並んでいる。**

## この弾で分かったことの意味

⚠️**最初の2年間、「この一戦の成果」で情報を持っている行は「監督指示の達成/未達」だけ**である
（26戦で達成12・未達14とほぼ半々＝実際に分岐している）。他は、

- 獲得ポイント／賞金＝**常に0**
- 予想超え＝**92%で出る**
- 人気度＝**毎回 +0.1〜0.2**
- 世界ランキング＝**ほぼ毎回「後退」**
- コースレコード＝**他人の記録**

⚠️**したがって「表示の作り直し」だけでできることには天井がある。**
並べ替えても、2年間ゼロであるという事実は変わらない。
これは表示の問題と**報酬設計の問題**（11位以下に何も無い）が重なっている。

⚠️**報酬設計に手を入れるのはバランス変更であり、§10の計測を伴う別の判断である。**
本弾では勝手に広げず、ユーザーに提示して選んでもらう。

---

# 実測6：8キャリアで測り直した報酬ゼロの分布（訂正版）

`scratchpad/reward_probe.mjs`。⚠️**8キャリア×3年＝288レース**、難易度normal、
毎月出走・練習なしの方策。

| 指標 | 実測 |
|---|---|
| **pts=0 かつ 賞金=0 の割合** | **76.4%**（288戦中） |
| 初めて報酬が出るレース番号 | 25, 18, 20, 28, 20, **6**, **8**, 23（**中央値20戦目**） |
| 10位以内に入った割合 | 23.6% |
| 着順の中央値 | **21位**（最小1・最大48） |
| 3年間の賞金累計 | 70 / 273 / 108 / 20 / 78 / 162 / 29 / 29 万円（中央値 約74万） |
| 「予想を上回る快走」が出る割合 | **86.1%** |

⚠️**単発ランの「25戦連続ゼロ」は外れ値寄りだった**（分布の端は6戦目・8戦目もある）。
正しい言い方は⚠️**「最初の3年は4回に3回が報酬ゼロ、初報酬は中央値で20戦目」**。

⚠️**「予想を上回る快走」は86.1%**——単発ランの92%より下がったが、TODO #32-kの78%より高い。
⚠️**ほぼ無条件に出る称賛という評価は変わらない。**（3つの測り方で78 / 86 / 92%）

## 参考：収入に占める賞金の割合

別ランの月次追跡（`scratchpad/income_dump.mjs`・3年37ヶ月）：

| 収入源 | 3年間の累計 |
|---|---|
| 年俸（月1/12ずつ） | 約873万円 |
| 人気度収入 | 約106万円 |
| **賞金** | **157万円（総収入の約14%）** |
| 生活費 | −437万円 |

⚠️**賞金はもともと収入の主役ではない**（年俸が毎月自動で入る）。
つまり「賞金ゼロ」は経済的な困窮ではなく、⚠️**レースの結果と手応えが結びついていない**という問題。

# 実測7：⚠️報酬はゼロではない。見えていないだけだった

`controllers/mylife/month.js:97`——レースに出た月は**出走経験で能力が伸びる**。

```
raceExpKeys.forEach(k => addAb(player, k, 1.0 * raceGradeMul * mentorMul * ... ));
```

`scratchpad/growth_probe.mjs`で実測（3キャリア×2年＝**72レース月**、normal）：

| 指標 | 実測 |
|---|---|
| **1レースで伸びる能力の数** | **平均2.53個** |
| 内訳（伸びた回数） | 平坦65 / 登坂51 / スプリント38 / 独走16 / スタミナ12 |
| 総合力の変化 | 平均 **+0.96**（最小−2・最大+6） |
| ⚠️総合力が0以下だった割合 | 44.4%（`overall()`は上位3能力の加重なので、伸びても総合力に出ないことがある） |
| **活力の消耗** | 実測サンプルで毎レース **−7〜−11** |

実際のサンプル（1レース月ぶん）：

```
{ dOvr: 3, dAb: { flat: +1.77, climb: +6.74 }, dVit: -11 }
{ dOvr: 2, dAb: { flat: +1.75, climb: +2.08, sprint: +1.27 }, dVit: -11 }
{ dOvr: 1, dAb: { flat: +0.83, sprint: +0.62, stamina: +6.00 }, dVit:  -7 }
```

⚠️**結果画面はこれを1つも出していない。** 「この一戦の成果」に並ぶのは
pts・賞金・予想・人気度・コースレコード・世界ランキングだけで、
⚠️**実際に起きた「能力が2〜3個伸びた」「活力を7〜11失った」はどちらも画面に出ない。**

⚠️**つまり「+0pt +0万円」は嘘ではないが、実態を表していない。**
プレイヤーは強くなり、かつ有限の活力を失っているのに、画面は「何も起きなかった」と言っている。

## ②の内訳が2つに割れる

ユーザー選択の②（表示＋報酬の裾を伸ばす）は、実測の結果2つの別物に分かれた。

- **②-i　見えていない報酬と消耗を出す**——バランス変更を伴わない。
  「成長」だけでなく⚠️**「活力の消耗」も同時に出す**のが要点（CLAUDE.md §0-2：
  報酬を足す前に何が締め付けるかを見せる）。
- **②-ii　金銭・ポイントの裾を伸ばす**——⚠️**本物のバランス変更**。
  ⚠️リスク2件：(1) §0-2の「均す」癖に触れる（勝てないのに金が入る）。
  (2) TODO 31-C「恒久需要が13.7年で枯渇」を早める。
  ⚠️またPTSは昇格（45pt）を決めるため、**ポイントに触ると進行が動く**＝§10の計測必須。
