# 第71弾：第2次UI改革・残り4件の再計測

**状態**：**計測完了・方針の合意待ち（2026-08）。実装未着手。**

DEVLOG TODO #12。項目に「⚠️実測値は第61弾Phase1（配色トークンの底上げ）より前のもの＝
当てにならない。着手するならまず測り直すこと」とあるため、まず再計測した。

## 計測方法

`devlog/wave35.md`の表と同じ定義：本文（ヘッダ・下部タブを除く）の末端テキストノードを採取し、
**文字数で重み付け**してフォントサイズ帯ごとの比率を出す。
スクリプトは`scratchpad/w71_measure.mjs`。
⚠️**当時のスクリプトは残っていないため書き直した。** 定義は合わせたが完全な同一は保証できない
（特に「本文」の切り出し境界）。傾向の比較として読むこと。

## 結果

| 対象 | 指標 | 旧値 | **再計測** | 目標 | 判定 |
|---|---|---|---|---|---|
| キャラ作成 | 16px以上 | 7.7% | **7.1%** | 15% | ❌ ほぼ変化なし |
| 共通ヘッダ | 10px以下 | 80% | **81.8%** | 60% | ❌ ほぼ変化なし |
| ショップ／パーツ | 16px以上 | 12.7% | **11.7%** | 18% | ❌ ほぼ変化なし |
| ショップ／恒久投資 | 縦 | 972〜1001px | **835px** | 900px | ✅ **達成済み** |

⚠️**恒久投資タブは目標を達成していた**（第37弾のコーチ節圧縮以降の改修で縮んだと見られる）。
残る3件は第61弾の配色変更では動かなかった——**配色は級数の分布を変えないので当然**であり、
「測り直す必要がある」という当時のメモは、結果として空振りだった。

## ⚠️より重要な発見：≤10px帯の中身

級数ごとの文字数（ショップ2画面）:

```
ショップ／パーツ   : {10px:114, 13px:37, 16px:17, 20px:3}   ← 全171字中114字(67%)が10px
ショップ／恒久投資 : {10px:362, 13px:38, 16px:49, 20px:3}   ← 全452字中362字(80%)が10px
```

⚠️**11〜12px帯（`label:11`）は両画面とも0%。** つまり実質「10px か 13px 以上か」の
2択になっており、`T.size`が10段用意されているのに中間段が使われていない。

≤10pxを占めている実物（文字数の多い順）:

| 画面 | 字数 | 内容 |
|---|---|---|
| 両方 | 38 | 年俸280万円/年（毎月23万円が振り込まれます・生活費/税 -12万/月） |
| パーツ | 30 | 上位パーツはAクラスで7点・PROクラスで4点が解禁されます |
| パーツ | 10×4 | 買える3点　28万〜（スロットごとの要約） |
| 恒久投資 | 34 | 早熟寄り・晩成寄りのどちらか一方向のみ、キャリアで1回だけ選び直せる |
| 恒久投資 | 28 | 昇格させるほど練習効果が高まる（月給制・契約金100万） |
| 恒久投資 | 25 | レース参加による疲労蓄積が10%軽減される（恒常） |
| 恒久投資 | 24 | デビュー3年目に成長力が判明してから購入できます |

⚠️**恒久投資タブの362字はほぼ全てが商品の説明文（`desc`）**で、これは共有部品`ShopRow`の
`detail`が`caption:10`で描かれるため。第35弾が「`ShopRow`に手を入れる必要があり影響範囲の
見積もりからやり直す」と書いて止まっていたのは、まさにこの構造。

## ⚠️目標指標そのものへの疑問（設計判断が要る）

「16px以上の比率を上げる」は、**達成手段が2つあり、方向が正反対**である。

1. **足す**：小さい説明文を大きくする → 画面の情報量は変わらず、視覚的な重量だけ増える
2. **引く**：小さい説明文を減らす／別の見せ方にする → 分母が減り比率が自動的に上がる

⚠️**CLAUDE.md §8は「デザインは足し算ではなく引き算」「小さい文字の長い説明で誤魔化して
いないか」を掲げている。** その基準で読むと、この4件の目標は
**「大きい文字を増やす仕事」ではなく「小さい説明文を減らす仕事」**として扱うべきであり、
指標(16px以上%)は結果を測る物差しであって、それ自体を目標にすると1の方向へ誘導されかねない。

実際、≤10px帯の中身を見ると**削れる／別の見せ方にできる候補**が並んでいる：
- 年俸の38字はショップの2タブ両方に出ている（同じ情報の重複表示）
- 「上位パーツはAクラスで7点…」はロック理由の説明で、ロックされた行の隣に置けば済む
- 恒久投資の商品説明は、1行に圧縮するか、選択時のみ展開する余地がある

## 次の判断（ユーザー合意待ち）

1. この4件を**「小さい説明文を減らす」仕事として再定義**してよいか
2. 恒久投資タブ（達成済み）はTODOから外してよいか
3. 着手するなら`ShopRow`（シーズン側も使用）に手を入れることになる——影響範囲の
   確認から始める
