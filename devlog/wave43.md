# 第43弾（設計確定）: 出走計画 — レース選択の操作量を増やす

2026-08 設計。第42弾で退行モデルを撤回した際、**根本原因は「プレイヤーがレースの地形を
ほとんど選べないこと」**だと判明した（`devlog/wave42.md`末尾）。この弾はそこを直す。

## 背景：なぜ操作量が足りないのか

| 事実 | 値 |
|---|---|
| コーステンプレの総数 | **6種**（クリテリウム／サーキットレース／丘陵ロード／山岳ロード／ヒルクライム／個人TT） |
| うち山岳を含むもの | **2種**のみ（山岳ロード0.76／ヒルクライム1.00） |
| 候補が1本しか出ない月 | クラス0で4/12、**クラス2以降で6/12**（1月石畳古典・4月丘陵古典・6月山岳古典・5月世界選手権・3月五輪・11月年度末） |
| `favors`が`flat`のテンプレ | **0種**（平坦区間はスプリント系コースから供給されている） |

狙って選んでも、**年の半分は選択肢が無い**。結果、山岳狙いのプレイヤーの露出率は0.496、
無頓着は0.222で、8戦窓のばらつきに埋もれる（第42弾の計測1〜3）。

## 計測：どの方式が効くか

12年×3クラス、24年分の候補生成をシミュレートして比較した。

| 案 | 無頓着 | 山岳宣言 | 差 | 8戦窓の分離 |
|---|---|---|---|---|
| 現状 | 0.222 | 0.496 | 0.274 | −0.049 |
| 通常月を5候補に | 0.222 | 0.622 | 0.400 | +0.076 |
| 全月3候補（看板月にも選択肢） | 0.222 | 0.695 | 0.473 | +0.140 |
| 重み w=2 | 0.222 | 0.564 | 0.342 | +0.030 |
| 重み w=3 | 0.222 | 0.599 | 0.377 | +0.061 |
| **★必ず1本含める（採用）** | 0.222 | **0.597** | 0.375 | +0.061 |

**「必ず1本含める」を採用。** 効果は重みw=3と同等（0.597対0.599）でありながら、
確率ではなく確定仕様なので**一文で正確に説明でき、値がぶれない**。

⚠️「全月3候補」は効果最大だが、`race.js`冒頭の第41弾の決定（看板レースは選択肢を出すと
格が下がる）を覆すため不採用。「5候補」は6種中5種を毎月見せることになり、狙った地形が
ほぼ必ず手に入る＝毎月の迷いが消えるため不採用。

### 年間本数（この弾の実効果・⚠️プレイヤーには出さない）

| 宣言 | 宣言なし | 宣言あり |
|---|---|---|
| 山岳 | 年3.4本 | 年8.1本 |
| 丘陵 | 年3.1本 | 年8.8本 |
| スプリント | 年3.3本 | 年8.0本 |
| 独走（TT） | **年1.2本** | 年7.0本 |

⚠️**独走の行が重要。現状のTT型は年1.2本しかTTを走れていない。** 第41弾で`soloist`が
「狙っても0.33止まり」だったのはこれが原因で、TT型のビルドは事実上成立していなかった。

### 却下した案：`badgeGoals`を宣言に流用する

各脚質の目標バッジ母集団10種のうち**地形に紐づくのは常に1種だけ**（しかも自分の脚質に
gateされたもの）。これでは「クライマーが意図して平坦を走り込み`flatlander`を獲る」が
できず、残り9種を選ぶと偏りがゼロになる。**成立しないため却下**（設計途中で一度
「`badgeGoals`に意味が生まれる」と述べたが誤りだった）。

## 確定仕様

### データ

| 項目 | 内容 |
|---|---|
| 保存先 | `ml.raceFocus`（新規・`null`＝未指定）。`ML_SAVE_FIELDS`（`state/mylifeState.js`）に追加 |
| 取りうる値 | `"climb"` / `"hill"` / `"sprint"` / `"solo"` / `null` |
| 選択肢の導出 | `DISCIPLINE_KEYS`のうち、`TEMPLATES`のいずれかが`FAVORS_TO_DISCIPLINE[t.favors]`で該当するもの。**`flat`は該当テンプレが0種のため選択肢に出さない** |
| 表示名 | `DISCIPLINES[k].label + "中心"`（山岳中心／丘陵中心／スプリント中心／独走（TT）中心）。`null`は「特に決めない」。**新語彙を作らない** |
| 初期値 | `initMyLife()`・`domain/mylife/createChar.js`とも`null` |

### 生成ロジック

`mlGenRaceCandidates(year, month, classIdx, focus)` に**第4引数を追加**する。

- `focus`が`null`／未指定なら**現状と完全に同一の出力**（RNGの消費順を1回も変えない）
- 効くのは**通常月（候補3本の月）のみ**。看板レース月（古典3種・世界選手権・五輪・
  11月年度末）は分岐の手前で`return`するため**一切変更しない**
- 通常月では、`FAVORS_TO_DISCIPLINE[t.favors] === focus` を満たすTEMPLATEから**1本を先に
  引き**、残り2本を従来どおり残りのプールから重複なしで引く
- `mlGenRace(year, month, classIdx)`は**シグネチャ据え置き**（他クラスの世界ランク用に
  偏りのない生成が必要なため。下表参照）

### 呼び出し6箇所の扱い

| 箇所 | 用途 | 対応 |
|---|---|---|
| `domain/mylife/createChar.js:252` | キャリア開始時の初月 | そのまま（この時点ではまだ出走計画を選んでいない）。下記「初月の扱い」参照 |
| `controllers/mylife/career.js:43` | クラス変動時の再生成 | `s.raceFocus`を渡す |
| `controllers/mylife/month.js:457` | 年跨ぎ | `s.raceFocus`を渡す |
| `controllers/mylife/month.js:468` | 年跨ぎ | `s.raceFocus`を渡す |
| `controllers/mylife/month.js:522` | 翌月へ | `s.raceFocus`を渡す |
| `controllers/mylife/month.js:492` | **他クラスの世界ランク用**（`mlGenRace`） | ⚠️**無変更**。他クラスの選手はプレイヤーの計画と無関係 |

**初月の扱い**：`mlConfirmBadgeGoals`（出走計画の確定もここで行う）で、`raceFocus`が
`null`以外なら`races`を`mlGenRaceCandidates(year, month, classIdx, raceFocus)`で
**再生成する**。まだ1戦も走っていないため安全であり、これをしないと「計画を立てた直後の
初月だけ計画が無視される」ことになる。

**変更の反映タイミング**：`ml.races`は生成済みのため、ホームで計画を変えても
**今見ている候補は入れ替わらず、翌月の候補から効く**（変更による鞍替えを防ぐ）。

### UI（§8の手順どおりモック提示→合意済み。案A採用）

**キャラ作成（`mylife_badge_goals`画面）**——順序は**目指すバッジ→出走計画**。
出走計画は手段の選択なので、目的（バッジ）を決めた直後に置く。

```
目指す選手像                        ← 画面タイトル
  目指すバッジ            3 / 3 選択  ← セクション
  クライマーが目指せるバッジから3つ選ぶ。あとから変えられる
  [既存の10種リスト]
  出走計画                          ← セクション（新規）
  山岳中心                     選択中
  丘陵中心 / スプリント中心 / 独走（TT）中心 / 特に決めない
  毎月の候補に山岳のレースが必ず1本入る   ← 選択中のものだけ1行
[この内容でキャリアを始める →]
```

⚠️**併せて既存の見出し重複を解消する**（ユーザー合意済み）。現行の`create.jsx`は
296行目の画面タイトルと308行目のセクション見出しが**どちらも「目指す選手像」**。
タイトル＝「目指す選手像」、セクション＝「目指すバッジ」「出走計画」に整理する。

**ホーム**——既存の「レース作戦」PressRowの直後に「出走計画」PressRowを追加。
アコーディオンの構造は`ML_TACTICS`のものをそのまま流用し、作成画面と同一の見え方にする。
開いた状態の末尾の一文は「毎月の候補に山岳のレースが必ず1本入る。変更は翌月の候補から効く」。

**⚠️数字は一切出さない**（ユーザー明示）。上の「年3.4本→年8.1本」は開発用の実測値であり
画面には出さない。効果は「毎月の候補に◯◯のレースが必ず1本入る」という確定仕様の一文で
伝える——平均ではないのでぶれず、数字より正確。

### empty state

- `raceFocus`が`null`（新規キャラの初期値・旧セーブ）→ 生成は現状と完全一致。
  ホームのPressRowの`value`は「特に決めない」、開いた末尾の一文は
  「毎月の候補は成り行きで決まる」。

## この弾でやらないこと

- **退行（金→銅）の再導入**。第42弾で撤回したまま。この弾の実装後に`devlog/wave42.md`の
  計測1〜3を測り直し、**N=8で分離するようになって初めて再検討する**。分離しないうちは入れない。
- 看板レース月への選択肢追加（第41弾の決定を維持）。
- コーステンプレの追加（6種のまま）。
- バッジ所持上限3個の撤廃・AIへの段階配布・33種への解放経路拡張。

## 検証項目（実装後に必ず全部）

1. **`focus=null`での出力が現行と完全一致**。全年（1〜24）×全月（0〜11）×全クラス（0〜3）で
   旧実装と新実装の出力を突き合わせ、差分ゼロを機械的に確認する。
2. 4適性それぞれについて、**通常月の候補に必ず1本その適性のレースが入る**ことを全年月で確認。
3. **看板レース月（古典3・世界選手権・五輪・11月）の出力が`focus`によらず不変**であることを確認。
4. `month.js:492`（他クラスの世界ランク用）が**偏っていない**ことを確認。
5. 作成画面で出走計画を選ぶと**初月の`races`が再生成される**ことを実プレイで確認。
6. ホームで計画を変えても**今月の候補が入れ替わらず**、翌月から反映されることを実プレイで確認。
7. 旧セーブ（`raceFocus`欠落）で**クラッシュせず現状と同一挙動**になることを確認。
8. `pageerror`ゼロ・**縦線ゼロ**（§8）・**絵文字ゼロ**（§8）。

## 実装結果（2026-08・Sonnet）

**①第42弾の撤回を先に実施**（本題の前提作業）：
- `src/sim/buildMyLifeSim.js`：`goldAbilities: liveGoldAbilities(player)`を
  `goldAbilities: player.goldAbilities`へ戻し、`liveGoldAbilities`のimportを削除。
- `src/screens/mylife/rider.jsx`：状態C（`drifting`）の分岐一式を削除し、第39弾の状態A/B/D/E
  のみに戻した。`badge.js`からの関連import（`badgeExposureScore`/`badgeReturnLabel`/
  `EXPOSURE_NORM`/`swapsToRestoreGold`）も削除。
- `src/domain/mylife/badge.js`：ファイル冒頭に「現在どこからも呼ばれていない」「N=8では
  分離できないと実測済み」「第43弾の後に計測1〜3を測り直し、分離するようになってから
  再接続すること」を明記。関数自体は削除せず残した（次弾以降の計測土台）。
- 実プレイで確認：平坦専門で8戦走っても`山の申し子`が「金」のまま（退行なし）。

**②第43弾（出走計画）の実装**：
- `src/domain/mylife/race.js`：`mlGenRaceCandidates`に第4引数`focus`を追加。通常月
  （候補3本）のみ、`FAVORS_TO_DISCIPLINE`が`focus`と一致するTEMPLATEを1本、候補の先頭へ
  差し替える（候補が複数あれば`rng()`を1回追加消費して選ぶ。`focus`未指定時はRNG消費が
  一切増えないため出力は不変）。`mlGenRace`は変更なし（他クラス用・シグネチャ据え置き）。
- `src/state/mylifeState.js`：`ml.raceFocus`（既定`null`）を追加し`ML_SAVE_FIELDS`に登録。
- `src/domain/mylife/createChar.js`：新キャリア開始時に`raceFocus: null`へリセット。
- `src/hooks/useMyLifeGame.js`：`mlSetRaceFocus(focus)`ハンドラを新設（既存の`mlSetFocus`＝
  練習メニューの集中先とは無関係な別概念）。`mlConfirmBadgeGoals`を拡張し、`raceFocus`が
  設定されていれば初月の`races`をその場で作り直す。
- `src/controllers/mylife/career.js`・`src/controllers/mylife/month.js`：クラス変動時・
  年跨ぎ・翌月への3箇所で`mlGenRaceCandidates`に`s.raceFocus`を追加で渡す。
  `month.js`の他クラス用`mlGenRace`呼び出し（世界ランク集計）は設計どおり無変更。
- `src/screens/mylife/create.jsx`：目標バッジ宣言画面に「出走計画」セクションを追加
  （案A：目指すバッジのあと）。選択肢は`DISCIPLINE_KEYS`のうち実際に対応するTEMPLATEが
  存在するもの（`flat`は自動的に除外される）。併せて画面タイトルとセクション見出しの重複
  （どちらも「目指す選手像」）を解消し、セクション見出しを「目指すバッジ」に変更。
- `src/screens/mylife/hub.jsx`：既存の「レース作戦」PressRowの直後に同型の「出走計画」
  PressRowを追加。開閉状態は`ml.uiRaceFocusOpen`で管理。

**検証**（検証項目1〜8を実施）：
1. `focus=null`／未指定について、旧実装の再現コードと新実装を年1〜20×月0〜11×クラス0〜3
   の960通りで突き合わせ、**差分ゼロ**を確認。
2. 4適性（climb/hill/sprint/solo）それぞれについて、通常月570通り全てで**候補に必ず1本
   その適性が含まれる**ことを確認。
3. 看板レース月（候補1本の月）について、`focus`の値によらず**出力が完全に不変**であることを
   全年月で確認。
4. `month.js`の他クラス用`mlGenRace`呼び出しは元々`focus`を渡さない設計のため、対象外
   （偏りが生じないことは自明——コード上`raceFocus`を一切参照しない）。
5. Playwrightで実際にキャラを作成し「独走（TT）中心」を選択、初月の`races`にTT区間
   （個人TT）が含まれることを確認。
6. Playwrightでホームから「山岳中心」に変更した直後、`ml.races`が変更前と**完全一致**
   （入れ替わらない）ことを確認。その後「完全休養」で翌月（通常月）へ進み、新しい候補に
   宣言した適性（TT）が含まれることを確認。
7. `raceFocus`フィールドを削除した旧セーブ相当を読み込ませ、クラッシュせず
   「特に決めない」表示にフォールバックすることを確認。
8. `pageerror`はテスト全体を通じて0件（無関係な404リソースエラーを除く）。
   縦線・絵文字は追加していない（既存の`PressRow`/アコーディオン構造を流用したため）。

**やらなかったこと（設計どおり）**：退行の再導入・看板レース月への選択肢追加・コーステンプレ
追加・バッジ所持上限の撤廃。

---

# 実装後の再計測：退行は成立するようになったか（2026-08・Opus）

`devlog/wave42.md`の計測1〜3を、第43弾実装後の状態で測り直した。基準線（無頓着＝
`focus=null`・候補[0]を選ぶ）と最適プレイ（そのバッジに最適な`focus`を宣言し、候補から
最も寄与の大きいものを選ぶ）を同一手法で比較。24年×月0〜11、N=8。

## 結果1：N=8で**7/7が分離**した（第43弾の前は0/6）

余裕＝`p10(狙う) − p90(無頓着)`。正なら分布が重ならない。

| バッジ | 第43弾の前 | 第43弾の後 | キャリア平均（無頓着→狙う） |
|---|---|---|---|
| mount | −0.079 | **+0.076** | 0.225 → 0.603（2.68倍） |
| puncheur | −0.102 | **+0.088** | 0.240 → 0.500（2.08倍） |
| flatlander | −0.035 | **+0.054** | 0.351 → 0.616（1.75倍） |
| sprinter_sp | −0.006 | **+0.007** | 0.056 → 0.098（1.77倍） |
| soloist | −0.125 | **+0.250** | 0.128 → 0.601（4.68倍） |
| allclimber | −0.008 | **+0.062** | 0.465 → 0.757（1.63倍） |

各バッジの最適な出走計画：mount/allclimber/climbengine→`climb`、puncheur→`hill`、
flatlander/sprinter_sp→`sprint`、soloist→`solo`。
⚠️`flatlander`の最適が`sprint`なのは、`favors`が`flat`のテンプレが存在せず、平坦区間は
スプリント系コース（クリテリウム0.86・サーキット0.65）から供給されるため（設計どおり）。

## 結果2：定数は**中央値どうし**で取る（p90/p10では第42弾の失敗を繰り返す）

⚠️分離判定に使った`p90(無頓着)`/`p10(狙う)`をそのまま`base`/`target`にすると、
mountの帯幅は`0.470−0.393=0.077`で**1戦が窓平均に与える影響（1.00/8=0.125）より狭い**。
これは第42弾で振動した条件そのもの。実測でも反転が最大30回出た。

**中央値どうしで取ると帯幅が1戦の影響の2.2〜4倍**になり安定する：

| バッジ | 帯幅 | 1戦の最大寄与 | 窓への影響 | 影響/帯幅 |
|---|---|---|---|---|
| mount | 0.421 | 1.00 | 0.125 | 30% |
| puncheur | 0.258 | 0.60 | 0.075 | 29% |
| flatlander | 0.261 | 0.86 | 0.108 | 41% |
| sprinter_sp | 0.043 | 0.14 | 0.017 | 40% |
| soloist | 0.500 | 1.00 | 0.125 | 25% |
| allclimber | 0.286 | 1.00 | 0.125 | 44% |

```js
// 中央値どうしで導出した新しいEXPOSURE_NORM（第43弾実装後の実測）
// ⚠️TEMPLATES・候補数・出走計画の仕様を変えたら必ず測り直すこと。
mount:       { base: 0.204, target: 0.625 },
puncheur:    { base: 0.241, target: 0.499 },
flatlander:  { base: 0.340, target: 0.601 },
sprinter_sp: { base: 0.054, target: 0.097 },
soloist:     { base: 0.125, target: 0.625 },
allclimber:  { base: 0.475, target: 0.761 },
climbengine: { base: 0.204, target: 0.625 },
```

## 結果3：ヒステリシス0.6/0.4で退行は成立する

| バッジ | 狙う側の金率 | 狙う側の反転 | 無頓着の金率 | 転向後に落ちるまで |
|---|---|---|---|---|
| mount | 100% | 2回 | 5% | 7戦 |
| puncheur | 100% | 0回 | 6% | 4戦 |
| flatlander | 99% | 6回 | 7% | 6戦 |
| sprinter_sp | 99% | 4回 | 11% | 6戦 |
| soloist | 97% | 6回 | 0% | 6戦 |
| allclimber | 100% | 0回 | 9% | 4戦 |

（第42弾は「狙い続けても金率72%・30回反転」だった。）反転回数は約280窓あたりの値。
転向応答（12年狙った後に無頓着へ切り替え）は4〜7戦＝半年〜1年弱で銅に落ちる。

**結論：技術的には成立する。** ただし「入れるべきか」は別問題として下記に整理した。

## 判断：**退行は入れない**（2026-08・ユーザー確定）

第43弾を入れた結果、退行の意味合いが第42弾の構想時から変わった。**出走計画を宣言したまま
放置すれば金は維持される**ため、退行が発動するのは「プレイヤーが自分で宣言を変えたとき」
だけ＝実質的に**乗り換えコスト**であって、「走り方が自然に変わると落ちる」仕組みではない。

入れない理由：
1. **ユーザーの目的と逆向き。** 「事前にビルドの完成形を考えて、そこに向かって育てる」に
   対し、達成した型が後から崩れるのは逆行する。第42弾の撤回時にも指摘済みで、それを含めて
   「退行を諦める」が選ばれた経緯がある。今回の計測は技術的成立を示しただけで、この判断を
   覆す材料ではない。
2. **本家も退行から離れている。** 2K24のバッジ退行は不評で、2K25/26では廃止され
   「能力値でティアが決まる」方式に置き換わった。業界的にも定着しなかった仕組み。
3. **プレイヤーが弱くなる方向の変更。** 金の維持条件が増えるだけで上限は上がらない。

⚠️**「山を走り続ければ山のバッジが育つ」という第39弾からの狙いは、第43弾で既に実現した**
（山岳を宣言すれば年3.4本→8.1本、露出2.68倍）。退行はそこに乗せる追加の仕掛けに過ぎず、
目的に対して逆向きで得られるものが小さい、というのが判断の骨子。

**この判断は恒久的なものとして扱う。** 上の計測表は「露出率で走り方を判別できる」ことの
証拠として残すが、退行そのものを再検討する必要はない。`domain/mylife/badge.js`の関数群の
去就（削除するか残すか）は、次弾（所持上限の撤廃）の設計で露出率を使うかどうかが決まって
から判断する。
