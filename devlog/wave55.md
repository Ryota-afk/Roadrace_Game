# 第55弾: 協調に速度の見返りを与え、逃げを成立させる

> ⚠️**このファイルの最初の設計（`brk`だけの再較正）は失効している。**
> ユーザーの指摘「逃げには単独逃げと集団逃げがある」を受けて測り直した結果、
> `brk`は`mode==='solo'||'attack'`にしか掛からず**集団逃げには一切効かない**と判明した。
> さらに構造実測で**集団の速度が人数に一切依存しない**ことが分かり、設計の土台が変わった。
> 失効した設計は「同じ案を再提案しないため」に下に残す。**確定仕様はこのファイル末尾**。

## 【失効】当初の発端

⚠️**「逃げを成立させる」試みは3回失敗した**（第52弾＝定数調整／第53弾＝集団ごとのテンポ／
第54弾＝レース全体のテンポ）。3回とも**集団の速度に手を入れる**方向で、3回とも
**基準点（`hold`）を犠牲にする**か**フィールドを融合させる**かで失敗した。

⭐しかし第54弾の判定作業中の決定実験で、**真の障壁が特定できた**（全表は`devlog/wave54.md`末尾）。

- `TEMPO_BASE=0.85`（逃げに**17.6%**の速度優位）でも`attack`は`hold`より**5.84着悪い**。
  ⚠️**現実的にあり得ない速度差を与えても逃げは成立しない＝速度は障壁ではない。**
- テンポをOFFにし、`energyDrain`の`brk`（`committedBreak`かつ`solo`/`attack`の選手に掛かる
  消耗割引）**だけ**を下げると、⭐**基準点を一切犠牲にせずに`attack`が成立した**。

| brk | terrain/hold | terrain: attack−hold | mid: attack−hold | グループ数 | 逃げ切り勝ち |
|---|---|---|---|---|---|
| 現行`{mtn:0.4 … sprint:0.85}` | 12.93 | +12.25 | +4.59 | 6.86 | 3.1% |
| 0.25 | 12.66 | +4.87 | +2.26 | 6.60 | 11.5% |
| ⭐**0.22** | ⭐**12.93** | ⭐**+1.43** | ⭐**+1.91** | ⭐**6.74** | ⭐**10.4%** |
| 0.18 | 13.00 | **−3.74** | +0.63 | 6.71 | 13.5% |
| 0.15 | 12.98 | −5.17 | +0.44 | 6.73 | 15.6% |

**なぜこれが効くのか**：集団に一切触らないため**基準点が動かず**、速度差を作らないため
**誰が千切れるかの選抜も壊れない**。テンポが3回とも壊したのは、まさにこの2つだった。

⚠️**brk=0.18以下は行き過ぎ**（`attack`が`hold`より明確に良くなり、今度は「常に仕掛けるのが
正解」になる。§35で潰した「早駆けが常時最適解」の裏返し）。

## 確定仕様

### 1. テンポ（第53・54弾）をコードごと撤去する

実測で成立しないと確定したため、使わない仕組みを残さない。

- `src/sim/ticks.js`から`TEMPO_BASE`／`CHASE_FULL_GAP`／`TEMPO_END_PROG`／
  `TEMPO_DRAIN_EXP`の4定数と、その定義コメントブロックを削除する。
- tickループ先頭の`racePaceMul`/`raceDrainMul`の計算ブロックを削除する。
- モード決定後の`active.forEach`（`groupPaceMul`/`groupDrainMul`を配る処理）を削除する。
- 移動パスの`dist *= en.groupPaceMul`と`* (en.groupDrainMul ?? 1)`を削除する。
- ドラフト勢の`drain`計算から`tempoDrainMul`の項を削除する。
- ⚠️**`TEMPO_KEEP_TIGHTEN`／`TEMPO_ENERGY_COST`／`TEMPO_MIN_TICKS`／`TEMPO_MAX_TICKS`は
  削除しない。** これらは第51弾の判断カードの一手`tempo`（「ふるいにかける」）のもので、
  名前が似ているだけの別物。⚠️**混同して消さないこと。**
- 撤去後の挙動は第52弾完了時点（`TEMPO_BASE=1.0`相当）と**完全に一致するはず**。

### 2. `brk`（逃げの消耗割引）を再較正する

`src/sim/ticks.js`の`energyDrain`内：

```js
const brk = en.committedBreak && (mode === "solo" || mode === "attack")
  ? ({ mtn: 0.4, climb: 0.45, hill: 0.55, flat: 0.75, sprint: 0.85, tt: 0.75 }[segType] ?? 1) : 1;
```

**実測の最適値は全地形一律0.22**。ただし現行テーブルは「登坂ほど逃げが決まりやすい」という
v35の設計意図を持っており、これは残す価値がある。**平均が0.22前後になるよう、現行の形を
保ったまま圧縮する**：

```js
{ mtn: 0.18, climb: 0.19, hill: 0.21, flat: 0.25, sprint: 0.27, tt: 0.25 }
```

（現行の相対的な大小関係はそのまま・平均0.2250。⚠️これは**実測値ではなく設計値**なので、
実装後に「一律0.22」と同等の結果になるかを確認する＝下記の検証項目3）

### 3. ⚠️平坦系コースでの確認測定を必ず行う

⚠️**上記の実測はすべて山岳系レース（`mlGenRaceCandidates(..., "climb")`）でしか行っていない。**
現行の`flat: 0.75`／`sprint: 0.85`という高い値は今回ほとんど動かされておらず、
**平坦系コースで逃げが強くなりすぎる／弱いままである可能性がどちらもある**。
実装後に`"flat"`／`"sprint"`のレース選択で同じ格子を測り直すこと（Opus担当）。

## やらないこと

- `attack`/`send`の定数（`ATTACK_MIN_TICKS`／`SEND_*`）は変更しない。
- `mid`カードの内容は変更しない（§82の課題として保留）。
- `send`は`committedBreak = false`のため今回の変更の対象外＝**全発火点で最下位のまま残る**
  （§82）。これは別途の課題として保留する。
- `chaseMode`が展開に出ない問題（第53弾で解こうとしたもの）は未解決のまま残す。

## 検証項目

1. **テンポの撤去が完全であること**：`TEMPO_BASE`／`CHASE_FULL_GAP`／`TEMPO_END_PROG`／
   `TEMPO_DRAIN_EXP`／`groupPaceMul`／`groupDrainMul`／`racePaceMul`/`raceDrainMul`の
   参照がソースに1つも残っていないこと。
   ⚠️**`TEMPO_KEEP_TIGHTEN`等（第51弾の`tempo`ムーブ用）は残っていること**を同時に確認する。
2. **撤去後の挙動が第52弾完了時点と完全一致すること**：`git stash`等で第52弾時点の
   `ticks.js`と比較し、同一シードで全選手の`finishTime`が一致することを機械検証する。
   （`brk`の変更を入れる**前**に、撤去だけの状態で確認すること）
3. **`brk`の再較正が実測どおりの結果になること**：`scratchpad/w54_sweep_one.mjs`で
   `terrain: attack−hold`が**+1.4前後**・`terrain/hold`が**12.9前後**・グループ数が
   **6.7前後**になること（一律0.22での実測値と同等であること）。
4. ⚠️**平坦系コースでの確認**（上記3）。
5. 個人TT・チームTT（`noGroup`）が無影響であること。
6. 第45〜52弾の既存検証スクリプトが全て通ること。
   ⚠️`w53_verify.mjs`・`w54_verify.mjs`は**テンポの撤去により失効する**（削除または
   スキップしてよい。その旨をこのファイルに記録すること）。
7. `npx vite build`成功・Playwright実機で`pageerror`ゼロ。

---

# 目指すレースの形の擦り合わせ（2026-08・Opus）

ユーザーから「そもそも目指しているレースの形を擦り合わせよう」との提起。
⚠️**これまでの実測はすべて山岳系レースでしか行っておらず、地形別の現状を誰も把握していなかった**
ため、まず現状を測った（`scratchpad/w55_shape.mjs`・テンプレート別・n=60・主力power74/A・
グレード2・ライバル無し）。

| テンプレート | 先頭+1秒 | +3秒 | +10秒 | 勝者の独走% | 逃げ切り% | 未完走率 | レース長 |
|---|---|---|---|---|---|---|---|
| クリテリウム（平坦） | 1.5 | 5.7 | **24.1** | 0.7% | **0.0%** | 3% | 2198秒 |
| サーキット（平坦+丘） | 1.7 | 6.5 | **26.0** | 1.0% | **0.0%** | 4% | 2312秒 |
| 丘陵ロード | 1.3 | 3.8 | **14.8** | 1.3% | **0.0%** | 7% | 2478秒 |
| 山岳ロード | 1.2 | 2.4 | **7.6** | 2.9% | **0.0%** | **20%** | 2806秒 |
| ヒルクライム | 1.8 | 4.1 | **8.2** | 1.0% | **0.0%** | **71%** | 3217秒 |

## ⭐ 発見1：「地形で決着の形が変わる」は**すでに実現している**

+10秒以内の人数が **平坦24〜26人 → 丘陵15人 → 山岳8人** と、地形によって決着の規模が
きれいに3段階に分かれている。⚠️**ユーザーが目指すと言った形（山岳＝少人数・平坦＝大集団
スプリント・丘陵＝中集団）は、集団規模という意味では既に達成されている。**

→ **不足しているのは「規模」ではなく「独走」。** 勝者の独走割合は山岳でも2.9%、
**逃げ切り勝ちは全地形で0.0%**。山岳で「独走で決まる」絵が一度も出ていない。
⚠️つまり第52〜55弾で追ってきた課題は、**全地形の問題ではなく「山岳で独走が起きない」
という一点に絞れる**。

## ⚠️ 発見2：未完走率は地形固有のバグ

未完走率が **平坦3〜4% / 丘陵7% / 山岳20% / ヒルクライム71%** と、登坂系だけが破綻している。
原因は明白で、`MAX_TICKS = 2500`が**固定値**なのに対しレース長が2198〜3217秒と地形で
大きく違うため。登坂は`terrainSpeedMul`（climb 0.65 / mtn 0.6）で遅く、
**ヒルクライムは平均3217秒＝tick予算を700秒超過している**。
⚠️超過分は`finishTime = MAX_TICKS + (remain / lastDist)`という**外挿**で埋められており、
着順がシミュレーションではなく外挿式で決まっている。

→ `MAX_TICKS`をコース長・地形から算出する形に変えるのが素直な直し方。
これは「3割が完走できていない」というユーザー指摘そのものの原因である。

## ⚠️ 発見3：「先頭1.0以内の人数」は序盤に暴れる

推移（20%→40%→60%→80%）が 10.9→24.8→9.2→17.5 のように非単調。
これは`role:"breakaway"`のAIがtick 0で自動アタックして先頭に出るため、序盤は
「先頭」が逃げの選手になり本隊が1.0圏外に落ちる（＝人数が少なく見える）ことによる。
⚠️**この指標を「選抜の進み方」として読むときは、序盤の値を額面どおり受け取らないこと。**

---

# ⭐ 逃げの「形」の実測と、構造の根本原因（2026-08・Opus）

ユーザーの指摘：**「逃げにも単独逃げと集団逃げ（ブリッジで合流する形／初めから数人で逃げる形）が
あるのは考慮できてる？」**

⚠️**考慮できていなかった。しかも測定自体が壊れていた。** 従来の「逃げ切り」判定は
`mode === "solo" || "attack"` の割合で見ていたが、**2人以上で逃げる選手は自分たちで1グループを
作るためmodeは`pull`/`draft`になる**。つまり**集団逃げは最初から一切カウントされていなかった**。
「逃げ切り0.0%」という報告は「単独逃げが0%」としか言っていない誤報だった。

## 測り直し（本隊＝最多人数のグループとの位置関係で判定・n=40）

| テンプレート | 本隊で決着 | 単独逃げ | **集団逃げ** | ブリッジ発生率 |
|---|---|---|---|---|
| クリテリウム（平坦） | 95.0% | 0.0% | **5.0%** | 100% |
| サーキット（平坦+丘） | 97.5% | 0.0% | **2.5%** | 100% |
| 丘陵ロード | 95.0% | 0.0% | **2.5%** | 100% |
| 山岳ロード | 97.5% | **2.5%** | 0.0% | 100% |
| ヒルクライム | 100% | 0.0% | 0.0% | 73% |

集団逃げは実際に2.5〜5%で決まっており、ブリッジも全レースで発生していた。
⭐**弱いながら形は既に正しい方向を向いている**——集団逃げが決まるのは**平坦**、
単独逃げが決まるのは**山岳**で、実際のロードレースと同じ対応になっている。

⚠️**この測定の限界**：「先行集団の最大人数」が平均11〜16人・最大26人と出たが、これは
**フィールドの分裂を先行集団と誤認している**（判定が「最多人数のグループより前にいる集団」
なので、集団が二分割されると前半分が先行扱いになる）。⚠️**この数字は信用しないこと。**

## ⭐⭐ 決定実験：集団の速度は人数に一切依存しない

同一能力・同一シードで人数だけ変えて500tick走らせた（`scratchpad/w55_coop.mjs`）。

| 人数 | 500tick後の位置 | 残脚 |
|---|---|---|
| 1人 | 98.1 | **−100.0** |
| 2人 | 119.3 | −55.4 |
| **3人** | **126.6** | 43.4 |
| **6人** | **126.8** | 90.9 |
| **12人** | **126.6** | 91.6 |
| **24人** | **126.6** | 91.2 |

⭐**3人以上は速度が完全に同一（126.6）。人数は速度に1ミリも効かず、効くのは残脚だけ**
（43.4→90.9で飽和）。1人だけ遅いのも「単独走が遅い」からではなく、脚が−100まで枯れて
`energyPenaltyMul`が0.55倍に張り付くため。**速度式は全員同じ**。

原因は`ticks.js`の`const groupDist = puller.lastOwnDist;`——**集団の速度は「牽引者1人の全力」で
決まり、人数の項が存在しない**。

### ⚠️ これが第52〜55弾すべての空振りの正体

**逃げは時間差を作れない。** 本隊と同速なので、できるのは「同じ位置を保ったまま、より疲れて
ゴールする」ことだけ（3人の逃げは残脚43.4、本隊は91.2）。決着で負けて当然である。
⚠️**逃げが成立しない理由は、速度でも消耗の割引でもなく「協調に速度の見返りが無い」こと**
だった。第52弾（定数）・第53/54弾（集団のテンポ）・第55弾当初案（`brk`）はすべて
この本質を外していた。

⚠️ちなみに実測で**出走の24%がエース**で、エースは`canPull`で牽引から除外される
（ロール内訳は lead 28% / flat 24% / breakaway 21% / sub 20% / mountain 7%）。
つまり「牽ける人数」は総人数とは別物であり、**協調の質**を表す軸として既に存在している。

---

# 確定仕様（ユーザー合意済み・2026-08）

## 1. 協調による速度 `coopMul` を導入する（中核）

集団の速度を「実際に牽ける人数（`eligible`）」で決める。⚠️**飽和させるのが肝**——
飽和しないと本隊が常に最速になり、逃げが永久に成立しない。

```js
// ticks.js の集団ループ内。eligibleはローテーション用に既に計算済みのものを流用する。
const workers = Math.max(1, eligible.length);
const coopMul = 1 + COOP_MAX * (1 - Math.exp(-(workers - 1) / COOP_K));
// groupDist = puller.lastOwnDist * coopMul  （後述の適用位置に注意）
```

**定数（ユーザー承認済みのカーブに合わせて逆算した値）**：`COOP_MAX = 0.17`／`COOP_K = 3.15`

| 牽ける人数 | `coopMul` |
|---|---|
| 1 | 1.000 |
| 3 | 1.080 |
| 6 | 1.135 |
| 12 | 1.165 |
| 24以上 | 1.170（頭打ち） |

⚠️**単独走者（`members.length === 1`）は`coopMul = 1`**（`workers = 1`なので式からも自動的に1）。

## 2. 本隊の協調は「レースが進むほど」立ち上がる

⚠️**1だけでは逃げは今より不利になる**——本隊（牽ける人数20前後→1.168）が
逃げ（6人→1.135）より速くなってしまうため。実際のレースで逃げが成立するのは
**序盤の本隊が本気で追わないから**であり、それを「協調の質」として表現する。

`eligible`に**やる気のゲート**を掛ける：

```js
// 序盤は本隊の全員が追うわけではない。レースが進むほど組織的な追走になる。
const organized = Math.min(1, prog / CHASE_ORGANIZE_PROG);
const willingFrac = CHASE_EARLY_WILLING + (1 - CHASE_EARLY_WILLING) * organized;
```

- ⚠️**逃げ（`committedBreak`の選手で構成される集団）にはこのゲートを掛けない**——
  覚悟を決めて出た選手は最初から全員が回す。これが集団逃げの速度優位の源。
- ⚠️**選ぶ選手は決定論的に選ぶこと**（`riderHash01(en.id, salt)`で安定した順序付けをして
  上位`willingFrac`だけを`eligible`に残す）。`Math.random()`で選ぶとtickごとに顔ぶれが
  変わり、牽引ローテーションが崩壊する。

**定数（すべて仮置き・実装後にOpusが実測して確定）**：
`CHASE_EARLY_WILLING = 0.45`／`CHASE_ORGANIZE_PROG = 0.60`

これにより序盤は 本隊（20人×0.45＝9人→1.146）< 逃げ（6人全員→1.135）… ⚠️まだ本隊が速い。
⚠️**この数値関係は設計段階では確定できない**（`eligible`の実際の分布が未知のため）。
実装後に`CHASE_EARLY_WILLING`を振って**序盤に逃げが本隊よりわずかに速くなる**点を探す。

## 3. `brk`（単独逃げの消耗割引）は据え置き、Phase 2で判断する

⚠️当初案の`brk`再較正は**この弾では入れない**。協調による速度が入ると前提が丸ごと変わり、
第54弾で測った「brk=0.22が最適」はすべて無効になるため。単独逃げ（山岳）が
まだ成立しないようなら、Phase 2で改めて測って決める。

## 4. `MAX_TICKS`の固定値問題は**この弾では扱わない**

⚠️未完走率（ヒルクライム71%・山岳20%）は`MAX_TICKS = 2500`固定が原因の**独立したバグ**
（発見2）。協調による速度でレース全体が速くなるため**自然に改善する可能性がある**。
まず協調を入れて測り直し、それでも残るならコース長からの算出式を別途設計する。

## やらないこと

- `attack`/`send`の定数・`mid`カード・`send`が最下位の件（§82）はこの弾では扱わない。
- `chaseMode`を展開に接続する件も扱わない。

## ⚠️ 予測と撤退基準（4回目なので実装前に明示する）

実装後にOpusが測る（`scratchpad/w55_shape.mjs`＋`w55_breaktype.mjs`＋`w54_sweep_one.mjs`）。

**成功の形**：

| 指標 | 現状 | 目標 |
|---|---|---|
| 集団逃げで決着（平坦・クリテ） | 5.0% | **10〜15%** |
| 集団逃げで決着（丘陵） | 2.5% | **8〜12%** |
| 単独逃げで決着（山岳） | 2.5% | **10〜15%**（Phase 2の`brk`込みで可） |
| +10秒以内の人数（平坦／丘陵／山岳） | 24／15／8 | **形が保たれること**（±20%以内） |
| `hold`の平均着順 | 12.9前後 | ⭐**基準から2着を超えて悪化しないこと** |

**⚠️撤退基準（1つでも該当したら協調による速度そのものを撤回する）：**

1. **`hold`の平均着順が基準から2着を超えて悪化する**（第53/54弾と同じ失敗）。
2. **地形ごとの決着規模（+10秒以内の人数）が±20%を超えて崩れる**——
   ⭐発見1で「既に達成できている」と確認した唯一の資産なので、これを壊したら本末転倒。
3. **レースの平均所要時間が現状比±15%を超えて変わる**（記録・コースレコードとの整合が壊れる）。

## 検証項目

1. **同一tickで、同じ`eligible`人数のグループは必ず同じ`coopMul`になること**
   （集団間に恣意的な速度差を作らない＝第53/54弾の失敗の再発防止）。
2. `coopMul`が上表（1人=1.000／3人=1.080／6人=1.135／12人=1.165／24人=1.170）に一致すること。
3. 単独走者の`coopMul`が1.000であること。
4. **逃げ集団（`committedBreak`で構成）にはやる気ゲートが掛からない**こと。
5. **やる気ゲートの選出が決定論的**であること（同一シード・同一tickで2回計算して同じ顔ぶれ）。
6. 個人TT・チームTT（`noGroup`）が無影響であること（同一シードで`finishTime`完全一致）。
7. 第45〜52弾の既存検証スクリプトが全て通ること。
   ⚠️`w53_verify.mjs`・`w54_verify.mjs`はテンポ撤去により**失効**（削除してよい）。
8. `npx vite build`成功・Playwright実機で`pageerror`ゼロ。

## ⚠️ 実装の順序（重要）

**テンポ（第53・54弾）の撤去を先に、単独のコミットで行うこと。** 撤去だけの状態で
「第52弾完了時点と同一シードで`finishTime`が完全一致する」ことを機械検証してから、
協調による速度を足す。⚠️2つを同時に入れると、問題が起きたときにどちらが原因か切り分けられない。

## 実装結果（Sonnet・2026-08）

### 手順1: テンポ撤去
仕様どおり4定数・計算ブロック・`groupPaceMul`/`groupDrainMul`配布・移動パスの乗算・
`tempoDrainMul`を削除。撤去後の`src/sim/ticks.js`は第52弾Phase2完了時点のコミット
（`c76c0b6`）と`git diff`で**無変更＝byte-for-byte一致**を確認（実行時の`finishTime`比較
より厳密な検証）。`TEMPO_KEEP_TIGHTEN`等は維持。

### 手順2: coopMul + やる気ゲート
仕様どおり実装（`eligible`はローテーション用のものを流用、`riderHash01(id, 55)`で
決定論的に選出、`groupDist = puller.lastOwnDist * coopMul`）。`mainBunchId`はモード決定
ループの直前で「最多人数の集団」として算出し、逃げ集団（`committedBreak`を1人でも含む
集団）には適用しない。

⚠️**副作用**：coopMulが集団全体の速度を最大+17%押し上げるため、第51弾の`tempo`カード
検証（`w51_verify.mjs`検証6）で使っていた較正値（強組climb=80 vs 弱組climb=51）が無効化
され、`hold`ですら弱組が即座に千切れる誤検知が発生した。**`tempo`本体のロジック
（`TEMPO_KEEP_TIGHTEN`）は無変更**であることをdiffで確認した上で、境界値を弱組climb=51→75
に再較正（id1の「mount正規化」climb=67→80も、coopMulの影響が支配的になったため不要に
なり撤去）。fromTick=1〜150の広い範囲でhold=0/6・tempo=6/6が安定することを実測確認済み。

### 検証結果（`w55_verify.mjs`新規作成）
1. 同一`eligible`人数→同一`coopMul`（純関数）：OK
2. `coopMul`が仕様表（1=1.000/3=1.080/6=1.135/12=1.165/24=1.170）と一致：OK
3. 単独走者`coopMul=1.000`：OK
4. 逃げ集団にやる気ゲートが掛からないこと・本隊には掛かること：合成コース
   （本隊9人 vs 逃げ集団3人・`committedBreak`）で牽引者以外の実際の移動距離を
   `tickDistance`から独立に再計算した期待値と突き合わせて確認。本隊は9人→4人
   （`CHASE_EARLY_WILLING=0.45`込み）に絞られ、逃げ集団は3人のまま：OK
5. やる気ゲートの選出が決定論的（`riderHash01`を同一salt・同一idで2回計算し一致）：OK
6. 個人TT（`noGroup`）が無影響（同一シードで`finishTime`完全一致）：OK
7. 第46〜52弾の既存検証スクリプトが全て通過（`w53_verify.mjs`・`w54_verify.mjs`は
   テンポ撤去に伴い削除）。`w51_verify.mjs`は上記の較正値のみ更新して通過。
8. `npx vite build`成功。Playwrightで`dist/index.html`初期表示時の`pageerror`ゼロ
   （実機での深い画面遷移までは確認していない・簡易スモークテストの範囲）。

### 次のアクション
Phase 2（Opus担当）：`w55_shape.mjs`・`w55_breaktype.mjs`・`w54_sweep_one.mjs`で
撤退基準（`hold`2着以内・+10秒人数±20%以内・レース長±15%以内）と目標表
（集団逃げ10〜15%/8〜12%・単独逃げ10〜15%）を測定する。
