# 第33弾: ユーザー指摘の修正バッチ（UI/動線/ナビ）＋スタッフ専用スプライト

2026-08。第32弾Phase B-3完了後にユーザーから寄せられた9件の指摘への対応。
第32弾（B-4第1バッチ実装・Phase C）はこの弾の完了後に再開する。

## 指摘と対応（合意済み・AskUserQuestionで3点確定）

| # | 指摘 | 対応 |
|---|---|---|
| 1 | スタッフの外見が選手と全く同一 | `reference/Staff.png`（61監督・62トレーナー・63チームドクター・64スカウト）から抽出した専用スプライトへ差し替え（後述） |
| 2 | 「調律」が伝わらない | アイテム名「コンディション調律」→**「調子アップ」**（ユーザー選択）。使用ボタンは「調子アップ（+2）」。items.js／list.jsx／cp.jsの3箇所 |
| 3 | 詳しく見るで上限と練習が被る | バグ。RadarChartのcorner注記（上限N）を`y=size+16`＝**SVGの高さの外**に描いていたため、レイアウト高さに含まれず後続要素が重なっていた。corner付きのときSVG高さを+22して解消 |
| 4 | 選手名の変更は不要 | シーズン選手一覧の展開内「名前を変更」ボタンを削除（チーム名変更は対象外・存続） |
| 5 | 展開と次の選手の境目が不明瞭 | **面(surface)で括る＋末尾にも「▲ 閉じる」**（ユーザー選択）。展開領域全体をパネル化し、範囲が面の色で読めるように |
| 6 | 専門トレーニングにアイコン残存 | gear.jsのラベルから🏔⚡⛰🧘を撤去 |
| 7 | 専門トレーニングで即翌月へ飛ぶ | 動線を通常練習と同じ**「選択→ホームの練習ボタンで実行」**へ統一。選択は**その月だけ**（ユーザー選択）＝どの形で月が進んでも消え、翌月は元の練習メニューへ自動で戻る。`ml.plannedSpecial`を新設し、month.jsの`mlAdvanceMonth`が"train"時に消費する（event経由の月送りも含め常にクリア） |
| 8 | 記録→殿堂等の戻り先が違う | 殿堂・系譜・因子図鑑はタイトル側（キャリア作成・引退後）と画面を共有しており、戻るが`mylife_create`固定だった。開いた場所を`careerBack`/`dexBack`に覚えて追従（記録タブから→記録へ、殿堂経由の系譜→殿堂へ、記録直行の系譜→記録へ。戻るボタンのラベルも「← 記録に戻る」等に切替） |
| 9 | クリア後も続けられる？ | **既存機能の確認のみ**。グランファイナル制覇画面の「この轍を継いでさらなる高みへ」で同一チームのまま周回継続（周ごとに他チーム強化）。変更なし |

付随修正：`BASE_VIEW_STAFF`のmanagerのラベルが古い「マネージャー」のままだった→ゲーム内の職名
（economy.js＝「監督」）に統一。

## 検証（修正バッチ）

`npm run build`通過。Playwright実プレイ（verify_fixes.mjs）で全項目確認・`pageerror`ゼロ：
- シーズン選手一覧：展開到達／「名前を変更」不在／「上限」注記の下端(412px) ≤ 練習セレクト上端(425px)＝重なり解消
- マイライフ：専門トレに絵文字なし／高地合宿選択で月が進まない／「練習（高地合宿）」ボタンで月が進む／実行後に選択が消える
- ナビ：記録→殿堂→戻る＝記録／殿堂→系譜→戻る＝殿堂／記録→系譜直行→戻る＝記録（ラベルも「← 記録に戻る」に切替）

## スタッフ専用スプライト（#1・実装記録）

- **抽出**：`reference/Staff.png`（832×1291・4体）をscratchpadのPillowスクリプトで機械抽出。
  4象限それぞれで非背景のバウンディングボックス→**高さ36マス**（選手STANDと同じ全高）へ
  最近傍量子化（各セルは中央25%領域9点の多数決色・過半が背景なら透明）→頻度順に
  距離<48の色を統合して体ごとの固定パレット（9〜11色）に。結果は監督16×36・
  トレーナー16×36・ドクター18×36・スカウト17×36。
- **部品**：`sprites/staffSprites.js`（rows+legendのデータ）＋`sprites/staffPerson.jsx`
  （`StaffPerson`・描画方式はPixelPersonと同じspriteImageUrl→`<image>`1ノード。スタッフは
  持ち場に常駐する動かない人なのでstand1種のみ）。選手のpersonLegendと違い**チーム色の
  動的recolorが不要**なため固定パレット方式にした。
- **配線**：`BaseView.jsx`のindoorPeople描画で、選手はPixelPerson・スタッフは
  `<StaffPerson kind={staffKey}/>`に分岐。`BASE_VIEW_STAFF`のkeyがそのままスプライト名
  （trainer/doctor/scout/manager）。
- **検証**：ビルド通過・`verify_baseview.mjs`全項目OK。セーブ注入（`roadrace_v12_save`の
  `staff`を4種Lv1に書き換え）で拠点画面を撮影し、4体とも参考画像どおりの見た目
  （トレーナー=グレーシャツ／監督=青キャップ＋青ジャケット／ドクター=白衣／スカウト=
  紺ジャケット＋カーキ）で持ち場に立つことを目視確認。選手（全身チーム色ジャージ）との
  混同は解消。`pageerror`ゼロ。
