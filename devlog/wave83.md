# 第83弾：実機での通しプレイ（第75〜82弾の8弾ぶんを実際に遊んで確かめる）

**状態**：**実機プレイで文言バグ2件を発見・修正。sim側の破綻は確認されず。**

## 動機：8弾連続で数値だけで進めていた

第75〜82弾は脚質バランスの是正で、⚠️**一度も実機で遊んでいなかった**。
DEVLOG §87（第57弾）に「**この弾は実機を1レース通しで遊んで見つけた問題から出発している**
（それ以前の5弾は数値だけで進めていた）」という記録があり、⚠️**まさに同じ状態**だった。

AI同士の優勝率は6/6で目標達成しているが、それは「プレイヤーが遊んで面白いか」とは
別の指標である。Playwrightでマイライフを操作し、実際の画面を観察した。

## ⚠️発見1：チームTTの結果画面で「null位から後退」

デビュー直後（世界ランキング未設定＝`worldRankPrev`が`null`）にチームTTを走ると：

> 世界ランキング **1位**
> **null位から後退**——ランキングpt +16

⚠️**1位になったのに「後退」**、かつ内部値`null`がそのまま画面に出ていた。

**原因**：⚠️**第60弾がこの不具合を直したとき、通常レースの結果画面
（`screens/mylife/race.jsx:185`）にだけ修正を入れ、チームTTの結果画面（同ファイル:126）
を取りこぼしていた。** 同じファイル内に同じ表示が2箇所あり、片方だけ直された形。

**修正**：チームTT側にも同じ初掲載分岐を入れた（`worldRankPrev == null`なら
「初めてランキングに載った」）。実機で「初めてランキングに載った——ランキングpt +4」と
正しく出ることを確認。

## ⚠️発見2：27位なのに「完勝。今日は完全にあなたの一日だった」

石畳の古典で**48人中27位**、好敵手が31位だった結果画面：

> 好敵手 阿部 颯 — 31位でフィニッシュ
> **5.5秒突き放す完勝。今日は完全にあなたの一日だった。**

**原因**：`domain/season/rival.js`の`rivalDrama`が、⚠️**`myRank`を引数で受け取りながら
一度も使っておらず**、勝敗(`beat`)とタイム差(`gapSec`)だけで文言を決めていた。
ライバルより前ならレース全体で何位でも「完勝」になる。

**修正**：`myRank > 10`のときは「先着。上位には届かなかったが、この一戦は譲らなかった」
という、二人の戦いに留める文言へ分岐。⚠️写真判定・接戦の分岐は順位に関わらず成立するため
変更していない。負け側（「完敗」）も自分の順位に依存しないためそのまま。

検証（`scratchpad/w83_rival_check.mjs`）：
- 27位で5.5秒先着 → 「5.5秒先着。上位には届かなかったが、この一戦は譲らなかった。」
- 3位で5.5秒先着 → 「5.5秒突き放す完勝。今日は完全にあなたの一日だった。」（従来どおり）
- 27位で写真判定 → 「写真判定にもつれ込む死闘。」（従来どおり）

## sim側の確認結果

1年目4月〜9月を実際にプレイし、以下を確認した：
- **出走計画（山岳中心）が効いている**——候補3件に山岳ロードが入る。
- **チームTTの下馬評◎○▲がすべて独走屋(TT)**——第81〜82弾のTT関連の変更が
  意図どおり効いている。
- **モニュメント（石畳の古典）が正常に開催・完走**。
- **`pageerror`・コンソールエラーはゼロ**。

⚠️**8弾ぶんのsim変更による破綻は観測されなかった。**

## ⚠️この弾で得た教訓

⚠️**同じ画面の同じ表示が複数箇所にある場合、片方だけ直すと必ず取りこぼす。**
発見1は第60弾が直したはずの不具合の再発ではなく、**最初から半分しか直っていなかった**もの。
今後この種の表示を直すときは、⚠️**同一ファイル内に同じ`<Item label="...">`が
他にも無いか必ずgrepすること**。

⚠️**引数として受け取っているのに使っていない値は、バグの温床。**
発見2の`myRank`がまさにそれで、呼び出し側は正しく渡していたのに関数が無視していた。

## 横断調査（CLAUDE.md §7「指摘は常にあらゆる画面に対するものとして扱う」）

発見1・2は**類型が違う**ので、2つの類型それぞれで全ソースを機械的に洗った。

### 類型①「同じ表示が複数箇所にあり、片方だけ直されている」

`scratchpad/w83_duplabel.mjs`：全`.js/.jsx`で**同一ファイル内に同じ`label=`が2回以上**
出る箇所を列挙。

| ファイル | 重複ラベル | 判定 |
|---|---|---|
| `screens/mylife/race.jsx` | 賞金・獲得ポイント・世界ランキング | ⚠️世界ランキングが発見1。他2件は文言一致で問題なし |
| `screens/season/race.jsx` | 賞金・チームポイント・中期目標 | 3箇所（チームTT／通常／総合）とも文言一致。**ただしGC結果だけ「ポイント」で他2つは「チームポイント」**（下記） |
| `screens/mylife/events.jsx` | 成長力・成長タイプ | if/elseの表裏。問題なし |
| `screens/mylife/career.jsx` | `—` | プレースホルダ。問題なし |

⚠️**新たな不具合は無し。** 語彙の揺れ1件のみ（`screens/season/race.jsx:434`が
「ポイント」、同217/264行が「チームポイント」）。

### 類型②「引数で受け取っているのに一度も使っていない」

`scratchpad/w83_unusedparam2.mjs`：全`.js/.jsx`の関数について、
**単純引数と分割代入(`{ a, b }`)のキー**を取り出し、本体に一度も現れないものを列挙。
⚠️**最初に書いた版は分割代入を除外していたため、既知の`myRank`バグすら検出できなかった**
（修正前のコミットで検証して気付いた）。**検出器は必ず既知のバグで当たりを取ること。**

現行`src`（1,421引数スロット）での結果は**3件のみ**、いずれも実害なし：

- `rivalDrama`の`rivalName`——第60弾が意図的に本文から落とした（コメント済み）
- `rivalDrama`の`rivalRank`——⚠️関数のコメントに「勝敗×接戦度×**格上/格下**で分岐」と
  あるが、格上/格下の分岐は実装されていない。**コメントだけが残った状態**
- `rivalScene`の`playerName`——地の文が一人称なので使い道がない

## ⚠️発見3：チームTTだけ、レース後の処理がほぼ丸ごと欠けている

横断調査の途中で見つけた**既存の欠落**（実機プレイ由来ではない）。
`controllers/mylife/result.js`の`mlFinishRace`（通常レース）と`mlFinishTeamTT`を並べると：

| 更新される状態 | 通常レース | チームTT |
|---|---|---|
| points / money / worldPoints / worldRank / careerPodiums / raceLog | ○ | ○ |
| **popularity・popMilestones**（人気度とその到達ボーナス金） | ○ | ⚠️**無し** |
| **bonds**（僚友との絆） | ○ | ⚠️**無し** |
| **managerEval**（監督評価） | ○ | ⚠️**無し** |
| **riderStats**（永続キャラの成績台帳） | ○ | ⚠️**無し** |
| **ambitionIdx / ambitionDone**（アンビション進行） | ○ | ⚠️**無し** |
| careerWins / careerBigWins / careerTitles / careerClassics | ○ | 無し |
| log（月間ログ）／newspaper（号外）／監督指示の達成判定 | ○ | 無し |

⚠️**チームTTはチーム4人で走る唯一の種目なのに、絆が一切育たない。**

### 影響量の実測（CLAUDE.md §10）

`scratchpad/w83_teamtt_freq.mjs` / `w84_ttt_gap.mjs`：

- チームTTが候補に出る月：**144ヶ月中20ヶ月（14%）**。グレード内訳は G1×9・G2×3・G3×8
- その20回を毎回走った場合に**取りこぼす人気度**：
  - 1位で走り続けた場合 **88.5**（⚠️人気度の上限は100）
  - 3位相当 **44.3**／8位相当 **14.8**
- 人気度の到達ボーナス（`POP_MILESTONES` 25/50/75/100 → 80/150/250/400万円）は
  最大 **880万円**ぶんが同時に遠のく

⚠️**「チームTTを選び続けるプレイヤーほど人気度が伸びない」という、選択を罰する形の
非対称**になっている。

## 未消化：長時間の自動プレイは完走しなかった

ボタンを総当たりで押す自動プレイは10分でタイムアウトした（イベント選択肢の語彙が
多様で、汎用のセレクタでは進行ボタンを取りこぼす）。⚠️**「1キャリア通しで遊ぶ」は
未達**で、確認できたのは1年目4月〜9月の6ヶ月ぶん。残りの期間・引退・配合等は未検証。
