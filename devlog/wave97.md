# 第97弾：判断材料の設計——攻めの手が成立する条件をプレイヤーに伝える

- 2026-09-02。第96弾§7.13で、バラバラに登録していた3つのTODO（#32-j・#34・#32-i）が
  **1つの構造**に繋がると判明したことを受けて着手（`TODO.md` #35）。
- ⚠️**個別に文言を直さない。** 第95弾は「カードごとに手を足す」方向で解こうとして、
  実際には同じ手に名前を付け替えていただけだった（第96弾§7.10で判明）。同じ轍を踏まない。

## 1. 何が壊れているか（第96弾の実測から）

`attack`・`send`の独走の持続（`attackLeft`）は⚠️**`legsLeft01`比例だが、残り距離を一切見ていない。**
そしてカードは、成立に必要などちらの条件もプレイヤーに表示していない。

| 欠けている条件 | 症状（実測） | 旧TODO |
|---|---|---|
| **脚** | 脚の無い新人が`send`を選ぶと全4層で負け（A層 Δ+7.81・勝率0%） | #34 |
| **残り距離** | 進捗0.01で`attack`を提示 → Δ+15〜+20着 | #32-j |
| **両方** | 千切れた後は何を選んでもΔ0.00 | #32-i |

⚠️**同じ`attack`が`mid`（進捗0.47）では円熟期に全手中最強（Δ-2.27・勝率76%／基準64%）**
なのに、`terrain`（進捗0.01〜0.42）ではΔ+7.00〜+15.00。手ではなく**出す場所**の問題。

## 2. 弾の構成（3部）

| 部 | 内容 | 対応する旧TODO |
|---|---|---|
| **A. ゲート** | 成立しない手はそもそも出さない | #32-j |
| **B. 材料** | 判断を分ける条件を見せる | #35・#34 |
| **C. 不活性区間** | 何をしても効かない場面の扱い | #32-i |

## 3. 判断時点で使える情報（実装で確認済み）

`RaceView.jsx:338-347`が組み立てて`composeCard`へ渡す`ctx`：

| 情報 | フィールド | 状態 |
|---|---|---|
| 進捗（＝残り距離） | `ctx.frac` | ⚠️**既に渡っている**（`composeCard`が使っていないだけ） |
| 脚 | `ctx.energy` | 既に渡っている・`DecisionCard`が`LegsBar`で表示中 |
| 集団の人数 | `ctx.groupSize` | 既に渡っている・未使用 |
| 先頭か | `ctx.isLeader` | 既に渡っている・真偽値のみ |
| 独走中か | `ctx.inBreak` | `react`カードの発火条件で使用中 |
| 僚友の数 | `ctx.mates` | 既に渡っている |
| ⚠️**順位** | **無い** | `riders.filter(r => r.frac > focusR.frac).length`で算出可能 |
| コース全長 | `course.length` | `composeCard`には渡っていない（`buildDecisions`は受け取る） |

⇒ ⚠️**Aのゲートは新しいデータを必要としない**（`ctx.frac`だけで足りる）。

## 4. 設計に先立つ実測（§10）

⚠️**閾値を設計で決めず実測で決める。** 「残り距離が足りないと`attack`は成立しない」ことは
§7.13で分かったが、⚠️**どこからなら成立するのかは未測定**だった。
同一レースの同一地点から`attack`と`hold`をフォークし、進捗別に比較する
（プローブ：`_w97_atk.mjs`・使い捨て）。

### 4.1 結果：⚠️境界は「進捗」だけでは決まらない

`hold`と`attack`を同一地点からフォークし差を取った（各n=320・4難易度をプール）。
⚠️**差がマイナス＝仕掛けたほうが良い。**

**円熟（能力100／PRO）**
| 進捗 | hold Δ | attack Δ | 差 | hold勝率 | attack勝率 |
|---|---|---|---|---|---|
| 0.05 | +0.39 | +1.18 | +0.80 | 29% | 33% |
| 0.15 | +0.50 | +2.63 | +2.13 | 30% | 30% |
| 0.25 | +0.28 | +3.62 | **+3.34** | 29% | 26% |
| 0.35 | +0.23 | +1.99 | +1.77 | 28% | 27% |
| **0.45** | -0.14 | -1.32 | **-1.18** | 29% | **38%** |
| **0.55** | +0.14 | -1.92 | **-2.06** | 29% | **40%** |

⚠️**0.35と0.45の間で符号が反転する。境界は進捗0.40前後。**
これは§7.13の発火位置と完全に噛み合う——`mid`は0.47〜0.50＝**成立圏のど真ん中**
（だから全手中最強だった）、`terrain`は0.01〜0.42＝**ほぼ全域が不成立圏**
（だからΔ+15〜+20だった）。⚠️**「terrainカードは有害」ではなく
「不成立圏でしか出ていなかった」が完全に裏付けられた。**

**新人（既定）**
| 進捗 | hold Δ | attack Δ | 差 |
|---|---|---|---|
| 0.05 | +0.90 | +7.20 | **+6.30** |
| 0.15 | +0.97 | +7.81 | **+6.85** |
| 0.25 | +0.84 | +6.42 | +5.57 |
| 0.35 | +0.42 | +5.28 | +4.86 |
| 0.45 | 0.00 | +4.07 | +4.07 |
| 0.55 | -0.07 | +3.06 | +3.13 |

⚠️⚠️**新人では全6地点で不利**（勝率は全域0〜2%）。差は後半ほど縮むが0.55でも+3.13。
⚠️**境界が存在しない。**

### 4.2 結論：#35の仮説が`attack`でも確認された

| | 円熟 | 新人 |
|---|---|---|
| 0.05〜0.35 | 不成立（+0.80〜+3.34） | 不成立（+4.86〜+6.85） |
| **0.45〜0.55** | ⚠️**成立**（-1.18〜-2.06・勝率38〜40%） | ⚠️**不成立**（+3.13〜+4.07） |

⚠️**第96弾§7.11で`send`について見つけたのと同型**（円熟C層では勝率3倍だが新人では
全層で負ける）。⚠️**攻めの手は「脚（＝能力）」と「残り距離」の両方が揃って初めて成立する**
という#35の仮説が、`attack`と`send`の両方で実測により確認された。

⇒ ⚠️**単純な`frac < 0.40`のゲート（当初のA案）では不十分。** 新人には0.45でも0.55でも
成立しないため、進捗だけで切ると「新人に不成立の`attack`を出し続ける」ことになる。
本来の成立条件は⚠️**「残りの距離を、`attackLeft`の持続で走り切れるか」**
（`attackLeft = ATTACK_MIN_TICKS + (BREAKAWAY_ATTACK_TICKS - ATTACK_MIN_TICKS) × legsLeft01`）。

### 4.3 方向の選択肢（⚠️未合意・ユーザー判断待ち）

| 案 | 内容 | 評価 |
|---|---|---|
| 1. ゲートで隠す | 成立しない場面では`attack`を出さない | ⚠️新人はほぼ常に攻めの手を持てなくなる＝**選択肢が減る**（§0-1に反する） |
| **2. 材料で伝える** | 出すが、成立しないことが読める材料を添える | ⚠️**推奨。** 桜井#24「屈辱感がない範囲で救済策」。AとBが1つの設計に統合され、本弾の主題そのものになる |
| 3. sim側を直す | `attackLeft`を残り距離にも比例させる | ⚠️#32-a/#32-hと同じ「simに手を入れて外す」リスク。第96弾で2度失敗した領域 |

⚠️**2を採る場合、A単独の実装は行わず、Bの材料設計に統合する。**

## 4.4 ⚠️真の機構：`attack`のコストは「回復できなくなること」だった

§4.2で「残り距離が足りないと成立しない」と書いたが、⚠️**これも正確ではなかった。**
`attackLeft`は実測でわずか**10〜30tick（＝10〜30秒）**（`ATTACK_MIN_TICKS = 10`,
`BREAKAWAY_ATTACK_TICKS = 30`）で、1700〜2600tickのレースに対して無視できる長さ。
⚠️**独走の持続そのものは、良し悪しをまったく説明しない。**

### コード上の事実
⚠️**`committedBreak`は一度立つと自動では絶対に消えない。** `ticks.js`全体で`false`に
戻すのは、⚠️**プレイヤーが別のカードで別の手を選んだときだけ**（`hold`/`conserve`/
`hangOn`/`kick`/`send`/`kickBig`/`sprintWait`/`tempo`/`assistLaunch`）。
そして`canPull`（`ticks.js:186`）：
```js
const committed = en.role === "breakaway" || en.committedBreak;
if (!committed) { /* 消耗したら牽引を降りて回復する（PULL_MIN/RESUME_ENERGYのヒステリシス） */ }
```
⚠️⚠️**`committedBreak`の選手は「消耗したら下がって回復する」処理を丸ごとスキップし、
脚が尽きるまで牽き続ける。**

### 実測（能力100/PRO・4難易度プール・各n=200）
| 仕掛けた進捗 | 手 | 脚50% | 脚75% | ゴール脚 | 平均着順 |
|---|---|---|---|---|---|
| 0.15 | hold | 91.8 | 69.9 | 26.9 | 14.70 |
| 0.15 | **attack** | **71.2** | **52.4** | 22.5 | 15.87 |
| 0.35 | hold | 91.1 | 71.8 | 29.7 | 14.41 |
| 0.35 | **attack** | **69.2** | **39.4** | ⚠️**4.7** | 15.98 |
| 0.55 | hold | 91.1 | 72.3 | 28.6 | 13.93 |
| 0.55 | **attack** | 91.6 | 66.1 | **28.6** | ⚠️**12.26** |

⚠️**0.55で仕掛けた場合だけ脚がholdとほぼ同じまま**（50%地点91.6対91.1・ゴール28.6対28.6）
で着順だけ改善（13.93→12.26）。0.35で仕掛けると⚠️**ゴール脚が29.7→4.7に崩壊**する。

### 結論：良し悪しを決めていたのは「次の判断がいつ来るか」
⚠️**`attack`の本当のコストは「独走の30秒」ではなく「次に別の手を選ぶまで回復できない区間の長さ」。**

| 選んだカード | 回復不能な区間 | 実測Δ |
|---|---|---|
| `terrain` 0.01〜0.42 | 次の`mid`(0.47)まで＝⚠️**レースの最大46%** | +7.00〜+15.00 |
| `mid` 0.47 | 次の`finale`(0.62〜0.80)まで＝**15〜33%** | ⚠️**-2.27**（全手中最強） |
| （0.55相当） | `finale`がすぐ来る＝**7〜25%** | ⚠️**-1.18〜-2.06** |

⚠️⚠️**「仕掛けの良し悪しは、次の判断がいつ来るかで決まっていた」——プレイヤーには
完全に不可視の要因である。** §4.2で「脚と残り距離の両方」と書いたのは症状の記述であり、
真の変数は⚠️**「回復不能区間の長さ」＝次のカードまでの距離**だった。

### 設計への含意
⚠️**現行のカードはこのコストを一言も伝えていない。** `attack`の`desc`は
「単独で飛び出す。決まれば独走、脚を使い切れば失速も」で、
⚠️**一番重い代償（この先ずっと脚を緩められない）が書かれていない。**
CLAUDE.md §7の「開発語彙を見せるな」以前に、⚠️**仕様そのものが伝わっていない。**

桜井#01「リスクとリターンはかなり近い位置に、適切な大きさで刺激的に織り込む」に照らすと、
`attack`は⚠️**リスクだけが遠い位置にある**——仕掛けた瞬間ではなく、その後の数十%の区間で
じわじわ効く。プレイヤーには因果が見えない。

⚠️**材料の設計は「残り距離」ではなく「次の判断まで（＝回復できない区間）」を軸にする。**

## 4.5 案2（`committedBreak`の自動解除）を実装前に測り、⚠️**却下した**

§4.4の機構特定を受け、ユーザー合意のもと「案2：sim側で`committedBreak`に自然な解除を
入れる」で進めることにした。⚠️**ただし過去2回のsim変更（#32-a/#32-h）は「効くはず」で
実装して外しているため、今回は実装前に測る**と宣言して着手した。

### 設計案（実装しなかったもの）
`ticks.js:437`——既に「集団の一員として処理されている（`members`内）かつ`attackLeft`が
尽きた」＝吸収された瞬間を検出している箇所で、`committedBreak`も降ろす。
副作用は全参照箇所を追跡して確認済みだった（`brk`割引はmode solo/attack限定で吸収後は既に
無効、AI逃げ抽選は`breakawayRolled`が抽選前に無条件で立つため頻度不変、`rejoinLeft`は
`rejoinDone`で保護、`role === "breakaway"`の逃げ屋はroleでcommitted判定されるため影響外）。

### 反実仮想の測り方（ゲームコードは無変更）
`hold`は`committedBreak = false`にする手なので、⚠️**`modeHist`から「実際に集団へ戻ったtick」
（attack後に初めてmodeがdraft/pullになる時点）を特定してそこで`hold`を当てる**ことで
自動解除を再現した。

⚠️**最初の試行は無効だった**——固定の35tick後に`hold`を当てたが、その時点では選手がまだ
独走中のことがあり、`energyDrain`の`brk`割引（`committedBreak`かつsolo/attackのとき
最大60%軽減・`ticks.js:166`）だけを剥がす**別物の（有害な）変更**を測っていた。
⚠️**「機構は完全に特定できている」と宣言した直後に、その機構の一部を見落としていた。**

### ⚠️計測器の妥当性検査（第96弾§7の教訓の適用）
`committedBreak`を1つ降ろすだけで平均着順が17→24着動くのは機構から見て不自然だったため、
⚠️**結論を出す前に計測器そのものを検査した**（第96弾§7.6-1「意外な発見ほど、対象より先に
計測系を疑う」）。同じ`hold`を1回／2回当てて比較：

| | n | ゴール脚 | 平均着順 |
|---|---|---|---|
| hold 1回 | 200 | 26.7 | 14.21 |
| hold 2回 | 200 | 26.7 | 14.11 |

⚠️**二重`resumeSim`は結果を汚染していない＝計測器は妥当。** 結果は本物である。

### 結果（能力100/PRO・4難易度プール・各n=200）
| 進捗 | 手 | 脚50% | 脚75% | ゴール脚 | 平均着順 | 勝率 |
|---|---|---|---|---|---|---|
| 0.15 | hold | 94.8 | 75.8 | 28.1 | 14.20 | 32% |
| 0.15 | attack（現状） | 66.2 | 43.8 | 16.3 | 16.75 | 31% |
| 0.15 | **自動解除** | 55.5 | 26.2 | ⚠️**-9.1** | ⚠️**20.97** | 23% |
| 0.35 | hold | 94.6 | 76.4 | 27.3 | 14.03 | 34% |
| 0.35 | attack（現状） | 68.1 | 37.7 | 3.4 | 16.98 | 29% |
| 0.35 | **自動解除** | 56.1 | 7.5 | ⚠️**-34.4** | ⚠️**23.69** | 17% |
| 0.55 | hold | 94.6 | 75.9 | 25.0 | 13.73 | 32% |
| 0.55 | attack（現状） | 95.0 | 70.5 | 30.0 | **11.39** | **41%** |
| 0.55 | **自動解除** | 93.9 | 47.1 | ⚠️**-9.5** | ⚠️**17.18** | 26% |

⚠️⚠️**全3地点で明確に悪化。案2は却下する。**

### ⚠️却下の理由：因果の読みが逆だった
`committedBreak`の選手が`canPull`のヒステリシスをスキップするのを、私は
⚠️**「消耗しても下がって回復できない罰」**と解釈した。しかし実際にはこれは
⚠️**「牽引権を持ち続けられる特権」**でもある。

解除すると、脚が減った選手は`recovering = true`になって⚠️**牽引から降ろされ集団後方へ回される**。
ところが`attack`直後の選手は既に脚が減っているため、⚠️**吸収された瞬間に後方へ落ち、そこから
`keepThresh`に弾かれて単独走→回復ゼロ→崩壊**という経路に入る。
⚠️**現状の`committedBreak`はむしろこの崩壊を防いでいた。**

⇒ ⚠️**「吸収されたのに永久に回復できないのは仕様の誤り」という§4.4末尾の前提そのものが
間違っていた。** あれは誤りではなく、崩壊を防ぐ支えだった。§4.4の機構の記述
（committedBreakが自動解除されないこと・canPullがヒステリシスをスキップすること）は
コード上の事実として正しいが、⚠️**それを「バグ」と解釈した部分は撤回する。**

### 残る事実（設計に持ち越すもの）
- `attack`の良し悪しが「次の判断がいつ来るか」で決まること自体は§4.4の実測どおり変わらない。
- ⚠️**ただし直し方は「解除を入れる」ではない。** 案1（ゲートで隠す）・案3（カード配置を直す）、
  あるいは§4.3の案2（材料で伝える＝当初の方針）へ戻る。
- ⚠️**`attack`の`desc`が最大の代償を書いていない**という指摘（§4.4末尾）は有効なまま。

## 4.6 案3（カード配置の修正）の設計——⚠️`attack`を外すだけでは「0択」が残る

§4.5で案2を却下した後、ユーザー合意のもと案3（カード配置を直す）へ移った。
着手時点では⚠️**「(b)発火位置は変えず、早い位置では`attack`を出さない」を推していた**が、
⚠️**外した後に何が残るかを先に測って、この推奨を撤回した。**

### 実測：terrainカードが実際に出る進捗で、候補になりうる手を横並び比較
（4難易度プール・各n=240。`tempo`＝脚を使って集団を削る／`conserve`＝脚を溜める／
`hold`＝基準点／`attack`＝現行の3択目）

**円熟（能力100/PRO）**
| 進捗 | hold | tempo | conserve | attack |
|---|---|---|---|---|
| 0.05 | +0.47／30%／42% | +0.37／32%／45% | +0.32／32%／45% | +0.81／33%／44% |
| 0.20 | +0.42／31%／43% | +0.40／33%／47% | +0.07／30%／43% | ⚠️**+3.83**／25%／34% |
| 0.35 | +0.36／33%／43% | +0.40／32%／44% | +0.29／30%／45% | ⚠️**+2.42**／28%／34% |

**新人（既定）**
| 進捗 | hold | tempo | conserve | attack |
|---|---|---|---|---|
| 0.05 | +1.07／1%／5% | +1.51／1%／5% | +1.27／2%／5% | ⚠️**+6.99**／1%／3% |
| 0.20 | +0.76／1%／5% | +0.95／1%／4% | +0.78／1%／4% | ⚠️**+6.12**／1%／3% |
| 0.35 | +0.43／3%／5% | +0.34／2%／5% | +0.18／2%／5% | ⚠️**+4.97**／0%／1% |

### ⚠️結論：序盤のterrainカードは「3択に見える0択」だった
両ステージ・全3地点で、⚠️**`attack`を除いた3つ（hold/tempo/conserve）は完全に横並び**
（円熟Δ0.07〜0.47・勝率30〜33%／新人Δ0.18〜1.51・勝率1〜3%）。
⚠️**3択目の候補として`conserve`を足しても軸にならない。**

⚠️⚠️**現状で唯一結果を動かしているのが`attack`であり、それが悪い方向にだけ動かしている。**
⇒ ⚠️**(b)「`attack`を外す」は有害な手を消すだけで、判断は生まれない——無害な0択が残るだけ。**
これは#32-i（不活性区間）そのものであり、⚠️**序盤の地形カードは何を並べても判断にならない。**

CLAUDE.md §0-1「正解が表示される選択・分散ゼロの購入は判断ではない」に照らすと、
⚠️**0択のカードを出してレースを止めること自体がマイナス**である
（プレイヤーの時間を奪う＝`DESIGN_PRINCIPLES.md`要点6「遅さは罪。プレイヤーの時間はコスト」）。

### 設計案（⚠️未合意・ユーザー判断待ち）
terrainカードを⚠️**成立圏（進捗0.40以降）でのみ出す**。§4.1の実測が閾値を与えている。
⚠️**ただしこれは「カードを1枚減らす」変更**であり、第51弾で地形バッジの報酬として
設計されたカードなので⚠️**バッジの価値が下がる**副作用がある。対応：

| 案 | 内容 | 評価 |
|---|---|---|
| 1 | 単純に0.40未満では出さない | バッジ持ちでも序盤の地形ではカードが出ない＝**報酬が減る** |
| **2** | 0.40未満の地形区間は**終盤の地形区間へ振り替える**（`terrainCardCap`の枚数は保つ） | ⚠️**推奨。バッジの価値を維持したまま、有害な位置から有効な位置へ移す** |
| 3 | 序盤にも出すが地形カード専用の新しい手を作る | ⚠️sim変更が要る。§4.5で2度読み違えた領域なので避ける |

## 4.8 #35本体：⚠️問題の立て方が違った——`LegsBar`は「残量」を見せ「実力」を見せていない

§4.7で#32-jは解決したが、新人側は変化しなかった。その残りが#35の本体である。
着手にあたり、⚠️**問題の立て方そのものを見直した。**

### 見落としていた事実
| 条件 | finale地点の脚 | `attack`/`send` |
|---|---|---|
| 新人/easy | ⚠️**「十分」が119/120件** | Δ+7.20〜+12.78（不成立） |

⚠️⚠️**新人は「脚が十分」なのに攻めの手が成立しない。** つまり本弾が当初「伝えるべき条件」と
していた**脚は、伝えるべき条件ではなかった**。

`legsLeft01`は⚠️**正規化された残量**（LEGS_EMPTY=-45〜LEGS_FULL=95）であり、能力60の選手も
能力100の選手も、満タンなら等しく「十分」と表示される。しかし⚠️**独走を維持できるかを
決めるのは能力そのもの**（`tickSpeedFactor`の`f = 1 + (ability - 70)/260`で速度が決まる）。

⚠️**`LegsBar`は「どれだけ残っているか」を見せ、「何ができるか」を見せていない。**
これが、terrainの配置を直した後も新人側が変わらなかった理由である。
⚠️**第96弾から一貫して現れている同じ穴の裏返し**——「余剰の能力が変換されない」に対して、
ここでは「不足している能力が表示されない」形をしている。

### 実測：攻めの手はどの能力から成立するか
設計の分岐（連続量としてバーで見せる／閾値でゲートする）を決めるため、能力を段階的に
変えて成立点が滑らかか崖かを測った（クラスPRO・進捗0.50＝成立圏・4難易度プール）。

| 能力 | 無入力の着順 | hold Δ | attack Δ | 差 | attack勝率 | send Δの差 |
|---|---|---|---|---|---|---|
| 60 | 35.0着 | +0.13 | +1.04 | +0.91 | **0%** | +1.66 |
| 70 | 30.0着 | -0.12 | +1.64 | +1.76 | 3% | +3.34 |
| 80 | 26.2着 | -0.04 | +1.36 | +1.41 | 3% | +3.94 |
| **90** | 20.4着 | +0.07 | +0.12 | **+0.04** | 18% | +5.20 |
| **100** | 14.7着 | -0.48 | **-2.34** | ⚠️**-1.86** | ⚠️**38%** | +5.33 |
| 110 | 7.8着 | -0.19 | -1.57 | -1.39 | ⚠️**57%** | +2.44 |

⚠️⚠️**崖である。能力90が境界で、100以上で明確に成立する。**
能力60〜80は一様に不成立（差+0.91〜+1.76）で、⚠️**その範囲内では差がつかない。**

（`send`は能力110でも+2.44とプラス＝不利。finaleカード用の手なので`mid`地点の
進捗0.50では機能しないのが正しい挙動であり、この表では対照として置いてある。）

### ⚠️設計への含意：「バーで見せる」は否定された
能力60〜80が一様に不成立である以上、⚠️**連続量として見せる意味がない**
（「あと少しで届く」という情報が存在しない）。⇒ ⚠️**閾値＝到達目標として扱うのが正しい。**

これは`DESIGN_PRINCIPLES.md`要点5「ご褒美は骨組み段階で真っ先に。達成直前の数値を画面に
吊るす（planning #03『イース』方式）」と噛み合う——⚠️**「まだ攻めが使えない」ことを
隠すのではなく、到達すべき目標として見せる**方向になる。

⚠️**未合意。設計候補は§8手順1に従って別途提示する。**

## 4.9 #35本体の設計図（⚠️ユーザー合意済み・実装はSonnet）

§4.8の実測（成立点は崖・能力90が境界）を受け、ユーザーと方針を確認した：

> ⚠️**「新人が攻めても失敗する」のは壊れているのではなく正しい。**
> ロードレースで力の無い選手が単独で飛び出しても吸収されるのは現実そのもので、
> CLAUDE.md §0-2「奪う側の設計」にも合致する。⚠️**直すべきは因果が見えないことだけ。**

到達目標として吊るす案（バッジ・解禁）は⚠️**却下**した——能力90〜100という閾値は
脚質・地形・難易度で動くはずで、単一の数値として画面に出すと嘘になる
（第95弾の「推奨表示」と同じ轍・第96弾§4.6で撤回済み）。

### 伝わっていない2点（実測から）
| 伝わっていないこと | 根拠 |
|---|---|
| ⚠️**一度出たら次の勝負所まで脚を緩められない** | `committedBreak`は次にカードで別の手を選ぶまで解除されず、`canPull`の回復処理をスキップし続ける（§4.4） |
| ⚠️**集団を振り切るには相応の実力が要る** | 能力90が境界。60〜80では勝率0〜3%（§4.8） |

現行の`desc`「単独で飛び出す。決まれば独走、⚠️**脚を使い切れば失速も**」は⚠️**過小評価**。
実際は「脚を使い切る」のではなく「⚠️**使い切るまで止まれない**」。
⚠️**4つの脚質別ラベルは、どれも代償に一言も触れていない**（「武器に」「申し子」「突き放す」＝良い面だけ）。

### 採用した案（案B＝desc修正＋実況で因果を返す）

**1. `raceDecisions.js`——`desc` 5箇所**

| 分岐 | ラベル（変更なし） | 新しい`desc` |
|---|---|---|
| mid 登坂型 | 登りで抜け出す | 登坂適性を武器に飛び出す。以後、脚を緩められない |
| mid 丘型 | 丘でアタック | 丘の申し子、パンチ力で抜け出す。以後、脚を緩められない |
| mid 逃げ屋 | 得意の逃げに持ち込む | 逃げ屋の脚で集団を突き放す。以後、脚を緩められない |
| mid 汎用 | 仕掛ける | 単独で飛び出す。以後、脚を緩められない |
| terrain | 仕掛ける | 単独で飛び出す。以後、脚を緩められない |

⚠️**「次の勝負所まで」とは書かない**——カードの配置という開発側の都合が漏れるため（§7）。
⚠️汎用は29字→21字と**短くなる**（§7の引き算に沿う）。

**2. `RaceView.jsx`——吸収時の実況を1本追加**

⚠️**simには触らない。** `tagHist`はRaceViewで未使用のため、simにタグを足しても結局
RaceView側の実装が要る。⚠️**RaceViewはプレイヤーが何を選んだかを既に知っている**
（`resolveDecision(moveId)`）ので、それを追跡するのが最も正確：

```
resolveDecision で moveId === "attack" のとき
  committedRef.current = { fromTick: d.fromTick, fired: false }
再生ループ内（既存の動的実況ブロック・RaceView.jsx 562〜585行）で
  committedRef が生きている かつ 未発火 かつ focusR.mode が "draft"|"pull"（＝集団に戻った）
    → 実況を出して fired = true
```
⚠️`r.e.committedBreak`は再生時には**シム終了後の値**なので使えない（選択の追跡が正しい）。

実況文（既存と同じく`pick()`で3種）：
```
「${focusName}、集団に戻った——だが脚は緩められない」
「${focusName}、捕まった。それでも踏み続けるしかない」
「${focusName}が飲み込まれた。仕掛けた脚は戻らない」
```

⚠️**桜井#01「リスクとリターンは近い位置に」の是正**——現状は代償が数十%先にじわじわ来る
だけで因果が見えない。吸収の瞬間に代償を名指しすることで、次からの判断材料になる。

### この設計で直らないこと（意図的）
- ⚠️**新人の攻めが成立しないこと自体は直さない**（仕様として正しいと判断したため）。
- ⚠️**#32-i（不活性区間）は未着手のまま**。

## 4.11 C（#32-i・不活性区間）の診断：⚠️**2つの別々の問題だった**

「何をしても効かない場面」をどう扱うかを設計するには、⚠️**判断時点でそれを検出できる条件が
必要**（カードを出さない設計も、別の目標へ切り替える設計も、判定できなければ実装できない）。
finale/mid/terrain の各判断地点で全選択肢をフォークし、⚠️**選択肢間の最終着順の開き
（max-min）が0.5着未満**を「不活性」と定義して、脚・順位・単独かで層別した。

⚠️**指標の限界を先に明示する**：これは「⚠️**そのレースで、どの手を選んでも最終着順が
変わらなかった割合**」であり、⚠️**「選択肢が期待値として等価である割合」ではない**
（乱数の1回引きでたまたま同着になった場合を含む）。プレイヤー体験としては前者で正しいが、
⚠️**設計上の等価性の証拠としては過大**である。

### 新人（既定）4難易度プール・40キャラ
| 層別 | n | 不活性率 | 開き平均 |
|---|---|---|---|
| 脚 十分 | 358 | ⚠️**6%** | 10.63着 |
| 脚 やや消耗 | 15 | 73% | 0.87着 |
| 脚 苦しい | 25 | 84% | 0.56着 |
| **脚 限界** | 302 | ⚠️**100%** | ⚠️**0.00着** |
| 集団内 | 400 | 19% | 9.24着 |
| **独走/千切れ** | 300 | ⚠️**93%** | 0.46着 |
| **限界×単独** | 260 | ⚠️**100%** | ⚠️**0.00着** |

⚠️⚠️**「脚が限界」は302件すべてが不活性で開きは厳密に0.00着＝完全な判定条件。**
新人の全判断700件のうち⚠️**302件（43%）が該当**する。
⚠️**偶然では起こりえない水準**なので、この結論は指標の限界の影響を受けない。

### 円熟（能力100/PRO）4難易度プール・40キャラ
| 層別 | n | 不活性率 | 開き平均 |
|---|---|---|---|
| 脚 十分 | 677 | ⚠️**34%** | 4.44着 |
| 脚 苦しい | 12 | 100% | 0.00着 |
| 脚 限界 | 12 | 83% | 0.25着 |
| A先頭圏 | 170 | 44% | 4.46着 |
| B射程 | 297 | 37% | 5.12着 |
| C後方 | 99 | ⚠️**12%** | 4.98着 |
| D圏外 | 142 | 46% | 1.76着 |
| 集団内 | 627 | 34% | 4.65着 |
| 独走/千切れ | 81 | 56% | 1.31着 |

⚠️**脚が「限界」なのは12件（1.7%）しかなく、新人の判定条件は円熟には効かない。**
そして⚠️**「脚が十分で集団内」という最も健全な状況でも34%が不活性**で、
⚠️**順位でも単独かでも予測できない**（12%〜56%に散らばる）。

### 結論：#32-iとして1つに括っていたものは2つの別問題だった
| | 新人 | 円熟 |
|---|---|---|
| 脚「限界」の件数 | ⚠️**302件（43%）** | 12件（1.7%） |
| 脚「十分」の不活性率 | 6% | ⚠️**34%** |
| 判定可能か | ⚠️**可能**（`energy < -60`で100%） | ⚠️**不可能**（条件が見つからない） |
| 原因 | 脚が尽きて何もできない | ⚠️**不明**（指標の限界により、乱数由来の分を含む） |

⚠️**新人側だけが、確実で判定可能な問題である。** 円熟側の34%は今回の指標では
⚠️**乱数由来と設計由来を分離できていない**ため、⚠️**この数字だけで壊れていると結論しない**
（結論するには選択肢ごとの期待値差を多数回で測り直す必要がある＝別途）。

⚠️**設計は未合意。** 新人側（判定可能・43%）に絞るか、円熟側の再計測から入るかを含めて
ユーザーと決める。

## 4.12 円熟の「不活性34%」は本物だった（§4.11の留保を撤回）

§4.11で「34%は乱数由来を含むので過大」と留保したため、⚠️**乱数と設計を分離して測り直した。**
同じ判断地点から各選択肢を**8回ずつ**フォークし、
①選択肢間の**平均**着順の差（＝設計上の判断の重み）と
②同じ手を繰り返したときのばらつき（＝乱数の床・制御不能な分）を分けて集計した。
⚠️**脚が「十分」の地点のみ**（§4.11で34%が不活性と出た層）。

### 結果（能力100/PRO・4難易度プール・判断地点169箇所）
| 指標 | 値 |
|---|---|
| ①選択肢間の平均の差 | **3.44着** |
| ②同じ手のばらつき（乱数の床） | 1.20着 |
| 比（①/②） | **2.88** |
| 乱数が判断を上回る地点（①<②） | 15% |
| ⚠️**本当に等価（①<0.5着）** | ⚠️**34%** |
| （参考）1回引きだけを見た場合 | 33%（＝§4.11の指標） |

⚠️⚠️**8回ずつ再走行して平均を取っても34%が等価で、1回引きの33%とほぼ一致した。**
⇒ ⚠️**§4.11の留保「乱数由来を含むので過大」は誤りだった。撤回する。**
§4.11の指標は、この用途に関しては正しく機能していた。

### 解釈：全体は健全だが、分布が二極化している
比2.88は⚠️**平均すると判断は乱数の3倍近く結果を動かしている**ことを意味し、
⚠️**判断カードは全体としては機能している。**

しかし内訳は：
| | 割合 |
|---|---|
| 判断が効いている | 66% |
| ⚠️**完全に等価（何を選んでも同じ）** | ⚠️**34%** |
| （うち乱数が判断を上回る） | 15% |

⚠️**「だいたい効いているが、3回に1回は完全に無意味」。** 平均値3.44着は健全に見えるが、
⚠️**それは効いている場面の大きな差が平均を押し上げているだけ**であり、
⚠️**3分の1の判断はプレイヤーが何を選んでも1ミリも動かない。**
しかもこれは⚠️**脚が「十分」＝最も健全な場面での数字**である。

### #32-iの全体像（確定）
| | 新人 | 円熟 |
|---|---|---|
| 症状 | 脚が尽きて何もできない | 健全な場面なのに何も変わらない |
| 頻度 | ⚠️**43%**（全判断700件中302件） | ⚠️**34%**（脚「十分」169地点中） |
| 判定条件 | ⚠️**あり**（`energy < -60`で100%的中） | ⚠️**なし**（脚・順位・単独のどれでも予測できない） |
| 確実性 | 確実（開き厳密に0.00） | ⚠️**確実**（8回平均でも34%） |

⚠️**どちらも実在し、どちらも大きい。** ただし⚠️**円熟側は判定条件が無い**ため、
「その場面ではカードを出さない」という設計は⚠️**円熟側には適用できない。**

⚠️**設計は未合意。** 円熟側の34%について「なぜ等価になるのか」の原因はまだ不明であり、
⚠️**原因を特定してから設計に入る**（第96弾で2度、原因を特定しないまま設計して外した）。

## 4.13 ⚠️⚠️ 計測対象の取り違え：「円熟期」を一度も測っていなかった

§4.12で確定した34%の原因を切り分けるため、カード種別・集団サイズ・地形・判断位置で
層別した。⚠️**4つの仮説はすべて外れ**（本命だった「大集団に埋もれると何をしても無駄」は
⚠️**完全に逆**で、大集団のほうが等価率が低かった＝18%対36%）、どの軸でも25〜38%に
散らばって34%を説明する構造が見つからなかった。

### 計測器の検査で発覚
地形の層別が⚠️**「丘」228件に対し「平坦」3件、登坂・山岳・スプリント・TTがゼロ件**
だったため、⚠️**結論を出す前に計測器を検査した**（第96弾§7.6-1の適用）。
コース構成を直接集計すると⚠️**丘68.1%／平坦31.9%のみ**。地形判定のバグではなく、
⚠️**本当にそういうコースしか走っていなかった。**

さらに遡ると：
```
s.races.length = 3   classIdx = 0   year = 1
  富士チームTT      （TTなので除外）
  日光グラベルレース
  瀬戸内丘陵ロード
```
⚠️⚠️**`mlCreateChar`が返す`races`は「その月の候補3件」であって年間日程ではない。**
`s.races`を全部使う書き方をしていたため、⚠️**毎回この非TT2レースだけを繰り返し測っていた。**

### 実際のレース日程（`mlGenRaceCandidates(year, month, classIdx)`を直接呼んで確認）
| キャリア段階 | 年間レース数 | 主な内訳 |
|---|---|---|
| 新人1年目/B1 | **28** | グラベル4・クリテ4・TTステージ4・ヒルクライム3・丘陵3… |
| 中堅5年目/A | **27** | 個人TT6・TTステージ3・平坦3・グラベル3… |
| 円熟9年目/PRO | **25** | ⚠️**山岳ロード4**・チームTT3・丘陵3・サーキット3… |

⚠️**ゲームには豊富なコース多様性がある。私が2種類しか測っていなかっただけだった。**
⚠️**円熟期は山岳ロードが最多**なのに、測っていたのは新人1年目のグラベルと丘陵ロード。

### ⚠️影響範囲
`--ability 100 --class 2`が効いていたのは⚠️**選手の能力とクラス判定だけ**で、
⚠️**走るコースは新人1年目の2レースのままだった。**
⇒ ⚠️⚠️**「円熟期のPROレース」を一度も測っていない。**

⚠️**第96弾§7.9以降のすべての「円熟期」の数値がこの制約を受ける**（§4.1・§4.4・§4.5・
§4.7・§4.8・§4.11・§4.12）。数値自体は⚠️**「その2コースにおける事実」としては正しい**が、
⚠️**「円熟期の実態」を代表していない。**

⚠️**設計判断には使えない。** 特に：
- §4.8の「能力90が境界」——⚠️**2コースでの境界**であり、山岳ロードでは違う可能性が高い
- §4.12の「34%が等価」——⚠️**2コースでの数字**
- §4.1の「進捗0.40が境界」——⚠️**同上**（ただし#32-jの修正自体は、terrainカードが
  不成立圏でしか出ていなかったという構造的事実に基づくので、結論は揺らがない）

### ⚠️教訓（第96弾§7.6の再発）
これは⚠️**第96弾§7の`es[0]`事故と同じ類型（計測対象の取り違え）であり、同じ弾の中で2度目**。
前回は「誰を測っているか」、今回は「何を走らせているか」を取り違えた。
⚠️**共通するのは「便利な既存フィールドをそのまま使い、それが何を指すか確認しなかった」こと。**
`es[0]`も`s.races`も、⚠️**それらしい値が返るので誤りに気づけない。**

⇒ ⚠️**恒久ツールに「実際の年間日程を使う」経路を用意する必要がある**
（`mlGenRaceCandidates(year, month, classIdx, focus)`を直接呼ぶ）。`TODO.md`へ登録。

## 4.14 計測基盤の修正（実際の年間日程を使う）

§4.13の取り違えを恒久ツール側で直した。
- ⚠️**`tools/_shared.mjs`を新設**し`careerRaces(R, year, classIdx)`を共通化
  （`decisioncard_ev`と`difficulty_check`の両方が必要とするため・CLAUDE.md §5）。
  `mlGenRaceCandidates`を12ヶ月ぶん呼んで実際の年間日程を組む。
- 両ツールに`--year`を追加（省略時はクラスに対応する年次：B1→1年目／A→5年目／PRO→9年目）。
- ⚠️`buildMyLifeSim`へ渡す`classIdx`と`year`も日程に揃えた
  （従来は`s.classIdx`/`s.year`をそのまま渡しており**常に1年目のB1**だった）。

| クラス | 非TTレース数 | 内訳 |
|---|---|---|
| idx0 (1年目) | 25 | グラベル4・クリテ4・TTステージ4・丘陵3・ヒルクライム3… |
| idx1 (5年目) | 20 | TTステージ3・平坦3・グラベル3・山岳2… |
| idx2 (9年目) | 21 | ⚠️**山岳ロード4**・丘陵3・サーキット3・平坦2… |

⚠️**従来はどのクラスでも「日光グラベルレース」「瀬戸内丘陵ロード」の2本だけだった。**

実際のPRO日程で`difficulty_check`を回し、難易度が正しく単調であることを再確認した
（勝率 easy 67%→normal 45%→hard 17%→oni 2%）。⚠️**第96弾の結論は正しい日程でも維持された。**

## 4.15 §4.12の再測：⚠️**36%は本物だった**（コースの単調さではない）

正しい年間日程（PRO・9年目・21レース）で測り直した。判断地点は169→⚠️**919箇所**（5.4倍）。

| 指標 | 旧（2コース） | ⚠️**新（21コース）** |
|---|---|---|
| ⚠️**本当に等価** | 34% | ⚠️**36%** |
| ①選択肢間の平均の差 | 3.44着 | 3.35着 |
| ②同じ手のばらつき | 1.20着 | 0.80着 |
| 比（①/②） | 2.88 | **4.17** |
| 乱数が判断を上回る | 15% | 14% |

⚠️⚠️**サンプルが5.4倍になっても36%。Cの問題は本物であり、コースの単調さのせいではない。**
（比が2.88→4.17に改善したのは、地形が増えて判断の効く場面が増えたため。
⚠️**全体としての健全性はむしろ上がったが、等価の3分の1はそのまま残った。**）

### ⚠️地形別に、解釈しやすい構造が初めて見えた
| 地形 | n | 等価率 | 平均の差 |
|---|---|---|---|
| **平坦** | 267 | ⚠️**46%** | 3.97着 |
| 丘 | 251 | 36% | 3.51着 |
| 登坂 | 236 | 34% | ⚠️**1.95着** |
| TT | 96 | 24% | 2.65着 |
| **スプリント** | 69 | ⚠️**23%** | ⚠️**6.16着** |

⚠️**平坦が最悪（46%が等価）、スプリントが最良（23%・差6.16着）。**
これは⚠️**現実の自転車競技の直感と一致する**（平坦は集団が保たれ、スプリントで決着する）。
⚠️**登坂は平均の差が1.95着と最小**なのが意外——山岳では差がつくはずが、
⚠️**実際には判断で動かせる幅が最も狭い。**

⚠️**旧計測（丘68%・平坦32%）は、たまたま「等価率の高い2地形」に偏っていた**——
34%という数字が結果的に正しかったのは偶然である。

## 5. 参照した外部知見（§11）

- **ui #15（HUD）**：⚠️**「頭上のプレーヤーカーソルは常時だとうるさいので距離や状況で見え隠れ」**
  ——本弾Bの中心的な指針。⚠️**材料を2本に増やすのではなく、その場面で実際に判断を分ける
  1本だけを出す**。これなら CLAUDE.md §7 の引き算原則と両立する。
  「ゲームがやりやすく、あるいは面白くなるという前提で設定する」。
- **ui #08（大事なものをより大きく）**：⚠️**採用しない。** 「選択肢の価値を変えることは
  汎用性を捨てること」。正解の選択肢を大きくするのは CLAUDE.md §0-1
  「正解が表示される選択は判断ではない」に反する。
- **gameplay #01（リスクとリターン）**：「リスクとリターンはかなり近い位置に、適切な大きさで
  刺激的に織り込む」——Cの設計方針の土台。
- **gameplay #24（クリアできないよりマシ）**：「⚠️**屈辱感がない範囲で何らかの救済策を持たせる**」
  ——Cで「もう何をしても無駄」な区間をどう扱うかの指針。

## 4.7 案2実装後のEV計測（§10のbefore/after）

`raceDecisions.js`のterrainブロックを`TERRAIN_MIN_FRAC = 0.40`へ寄せる修正を実装後、
`tools/decisioncard_ev.mjs`で新人・円熟をeasy/normalで再計測した（各n=120）。

### terrainカード（Δ着順・勝率・表彰台）

| 条件 | 修正前（§4.6） | 修正後 |
|---|---|---|
| 円熟/easy attack | +15.13〜+20.33 | ⚠️**+1.40**／勝率60% |
| 円熟/normal attack | +9.33 | ⚠️**-1.11**／勝率**89%**（基準47%から大幅改善） |
| 新人/easy attack | +6.99〜+7.20 | +7.20（変化なし・n=15） |
| 新人/normal attack | +2.28〜+5.28 | +2.29（変化なし・n=7） |

⚠️**円熟期は狙いどおり直った。** `terrain/attack`は`hold`/`tempo`と同程度かそれ以上になり、
0択だった序盤の地形カードが意味のある3択に戻った（normalでは`attack`が最善手に転じた）。

⚠️**新人は変化しなかった。** これは§4.2で判明済みの別問題——新人には`attack`が
進捗のどこでも成立しない（0.05〜0.55の全域で不利）——がそのまま残っているため。
`TERRAIN_MIN_FRAC = 0.40`は「円熟には成立する位置」への移動であり、
「新人にも成立する位置」は存在しないため当然の結果である。

### 総括
§4.1〜§4.6で特定した「terrainカードは不成立圏でしか出ていなかった」という診断は、
実装後の計測で円熟期について確認された。⚠️**#32-jはここで解決とする。**
残る新人側の問題（攻めの手が能力不足で構造的に成立しない）は#35の本体として持ち越す。

## 4.10 #35本体の実装完了（Sonnet）

§4.9の設計図どおり実装した。

### 実装内容
1. `src/domain/shared/raceDecisions.js`——`attack`の`desc` 5箇所（mid×4分岐＋terrain×1）を
   「以後、脚を緩められない」に統一。ラベルは無変更。「次の勝負所まで」とは書いていない。
2. `src/components/RaceView.jsx`——`committedRef`を追加し、`resolveDecision`で
   `moveId === "attack"`のとき`{ fired: false }`をセット（他の手を選んだ場合は`null`に戻す）。
   既存の動的実況ループに専用ブロックを追加し、`riders`から注目選手の`mode`を毎tick確認して
   `draft`/`pull`に戻った瞬間（＝吸収された瞬間）に3種の実況からランダムに1つを表示する。
   ⚠️`ticks.js`（sim）は無変更——`r.e.committedBreak`は再生時にはシム終了後の値のため
   使わず、プレイヤーの選択そのものを追跡する設計どおりに実装した。

### 検証
- `composeCard`を直接呼び、mid4分岐・terrain1分岐すべてで新しい`desc`が出ることを確認。
- `npx vite build`が成功することを確認。
- ⚠️**§10の確認**：文言・演出のみの変更で数値を動かさないはずのため、
  `tools/decisioncard_ev.mjs`で`mid/attack`・`terrain/attack`のΔ着順・勝率を再計測し、
  §4.7の実測（円熟/easy mid/attack Δ-1.83〜-2.27等）とサンプル分散内で一致することを確認した。
  ⚠️数値は動いていない。

これで第97弾のA（terrainカードの配置）・B（材料と因果の伝達）が完了した。
残るC（#32-i・不活性区間の扱い）は別途着手する。
