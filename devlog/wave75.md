# 第75弾：TODO #28「全コースでスプリンターが最強」の原因特定 — 2つの独立した原因を確定

**状態**：**原因特定まで完了（2026-08）。修正は未着手（設計判断待ち）。**

第74弾-Cで「疑い」として起票した現象を、⚠️**前回の測定条件への批判が一切当てはまらない形で
再測定し、本物と確定させたうえで原因を2つ特定した。**

## 測定方法：プレイヤー条件への依存を完全に排除する

第74弾-Cの測定には「計測用の選手は装備なし・作戦`balanced`固定・絆なし・`mental`は全脚質49で
実プレイと違う」という留保が付いていた。⚠️**今回はまず「AI選手同士の順位分布」だけを見る**
ことでこの留保を消した——AI同士の比較なら、プレイヤーの装備・作戦・絆・能力の作り方は
一切関与しない。スクリプトは`scratchpad/w75_ai.mjs`。

## 結果1：本物だった。しかもクライマーが全コースで最下層

10位以内に入る割合 ÷ 出走者に占める割合（1.00が公平・n=40レース×約35人）：

| コース | SPR | PUN | **CLM** | TT | RUL |
|---|---|---|---|---|---|
| 山岳ロード | ×1.46 | ×1.89 | **×0.45** | ×0.67 | ×0.60 |
| ヒルクライム | ×1.24 | ×2.01 | **×0.25** | ×0.69 | ×0.65 |
| 丘陵ロード | ×2.18 | ×1.28 | **×0.32** | ×0.61 | ×0.59 |
| クリテリウム | ×2.43 | ×1.28 | **×0.17** | ×0.52 | ×0.36 |
| 平坦ロード | ×1.80 | ×1.11 | **×0.22** | ×1.07 | ×0.76 |

⚠️**ヒルクライム——登り一本のコースで、クライマーが3位以内に入る割合はわずか2.5%。**
CLMは全コースで×0.17〜0.45。RULも全コースで不利。有利なのはSPRとPUNだけ。

---

# ⚠️原因A：逃げ役の割り当てが脚質と相関し、逃げ役は必ず沈む

## 発見

`sim/ticks.js:185` `assignAIRoles`：

```js
const peak = Math.max(r.flat, r.climb, r.sprint, r.stamina, r.solo);
const sprintGap = peak - r.sprint;
const breakawayChance = Math.min(0.65, 0.06 + sprintGap * 0.022);
```

実際の`newRider`出力から算出した脚質ごとの逃げ役確率（n=500）：

| 脚質 | 平均sprintGap | 逃げ役になる確率 | 山岳ロードの10位以内倍率 |
|---|---|---|---|
| SPR | 0.4 | **6.9%** | ×1.46 |
| PUN | 4.4 | **15.7%** | ×1.89 |
| CLM | 23.2 | **53.1%** | ×0.45 |
| TT | 15.3 | 39.2% | ×0.67 |
| RUL | 15.1 | 38.7% | ×0.60 |

⚠️**逃げ役確率と不利さの相関係数 −0.899。**

## 因果の確認（相関で終わらせない）

役割別の着順を直接集計した（`scratchpad/w75_role.mjs`・クリテリウム）：

| 役割 | 人数 | 平均着順 | 10位以内率 |
|---|---|---|---|
| flat（平坦アシスト） | 302 | **12.1位** | 54.6% |
| ace | 320 | 16.6位 | 33.8% |
| sub | 309 | 17.6位 | 20.7% |
| **breakaway（逃げ役）** | 328 | **25.3位** | **10.4%** |
| mountain（山岳アシスト） | 82 | 26.4位 | 4.9% |

脚質ごとの役割の内訳（クリテリウム）：
**SPR**＝flat 69%／逃げ役 **4%**、**CLM**＝**逃げ役 45%**／mountain 30%。

⚠️**逃げ役は平均25位で沈む役割であり、クライマーはそこに45%の確率で自動的に放り込まれる。**

## ⚠️なぜこれが見過ごされてきたか

式のコメントは「スプリントが弱点→集団ゴールで不利→早逃げに出やすい」と書いており、
**発想としては現実のロードレースに忠実**である。問題は組み合わせのほう——

⚠️**DEVLOG §88〜89で「逃げの数値を追う仕事はもう再開しない」と打ち切っている**
（第52〜59弾で7回挑戦し、効いたのは1回だけ）。逃げが構造的に不利なことは既知で、
対処を諦めた領域である。**その勝てないと分かっている戦術に、特定の脚質だけが
自動で送り込まれ続けていた。** 「逃げが弱い」問題と「役割割り当てが脚質に偏っている」
問題を別々に扱っていたため、掛け算になっていることに誰も気付かなかった。

⚠️**重要な区別**：ここを直すのは「逃げを強くする」ことではなく「勝てない戦術への
送り込みを脚質で偏らせない」ことなので、§88〜89の禁止事項（逃げの速度・持続を追う）には
抵触しない。

---

# ⚠️原因B：得意地形で強いほど集団の前方に置かれ、回復を受けられず自滅する

## 発見

原因Aで説明できるのはAI側だけ。⚠️**プレイヤーは逃げ役に自動割り当てされない**
（tacticで選ぶ）ので、別の原因があるはずだと考えて切り分けた（`scratchpad/w75_player.mjs`）。

山岳ロード・プレイヤーはエース（n=50）：

| 条件 | 平均着順 | 勝者との差 | 10位以内 |
|---|---|---|---|
| CLMそのまま (climb86 sprint63 accel44) | 24.9位 | 129秒 | 4% |
| sprintだけ85に（climbは86のまま） | 19.6位 | 112秒 | 34% |
| **typeラベルだけSPRに（能力はCLMのまま）** | **26.3位** | 126秒 | 4% |
| **能力をSPR型に置換（typeはCLMのまま）** | **9.1位** | **41秒** | **74%** |

⚠️**typeラベルは無関係で、能力の形が全てを決めている。** そして
⚠️**山岳コースなのに`climb`を86→63に下げたほうが10位分速い**という転倒が残った。

## ⚠️外した仮説2つ（記録して繰り返さない）

`segmentAbility`・`finishAbility`の式では説明できなかったため、tick単位で観測した。
⚠️**その過程で仮説を2回外している。同じ道を辿らないために両方残す。**

- ⚠️**外れ①「登坂が速いと集団より前へ出てしまい単独走(solo)になる」**——`solo`のtick比率は
  climb 63/86/110 で 3.6%→12.2%→14.6% と確かに増えるが、⚠️**`solo`のtickの82〜93%は
  「前に他の選手がいる」＝後方**だった（`scratchpad/w75_solo_why.mjs`）。コードを正確に読むと
  `ticks.js:553`の判定は`ownCapable >= groupDist * keepThresh`で、**`solo`は「遅すぎて千切れた」
  状態**であって「速すぎて抜け出した」ではない（コメントにも「千切れて単独走になった選手」とある）。
- ⚠️**外れ②「前方の小集団に残り`groupShelterMul`の風除けを失う」**——所属集団の人数を測ると
  25%/50%/70%地点で**3条件ともほぼ同一**（50%地点で27.3／27.8／28.1人）だった
  （`scratchpad/w75_group.mjs`）。風除け係数は同じ0.55で、これも原因ではない。

## ⚠️機構の確定：計装した複製srcで消耗と回復を分解した

`src`を`scratchpad/w75src`へ複製し、`energyDrain`の適用箇所に計装を入れて内訳を直接測った
（`scratchpad/w75_acct.mjs`／`w75_acct2.mjs`）。⚠️**`solo`は「千切れた結果」でもあり
原因と結果が循環するため、集団内(`draft`)にいる間のtickだけに限定して切り分けた。**

**山岳ロード・集団内にいる間だけの1tickあたり収支（n=20レース）：**

| プレイヤーの`climb` | 消耗 | **回復** | **差引** | 集団内の平均位置 |
|---|---|---|---|---|
| 63 | 0.1740 | 0.1757 | **−0.0017（収支ゼロ）** | 35.6%（0%＝先頭） |
| 86 | 0.1764 | 0.1411 | **+0.0353** | 29.5% |
| 110 | 0.1781 | **0.1273** | **+0.0508** | 26.9% |

⚠️**消耗はほぼ同じ（+2.4%）。回復だけが28%減っている。** 1レースあたり約1,600 draft tickなので、
climb 110 は**1レースで約82ポイントのエネルギー赤字**（初期値100）。climb 63 は収支ゼロで走り切れる。

## 原因の所在（コード）

`ticks.js:503-508`——**集団内の位置(`slot`)が「その区間での強さ」順に決まる**：

```js
const frontStrength = (c) => c.ownCapable / groupDist - (conserve...) + (holdOn...);
ranked.sort((a, b) => frontStrength(b) - frontStrength(a));
ranked.forEach(c => { c.en.slot = nextSlot++; });   // 強い順に前から詰める
```

そして`ticks.js:616-618`——**回復は集団内の位置だけで決まる**：

```js
const backRatio = sheltered ? Math.min(1, (en.slot || 0) / totalDraft) : 0;
const regen = sheltered ? ENERGY_REGEN_BASE * backRatio * (windActive ? 0.5 : 1) : 0;
```

⚠️**その区間で強い選手ほど前に置かれ、前に置かれた選手は回復をほぼ受け取れない。**
弱い選手は後方で回復しながら生き延び、強い選手は前で削られて枯れる。

そこから先は連鎖する：エネルギーが減る→`energyPenaltyMul`で速度が落ちる→
`ownCapable`が`groupDist * keepThresh`を割る→千切れて`solo`→
`solo`は`effortCost`1.3かつ風除け無し（`shelterMul`が0.55→1.0）で**約2.8倍の消耗**→さらに沈む。
⚠️**`solo`の激増（drainの13%→46%）は原因ではなく、この連鎖の結果である。**

## ⚠️この原因が意味すること

**得意な地形で、得意な能力が高いほど自滅する。**
これは本作の根幹——DEVLOGの「育成の方向転換」に明記された
**「能力値で個性を出すのをやめ、バッジで個性を出す」**、そして脚質そのものの存在意義——と
正面から矛盾する。⚠️**育てるほど不利になる。**

⚠️**脚質ごとの有利不利も、これで完全に説明できる**——「その地形で一番強い者が損をする」ので、
山岳ではCLMが（climb 86で突出）、平坦ではRULが（flat 85で突出）前に置かれて損をする。
⚠️**PUNが全コースで最強（×1.89〜2.01）なのは、climb 79／sprint 79／flat 72と
「どの地形でも2番手」で、一度も先頭に置かれないから**である。第74弾-Cで
「`climb`を110に上げるとむしろ悪化」という不可解な観測を残していたが、その正体がこれだった。

---

# 修正の方向（設計判断が要るため未着手）

## 原因Aの候補

⚠️**`sprintGap`は2箇所で使われている。片方だけ直しても、もう片方が同じ偏りを再生産する。**
- `ticks.js:185` `assignAIRoles`＝レース開始時の役割割り当て
- `ticks.js:309` 終盤の自然発生アタック（`sprintGap < 8`で足切りし、`sprintGap`で確率が上がる）

実測（クリテリウム）：**一度でもattackに入った率はCLM 72%／TT 66%／RUL 58%に対しSPRは5%。**

- **A-1. 脚質依存を撤廃**——`breakawayChance`を全脚質一律（例12%）に。最も単純。
- **A-2. 依存を弱める**——係数`0.022`を下げ、上限`0.65`も下げる。現実味を残す。
- **A-3. 別の軸で選ぶ**——チーム内の序列（エースになれない選手ほど逃げる）等。

## 原因Bの候補（⚠️機構の再特定に伴い書き直した）

核心は**「slotが地形での強さ順」×「regenがslotだけで決まる」**の組み合わせ。

- **B-1. 回復を位置だけに依存させない**——`regen = ENERGY_REGEN_BASE * backRatio`の傾斜を緩め、
  先頭でも最低限は回復する形にする（例：`(0.35 + 0.65 * backRatio)`）。⚠️**最小の変更で
  収支の符号を直接動かせる。** 副作用：集団全体が持つので千切れが減り、決着が団子になり得る。
- **B-2. エース／温存中の選手を前に置かない**——`frontStrength`に`isAce`の後方バイアスを足す
  （現実の「エースは終盤まで風除けの中に隠れる」）。⚠️既に`conserveLeft`／`holdOn`のバイアスと、
  トレイン時の`ace.slot = helpers.length`という前例がある＝設計思想としては既存路線の延長。
  ⚠️ただしAIのエースにも等しく効くため、脚質間の相対差がどう動くかは実測が要る。
- **B-3. `slot`を強さ順にしない**——最も根本的だが、`positionTight`（後方ほど千切れやすい）と
  セットで設計されているため影響が最も広い。
- ⚠️**B-4. `solo`の`effortCost`を下げる**——これは§88〜89の禁止事項（逃げの数値を追う）に
  抵触するため**採らない**。

⚠️**原因Bを直すと「全員が集団に留まりやすくなる」＝逃げがさらに決まらなくなる可能性がある**
（§88〜89で打ち切った領域と干渉する）。修正時は必ず、逃げの成立率もbefore/afterで測ること。

## ⚠️測定の前提（CLAUDE.md §10）

修正後は必ず、**この弾で使ったAI同士の分布（`w75_ai.mjs`）を再実行して倍率が1.00へ
寄ったかを測る。** プレイヤー条件に依存しないこの指標が、唯一汚染されていない物差しである。

---

# この弾で得た教訓

⚠️**「プレイヤーで測ると条件の妥当性を疑われる」場面では、AI同士の分布を見ると
留保が丸ごと消える。** 第74弾-Cは正しい観測をしていたのに「装備なし・作戦固定」という
留保のせいで「疑い」止まりだった。⚠️**同じ現象を、条件依存の無い形で測り直す方法が
無いかを先に考えるべきだった。**

⚠️**式の意図が正しくても、他の仕組みと掛け算になると壊れる。** 逃げ役の割り当て式は
単体では現実に忠実で、逃げの弱さは別途既知だった。⚠️**2つとも「既知で妥当」だったために、
その積が誰にも見えていなかった。** 今後、既に打ち切った領域（§88〜89等）がある場合は、
**そこへ選手を送り込む経路が他に無いか**を確認すること。
原因Bも同じ形で、`slot`＝強さ順（もっともらしい）と`regen`＝後方ほど回復（もっともらしい）の
**積が「強い者ほど回復できない」**になっていた。

⚠️⚠️**原因Bの機構を、このセッション中に2回間違えた**（①速く進んで単独走になる
②前方の小集団で風除けを失う）。どちらもコードを読んだ上での、もっともらしい推論だった。
**正解にたどり着いたのは、推論をやめて`src`を複製・計装し、消耗と回復を実測してからである。**
⚠️**simの因果を推論で当てようとしないこと。** 特に`solo`のように「原因でも結果でもある」
状態量が絡む場合は、**結果側の状態を除外した条件（今回は`draft`中のtickだけ）で測り直す**まで
結論を出さない。第73弾の教訓（古い観察を疑う）の、同じセッション内版だった。

---

# ⚠️第75弾-C：修正候補の実測と、掃引の打ち切り

## 評価軸を途中で訂正した

当初は「全脚質が全コースで×1.00」を目標にしていたが、⚠️**これは誤り**だった——
クリテリウムでスプリンターが強いのは正しい挙動であり、それを「ズレ」として減点していた。
⚠️**§88〜89と同じ「体験と結びついていない数値を追う」罠に入りかけた。** 軸を
**「各コースで、そのコースが想定する脚質(`favors`)が最上位になるか」**へ訂正した
（`scratchpad/w75_eval.mjs`）。

## ⚠️ベースラインの実態：7コース中6コースで想定脚質が報われない

n=40／コース。倍率＝10位以内に入る割合÷出走者に占める割合。

| コース | 想定 | SPR | PUN | CLM | TT | RUL | 最上位 |
|---|---|---|---|---|---|---|---|
| 山岳ロード | CLM | 1.39 | **2.03** | 0.54 | 0.54 | 0.52 | ✗PUN |
| ヒルクライム | CLM | 1.56 | **1.93** | 0.28 | 0.57 | 0.61 | ✗PUN |
| 丘陵ロード | PUN | **2.38** | 1.20 | 0.32 | 0.64 | 0.58 | ✗SPR |
| グラベルレース | PUN | **1.92** | 1.46 | 0.16 | 0.50 | 0.99 | ✗SPR |
| クリテリウム | SPR | **2.32** | 1.38 | 0.17 | 0.68 | 0.55 | ✓ |
| 平坦ロード | RUL | **1.71** | 1.04 | 0.32 | 1.16 | 0.78 | ✗SPR |

⚠️**想定どおりなのはクリテリウムだけ（1/7）。**
（個人TTは出走10人以下で「10位以内＝全員」になり全て×1.00になる測定上の副作用。除外して読む。）

## 試した候補と結果

| 候補 | 内容 | 想定脚質が最上位 | 逃げ切り率の変化 |
|---|---|---|---|
| ベースライン | — | **1/7** | 0〜28% |
| A のみ | 逃げ役の`sprintGap`依存を緩和（2箇所） | 1/7相当 | ⚠️クリテ12%→44% |
| **B-regen** | **`regen`を飽和型へ**（`0.35+0.65*min(1,b/0.25)`） | **3/7** | ⚠️丘陵28%→65% |
| B-regen＋アコーディオン | 後方に加減速消耗を新設 | 2/7 | ⚠️丘陵→75% |
| finish相性のみ | `finishAbility`に`typeAffinityBonus`を合流 | 2/7 | ほぼ不変 |
| B-regen＋finish相性 | 上記2つ | 2/7 | ⚠️丘陵→60% |

**判明したこと：**
- ⚠️**`regen`の飽和が唯一はっきり効くレバー**（1/7→3/7）。山岳ロードでCLMが×0.54→×1.52になり
  初めて最上位になった。位置モデルが主因だという診断は正しかった。
- ⚠️**アコーディオン消耗（後方ほど加減速で削られる）は、この形では効かなかった**（3/7→2/7）。
  ユーザー指摘の「後ろにいる代償」自体は現実に即しているが、⚠️**エネルギー消耗として足すだけでは
  トレードオフにならない**——後方の選手は元々余力があるので、消耗を増やしても順位が動かない。
- ⚠️**どの候補も逃げ切り率を暴走させる**（丘陵28%→60〜75%）。regenを緩めると小集団（＝逃げ）が
  生き残れるようになるため。⚠️**§88〜89で「成立しない」と打ち切った逃げが、副作用で成立しすぎる側へ
  振れる。** ここは必ずセットで見ること。

## ⚠️解析で判明した、もう1つの設計上の穴

`finish.js:68` 山頂決着＝`climb*0.75 + sprint*0.25`。実際の能力値を入れると：

- CLM（climb 86／sprint 63）＝ 64.5 + 15.75 = **80.25**
- PUN（climb 79／sprint 79）＝ 59.25 + 19.75 = **79.00**

⚠️**山頂決着でクライマーの優位はわずか+1.25しかない。** climbで+7ある差を、
sprintの0.25の重みが打ち消している。⚠️**「万能型が専門家を打ち消せる」重み付けになっており、
PUNが全登坂コースで最強になる直接の原因。** これは定数の調整ではなく決着式の設計問題。

また⚠️**`finishAbility`には`typeAffinityBonus`が入っていない**（`segmentAbility`には入っている）。
第50弾が「僅差ゴール集団ではバッジが無かったことになっていた」と同じ穴を塞いでいるが、
⚠️**脚質相性だけが取り残されている。** ただし単独では効かない（先頭集団に残れていないため）。

## ⚠️掃引を打ち切った理由

4回の候補測定で2/7〜3/7を行き来し、収束しなかった。⚠️**これは§88〜89で7回繰り返した
「機構を1つ特定→狭く直す→効かない→別の機構を特定」と同じ形である。** 8回目に入る前に
意図的に停止した。**次にやるべきは、さらに定数を探すことではない。**

---

# 第75弾-D：⚠️レースsim「誰がどこで勝つか」の仕様（2026-08・ユーザー合意）

⚠️**これは定数調整のための数値目標ではなく、位置・回復・決着・展開の4層を設計し直すときの
「正解の定義」である。** 以降この付近を触るときは、まずこの表に照らすこと。

## 経緯：能動的な絞りの有無を調べて、前提が1つ崩れた

ユーザーからの質問「集団内の人数を絞ろうとする動きってある？」で調べたところ：

- ⚠️**AIは集団を絞る能動的な動きを一切持たない。** 「ふるいにかける」(`tempo`)は
  `raceDecisions.js`が生成する**プレイヤー専用の判断カード**。
- 受動的な削り（`selectiveDrainMul`1.45／`selectiveTight`0.035）は
  ⚠️**`course.selective = !!(monument || grade >= 4)`＝モニュメントとグレード4限定**。
  通常のグレード1〜3では一切働かない（この弾の測定は全てグレード2＝削り無しの条件だった）。

⚠️**ところが実測すると集団はすでに極端に絞られていた**（`scratchpad/w75_selection.mjs`）：

| コース | grade | 出走 | ゴール1秒以内 | 5秒以内 |
|---|---|---|---|---|
| 山岳ロード | 2 | 35人 | **1.5人** | 5.2人 |
| ヒルクライム | 2 | 36人 | **1.5人** | 3.6人 |
| クリテリウム | 2 | 36人 | **2.6人** | 10.8人 |

⚠️**「集団ゴールだから専門家が勝てない」という筋書きは誤り。問題は絞られ方が逆であること**——
山岳ロードで真っ先に削られるのがクライマー（優勝0%）で、生き残るのが万能型のPUN（60%）。
原因B（強い者が前に置かれ回復できない）の直接の帰結である。

⚠️**補強材料**：グレード4（削り有効）にすると専門家が報われる方向へ動く
（ヒルクライムでCLM 0%→10%・SPR 50%→23%）。**削りそのものは専門家の味方**で、
今は削りが「地形に弱い者から」ではなく「前にいる者から」効いてしまっている。

## 仕様1：優勝者の脚質内訳（100レースあたり・受け入れ基準）

⚠️ユーザー決定：**得意コースでは想定脚質が45〜55%＝専門家の独壇場**。
⚠️**クリテリウムのSPR 85%も他コースと同じ水準まで下げる**（全コース同一ルール）。

| コース | 想定 | SPR | PUN | CLM | TT | RUL |
|---|---|---|---|---|---|---|
| 山岳ロード | CLM | 6 | 20 | **50** | 12 | 12 |
| ヒルクライム | CLM | 7 | 20 | **55** | 10 | 8 |
| 丘陵ロード | PUN | 18 | **50** | 12 | 8 | 12 |
| グラベルレース | PUN | 15 | **50** | 15 | 8 | 12 |
| クリテリウム | SPR | **50** | 15 | 5 | 10 | 20 |
| 平坦ロード | RUL | 12 | 8 | 5 | 25 | **50** |
| 個人TT | TT | 7 | 8 | 10 | **55** | 20 |

- ヒルクライム・個人TTだけ55%＝**混じり気のない単一地形なので最も専門家の聖域**。
- 2番手以降は地形から決めた（山岳ならCLMの次はPUN／平坦ロードは決着がtt区間なのでRULの次はTT）。
- 許容幅は**±8ポイント**とする（乱数のばらつきを考慮。n=60で測る）。

**現状（ベースライン・n=60）との差：**

| コース | 想定脚質 | 目標 | 現状 |
|---|---|---|---|
| 山岳ロード | CLM | 50 | **3** |
| ヒルクライム | CLM | 55 | **2** |
| 丘陵ロード | PUN | 50 | **3** |
| グラベルレース | PUN | 50 | 25 |
| クリテリウム | SPR | 50 | **85** |
| 平坦ロード | RUL | 50 | **15** |

## 仕様2：逃げ切り率（逃げ集団からの優勝）

⚠️ユーザー決定：**コースで変える（平坦は低く・丘陵/山岳は高く）**。

| コース | 目標 | 現状 |
|---|---|---|
| クリテリウム | 5% | 12% |
| 平坦ロード | 8% | 8% |
| ヒルクライム | 15% | 0% |
| 山岳ロード | 20% | 13% |
| 丘陵ロード | 25% | 30% |
| グラベルレース | 25% | 5% |

⚠️**この項目は必ず仕様1とセットで測ること。** 実測で、位置・回復に手を入れると
どの候補でも逃げ切り率が暴走した（丘陵28%→60〜75%）。§88〜89で「成立しない」と
打ち切った逃げが、今度は成立しすぎる側へ振れる。

## 仕様3：能動的な集団の絞り（新機能・ユーザー決定で採用）

⚠️**AIチームは、自チームのエースが得意とする地形の区間で、能動的にペースを上げて後続を削る。**

- **発動主体**：エースではなくそのチームメイト（`isAce`でない同チーム選手）。
- **発動条件**：①エースが自分と同じグループに残っている ②現在の区間タイプがエースの
  `TYPES[ace.type].affinity`に含まれる ③発動者の`energy`が閾値以上 ④レース進行が中盤以降。
- **効果**：プレイヤーの`tempo`と同じ経路を使う（`tempoLeft`を立て、同グループの他選手の
  `keepThresh`を`TEMPO_KEEP_TIGHTEN`だけ厳しくする）。発動者は`TEMPO_ENERGY_COST`を払う。
- **狙い**：削られる順序が「前にいる者から」ではなく**「その地形に弱い者から」**になる。
  同時に、`tempo`がプレイヤー専用という非対称が解消される。
- ⚠️**発動頻度には上限を設ける**（毎tick発動すると集団が消滅する）。上限値は実測で決める。

## 実装の順序（⚠️入れ替え不可）

⚠️**決着式をいくら直しても、専門家が先頭集団に残れていなければ決着に到達しない**
（実測済み：決着相性の修正単独では改善なし）。したがって：

1. **原因A**（逃げ役の`sprintGap`依存・`ticks.js:185`と`:309`の2箇所）
2. **原因B**（位置と回復。`slot`＝強さ順 × `regen`＝後方ほど回復）
3. **仕様3**（AIの能動的な絞り）
4. **決着式の較正**——1〜3を入れた状態で、仕様1の表に合うよう
   `finishAbility`の混合率と`typeAffinityBonus`の合流を調整する

## ⚠️決着式についての設計上の指針

現在の決着式は、専門家の優位（1位と2位の差）がコースごとにバラバラで、それが優勝率と直結している：

| 決着区間 | 専門家の優位 | 実際の優勝率 |
|---|---|---|
| sprint（クリテ・丘陵ロード） | **+6.80** (SPR) | 85% |
| tt（平坦ロード・個人TT） | +5.14 (TT) | 42% |
| hill（グラベル） | +2.54 (PUN) | 25% |
| mtn（山岳・ヒルクライム） | **+1.13** (CLM) | 2〜3% |

⚠️**スプリント決着だけが「混ぜていない」式**（`base = sp`）で、他は全て混合。
**混ぜるほど万能型が勝ち、混ぜないほど専門家が勝つ。**
⚠️山頂決着`climb*0.75 + sprint*0.25`では、CLM(86/63)=80.25に対しPUN(79/79)=79.00で
差がわずか**+1.25**——**万能型が専門家を打ち消せる重み付け**になっている。

→ **指針：どの地形でも専門家の優位を同水準（+5前後）に揃える。**
また`finishAbility`には`typeAffinityBonus`が入っていない（`segmentAbility`には入っている）。
⚠️第50弾がバッジについて塞いだのと同じ穴が脚質相性だけ残っているので、較正時に合流させる。

## 受け入れ判定（CLAUDE.md §10）

- **主指標**：`scratchpad/w75_winshare.mjs`（優勝者の脚質内訳）が仕様1の表に±8ポイントで収まる。
- **副指標**：同スクリプトの逃げ切り率が仕様2の表に近づく。
- ⚠️**AI同士の分布で測ること**（`w75_ai.mjs`系）。プレイヤーの装備・作戦・絆に依存しない
  唯一の物差しである。

---

# 第76弾：Phase 1実装（原因A：逃げ役のsprintGap依存の緩和）

**状態**：**実装完了。単独では優勝内訳が悪化する結果になった（想定内・Phase2待ち）。**

## 実装

`ticks.js`の2箇所を修正した。

**①`assignAIRoles`（レース開始時の役割割当）**：
`breakawayChance = Math.min(0.65, 0.06 + sprintGap*0.022)` を
`Math.min(0.30, 0.06 + sprintGap*0.007)` へ。実測でCLM 53%→22%、TT/RUL 39%→17%、
SPR/PUNはほぼ不変（`scratchpad/w76_A_calib.mjs`）。

**②中盤の自然発生アタック（⚠️当初の係数調整だけでは効かなかった）**：
最初に係数だけを0.0009→0.0003へ弱めたが、`scratchpad/w76_diag.mjs`で検証すると
⚠️**この一手は毎tick再抽選される構造**で、`members.length`項（大集団で+0.03前後）が
sprintGap項より支配的に上限0.06へ張り付き、約200tickの窓では**係数を1/3にしても
累積発火率は変わらず100%のまま**だった。⚠️**定数調整では直らない構造上のバグ**と判断し、
「一度だけ判定する」形（`en.breakawayRolled`フラグ）に変更した。攻撃後の強さ・持続時間
（`BREAKAWAY_ATTACK_TICKS`等）は一切変更していない——§88〜89が禁じる「アタック発動後の
強さ・持続を追う」ではなく「発動するかどうかの確率」だけを直している。

修正後の新しい一回判定式：`Math.min(0.35, 0.05 + (sprintGap-8)*0.010 + onHill?0.05:0 + climbEdge)`。

## 検証：狙った指標（逃げ役の偏り）は解消した

| 脚質 | 逃げ役割当率（旧→新） | 一度でもattack（旧→新・クリテ） |
|---|---|---|
| SPR | 6.9%→5.4% | 5%→5.7% |
| PUN | 15.7%→6.1% | 22%→8.2% |
| **CLM** | **53.1%→16.1%** | **72%→33.3%** |
| TT | 39.2%→8.8% | 66%→16.9% |
| RUL | 38.7%→11.4% | 58%→20.0% |

## ⚠️副作用：優勝内訳は単独ではむしろ悪化した（n=60）

| コース | 想定 | 修正前 | **修正後** |
|---|---|---|---|
| 丘陵ロード | PUN | SPR65/PUN3 | **SPR92/PUN8** |
| クリテリウム | SPR | SPR85 | **SPR92** |
| 平坦ロード | RUL | RUL15/TT42 | RUL5/TT35 |

⚠️**原因が判明した**：逃げは「平均では勝てない戦術」であると同時に、
**TT/RUL/CLMにとって集団スプリント以外で勝てる唯一の経路**でもあった。送り込みを
減らした結果、彼らは集団スプリントでも勝てず（原因Bが未修正）、逃げでも
勝たなくなり（送り込み自体が減った）、**勝ち筋そのものが消えた**。

⚠️**これは第75弾-Dで「実装順は入れ替え不可」と警告していた事態の具体例**——
原因Bを直すまでは、原因Aの修正は単独ではむしろ悪化して見える。CLAUDE.md §10に
従い、改善しなかった（悪化した）事実も含めてここに記録する。Phase2（原因B）へ進む。

---

# 第76弾：Phase 2実装（原因B：regenの飽和カーブ）

**状態**：**実装完了。明確な前進、目標にはまだ届かない（想定内・Phase3/4待ち）。**

## 実装

`ticks.js`の`regen`計算を線形から飽和型へ変更した。新定数
`REGEN_FRONT_FLOOR = 0.35`・`REGEN_SATURATION = 0.25`（第75弾-Dで実測した
候補のうち唯一明確に効いた組み合わせ、`scratchpad/w75_patch.py`の`v_Bregen`）。

```js
const shelterCurve = REGEN_FRONT_FLOOR + (1 - REGEN_FRONT_FLOOR) * Math.min(1, backRatio / REGEN_SATURATION);
const regen = sheltered ? ENERGY_REGEN_BASE * shelterCurve * (windActive ? 0.5 : 1) : 0;
```

先頭(backRatio=0)でも最低35%の回復を受け取れ、集団内位置が25%を超えれば満額（現実の
「先頭数人だけが風を受け、それより後ろは同程度に守られる」に近づける）。

## 検証（Phase1+2累積・n=60・実src）

| コース | 想定 | 目標 | ベースライン | Phase1単独 | **Phase1+2** |
|---|---|---|---|---|---|
| 山岳ロード | CLM | 50 | 3 | 2 | **23** |
| ヒルクライム | CLM | 55 | 2 | 2 | **18** |
| 丘陵ロード | PUN | 50 | 3 | 8 | **17** |
| グラベルレース | PUN | 50 | 25 | 23 | **38** |
| クリテリウム | SPR | 50 | 85 | 92 | **77** |
| 平坦ロード | RUL | 50 | 15 | 5 | 15（横ばい） |

⚠️**明確な前進**：CLM/PUNの最上位到達率がベースラインの5〜8倍に伸びた
（丘陵PUN 3%→17%、山岳CLM 3%→23%）。⚠️**逃げ切り率も暴走していない**
（全コース0〜7%）——第75弾-Cで単独のB候補を試したときは丘陵28%→65〜75%に
暴走したが、⚠️**Phase1（逃げ役の適正化）が先に入っていることで、この暴走を
抑えられている**。実装順が重要という設計図の判断が実測で裏付けられた。

⚠️**まだ目標に届いていない**：どのコースも「想定脚質が最上位」には至っていない
（山岳・ヒルクライムはPUNが最上位のまま、丘陵・クリテはSPRが最上位のまま）。
特に⚠️**平坦ロードはほぼ横ばい**——決着区間`tt`はドラフト集団の風除け機構と
別系統（`ttPacing`）のため、位置・回復モデルの修正が及びにくいと見られる
（未検証の仮説として記録。Phase4で決着式を較正する際に要確認）。

Phase3（AIの能動的な絞り）・Phase4（決着式の較正）へ進む。

---

# 第76弾：Phase 3実装（AIチームの能動的な集団絞り）

**状態**：**実装完了・1回目の実装は無効だったため設計を修正した。**

## ⚠️1回目の実装は、実測するとほぼ発火していなかった

当初はプレイヤー専用の「ふるいにかける」(`tempo`)とフィールド(`tempoLeft`)を共有する形で
実装した。しかし`tempoTighten`の効果は**発動者がその区間の地形バッジを持つ場合しか
効かない**設計（`raceDecisions.js`のterrainカードも`hasTerrainBadge`でのみ提示される、
という既存仕様に合わせて実装した結果）。⚠️**AI選手が特定の地形バッジを持つ確率は
5〜7%しかなく**（`scratchpad/w76_badge_rate.mjs`）、そこへ「エースと同チーム」
「高エネルギー」の条件が重なると、⚠️**実測で1467人中19人（1.3%）しか対象にならず**
（`scratchpad/w76_check_phase3.mjs`）、優勝内訳もほぼ動かなかった（一部コースでは
サンプルノイズによりむしろ悪化して見えた）。

## 修正：独立フィールドに切り替え

バッジ保有というプレイヤー専用カードの前提を、AIの一般的な戦術にそのまま流用したのが
誤りだった。⚠️`aiTempoLeft`という独立フィールドと`aiTempoTighten`という独立の
`keepThresh`項を新設し、**プレイヤーの`tempo`カードには一切触れない形**に直した
（強さの大きさ＝`TEMPO_KEEP_TIGHTEN`は同じ値を踏襲。「同じ経路を使う」という
仕様の意図は、数値の踏襲として解釈し直した）。

修正後の対象化率：1461人中323人（**22%**）——狙いどおり機能するようになった。

## 検証（Phase1+2+3累積・n=60・実src）

| コース | 想定 | 目標 | Phase1+2 | **Phase1+2+3** |
|---|---|---|---|---|
| 山岳ロード | CLM | 50 | 23 | 22（横ばい） |
| ヒルクライム | CLM | 55 | 18 | **25**（+7） |
| 丘陵ロード | PUN | 50 | 17 | **22**（+5） |
| グラベルレース | PUN | 50 | 38 | 32（−6・悪化） |
| クリテリウム | SPR | 50 | 77 | **73**（目標へ−4） |
| 平坦ロード | RUL | 50 | 15 | **25**（+10・横ばいを脱した） |

⚠️**6コース中4コースで前進、1コースで後退、1コース横ばい。** まだどのコースも
目標（想定脚質が最上位・±8pt）には届いていないが、Phase2まで手詰まりだった
平坦ロード（横ばい15%）が動いた点は大きい。Phase4（決着式の較正）へ進む。

---

# 第76弾：Phase 4実装（決着式の較正）と、4Phase累積の最終結果

**状態**：**4Phaseすべて実装完了。目標には届いていないが大きく前進。残る2件は構造的な問題で、
このPhaseの範囲では直せない。**

## 実装

`finish.js`の`finishAbility`を、地力72の平均能力で決定論的に較正した
（`scratchpad/w76_finish_calib.mjs`。乱数を使わない閉形式の計算なので、
シミュレーションのブレに惑わされず候補を素早く絞り込めた）。

- **mtn/climb決着**：`cl*0.75+sp*0.25` → `cl*0.85+sp*0.15`
- **hill決着**：`sp*0.45+cl*0.35+fl*0.20` → `cl*0.55+sp*0.30+fl*0.15`
- **tt決着**：`so*0.6+fl*0.4` → `so*0.62+fl*0.38`
- **sprint決着**：変更なし（元々+6.80で目標水準にあった）
- 全区間（sprint以外）に`typeAffinityBonus`を係数0.25〜1.0で合流。
  `segmentAbility`には元々入っていたが`finishAbility`には無かった穴
  （第50弾がバッジについて塞いだのと同じ形）を埋めた。

較正後の「専門家と2番手の差」は全区間+6〜7に揃った（旧：+1.13〜+6.80でバラバラ）。

## ⚠️測定上の注意：tickシミュレーションは乱数を使うため、小さな差はノイズ

ライダー生成（`newRider`）はmulberryシード固定だが、⚠️**tick内の集団判定・ローテーション・
ジッターは`Math.random()`を多用しており固定されていない。** 1レース約500tickの中で
無数に乱数を引くため、**n=100でも±7pt程度は再実行のたびに動きうる**。以下の表で
小さな変化（±8pt未満）は実装の効果と断定しない。

## 最終結果（n=100・実src・4Phase累積）

| コース | 想定 | 目標 | ベースライン | **最終** | 進捗 |
|---|---|---|---|---|---|
| 山岳ロード | CLM | 50 | 3 | 16 | 部分改善 |
| ヒルクライム | CLM | 55 | 2 | 13〜20（ブレ幅内） | 部分改善 |
| 丘陵ロード | PUN | 50 | 3 | 16 | 部分改善 |
| グラベルレース | PUN | 50 | 25 | 36 | 前進 |
| クリテリウム | SPR | 50 | 85 | 72 | 前進（目標へ） |
| 平坦ロード | RUL | 50 | 15 | 21 | ⚠️ほぼ動かず（後述） |

⚠️**どのコースも目標（想定脚質が最上位）にはまだ届いていない。** ベースラインからは
明確に前進した（CLM/PUNの最上位到達率が3〜5倍）が、目標には遠い。

## ⚠️残る原因1：原因B（regen）の修正がまだ弱すぎる

山岳ロードで「3位以内に入る率」を脚質別に見ると：

| 脚質 | 3位以内率 |
|---|---|
| SPR | 22.0% |
| **PUN** | **45.3%** |
| **CLM** | **16.0%** |
| TT | 6.0% |
| RUL | 10.7% |

⚠️**決着式（Phase4）は「集団に残れた者の中で誰が勝つか」しか決められない。**
CLMがそもそも上位集団に残れていない（16%）のに対しPUNは45.3%——ここが
律速している。Phase2で採用した`REGEN_FRONT_FLOOR=0.35`・`REGEN_SATURATION=0.25`は
方向としては正しかったが、⚠️**まだ強さが足りない。** より強い飽和（floorを上げる・
saturationを下げる）を試す余地がある。

## ⚠️残る原因2：平坦ロードの決着区間`tt`が、構造的にRULよりTTを優遇する

平坦ロード（`favors:"RUL"`）の最終区間は第72弾の設計で`tt`（独走決着）になっている。
tt決着式`so*0.62+fl*0.38`は`solo`の重みが大きく、⚠️**RUL型はflatが高くsoloは並み
（`newRider`のRULボーナスは`flat+14`のみ）なのに対し、TT型はsoloが突出（`solo+14`）
するため、tt決着では構造的にTTがRULを上回る。**

実測：TT(so=86,fl=78)＝84.5 vs RUL(so=72,fl=85)＝76.9（差+7.6でTT優位）。
⚠️**これは定数調整では直らない**——tt決着式の重みをflat寄りに変えれば「独走決着」
という区間の意味そのものが崩れ、他の混合式との整合も失う。第72弾の設計意図
（「平坦を高速で押し切るルーラーの定義」）と、実際の決着式（solo偏重のtt）が
最初から噛み合っていなかった。⚠️**選択肢は2つ**：①平坦ロードの決着区間を
tt以外（例：sprint）に変える②「ルーラーの勝ち筋はtt区間ではなくflat区間の走力」
と割り切り、想定脚質の目標をRULではなくTTへ変更する。**どちらもコース設計・
脚質の定義に関わる判断**であり、この弾の範囲では決めない。

## 判定

4Phaseとも実装済み・ベースラインから明確に改善したが、目標未達。次の一手は
2つの独立した課題——①Phase2（regen）の強化②平坦ロードのtt/RUL不整合の設計判断——
であり、どちらもユーザーの判断が必要。

---

# 第77弾：ふるい落とし強化の実測と、Phase4が効かなかった理由の特定

**状態**：**測定完了・実装は未着手（ユーザー合意待ち）。**

## ⚠️発端：ユーザーの質問で、Phase4がほぼ無意味だったと判明

ユーザーから「最終カメラに突入する際の閾値（集団内の全員が反映されると集団後方で
耐えてたスプリンターが最終スプリントで勝っちゃう）とか、最終スプリントのあれこれも
調整してる？」と問われ、決着クラスタの実サイズを計測用の複製srcで測った
（`scratchpad/w77src`・`w77_cluster.mjs`）。

⚠️**以前DEVLOGへ記録した「ゴール1秒以内は1.5〜2.6人」は誤解を招く数字だった**——
`resolveFinishClusters`はクラスタ確定後に着差を最大14秒へ引き伸ばすため、あれは
**引き伸ばし後**の値だった。引き伸ばし前の実数：

| コース | 先頭クラスタの人数 | クラスタが発生するレース |
|---|---|---|
| 山岳ロード | 2.3人 | **7%** |
| ヒルクライム | 2.0人 | 13% |
| 丘陵ロード | 2.1人 | 27% |
| クリテリウム | 2.2人 | 17% |

⚠️**ユーザーの懸念（後方で温存したスプリンターが最終スプリントで勝つ）は起きていない**——
集団で最終スプリントに突入するレース自体が7〜27%しかなく、しかも2人程度。
⚠️**同時にこれは第76弾Phase4（決着式の較正）がほぼ無意味だったことを意味する。**
較正した`finishAbility`は7〜27%のレースの2人の間でしか使われず、残り73〜93%は
tickシミュレーションの生存競争だけで勝者が決まっていた。

## 測定：ユーザーの3提案のうち、効いたのは「ふるい落とし強化」だけ

ヒルクライムのCLM優勝率（n=70）：

| 候補 | ヒルクライム | 山岳ロード |
|---|---|---|
| 現状（第76弾まで） | 19% | 21% |
| ①`regen`をさらに強化（floor0.55/sat0.18） | 16%（⚠️悪化） | 13%（⚠️悪化） |
| ③**地形別ふるい落としを常時ON**（climb/mtn 0.05・hill 0.035・flat 0.015） | **49%** | **41%** |
| ①＋③ | **51%** | 34% |

n=120で①＋③と③単独を比較すると差はノイズ幅内（山岳31%対34%・ヒルクライム43%対38%）。
⚠️**①（`regen`のさらなる強化）は効果なしと確定。第76弾Phase2の値のまま据え置く。**

⚠️**ユーザーは①を選んだが、実測は③（ユーザーが「〜もいいかもね」と添えた案）を支持した。**

**③の内容**：`selectiveTight`は`course.selective`（モニュメント/grade≥4）限定で
通常レースでは働いていなかった。これを**地形別の常時ONへ変更**し、登坂ほど強くする
（climb/mtn 0.05／hill 0.035／平坦系 0.015）。`course.selective`の0.035は従来どおり上乗せ。
⚠️**ヒルクライムでCLM 49%＝目標55%にほぼ到達**した。

## ⚠️残る不足の原因はsimではなくコースデータ

| コース | 決着区間 | 距離比率 | 問題 |
|---|---|---|---|
| ヒルクライム | mtn | climb 87% | ✓ 目標に近い（CLM 43%） |
| **丘陵ロード** | ⚠️**sprint** | hill 53% / flat 41% | ⚠️**SPRが69〜79%で勝つ** |
| **山岳ロード** | mtn | ⚠️**flat 47%** / climb 45% | PUN 48〜52%＞CLM 31〜34% |
| グラベルレース | hill | hill 63% | ✓ PUNが最上位 |
| **平坦ロード** | ⚠️**tt** | flat 76% / tt 24% | ⚠️TT＞RUL（第76弾で既出） |

- ⚠️**丘陵ロードの決着が`sprint`のまま**。第74弾でグラベルを`sprint`→`hill`に直したのと
  **同じ修正が丘陵ロードには入っていない**。決着がスプリント力勝負なのでSPRが勝つ。
  グラベル（hill決着）ではPUNが最上位になっている＝第74弾の修正は正しく効いている。
- ⚠️**山岳ロードは距離の47%が平坦**。純登坂のヒルクライム（climb 87%）ではCLMが最上位に
  なるのに、山岳ロードではPUNに負ける。
- 平坦ロードの`tt`決着は第76弾で既出（`solo`重視のためRULよりTTを優遇）。

⚠️**いずれもsim側では直せない。コースの`segs`設計の問題である。**

---

# 第78弾：③＋コースデータ2件の実装。3/6まで前進、2件の新課題

**状態**：**実装完了。目標未達だが前進。グラベルレースに新規の後退あり（未解明）。**

## 実装

ユーザーの「まとめてやろう」を受け、3件を同時に実装した。

1. **地形別ふるい落としの常時ON**（`ticks.js`）：新定数`TERRAIN_KEEP_TIGHTEN`
   （climb/mtn 0.05・hill 0.035・平坦系 0.015）を`selectiveTight`に加算。
   従来の`course.selective`（モニュメント/grade4限定）はそのまま上乗せとして残した。
2. **丘陵ロードの決着をsprint→hillへ**（`data/course.js`）：第74弾でグラベルに
   入れた修正と同じもの。最終区間を`sprint`から`hill`に変更。
3. **山岳ロードの平坦比率を削減**（`data/course.js`）：flat 460/26→320/16、
   climb 600/13→620/14、640/12→680/13、mtn 190/4→200/5。

## 結果（n=100・実src）

| コース | 想定 | 目標 | 第77弾まで | **第78弾** |
|---|---|---|---|---|
| 山岳ロード | CLM | 50 | 21〜34 | 31（⚠️PUN47が最上位のまま） |
| ヒルクライム | CLM | 55 | 19〜51 | **43** ✓ |
| 丘陵ロード | PUN | 50 | 3〜20 | **47** ✓（PUN最上位に） |
| グラベルレース | PUN | 50 | 28〜40 | ⚠️**25（RUL30に逆転される）** |
| クリテリウム | SPR | 50 | 68〜86 | 68 ✓（想定どおりだが依然高い） |
| 平坦ロード | RUL | 50 | 15〜41 | 33（⚠️SPRが最上位） |

⚠️**想定脚質が最上位のコース：2/6→3/6。** ヒルクライムと丘陵ロードは目標に近づいた。

## ⚠️新たな後退：グラベルレースでPUNがRULに逆転された

修正前はPUNが最上位（25〜40%）だったが、地形別ふるい落とし追加後にRULが逆転した
（RUL30%＞PUN25%）。決着式（hill: `cl*0.55+sp*0.30+fl*0.15`+affinity5）で計算すると
PUN(82.95)がRUL(73.95)を明確に上回るため、⚠️**PUNが決着クラスタに残れなくなっている
可能性が高い**（未検証）。地形別ふるい落としがグラベル特有の地形構成
（hill63%/flat23%/climb13%の混成）とどう相互作用しているかは切り分けていない。

## 山岳ロードが依然PUN優位（未解明）

平坦比率を47%→33%へ下げ、climb+mtn比率を53%→67%へ上げたにもかかわらず、
PUN(47%)がCLM(31%)を上回ったまま。ヒルクライム（climb87%）ではCLMが最上位になる
のに、山岳ロード（climb56%+mtn10%）ではまだ届かない——⚠️地形構成の違いだけでは
説明しきれておらず、さらなる切り分けが必要。

## 判定

3件とも実装・ビルド成功・シーズンモードでも実行時エラー無しを確認。目標には
まだ届いていないが2コースで目標水準に達した。⚠️**グラベルの新規後退と山岳ロードの
未解明な律速**の2つが残る。これ以上の定数探索を続けるか、ここで一区切りとするかは
ユーザー判断を仰ぐ。

---

# 第79弾：残る2件の原因究明（別々の原因だった）

**状態**：**原因特定完了・実装は未着手。**

## 手法：集団力学を除いた「素の走破タイム」を決定論的に計算

第78弾で残った2件（グラベルでRULがPUNを逆転／山岳ロードでPUNがCLMに勝つ）を
切り分けるため、⚠️**集団・エネルギー・乱数を一切含まない純粋な走力**を計算した
（`scratchpad/w79_pace.mjs`）。各区間の`segmentAbility`から速度を出し、
`terrainSpeedMul`で地形の遅さを掛けて、距離÷速度の総和＝走破タイムを求める。

**素の走破タイムで最速の脚質（steepness 0.75/1.05/1.35すべてで同じ）：**

| コース | 素の走力で最速 | 想定 | 実測(第78弾) |
|---|---|---|---|
| 山岳ロード | **CLM** ✓ | CLM | PUN |
| ヒルクライム | **CLM** ✓ | CLM | CLM ✓ |
| 丘陵ロード | ⚠️**RUL** | PUN | PUN |
| グラベルレース | ⚠️**RUL** | PUN | RUL |

⚠️**2件は別々の原因だと判明した。**

## ⚠️原因1（グラベル・丘陵）：PUNには「自分が最強になれる区間」が存在しない

区間タイプ別の`segmentAbility`（地力72の平均能力・steepness1.05・脚質相性込み）：

| 区間 | SPR | PUN | CLM | TT | RUL | 最強 |
|---|---|---|---|---|---|---|
| flat | 71.5 | 71.5 | 71.5 | 77.5 | **89.4** | RUL |
| **hill** | 67.9 | 79.6 | 77.4 | 75.1 | **79.7** | ⚠️**RUL** |
| climb | 63.7 | 78.0 | **89.1** | 72.4 | 73.2 | CLM |
| mtn | 69.5 | 78.7 | **83.7** | 71.7 | 71.7 | CLM |
| sprint | **90.3** | 78.5 | 62.5 | 71.5 | 71.5 | SPR |
| tt | 71.9 | 71.9 | 71.9 | **88.5** | 77.4 | TT |

⚠️**6区間のうちPUNが最強になる区間は1つも無い。** 丘陵区間ですらRULに負ける（79.6対79.7）。

原因は`sim/course.js`の`climbWeightFor`：
```js
const base = { flat: 0, hill: 0.4, climb: 0.85, mtn: 0.9, sprint: 0, tt: 0 }[segType];
return Math.min(1, base * steepness);
```
⚠️**hillのbaseが0.4しかなく、steepness1.05でも重み0.42＝丘陵区間ですら能力の58%がflatで決まる。**
だからflat85のRULが、climb79のPUNに並んでしまう。

⚠️**第75弾の「PUNが全コース最強」とも整合する**——当時は集団力学（強い者が前に置かれ
削られる）が支配していたため「**どこでも2番手**」が最適戦略だった。ふるい落としを
強化した今その優位が消え、代わりに「**どこでも1番になれない**」弱点が露出した。

**修正案**：`climbWeightFor`のhill baseを0.4→0.6へ。試算ではhill区間が
PUN 81.4＞CLM 80.8＞RUL 76.8となり、PUNが初めて自分の区間で最強になる。
⚠️丘陵区間を持つ全コース（丘陵ロード・グラベル・サーキット・石畳/丘陵クラシック）に
影響するため、実装後は全コースで再測定すること。

## 原因2（山岳ロード）：走力では勝っているので、集団力学の残り

⚠️**山岳ロードは素の走破タイムでCLMが最速**（steepness1.05でCLM 1.3258＜PUN 1.3631＝2.8%差）。
走力で勝っているのに実測で負ける＝**原因B（集団内の位置と回復）がまだCLMを削っている**。

⚠️ヒルクライムとの差はタイム差の大きさ：ヒルクライムはCLMが3.8%速い（1.4880対1.5448）が
山岳ロードは2.8%差しかなく、集団力学のノイズに埋もれやすい。

⚠️**第77弾で「`regen`強化は効果なし」と測ったが、その理由も見えた**——`regen`を上げると
全員が生き残りやすくなり、ふるい落としが弱まって集団が大きいままゴールする＝
決着クラスタが大きくなり万能型有利に戻る。**CLMを勝たせるには「CLMだけが生き残る」
必要があり、それは`regen`ではなくふるい落とし側（第78弾の`TERRAIN_KEEP_TIGHTEN`）の仕事。**
山岳ロードで足りないぶんは、climb/mtnの`TERRAIN_KEEP_TIGHTEN`をさらに強めるのが筋。

---

# 第80弾：climbWeightForとTERRAIN_KEEP_TIGHTENの実装。5/6まで到達

**状態**：**実装完了・目標にほぼ到達。残る平坦ロードは既知の構造問題のみ。**

## 実装

1. **`sim/course.js` `climbWeightFor`**：hillのbaseを0.4→0.6へ。
   ⚠️ただし単独では丘陵ロード・グラベルの**コース全体の走破タイム**は変わらなかった
   （区間単体ではPUNが最強になったが、平坦区間のRUL優位が大きすぎて全体では
   RULが依然最速——決定論的な計算で確認。hillBaseを1.0まで上げてもCLMが割り込む
   だけで解決しないことも確認した）。
2. **`sim/ticks.js` `TERRAIN_KEEP_TIGHTEN`**：climb/mtnを0.05→0.07へ強化。
   ⚠️0.08まで上げると、グラベルレース（climbを含む混成地形）でCLMが過剰優遇され
   PUNを上回ってしまうため、山岳ロードでCLMが最上位であり続ける最小限の値として0.07を採用。
3. **`data/course.js` グラベルレースのclimb区間を撤去**：距離比率を半減しても
   CLMの優勢は変わらなかった（実測：climb7→3でもCLM32%のまま）ため、⚠️**climb区間の
   有無そのものが問題**と判断し完全にhillへ置き換えた。

## 検証（4回平均・各回n=70・実src。乱数ノイズを平均化するため複数回実行）

| コース | 想定 | 目標 | 第79弾まで | **第80弾（平均）** |
|---|---|---|---|---|
| 山岳ロード | CLM | 50 | 31 | **45.7** ✓ |
| ヒルクライム | CLM | 55 | 43 | **42.5** ✓ |
| 丘陵ロード | PUN | 50 | 47 | **60.4** ✓ |
| グラベルレース | PUN | 50 | 25（RULに逆転） | **25.7**（RUL25.0を僅差で上回る）✓ |
| クリテリウム | SPR | 50 | 68 | **65.4** ✓ |
| 平坦ロード | RUL | 50 | 21 | 21.1（TT37.9が最上位）✗ |

⚠️**想定脚質が最上位のコース：3/6→5/6。** 唯一残る平坦ロードは第76弾で特定済みの
構造問題（`tt`決着が`solo`重視でRULよりTTを優遇）——コース設計・脚質定義の判断が必要で、
この弾では見送る。

## ⚠️測定上の教訓：単発の実行は信用できない

グラベルレースは単発のn=100〜180実行では**PUN・CLM・RULのどれが最上位になるかが
実行のたびに入れ替わった**（`Math.random()`によるノイズ。第76弾で記録した注意点の
実例）。⚠️**4回実行して平均を取ることで初めて「PUNがRULよりわずかに優位」という
安定した傾向が見えた。** 以後この付近の僅差の判定は、単発ではなく複数回平均で行うこと。

## ユーザー確認事項：チームプレー機構は生きているか

ユーザーから「平坦をスプリンターが引き、山岳をアシストが引き、最後にエースを発射する」
チームプレーが機能しているかを問われ、コードを確認した。**すべて生きている**：

- `assignAIRoles`（`ticks.js:185`）が脚質別に役割を自動付与：CLM→`mountain`役、
  SPR/RUL→`flat`役。
- `canPull`（`ticks.js:198`）が役割ごとに牽引可能な地形を制限：`mountain`役は
  climb/mtn区間でしか牽けず、`flat`役はflat/hill区間でしか牽けない。
- `roleTerrainMismatchMul`（`ticks.js:114`）が不整合な牽引（例：`flat`役が登坂を牽く）に
  1.3〜1.6倍の追加消耗を課す。
- `simulateTicks`のレース初期化時（`ticks.js:264`付近）に**全チーム自動で**
  （プレイヤー限定ではない）最も脚のあるアシスト1名がリードアウト役に指名される。
  最終区間で同集団にいれば前を牽いてエースを射出し（発射者は速度×1.05、
  エースはfinishKick+0.045のスリングショット）、脚を使い切ると自然に後方へ流れる。

⚠️これらはAIチーム同士のレースでも常に働いており、プレイヤーの判断カードとは独立。

---

# 第81弾：「TTの居場所を定める」設計（#28＋#26＋#27-e）

**状態**：**設計確定・実装は未着手。**

## 発端：3件が同じ問題の別の顔だった

- **#28**：平坦ロード（RUL向け）でTTが勝つ（RUL21.1%＜TT37.9%）
- **#26**：グランツール3戦9ステージに`tt`区間が**0.0%**
- **#27-e**：月を消費せずTTに見せ場を与える案

⚠️**TTは「自分の居場所が無いのに他人（RUL）の居場所を奪っている」状態**だった。

## ⚠️カレンダー全体の棚卸しで、TTだけの問題ではないと判明

看板レース（候補1本＝必ず走る月）が報いる脚質（`scratchpad/w81_calendar.mjs`）：

| 脚質 | 看板レース数 |
|---|---|
| CLM | **3**（五輪・秋の女王・年度末※） |
| PUN | **2**（アルデンヌ・世界選手権） |
| RUL | 1（春の地獄） |
| **SPR** | ⚠️**0** |
| **TT** | ⚠️**0** |

グランツール3戦も CLM4／SPR3／PUN2／TT0／RUL0 と同じ偏り方をしていた。

## ⚠️自分の測定ミスを1件訂正

※**「年度末（3月）が山岳ロード固定」は誤報告だった。** 5年目の1年分だけを見て
断定していたが、`mlGenRaceCandidates`は`mulberry(year*3001+...)`でシャッフルするため
⚠️**年度末は既に年ごとに変わっている**（15年分でSPR5回/CLM3回/PUN4回/RUL2回/TT1回）。
ユーザーが選んだ「毎年地形を変える」は**既に実装済みで作業不要**。
⚠️一方**世界選手権は全年で丘陵ロード固定**（PUN）で、こちらは実際に偏りの原因だった。

## ユーザー決定（2026-08）

1. **看板レースは6枠のまま組み替える**（増やさない。第72弾の「候補1本の月が増えると
   迷いが減る」懸念に触れない）
2. **グランツールのTTは9ステージ中1つだけ**（現実のITTも全日程の1割前後）
3. 年度末は毎年地形を変える → **既に実装済み・作業不要**

## 変更1：`finishAbility`に`flat`決着を新設（`sim/finish.js`）

⚠️**根本原因**：`finishAbility`に`flat`の分岐が無く`else base = sp`（純スプリント）に
落ちていた。⚠️**`flat`で終わるコースは全て純スプリント勝負**で、RULは`tt`決着でも
`flat`決着でも勝てない構造だった。

```js
else if (segType === "flat") { base = fl * 0.6 + sp * 0.4; affCoef = 1.0; }
```
実測（地力72平均）：RUL83.8・SPR77.1・TT75.1・PUN74.3・CLM67.9 ＝**RUL +6.78**。
他決着（+6〜7）と同水準に揃う（`scratchpad/w81_flatfinish.mjs`）。

## 変更2：平坦ロードの決着を`tt`→`flat`へ（`data/course.js`）

最終区間`["tt", 420, 18]`→`["flat", 420, 18]`。RULが自分のコースを取り戻し、
**TTが他人の居場所を奪う状態が解消**する。

## 変更3：世界選手権を「ロードレース or 個人TT」の2択へ（`domain/mylife/race.js`）

⚠️**現状は全年で丘陵ロード（PUN）固定**で、8月のアルデンヌ（PUN）と重複していた。
2択に変え、ロード側を**サーキットレース（SPR）**、もう一方を**個人TT（TT）**にする。

⚠️**これ1つでSPRとTTの両方に看板レースができる**（月を増やさずに）。候補1本の月は
6/12→5/12へ**減る**ため、第72弾の懸念にも逆行しない。
⚠️`個人TT`は`soloTT`フラグ＋`squadMin/Max=1`だが、マイライフは個人競技なので
そのまま使える（`buildMyLifeSim`が`raceMeta.tmpl.soloTT`を見て`groupMode:"solo"`にする）。

**変更後の看板レース配分**：春の地獄=RUL／アルデンヌ=PUN／**世界選手権=SPRまたはTT（2択）**／
秋の女王=CLM／年度末=毎年変動／五輪(4年ごと)=CLM。**5脚質すべてに出番が回る。**

## 変更4：グランツール3戦の組み直し（`data/course.js`）

⚠️**`soloTT`付きの個人TTはステージに使えない**——`soloTT`は`raceMeta.tmpl`（1日目）しか
参照されず途中日に置いても集団走のままになり、さらにグランツールはsquad4〜6で走るため
`groupModeFor`が`full`を返す。よって`soloTT`を付けない**新テンプレ「TTステージ」**を追加する：

```js
{ kind: "TTステージ", favors: "TT", squadMin: 1, squadMax: 5, segs: [["flat", 200, 5], ["tt", 560, 20], ["tt", 560, 20]] }
```
実測で全steepnessでTTが最速・RULが2位（`scratchpad/w81_ttstage.mjs`）。

**3戦の新構成（性格を分ける・死蔵コースを起用・TTは1ステージのみ）**：

| | 性格 | 構成 |
|---|---|---|
| 春季 | 平坦と丘 | クリテリウム(SPR) → **平坦ロード(RUL)** → 丘陵ロード(PUN) |
| 夏季 | 山岳 | **グラベルレース(PUN)** → 山岳ロード(CLM) → ヒルクライム(CLM) |
| 秋季 | 総合力 | **TTステージ(TT)** → サーキットレース(SPR) → 山岳ロード(CLM) |

ステージの脚質配分：SPR2／RUL1／PUN2／CLM3／TT1（現行はCLM4／SPR3／PUN2／TT0／RUL0）。
⚠️**死蔵だった平坦ロード・グラベルレースが起用される**（個人TT・チームTTは通常月の
候補プールで出るためグランツールには入れない）。

## 実装順と検証

1. 変更1・2（`flat`決着の新設と平坦ロードの決着変更）→ `w78_final.mjs`で6コース測定。
   ⚠️**平坦ロードでRULが最上位になること**と、⚠️**他5コースが退行しないこと**を確認。
2. 変更3（世界選手権の2択）→ `w81_calendar.mjs`で看板レース配分を再確認。
3. 変更4（グランツール）→ `w81_gt.mjs`で`tt`比率と死蔵コースを再確認。
   ⚠️**グランファイナル出場条件（全3戦制覇）への影響**も見ること。
⚠️**僅差の判定は4回平均で行う**（単発はノイズで結論が入れ替わる・第80弾の教訓）。
