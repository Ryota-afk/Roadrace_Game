# 設計差分メモ v8→v9（プレイテストFB反映）

実装は`roadrace_v9.jsx`/`roadrace_v9.html`（Downloads）。

## 1. 3D観戦を廃止 → 俯瞰マップ＋側面マップ（SVG）

Three.jsによる3D描画を完全に廃止した。理由：横方向の動きがほとんど見えず単調、倍速時に不具合が出る、実装・保守コストが高い。

- **俯瞰マップ**：コースの横揺れ形状（`course.amp1/f1/ph1`＋`amp2/f2/ph2`、既存の3D生成で使っていたのと同じ式）をSVGの折れ線として描画し、選手を丸アイコンで表示
- **側面マップ**：標高プロファイル（`course.elevationProfile`、編成画面の標高グラフと共通データ）を同時に表示し、同じ選手アイコンを標高に応じた高さに配置
- 選手の実位置（`r.frac`）をそのまま2枚のマップに反映するだけなので、アンカー追従テレポートのようなごまかしが不要。牽引中は白縁、アタック中は赤い輪で強調
- 描画をReact状態（`ridersUi`）に統一したことで、倍速時に不具合が出ていた問題も解消（実機で×2→×4への切り替え、×4のままレース完走までエラーなしを確認）
- ロード演出も内部的にはシンプル化（WebGL初期化が不要になったため、SVGは即座に描画可能）

## 2. 出走人数の可変化（1〜5名）

- `TEMPLATES`の`squad: N`（固定値）を`squadMin`/`squadMax`（範囲）に変更。個人TTのみ1名固定（性質上ソロが妥当なため）、それ以外は全て1〜5名の範囲
- 編成画面に出走人数セレクターを追加。人数を変えると出走選手・エース・役割の選択がリセットされる
- AI側も選んだ人数と同じ人数で編成し、対等な条件を維持
- 実機で丘陵ロードを2名・5名それぞれで編成し、役割UIの表示（2名時は逃げ要員オプションが非表示になる`pelotonOnly`挙動）が人数に連動して切り替わることを確認済み

## 3. 成長速度のさらなる鈍化

- 基礎成長量：2.2→1.5
- 指定外能力の伸び：20%→14%
- 成長期（全盛期前）の成長倍率：1.25→1.0
- 即戦力型スカウト（基礎値+8%）と将来性型スカウト（基礎値-20%）の差はそのままに、成長のスピードを落としたことで「若手が数年で追い抜く」までの期間が伸び、即戦力・バランス型を選ぶ理由ができた（体感値。追加のプレイテストでの検証が望ましい）

## 実機確認状況

コンソールエラーなしで、スカウト方針確認→契約→ホーム（出走人数の幅表示を確認）→編成（人数セレクター・役割UIの人数連動を確認）→2D観戦（俯瞰・側面マップの選手数一致を確認）→倍速×2/×4→リザルト→月送り→イベント、の一連を確認済み。

## 今回見送った項目（ユーザーの意向により）

FA移籍市場・スタッフ雇用・成長推移の可視化画面は、v9では着手せず次バージョン以降の候補として保留。
